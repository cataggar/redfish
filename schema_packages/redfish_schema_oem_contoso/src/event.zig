//! Namespace `Event`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_oem_contoso`.

const std = @import("std");

const core = @import("redfish_core");

/// Deprecated in v1_3_0.
/// Starting with Redfish Specification v1.6 (Event v1.3), subscriptions are based on the `RegistryPrefix` and `ResourceType` properties and not on the `EventType` property.
pub const EventType = enum {
    /// The status of a resource has changed.
    StatusChange,
    /// A resource has been updated.
    ResourceUpdated,
    /// A resource has been added.
    ResourceAdded,
    /// A resource has been removed.
    ResourceRemoved,
    /// A condition requires attention.
    Alert,
    /// The telemetry service is sending a metric report.
    ///
    /// Events of type `MetricReport` shall be sent to a client in accordance with the `MetricReport` schema definition.
    MetricReport,
    /// Because `EventType` is deprecated as of Redfish Specification v1.6, the event is based on a registry or resource but not an `EventType`.
    ///
    /// Events of type `Other` shall be sent to a client in accordance with subscriptions to `RegistryPrefixes` or `ResourceTypes`.
    Other,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

test {
    std.testing.refAllDecls(@This());
}
