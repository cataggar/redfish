//! Namespace `CoolantConnector`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const chassis = @import("chassis.zig");
const control = @import("control.zig");
const cooling_loop = @import("cooling_loop.zig");
const cooling_unit = @import("cooling_unit.zig");
const redundancy = @import("redundancy.zig");
const resource = @import("resource.zig");
const sensor = @import("sensor.zig");

pub const CoolantConnectorType = enum {
    /// A connection pair.
    Pair,
    /// A supply or intake connection.
    Supply,
    /// A return or outflow connection.
    Return,
    /// An inline connection or measurement point.
    Inline,
    /// A closed or self-contained loop.
    Closed,
    /// A member of a group of connection pairs.
    PairGroupMember,
    /// A member of a group of supply connections.
    SupplyGroupMember,
    /// A member of a group of return connections.
    ReturnGroupMember,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ValveState = enum {
    /// Open the valve for this connector to its normal operating position.
    Open,
    /// Close the valve for this connector.
    Closed,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ValveStateReason = enum {
    /// Normal operation.
    ///
    /// This value shall indicate a normal operation of the valve, or a return to a normal operating state.  The `State` property within `Status` shall reflect the chosen value of `ValveState`, where a value of `Open` shall indicate an `Enabled` state, and a `Closed` value shall indicate a `Disabled` state.
    Normal,
    /// The valve is not in use.
    ///
    /// This value shall indicate the valve is not in use or is not connected, and therefore should remain closed.  The `State` property within `Status` shall indicate `Absent`.
    NotInUse,
    /// A leak was detected.
    ///
    /// This value shall indicate a leak was detected by an external source.  The `Health` of the resource may be affected by this change in state.  The `Health`, `State`, and `Condition` properties within `Status` should reflect the reaction taken by the service in response to a detected leak.
    LeakDetected,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `CoolantConnector.ValveControl`.
///
/// This action sets the state of the connector.
pub const CoolantConnectorValveControlAction = struct {
    /// The desired state of the connector.
    ///
    /// This parameter shall contain the desired valve state for the coolant connector.  If this parameter is not provided, the service shall not change the valve state for this connector.  Upon successful completion, the value of the `State` property within `Status` shall reflect this value.
    ValveState: ?ValveState = null,
    /// The reason for the desired state of the connector.
    ///
    /// This parameter shall contain the reason for desired state for the coolant connector.  Upon successful completion, the value of the `Health` property within `Status` shall reflect the result of the `ValveState` if the reason is applicable.  If this parameter is not provided, the value shall be `Normal`.
    ValveStateReason: ?ValveStateReason = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action sets the state of the connector.
    ///
    /// This action shall set the operating state of the coolant connector represented by the resource.
    @"#CoolantConnector.ValveControl": ?core.Action(CoolantConnectorValveControlAction, std.json.Value) = null,

    /// This action sets the state of the connector.
    ///
    /// This action shall set the operating state of the coolant connector represented by the resource.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn valveControl(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: CoolantConnectorValveControlAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#CoolantConnector.ValveControl" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// A link to the cooling loop at the other end of the connection.
    ///
    /// This property shall contain a link to a resource of type `CoolingLoop` that represents the cooling loop at the other end of the connection.
    ConnectedCoolingLoop: ?core.NavProperty(cooling_loop.CoolingLoop) = null,
    /// A link to the cooling unit at the other end of the connection.
    ///
    /// This property shall contain a link to a resource of type `CoolingUnit` that represents the cooling unit at the other end of the connection.
    ConnectedCoolingUnit: ?core.NavProperty(cooling_unit.CoolingUnit) = null,
    /// Any array of links to chassis at the other end of the connection.
    ///
    /// This property shall contain an array of links to resources of type `Chassis` that represent the chassis at the other end of the connection.
    ConnectedChassis: ?[]const core.NavProperty(chassis.Chassis) = null,
    /// How many members `ConnectedChassis` has in total, which is not how many this response carries.
    @"ConnectedChassis@odata.count": ?i64 = null,
    /// The next page of `ConnectedChassis`. Present only when this response left members out.
    @"ConnectedChassis@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `CoolantConnector.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `CoolantConnector` schema describes a liquid coolant connector, including any associated instrumentation.
///
/// This resource shall represent a coolant connector for a Redfish implementation.
pub const CoolantConnector = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The type of coolant connector.
    ///
    /// This property shall contain the type of coolant connector.
    CoolantConnectorType: ?CoolantConnectorType = null,
    /// Details about the coolant used in this unit.
    ///
    /// This property shall contain details regarding the coolant contained or used by this unit.
    Coolant: ?cooling_loop.Coolant = null,
    /// The rated liquid flow (L/min) for this loop interface.
    ///
    /// This property shall contain the rated liquid flow, in liters per minute units, for this loop interface.
    RatedFlowLitersPerMinute: ?core.Decimal = null,
    /// The pressure (kPa) at which the rated liquid flow is valid.
    ///
    /// This property shall contain the pressure, in kilopascal units, that the rated liquid flow is valid for this connector.
    RatedFlowPressurekPa: ?core.Decimal = null,
    /// The rated pressure (kPa) for this connector.
    ///
    /// This property shall contain the rated maximum pressure, in kilopascal units, for this connector.
    RatedPressurekPa: ?core.Decimal = null,
    /// The link to the application that manages the cooling loop.
    ///
    /// This property shall contain a URI to the application or device that provides administration or management of the cooling loop associated with this interface.
    CoolingManagerURI: ?[]const u8 = null,
    /// The name of the cooling loop attached to this interface.
    ///
    /// This property shall contain the name of the cooling loop attached to this interface.  If the `CoolingLoop` link property is present, this property shall contain the value of the `Id` property in the resource referenced by that link.
    CoolingLoopName: ?[]const u8 = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.
    LocationIndicatorActive: ?bool = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The manufacturer of this component.
    ///
    /// This property shall contain the name of the organization responsible for producing the component.  This organization may be the entity from whom the component is purchased, but this is not necessarily true.  This property is generally used only for replaceable or user-configurable components.
    Manufacturer: ?[]const u8 = null,
    /// The model number of the component.
    ///
    /// This property shall contain the name by which the manufacturer generally refers to the component.  This property is generally used only for replaceable or user-configurable components.
    Model: ?[]const u8 = null,
    /// The SKU of the component.
    ///
    /// This property shall contain the stock-keeping unit number for this component.  This property is generally used only for replaceable or user-configurable components.
    SKU: ?[]const u8 = null,
    /// The serial number of the component.
    ///
    /// This property shall contain a manufacturer-allocated number that identifies the component.  This property is generally used only for replaceable or user-configurable components.
    SerialNumber: ?[]const u8 = null,
    /// The part number of the component.
    ///
    /// This property shall contain a part number assigned by the organization that is responsible for producing or manufacturing the component.  This property is generally used only for replaceable or user-configurable components.
    PartNumber: ?[]const u8 = null,
    /// The spare part number of the component.
    ///
    /// This property shall contain the spare part number of the component.  This property is generally used only for replaceable or user-configurable components.
    SparePartNumber: ?[]const u8 = null,
    /// The group of coolant connectors represented by this resource.
    ///
    /// This property shall contain information for a group of coolant connectors that provide the full capacity represented by this coolant connector instance.  The `CoolantConnectorType` of each member in this group shall contain a value of `SupplyGroupMember`, `ReturnGroupMember` or 'PairGroupMember`, to align with the `Supply`, `Return` or `Pair` value of this resource.
    ConnectorGroup: ?redundancy.RedundantGroup = null,
    /// The heat removed (kW) through this connector.
    ///
    /// This property shall contain the amount of heat removed, in kilowatt units, by the coolant flow through this connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Heat`.
    HeatRemovedkW: ?sensor.SensorExcerpt = null,
    /// The liquid flow (L/min).
    ///
    /// This property shall contain the liquid flow rate, in liters per minute units, for this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `LiquidFlowLPM`.
    FlowLitersPerMinute: ?sensor.SensorExcerpt = null,
    /// The supply temperature (C).
    ///
    /// This property shall contain the temperature, in degree Celsius units, for the intake or supply connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Temperature`.
    SupplyTemperatureCelsius: ?sensor.SensorExcerpt = null,
    /// The return temperature (C).
    ///
    /// This property shall contain the temperature, in degree Celsius units, for the outflow or return connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Temperature`.
    ReturnTemperatureCelsius: ?sensor.SensorExcerpt = null,
    /// The differential temperature (C).
    ///
    /// This property shall contain the change in temperature, in degree Celsius units, between the supply connection and the outflow or return connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Temperature`, and the `ReadingBasis` property containing the value `Delta`.
    DeltaTemperatureCelsius: ?sensor.SensorExcerpt = null,
    /// The supply pressure (kPa).
    ///
    /// This property shall contain the pressure, in kilopascal units, for the intake or supply connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `PressurekPa`.
    SupplyPressurekPa: ?sensor.SensorExcerpt = null,
    /// The return pressure (kPa).
    ///
    /// This property shall contain the pressure, in kilopascal units, for the outflow or return connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `PressurekPa`.
    ReturnPressurekPa: ?sensor.SensorExcerpt = null,
    /// The differential pressure (kPa).
    ///
    /// This property shall contain the pressure, in kilopascal units, for the difference in pressure between the supply and outflow or return connection to the cooling loop.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `PressurekPa`, and the `ReadingBasis` property containing the value `Delta`.
    DeltaPressurekPa: ?sensor.SensorExcerpt = null,
    /// The desired liquid flow rate (L/min).
    ///
    /// This property shall contain the desired liquid flow rate, in liters per minute units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `LiquidFlowLPM`.  This property shall only be present for secondary coolant connectors.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    FlowControlLitersPerMinute: ?control.ControlExcerptSingleLoop = null,
    /// The desired supply temperature (C).
    ///
    /// This property shall contain the desired supply temperature, in degree Celsius units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `Temperature`.  This property shall only be present for secondary coolant connectors.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    SupplyTemperatureControlCelsius: ?control.ControlExcerptSingleLoop = null,
    /// The desired return temperature (C).
    ///
    /// This property shall contain the desired return temperature, in degree Celsius units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `Temperature`.  This property shall only be present for secondary coolant connectors.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    ReturnTemperatureControlCelsius: ?control.ControlExcerptSingleLoop = null,
    /// The desired differential pressure (kPa).
    ///
    /// This property shall contain the desired differential pressure, in kilopascal units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `PressurekPa`.  This property shall only be present for secondary coolant connectors.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    DeltaPressureControlkPa: ?control.ControlExcerptSingleLoop = null,
    /// The desired differential temperature (C).
    ///
    /// This property shall contain the desired differential temperature, in degree Celsius units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `Temperature`.  This property shall only be present for secondary coolant connectors.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    DeltaTemperatureControlCelsius: ?control.ControlExcerptSingleLoop = null,
    /// The valve position (% open) of this connector.
    ///
    /// This property shall contain the valve position, in percent units, of this connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Valve`.
    ValvePositionPercent: ?sensor.SensorExcerpt = null,
    /// The desired valve position (% open).
    ///
    /// This property shall contain the desired valve position, in percent units, of this coolant connector.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value `Valve`.  Services may automatically change other controls if a client attempts to enable this control to prevent conflicts.
    ValvePositionControlPercent: ?control.ControlExcerptSingleLoop = null,
    /// The proximity of the coolant temperature to the dew point.
    ///
    /// This property shall contain the difference, in degrees celsius, between the coolant temperature and the dew point.  The `DataSourceUri` property, if present, shall reference a resource of type `Sensor` with the `ReadingType` property containing the value `Temperature`, and the `ReadingBasis` property containing the value `Delta`.
    DewPointProximityCelsius: ?sensor.SensorExcerpt = null,
};

/// What a client may change of `CoolantConnector.CoolantConnector`.
///
/// The `CoolantConnector` schema describes a liquid coolant connector, including any associated instrumentation.
pub const CoolantConnectorUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Details about the coolant used in this unit.
    ///
    /// This property shall contain details regarding the coolant contained or used by this unit.
    Coolant: ?cooling_loop.CoolantUpdate = null,
    /// The link to the application that manages the cooling loop.
    ///
    /// This property shall contain a URI to the application or device that provides administration or management of the cooling loop associated with this interface.
    CoolingManagerURI: core.Nullable([]const u8) = .absent,
    /// The name of the cooling loop attached to this interface.
    ///
    /// This property shall contain the name of the cooling loop attached to this interface.  If the `CoolingLoop` link property is present, this property shall contain the value of the `Id` property in the resource referenced by that link.
    CoolingLoopName: core.Nullable([]const u8) = .absent,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.
    LocationIndicatorActive: core.Nullable(bool) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The group of coolant connectors represented by this resource.
    ///
    /// This property shall contain information for a group of coolant connectors that provide the full capacity represented by this coolant connector instance.  The `CoolantConnectorType` of each member in this group shall contain a value of `SupplyGroupMember`, `ReturnGroupMember` or 'PairGroupMember`, to align with the `Supply`, `Return` or `Pair` value of this resource.
    ConnectorGroup: ?redundancy.RedundantGroupUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
