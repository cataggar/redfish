//! Namespace `Storage`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const assembly = @import("assembly.zig");
const certificate_collection = @import("certificate_collection.zig");
const chassis = @import("chassis.zig");
const computer_system = @import("computer_system.zig");
const connection_collection = @import("connection_collection.zig");
const consistency_group_collection = @import("consistency_group_collection.zig");
const drive = @import("drive.zig");
const endpoint = @import("endpoint.zig");
const endpoint_group_collection = @import("endpoint_group_collection.zig");
const file_system_collection = @import("file_system_collection.zig");
const pcie_device = @import("pcie_device.zig");
const pcie_function = @import("pcie_function.zig");
const port_collection = @import("port_collection.zig");
const protocol = @import("protocol.zig");
const redundancy = @import("redundancy.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");
const simple_storage = @import("simple_storage.zig");
const software_inventory = @import("software_inventory.zig");
const storage_controller_collection = @import("storage_controller_collection.zig");
const storage_group_collection = @import("storage_group_collection.zig");
const storage_metrics = @import("storage_metrics.zig");
const storage_pool_collection = @import("storage_pool_collection.zig");
const storage_service = @import("storage_service.zig");
const volume = @import("volume.zig");
const volume_collection = @import("volume_collection.zig");

pub const AutoVolumeCreate = enum {
    /// Do not automatically create volumes.
    Disabled,
    /// Automatically create non-RAID volumes.
    NonRAID,
    /// Automatically create RAID0 volumes.
    RAID0,
    /// Automatically create RAID1 volumes.
    RAID1,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ConfigLockOptions = enum {
    /// The command is supported, able to be locked, and is currently unlocked.
    Unlocked,
    /// The command is supported and is currently locked.
    Locked,
    /// The command is supported but is not able to be locked.
    LockdownUnsupported,
    /// The command is not supported, therefore lockdown does not apply.
    CommandUnsupported,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ConfigurationLock = enum {
    /// In-band configuration requests are locked as specified by `TargetConfigurationLockLevel`.
    ///
    /// This value shall indicate in-band configuration requests are locked as specified by `TargetConfigurationLockLevel`.
    Enabled,
    /// In-band configuration requests are not locked.
    ///
    /// This value shall indicate in-band configuration requests are not locked.
    Disabled,
    /// Some in-band configuration requests are not locked while others are locked.  This value is used for status reporting to indicate that the storage subsystem is partially locked and client action is recommended.
    ///
    /// This value shall indicate some in-band configuration requests are not locked while others are locked.
    Partial,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EncryptionMode = enum {
    /// Encryption is disabled on the storage subsystem.
    Disabled,
    /// The storage subsystem uses one or more external keys for encryption.
    UseExternalKey,
    /// The storage subsystem uses a local key for encryption.
    UseLocalKey,
    /// The storage subsystem uses a password, but no keys for encryption.
    PasswordOnly,
    /// The storage subsystem uses a password and one or more external keys for encryption.
    PasswordWithExternalKey,
    /// The storage subsystem uses a password and a local key for encryption.
    PasswordWithLocalKey,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const HotspareActivationPolicy = enum {
    /// The hot spare drive will take over for the original drive when the original drive has been marked as failed by the storage domain.
    OnDriveFailure,
    /// The hot spare drive will take over for the original drive when the original drive has been predicted to fail in the future by the storage domain.
    OnDrivePredictedFailure,
    /// The hot spare drive will take over for the original drive in an algorithm custom to the OEM.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmveMinimumRequiredResetType = enum {
    /// No reset required.
    NoReset,
    /// A Controller Level Reset is required.
    ControllerLevel,
    /// A Controller Level Reset other than a Controller Level Reset initiated by a Controller Reset is required.
    LimitedControllerLevel,
    /// An NVM Subsystem Reset is required.
    NVMSubsystemReset,
    /// A power cycle is required.
    PowerCycle,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmvePersonalityKeyAlgorithm = enum {
    /// HMAC-SHA-384.
    HMAC_SHA384,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmveUnfreezeAuthMode = enum {
    /// Programmable Key Authentication.
    ProgrammedKey,
    /// Physical Credential Authentication.
    PhysicalId,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ResetToDefaultsType = enum {
    /// Reset all settings to factory defaults and remove all volumes.
    ResetAll,
    /// Reset all settings to factory defaults but preserve the configured volumes on the controllers.
    PreserveVolumes,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TargetConfigurationLockLevel = enum {
    /// The standard configuration lock level, corresponding to applying firmware, updating security keys, and modifying other hardware settings.  It does not include managing the volumes or data within the storage subsystem.
    Baseline,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const VolumeAssignmentPolicy = enum {
    /// Newly created volumes are not assigned to physical functions, but are added to `UnassignedVolumes` in the `Links` property within this resource.
    Unassigned,
    /// Newly created volumes are assigned to the supervisor function.
    Supervisor,
    /// Newly created volumes are assigned to the physical functions in a round-robin fashion.
    WeightedRoundRobin,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Storage.FreezePersonality`.
///
/// This action freezes a personality for the NVMe subsystem.
pub const StorageFreezePersonalityAction = struct {
    /// The personality to freeze.  `255` indicates all personalities.
    ///
    /// This parameter shall contain the personality to freeze as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.  The value `255` shall indicate all personalities.
    Personality: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.GetPersonalityNonce`.
///
/// This action obtains a nonce required to unfreeze personalities with a programmed key.
pub const StorageGetPersonalityNonceAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.ImportForeignDrives`.
///
/// This action imports encrypted foreign drives to a host-based storage controller by supplying encryption access credentials.
pub const StorageImportForeignDrivesAction = struct {
    /// The current controller password.  This parameter is not required if the controller does not currently have a password.
    ///
    /// This parameter shall contain the current controller password.  Services may reject the action request if this parameter is not provided or the value supplied does not match the current password.
    ControllerPassword: ?[]const u8 = null,
    /// The encryption key to unlock the drives.
    ///
    /// This parameter shall contain the encryption key to unlock the drives.
    DriveEncryptionKey: ?[]const u8 = null,
    /// An encryption key identifier to filter the drives that are imported.  If not provided, the controller attempts to import all foreign drives.
    ///
    /// This parameter shall contain an encryption key identifier to filter the drives that are imported.  If this parameter is not provided, the service shall not perform filtering of foreign drives.
    DriveEncryptionKeyIdentifier: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.RekeyExternalKey`.
///
/// This action causes the controllers of the storage subsystem to request new encryption keys managed by an external key service.
pub const StorageRekeyExternalKeyAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.ResetToDefaults`.
///
/// The reset action resets the storage device to factory defaults.  This can cause the loss of data.
pub const StorageResetToDefaultsAction = struct {
    /// The type of reset to defaults.
    ///
    /// This parameter shall contain the type of reset to defaults.
    ResetType: ?ResetToDefaultsType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.RevertPersonalitiesToDefaults`.
///
/// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
pub const StorageRevertPersonalitiesToDefaultsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.SetControllerPassword`.
///
/// This action sets the controller boot password for a host-based storage controller.
pub const StorageSetControllerPasswordAction = struct {
    /// The current controller password.  This parameter is not needed if the controller does not currently have a password.
    ///
    /// This parameter shall contain the current controller password.  Services may reject the action request if this parameter is not provided or the value supplied does not match the current password.
    CurrentPassword: ?[]const u8 = null,
    /// The new password to set for the controller.
    ///
    /// This parameter shall contain the new password to set for the controller.
    NewPassword: ?[]const u8 = null,
    /// The security key for the controller.  This parameter is not needed if the controller does not have a security key.
    ///
    /// This parameter shall contain the controller security key.  Services may reject the action request if this parameter is not provided or the value provided does not match the security key for the controller.
    SecurityKey: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.SetEncryptionKey`.
///
/// This action sets the local encryption key for the storage subsystem.
pub const StorageSetEncryptionKeyAction = struct {
    /// The local encryption key to set on the storage subsystem.
    ///
    /// This parameter shall contain the local encryption key to set on the storage subsystem.
    EncryptionKey: ?[]const u8 = null,
    /// The local encryption key identifier used by the storage subsystem.
    ///
    /// This property shall contain the local encryption key identifier used by the storage subsystem.
    EncryptionKeyIdentifier: ?[]const u8 = null,
    /// The current local encryption key on the storage subsystem.  This parameter is not needed if the controller does not currently have an encryption key.
    ///
    /// This parameter shall contain the current local encryption key on the storage subsystem.  Services may reject the action request if this parameter is not provided or the value supplied does not match the current encryption key.
    CurrentEncryptionKey: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.SetPersonalityKey`.
///
/// This action sets the personality key for the NVMe subsystem.
pub const StorageSetPersonalityKeyAction = struct {
    /// The type of authentication algorithm for the key.
    ///
    /// This parameter shall contain the type of authentication algorithm for the key as defined by the 'CDP Authentication Algorithm' field in the NVMe Base Specification.
    Algorithm: ?NmvePersonalityKeyAlgorithm = null,
    /// The personality key to set on the NVMe subsystem.
    ///
    /// This parameter shall contain the personality key to set on the NVMe subsystem as a hex-encoded string.
    Key: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.SetPredictedMediaLifeLeftThreshold`.
///
/// This action configures the `PredictedMediaLifeLeftThreshold` for all drives attached to this storage subsystem.
pub const StorageSetPredictedMediaLifeLeftThresholdAction = struct {
    /// The predicted media life left threshold.
    ///
    /// This parameter shall contain the predicted media life left threshold to be established for all drives attached to this storage subsystem.
    PredictedMediaLifeLeftThreshold: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Storage.UnfreezePersonality`.
///
/// This action unfreezes a personality for the NVMe subsystem.
pub const StorageUnfreezePersonalityAction = struct {
    /// The personality to freeze.  `255` indicates all personalities.
    ///
    /// This parameter shall contain the personality to freeze as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.  The value `255` shall indicate all personalities.
    Personality: ?i64 = null,
    /// The authentication method for the unfreeze operation.
    ///
    /// This parameter shall contain the authentication method for the unfreeze operation as defined by the 'Authenticated Unfreeze Support' field in the NVMe Base Specification.
    AuthenticationMethod: ?NmveUnfreezeAuthMode = null,
    /// The authentication key for the unfreeze operation.
    ///
    /// This parameter shall contain the authentication key for the unfreeze operation as a hex-encoded string.  If `AuthenticationMethod` contains `ProgrammedKey`, this parameter contains a key that is derived from the original key provided in the `SetPersonalityKey` action.  If `AuthenticationMethod` contains `PhysicalId`, this parameter contains the physical secure ID (PSID) of the drive converted to a hex-encoded string.
    Key: ?[]const u8 = null,
    /// The personality nonce provided by the `GetPersonalityNonce` action.
    ///
    /// This parameter shall contain the personality nonce provided by the `GetPersonalityNonce` action.  This parameter shall be required if `AuthenticationMethod` contains `ProgrammedKey`.
    Nonce: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action freezes a personality for the NVMe subsystem.
    ///
    /// This action shall freeze a personality for the NVMe subsystem as defined by the 'Configurable Device Personality' feature in the NVMe Base Specification.
    @"#Storage.FreezePersonality": ?core.Action(StorageFreezePersonalityAction, std.json.Value) = null,
    /// This action obtains a nonce required to unfreeze personalities with a programmed key.
    ///
    /// This action shall obtain a nonce required to unfreeze personalities with a programmed key as defined by the 'CDP Random Nonce Data' clause in the NVMe Base Specification.
    @"#Storage.GetPersonalityNonce": ?core.Action(StorageGetPersonalityNonceAction, GetPersonalityNonceResponse) = null,
    /// This action imports encrypted foreign drives to a host-based storage controller by supplying encryption access credentials.
    ///
    /// This action shall import encrypted foreign drives to a host-based storage controller by supplying encryption access credentials.  Upon successful completion, the `EncryptionStatus` property of the affected `Drive` shall change from `Foreign` to `Unlocked`.
    @"#Storage.ImportForeignDrives": ?core.Action(StorageImportForeignDrivesAction, std.json.Value) = null,
    /// This action causes the controllers of the storage subsystem to request new encryption keys managed by an external key service.
    ///
    /// This action shall cause the controllers of the storage subsystem to request new encryption keys managed by an external key service.
    @"#Storage.RekeyExternalKey": ?core.Action(StorageRekeyExternalKeyAction, std.json.Value) = null,
    /// The reset action resets the storage device to factory defaults.  This can cause the loss of data.
    ///
    /// This action shall reset the storage device.  This action can impact other resources.
    @"#Storage.ResetToDefaults": ?core.Action(StorageResetToDefaultsAction, std.json.Value) = null,
    /// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
    ///
    /// This action shall revert all personalities to their manufacturing default state for the NVMe subsystem as defined by the 'Manufacturing Default Personality' clause of the NVMe Base Specification.
    @"#Storage.RevertPersonalitiesToDefaults": ?core.Action(StorageRevertPersonalitiesToDefaultsAction, std.json.Value) = null,
    /// This action sets the controller boot password for a host-based storage controller.
    ///
    /// This action shall set the controller boot password for a host-based storage controller.
    @"#Storage.SetControllerPassword": ?core.Action(StorageSetControllerPasswordAction, std.json.Value) = null,
    /// This action sets the local encryption key for the storage subsystem.
    ///
    /// This action shall set the local encryption key for the storage subsystem.
    @"#Storage.SetEncryptionKey": ?core.Action(StorageSetEncryptionKeyAction, std.json.Value) = null,
    /// This action sets the personality key for the NVMe subsystem.
    ///
    /// This action shall set the personality key for the NVMe subsystem as defined by the 'Programmable Key Authentication Considerations' clause in the NVMe Base Specification.
    @"#Storage.SetPersonalityKey": ?core.Action(StorageSetPersonalityKeyAction, std.json.Value) = null,
    /// This action configures the `PredictedMediaLifeLeftThreshold` for all drives attached to this storage subsystem.
    ///
    /// This action shall configure the `PredictedMediaLifeLeftThreshold` for all drives attached to this storage subsystem.
    @"#Storage.SetPredictedMediaLifeLeftThreshold": ?core.Action(StorageSetPredictedMediaLifeLeftThresholdAction, std.json.Value) = null,
    /// This action unfreezes a personality for the NVMe subsystem.
    ///
    /// This action shall unfreeze a personality for the NVMe subsystem as defined by the 'Authenticated Unfreeze Operation' clause in the NVMe Base Specification.
    @"#Storage.UnfreezePersonality": ?core.Action(StorageUnfreezePersonalityAction, std.json.Value) = null,

    /// This action freezes a personality for the NVMe subsystem.
    ///
    /// This action shall freeze a personality for the NVMe subsystem as defined by the 'Configurable Device Personality' feature in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn freezePersonality(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageFreezePersonalityAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.FreezePersonality" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action obtains a nonce required to unfreeze personalities with a programmed key.
    ///
    /// This action shall obtain a nonce required to unfreeze personalities with a programmed key as defined by the 'CDP Random Nonce Data' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn getPersonalityNonce(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageGetPersonalityNonceAction,
    ) !core.Owned(core.ModificationResponse(GetPersonalityNonceResponse)) {
        const target = self.@"#Storage.GetPersonalityNonce" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action imports encrypted foreign drives to a host-based storage controller by supplying encryption access credentials.
    ///
    /// This action shall import encrypted foreign drives to a host-based storage controller by supplying encryption access credentials.  Upon successful completion, the `EncryptionStatus` property of the affected `Drive` shall change from `Foreign` to `Unlocked`.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn importForeignDrives(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageImportForeignDrivesAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.ImportForeignDrives" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action causes the controllers of the storage subsystem to request new encryption keys managed by an external key service.
    ///
    /// This action shall cause the controllers of the storage subsystem to request new encryption keys managed by an external key service.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn rekeyExternalKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageRekeyExternalKeyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.RekeyExternalKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The reset action resets the storage device to factory defaults.  This can cause the loss of data.
    ///
    /// This action shall reset the storage device.  This action can impact other resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageResetToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.ResetToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
    ///
    /// This action shall revert all personalities to their manufacturing default state for the NVMe subsystem as defined by the 'Manufacturing Default Personality' clause of the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn revertPersonalitiesToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageRevertPersonalitiesToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.RevertPersonalitiesToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action sets the controller boot password for a host-based storage controller.
    ///
    /// This action shall set the controller boot password for a host-based storage controller.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setControllerPassword(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageSetControllerPasswordAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.SetControllerPassword" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action sets the local encryption key for the storage subsystem.
    ///
    /// This action shall set the local encryption key for the storage subsystem.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setEncryptionKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageSetEncryptionKeyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.SetEncryptionKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action sets the personality key for the NVMe subsystem.
    ///
    /// This action shall set the personality key for the NVMe subsystem as defined by the 'Programmable Key Authentication Considerations' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setPersonalityKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageSetPersonalityKeyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.SetPersonalityKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action configures the `PredictedMediaLifeLeftThreshold` for all drives attached to this storage subsystem.
    ///
    /// This action shall configure the `PredictedMediaLifeLeftThreshold` for all drives attached to this storage subsystem.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setPredictedMediaLifeLeftThreshold(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageSetPredictedMediaLifeLeftThresholdAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.SetPredictedMediaLifeLeftThreshold" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action unfreezes a personality for the NVMe subsystem.
    ///
    /// This action shall unfreeze a personality for the NVMe subsystem as defined by the 'Authenticated Unfreeze Operation' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn unfreezePersonality(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageUnfreezePersonalityAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Storage.UnfreezePersonality" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// This type describes the cache memory of the storage controller in general detail.
///
/// This type shall contain properties that describe the cache memory for a storage controller.
pub const CacheSummary = struct {
    /// The total configured cache memory, measured in MiB.
    ///
    /// This property shall contain the amount of configured cache memory as measured in mebibytes.
    TotalCacheSizeMiB: ?i64 = null,
    /// The portion of the cache memory that is persistent, measured in MiB.
    ///
    /// This property shall contain the amount of cache memory that is persistent as measured in mebibytes.  This size shall be less than or equal to the `TotalCacheSizeMiB`.
    PersistentCacheSizeMiB: ?i64 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
};

/// The personality nonce from a drive.
///
/// This object shall contain the personality nonce from a drive.
pub const GetPersonalityNonceResponse = struct {
    /// The personality nonce.
    ///
    /// This property shall contain the personality nonce as a hex-encoded string as defined by the 'CDP Random Nonce Data' clause of the NVMe Base Specification.
    Nonce: ?[]const u8 = null,
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the chassis to which this storage subsystem is attached.
    ///
    /// This property shall contain an array of links to resources of type `Chassis` that represent the physical containers attached to this resource.
    Enclosures: ?[]const core.NavProperty(chassis.Chassis) = null,
    /// How many members `Enclosures` has in total, which is not how many this response carries.
    @"Enclosures@odata.count": ?i64 = null,
    /// The next page of `Enclosures`. Present only when this response left members out.
    @"Enclosures@odata.nextLink": ?core.ODataId = null,
    /// The link to the simple storage instance that corresponds to this storage.
    ///
    /// This property shall contain a link to a resource of type `SimpleStorage` that represents the same storage subsystem as this resource.
    SimpleStorage: ?core.NavProperty(simple_storage.SimpleStorage) = null,
    /// An array of links to the storage services that connect to this storage subsystem.
    ///
    /// This property shall contain an array of links to resources of type `StorageService` with which this storage subsystem is associated.
    StorageServices: ?[]const core.NavProperty(storage_service.StorageService) = null,
    /// How many members `StorageServices` has in total, which is not how many this response carries.
    @"StorageServices@odata.count": ?i64 = null,
    /// The next page of `StorageServices`. Present only when this response left members out.
    @"StorageServices@odata.nextLink": ?core.ODataId = null,
    /// The storage systems that host this storage subsystem.
    ///
    /// This property shall contain an array of links to resources of type `ComputerSystem` that represent the storage systems that host this storage subsystem.  The members of this array shall be in the `StorageSystems` resource collection off the service root.
    HostingStorageSystems: ?[]const core.NavProperty(computer_system.ComputerSystem) = null,
    /// How many members `HostingStorageSystems` has in total, which is not how many this response carries.
    @"HostingStorageSystems@odata.count": ?i64 = null,
    /// The next page of `HostingStorageSystems`. Present only when this response left members out.
    @"HostingStorageSystems@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the discovery subsystems that discovered this subsystem in an NVMe-oF environment.
    ///
    /// This property shall contain an array of links to resources of type `Storage` that represent the discovery subsystems that discovered this subsystem in an NVMe-oF environment.
    NVMeoFDiscoverySubsystems: ?[]const core.NavProperty(Storage) = null,
    /// How many members `NVMeoFDiscoverySubsystems` has in total, which is not how many this response carries.
    @"NVMeoFDiscoverySubsystems@odata.count": ?i64 = null,
    /// The next page of `NVMeoFDiscoverySubsystems`. Present only when this response left members out.
    @"NVMeoFDiscoverySubsystems@odata.nextLink": ?core.ODataId = null,
    /// The set of drives in this storage subsystem that do not support blocking of TCG-defined security ID (SID).
    ///
    /// This property shall contain an array of links to resources of type `Drive` that represent the drives in this storage subsystem that do not support the TCG-defined 'Block SID' command.  This property should only be present if at least one drive does not support the TCG-defined 'Block SID' command.
    BlockSecurityIDUnsupportedDrives: ?[]const core.NavProperty(drive.Drive) = null,
    /// How many members `BlockSecurityIDUnsupportedDrives` has in total, which is not how many this response carries.
    @"BlockSecurityIDUnsupportedDrives@odata.count": ?i64 = null,
    /// The next page of `BlockSecurityIDUnsupportedDrives`. Present only when this response left members out.
    @"BlockSecurityIDUnsupportedDrives@odata.nextLink": ?core.ODataId = null,
    /// The set of drives in this storage subsystem for which the most recent attempt to block TCG-defined security ID (SID) failed.
    ///
    /// This property shall contain an array of links to resources of type `Drive` that represent the drives in this storage subsystem for which the most recent attempt to block TCG-defined security ID (SID) with the TCG-defined 'Block SID' command failed.  This property should only be present if the most recent attempt to block TCG-defined security ID (SID) with the TCG-defined 'Block SID' command failed for at least one drive.
    BlockSecurityIDUpdateUnsuccessfulDrives: ?[]const core.NavProperty(drive.Drive) = null,
    /// How many members `BlockSecurityIDUpdateUnsuccessfulDrives` has in total, which is not how many this response carries.
    @"BlockSecurityIDUpdateUnsuccessfulDrives@odata.count": ?i64 = null,
    /// The next page of `BlockSecurityIDUpdateUnsuccessfulDrives`. Present only when this response left members out.
    @"BlockSecurityIDUpdateUnsuccessfulDrives@odata.nextLink": ?core.ODataId = null,
    /// The set of volumes in this storage subsystem that are not assigned to a physical function.
    ///
    /// This property shall contain an array of links to resources of type `Volume` that represent the volumes in this storage subsystem that are not assigned to a physical function.  This property should only be present if the storage subsystem supports the use of multiple physical functions to parallelize data transfer.
    UnassignedVolumes: ?[]const core.NavProperty(volume.Volume) = null,
    /// How many members `UnassignedVolumes` has in total, which is not how many this response carries.
    @"UnassignedVolumes@odata.count": ?i64 = null,
    /// The next page of `UnassignedVolumes`. Present only when this response left members out.
    @"UnassignedVolumes@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Storage.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Multiple physical function-related properties for storage controllers in a storage subsystem.
///
/// This type shall contain multiple physical function-related properties for storage controllers in a storage subsystem.
pub const Mpf = struct {
    /// The maximum number of physical functions supported by the storage controller in this storage subsystem.
    ///
    /// This property shall contain the maximum number of physical functions supported by the storage controller in this storage subsystem.
    MaximumSupportedPhysicalFunctions: ?i64 = null,
    /// The current number of physical functions configured for the storage controller in this storage subsystem.
    ///
    /// This property shall contain the current number of physical functions configured for the storage controller in this storage subsystem.
    ConfiguredPhysicalFunctions: ?i64 = null,
    /// The current volume assignment policy configured for the storage controller in this storage subsystem.
    ///
    /// This property shall contain the current volume assignment policy configured for the storage controller in this storage subsystem.
    VolumeAssignmentPolicy: ?VolumeAssignmentPolicy = null,
};

/// What a client may change of `Storage.MPF`.
///
/// Multiple physical function-related properties for storage controllers in a storage subsystem.
pub const MpfUpdate = struct {
    /// The current number of physical functions configured for the storage controller in this storage subsystem.
    ///
    /// This property shall contain the current number of physical functions configured for the storage controller in this storage subsystem.
    ConfiguredPhysicalFunctions: core.Nullable(i64) = .absent,
    /// The current volume assignment policy configured for the storage controller in this storage subsystem.
    ///
    /// This property shall contain the current volume assignment policy configured for the storage controller in this storage subsystem.
    VolumeAssignmentPolicy: core.Nullable(VolumeAssignmentPolicy) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The configurable features that are able to be locked on an NVMe subsystem and their current lock state.
///
/// This type shall contain the configurable features that are able to be locked on an NVMe subsystem and their current lock state.
pub const NvmeConfigurationLockState = struct {
    /// The lock state of the NVMe-defined Firmware Commit command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Firmware Commit command.
    FirmwareCommit: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Lockdown command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Lockdown command.
    Lockdown: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Security Send command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Security Send command.
    SecuritySend: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Firmware Image Download command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Firmware Image Download command.
    FirmwareImageDownload: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-MI-defined VPD Write command.
    ///
    /// This property shall contain the lock state of the NVMe-MI-defined VPD Write command.
    VPDWrite: ?ConfigLockOptions = null,
};

/// An active personality setting of an NVMe subsystem.
///
/// This type shall contain an active personality setting of an NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
pub const NvmePersonality = struct {
    /// The personality identifier.
    ///
    /// This property shall contain the personality identifier as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.
    Identifier: ?i64 = null,
    /// An indication of whether this personality is set to the manufacturing default values.
    ///
    /// This property shall indicate whether this personality is set to the manufacturing default values as defined by the 'Personality Manufacturing Default Settings State' field in the NVMe Base Specification.
    Default: ?bool = null,
    /// An indication of whether this personality is frozen.
    ///
    /// This property shall indicate whether this personality is frozen as defined by the 'Personality Freeze State' field in the NVMe Base Specification.
    Frozen: ?bool = null,
    /// An indication of whether this personality contains pending changes.
    ///
    /// This property shall indicate whether this personality contains pending changes as defined by the 'Pending Personality Settings Change' field in the NVMe Base Specification.
    Pending: ?bool = null,
    /// The minimum reset type required to change the personality settings.
    ///
    /// This property shall contain the minimum reset type required to change the personality settings as defined by the 'Minimum Required Reset Type' field in the NVMe Base Specification.
    MinimumRequiredResetType: ?NmveMinimumRequiredResetType = null,
    /// An indication of whether changing this personality affects user data.
    ///
    /// This property shall indicate whether changing this personality affects user data as defined by the 'Personality Settings Change User Data Effect' field in the NVMe Base Specification.
    ChangeAffectsUserData: ?bool = null,
    /// The authentication modes supported to unfreeze this personality.
    ///
    /// This property shall contain the authentication modes supported to unfreeze this personality as defined by the 'Authenticated Unfreeze Support' field in the NVMe Base Specification.
    UnfreezeAuthenticationModes: ?[]const ?NmveUnfreezeAuthMode = null,
    /// The personality data.
    ///
    /// This property shall contain the personality data as a hex-encoded string.
    Data: ?[]const u8 = null,
};

/// What a client may change of `Storage.NVMePersonality`.
///
/// An active personality setting of an NVMe subsystem.
pub const NvmePersonalityUpdate = struct {
    /// The personality data.
    ///
    /// This property shall contain the personality data as a hex-encoded string.
    Data: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const NvmeSubsystemProperties = struct {
    /// The maximum number of namespace attachments supported by this NVMe subsystem.
    ///
    /// This property shall contain the maximum number of namespace attachments supported by this NVMe subsystem.  If no maximum is specified, this property should not be implemented.
    MaxNamespacesSupported: ?core.Decimal = null,
    /// Indicates whether the subsystem supports shared namespace controller attachment, allowing a shared namespace to be attached concurrently to two or more controllers in an NVMe subsystem.
    ///
    /// This property shall indicate whether the subsystem supports shared namespace controller attachment, allowing a shared namespace to be attached concurrently to two or more controllers in an NVMe subsystem.
    SharedNamespaceControllerAttachmentSupported: ?bool = null,
    /// The configurable features that are able to be locked from in-band usage on an NVMe subsystem and their current lock state.
    ///
    /// This property shall contain the configurable features that are able to be locked from in-band usage on an NVMe subsystem and their current lock state.
    ConfigurationLockState: ?NvmeConfigurationLockState = null,
    /// The active personality settings of the NVMe subsystem.
    ///
    /// This property shall contain the active personality settings of the NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
    ActivePersonalities: ?[]const ?NvmePersonality = null,
};

/// What a client may change of `Storage.NVMeSubsystemProperties`.
pub const NvmeSubsystemPropertiesUpdate = struct {
    /// The active personality settings of the NVMe subsystem.
    ///
    /// This property shall contain the active personality settings of the NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
    ActivePersonalities: ?[]const ?NvmePersonalityUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// This type describes the various controller rates used for processes such as volume rebuild or consistency checks.
///
/// This type shall contain all the rate settings available on the controller.
pub const Rates = struct {
    /// The percentage of controller resources used for rebuilding/repairing volumes.
    ///
    /// This property shall contain the percentage of controller resources used for rebuilding volumes.
    RebuildRatePercent: ?i64 = null,
    /// The percentage of controller resources used for transforming volumes from one configuration to another.
    ///
    /// This property shall contain the percentage of controller resources used for transforming volumes.
    TransformationRatePercent: ?i64 = null,
    /// The percentage of controller resources used for performing a data consistency check on volumes.
    ///
    /// This property shall contain the percentage of controller resources used for checking data consistency on volumes.
    ConsistencyCheckRatePercent: ?i64 = null,
};

/// What a client may change of `Storage.Rates`.
///
/// This type describes the various controller rates used for processes such as volume rebuild or consistency checks.
pub const RatesUpdate = struct {
    /// The percentage of controller resources used for rebuilding/repairing volumes.
    ///
    /// This property shall contain the percentage of controller resources used for rebuilding volumes.
    RebuildRatePercent: core.Nullable(i64) = .absent,
    /// The percentage of controller resources used for transforming volumes from one configuration to another.
    ///
    /// This property shall contain the percentage of controller resources used for transforming volumes.
    TransformationRatePercent: core.Nullable(i64) = .absent,
    /// The percentage of controller resources used for performing a data consistency check on volumes.
    ///
    /// This property shall contain the percentage of controller resources used for checking data consistency on volumes.
    ConsistencyCheckRatePercent: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const StorageControllerActions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?StorageControllerOemActions = null,
};

/// What a client may change of `Storage.StorageControllerActions`.
///
/// The available actions for this resource.
pub const StorageControllerActionsUpdate = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?StorageControllerOemActionsUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const StorageControllerLinks = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the endpoints that connect to this controller.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` with which this controller is associated.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `Endpoints` has in total, which is not how many this response carries.
    @"Endpoints@odata.count": ?i64 = null,
    /// The next page of `Endpoints`. Present only when this response left members out.
    @"Endpoints@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the storage services that connect to this controller.
    ///
    /// This property shall contain an array of links to resources of type `StorageService` with which this controller is associated.
    ///
    /// Deprecated in v1_9_0.
    /// This property has been deprecated in favor of `StorageServices` within the `Links` property at the root level.
    StorageServices: ?[]const core.NavProperty(storage_service.StorageService) = null,
    /// How many members `StorageServices` has in total, which is not how many this response carries.
    @"StorageServices@odata.count": ?i64 = null,
    /// The next page of `StorageServices`. Present only when this response left members out.
    @"StorageServices@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the PCIe functions that the storage controller produces.
    ///
    /// This property shall contain an array of links to resources of type `PCIeFunction` that represent the PCIe functions associated with this resource.
    PCIeFunctions: ?[]const core.NavProperty(pcie_function.PcieFunction) = null,
    /// How many members `PCIeFunctions` has in total, which is not how many this response carries.
    @"PCIeFunctions@odata.count": ?i64 = null,
    /// The next page of `PCIeFunctions`. Present only when this response left members out.
    @"PCIeFunctions@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Storage.StorageControllerLinks`.
pub const StorageControllerLinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const StorageControllerOemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// What a client may change of `Storage.StorageControllerOemActions`.
///
/// The available OEM-specific actions for this resource.
pub const StorageControllerOemActionsUpdate = struct {
    /// Members to send that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `Storage` schema defines a storage subsystem and its respective properties.  A storage subsystem represents a set of physical or virtual storage controllers and the resources, such as volumes, that can be accessed from that subsystem.
///
/// This resource shall represent a storage subsystem in the Redfish Specification.
pub const Storage = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The durable names for the storage subsystem.
    ///
    /// This property shall contain a list of all known durable names for the storage subsystem.
    Identifiers: ?[]const resource.Identifier = null,
    /// The policy under which hot spare drives in this storage domain will activate.
    ///
    /// This property shall contain the policy under which all drives operating as hot spares in this storage domain will activate.
    HotspareActivationPolicy: ?HotspareActivationPolicy = null,
    /// The encryption mode of this storage subsystem.
    ///
    /// This property shall contain the encryption mode of this storage subsystem.
    EncryptionMode: ?EncryptionMode = null,
    /// The local encryption key identifier used by the storage subsystem.
    ///
    /// This property shall contain the local encryption key identifier used by the storage subsystem when `EncryptionMode` contains `UseLocalKey`.
    LocalEncryptionKeyIdentifier: ?[]const u8 = null,
    /// Indicates if this storage subsystem automatically creates new volumes for unassigned drives.
    ///
    /// This property shall indicate if volumes are created automatically for each unassigned drive attached to this storage subsystem.
    AutoVolumeCreate: ?AutoVolumeCreate = null,
    /// Information specific to NVMe subsystems.
    ///
    /// This property shall contain information specific to NVMe subsystems.  This property shall only be present if this resource represents an NVMe subsystem.
    NVMeSubsystemProperties: ?NvmeSubsystemProperties = null,
    /// Indicates whether in-band configuration requests to the storage subsystem are locked.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall indicate whether configuration requests to the storage subsystem are locked.  Services shall reject modification requests that contain the value `Partial`.  Modifying the value of this property may affect the `ConfigurationLock` property in `Drive` resources referenced by the `Drives` property.
    ConfigurationLock: ?ConfigurationLock = null,
    /// Indicates the target configuration lock level for the drive based upon the state of the `ConfigurationLock` property.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall contain the target configuration lock level for the drive.  For NVMe subsystems, services shall implement the locking requirements specified by SNIA's Swordfish NVMe Model Overview and Mapping Guide.
    TargetConfigurationLockLevel: ?TargetConfigurationLockLevel = null,
    /// Indicates if the storage controller sends a command to block establishment of a TCG-defined security ID (SID) during each drive boot sequence for drives that support it.
    ///
    /// This property shall indicate if the storage controller sends the TCG-defined 'Block SID' command to block establishment of a TCG-defined security ID (SID) during each drive boot sequence for drives that support it.  The value `true` shall indicate the TCG-defined 'Block SID' command is sent to supporting drives during each drive boot sequence.
    BlockSecurityIDPolicy: ?bool = null,
    /// The multiple physical function-related properties for storage controllers in this storage subsystem.
    ///
    /// This property shall contain multiple physical function-related properties for storage controllers in this storage subsystem.  This property should only be present if the storage subsystem supports the use of multiple physical functions to parallelize data transfer.
    MPF: ?Mpf = null,
    /// The set of storage controllers that this resource represents.
    ///
    /// This property shall contain a set of the storage controllers that this resource represents.
    ///
    /// Deprecated in v1_13_0.
    /// This property has been deprecated in favor of `Controllers` to allow for storage controllers to be represented as their own resources.
    StorageControllers: ?[]const core.NavProperty(StorageController) = null,
    /// How many members `StorageControllers` has in total, which is not how many this response carries.
    @"StorageControllers@odata.count": ?i64 = null,
    /// The next page of `StorageControllers`. Present only when this response left members out.
    @"StorageControllers@odata.nextLink": ?core.ODataId = null,
    /// The set of drives attached to the storage controllers that this resource represents.
    ///
    /// This property shall contain a set of the drives attached to the storage controllers that this resource represents.
    Drives: ?[]const core.NavProperty(drive.Drive) = null,
    /// How many members `Drives` has in total, which is not how many this response carries.
    @"Drives@odata.count": ?i64 = null,
    /// The next page of `Drives`. Present only when this response left members out.
    @"Drives@odata.nextLink": ?core.ODataId = null,
    /// The set of volumes that the storage controllers produce.
    ///
    /// This property shall contain a link to a resource collection of type `VolumeCollection`.
    Volumes: ?core.NavProperty(volume_collection.VolumeCollection) = null,
    /// Redundancy information for the storage subsystem.
    ///
    /// This property shall contain redundancy information for the storage subsystem.
    Redundancy: ?[]const core.NavProperty(redundancy.Redundancy) = null,
    /// How many members `Redundancy` has in total, which is not how many this response carries.
    @"Redundancy@odata.count": ?i64 = null,
    /// The next page of `Redundancy`. Present only when this response left members out.
    @"Redundancy@odata.nextLink": ?core.ODataId = null,
    /// All file systems that are allocated by this storage subsystem.
    ///
    /// This property shall contain a link to a resource collection of type `FileSystemCollection`.  This property shall be used when file systems are shared or exported by the storage subsystem.
    FileSystems: ?core.NavProperty(file_system_collection.FileSystemCollection) = null,
    /// The set of all storage pools that are allocated by this storage subsystem.  A storage pool is the set of storage capacity that can be used to produce volumes or other storage pools.
    ///
    /// This property shall contain a link to a resource collection of type `StoragePoolCollection`.  This property shall be used when an abstraction of media, rather than references to individual media, are used as the storage data source.
    StoragePools: ?core.NavProperty(storage_pool_collection.StoragePoolCollection) = null,
    /// All of the storage groups, each of which contains a set of volumes and endpoints that are managed as a group for mapping and masking, that belong to this storage subsystem.
    ///
    /// This property shall contain a link to a resource collection of type `StorageGroupsCollection`.  This property shall be used when implementing mapping and masking.
    ///
    /// Deprecated in v1_15_0.
    /// This property was deprecated in favor of the `Connections` property.
    StorageGroups: ?core.NavProperty(storage_group_collection.StorageGroupCollection) = null,
    /// All of the endpoint groups, each of which contains a set of endpoints that are used for a common purpose such as an ACL or logical identification, that belong to this storage subsystem.
    ///
    /// This property shall contain a link to a resource collection of type `EndpointGroupCollection`.  This property shall be implemented when atomic control is needed to perform mapping, masking, and zoning operations.
    EndpointGroups: ?core.NavProperty(endpoint_group_collection.EndpointGroupCollection) = null,
    /// The consistency groups, each of which contains a set of volumes that are treated by an application or set of applications as a single resource, that are managed by this storage subsystem.
    ///
    /// This property shall contain a link to a resource collection of type `ConsistencyGroupCollection`.  The property shall be used when groups of volumes are treated as a single resource by an application or set of applications.
    ConsistencyGroups: ?core.NavProperty(consistency_group_collection.ConsistencyGroupCollection) = null,
    /// The set of controllers instantiated by this storage subsystem.
    ///
    /// This property shall contain a link to a resource collection of type `StorageControllerCollection` that contains the set of storage controllers allocated to this storage subsystem.
    Controllers: ?core.NavProperty(storage_controller_collection.StorageControllerCollection) = null,
    /// The collection of links to the connections that this storage subsystem contains.
    ///
    /// This property shall contain a link to a resource collection of type `ConnectionCollection`.  The members of this collection shall reference Connection resources subordinate to Fabric resources.
    Connections: ?core.NavProperty(connection_collection.ConnectionCollection) = null,
    /// The link to the metrics associated with this storage subsystem.
    ///
    /// This property shall contain a link to a resource of type `StorageMetrics` that represents the metrics associated with this storage subsystem.
    Metrics: ?core.NavProperty(storage_metrics.StorageMetrics) = null,
};

/// What a client may change of `Storage.Storage`.
///
/// The `Storage` schema defines a storage subsystem and its respective properties.  A storage subsystem represents a set of physical or virtual storage controllers and the resources, such as volumes, that can be accessed from that subsystem.
pub const StorageUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The policy under which hot spare drives in this storage domain will activate.
    ///
    /// This property shall contain the policy under which all drives operating as hot spares in this storage domain will activate.
    HotspareActivationPolicy: core.Nullable(HotspareActivationPolicy) = .absent,
    /// The encryption mode of this storage subsystem.
    ///
    /// This property shall contain the encryption mode of this storage subsystem.
    EncryptionMode: core.Nullable(EncryptionMode) = .absent,
    /// Indicates if this storage subsystem automatically creates new volumes for unassigned drives.
    ///
    /// This property shall indicate if volumes are created automatically for each unassigned drive attached to this storage subsystem.
    AutoVolumeCreate: core.Nullable(AutoVolumeCreate) = .absent,
    /// Information specific to NVMe subsystems.
    ///
    /// This property shall contain information specific to NVMe subsystems.  This property shall only be present if this resource represents an NVMe subsystem.
    NVMeSubsystemProperties: core.Nullable(NvmeSubsystemPropertiesUpdate) = .absent,
    /// Indicates whether in-band configuration requests to the storage subsystem are locked.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall indicate whether configuration requests to the storage subsystem are locked.  Services shall reject modification requests that contain the value `Partial`.  Modifying the value of this property may affect the `ConfigurationLock` property in `Drive` resources referenced by the `Drives` property.
    ConfigurationLock: core.Nullable(ConfigurationLock) = .absent,
    /// Indicates the target configuration lock level for the drive based upon the state of the `ConfigurationLock` property.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall contain the target configuration lock level for the drive.  For NVMe subsystems, services shall implement the locking requirements specified by SNIA's Swordfish NVMe Model Overview and Mapping Guide.
    TargetConfigurationLockLevel: core.Nullable(TargetConfigurationLockLevel) = .absent,
    /// Indicates if the storage controller sends a command to block establishment of a TCG-defined security ID (SID) during each drive boot sequence for drives that support it.
    ///
    /// This property shall indicate if the storage controller sends the TCG-defined 'Block SID' command to block establishment of a TCG-defined security ID (SID) during each drive boot sequence for drives that support it.  The value `true` shall indicate the TCG-defined 'Block SID' command is sent to supporting drives during each drive boot sequence.
    BlockSecurityIDPolicy: core.Nullable(bool) = .absent,
    /// The multiple physical function-related properties for storage controllers in this storage subsystem.
    ///
    /// This property shall contain multiple physical function-related properties for storage controllers in this storage subsystem.  This property should only be present if the storage subsystem supports the use of multiple physical functions to parallelize data transfer.
    MPF: core.Nullable(MpfUpdate) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The StorageController schema describes a storage controller and its properties.  A storage controller represents a physical or virtual storage device that produces volumes.
///
/// This resource shall represent a storage controller in the Redfish Specification.
pub const StorageController = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The unique identifier for the member within an array.
    ///
    /// This property shall contain the unique identifier for this member within an array.  For services supporting Redfish v1.6 or higher, this value shall contain the zero-based array index.
    MemberId: ?[]const u8 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The maximum speed of the storage controller's device interface.
    ///
    /// This property shall represent the maximum supported speed of the storage bus interface, in Gbit/s.  The specified interface connects the controller to the storage devices, not the controller to a host.  For example, SAS bus not PCIe host bus.
    SpeedGbps: ?core.Decimal = null,
    /// The firmware version of this storage controller.
    ///
    /// This property shall contain the firmware version as defined by the manufacturer for the associated storage controller.
    FirmwareVersion: ?[]const u8 = null,
    /// The manufacturer of this storage controller.
    ///
    /// This property shall contain the name of the organization responsible for producing the storage controller.  This organization may be the entity from which the storage controller is purchased, but this is not necessarily true.
    Manufacturer: ?[]const u8 = null,
    /// The model number for the storage controller.
    ///
    /// This property shall contain the name by which the manufacturer generally refers to the storage controller.
    Model: ?[]const u8 = null,
    /// The SKU for this storage controller.
    ///
    /// This property shall contain the stock-keeping unit number for this storage controller.
    SKU: ?[]const u8 = null,
    /// The serial number for this storage controller.
    ///
    /// This property shall contain a manufacturer-allocated number that identifies the storage controller.
    SerialNumber: ?[]const u8 = null,
    /// The part number for this storage controller.
    ///
    /// This property shall contain a part number assigned by the organization that is responsible for producing or manufacturing the storage controller.
    PartNumber: ?[]const u8 = null,
    /// The user-assigned asset tag for this storage controller.
    ///
    /// This property shall track the storage controller for inventory purposes.
    AssetTag: ?[]const u8 = null,
    /// The supported set of protocols for communicating with this storage controller.
    ///
    /// This property shall contain the supported set of protocols for communicating with this storage controller.
    SupportedControllerProtocols: ?[]const protocol.Protocol = null,
    /// The protocols that the storage controller can use to communicate with attached devices.
    ///
    /// This property shall contain the set of protocols this storage controller can use to communicate with attached devices.
    SupportedDeviceProtocols: ?[]const protocol.Protocol = null,
    /// The durable names for the storage controller.
    ///
    /// This property shall contain a list of all known durable names for the associated storage controller.
    Identifiers: ?[]const resource.Identifier = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?StorageControllerLinks = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?StorageControllerActions = null,
    /// The name of the storage controller.
    ///
    /// This property shall contain the name of the storage controller.
    Name: ?[]const u8 = null,
    /// The location of the storage controller.
    ///
    /// This property shall contain the location information of the associated storage controller.
    Location: ?resource.Location = null,
    /// The cache memory of the storage controller in general detail.
    ///
    /// This property shall contain properties that describe the cache memory for this resource.
    CacheSummary: ?CacheSummary = null,
    /// The PCIe interface details for this controller.
    ///
    /// This property shall contain details on the PCIe interface that connects this PCIe-based controller to its host.
    PCIeInterface: ?pcie_device.PcieInterface = null,
    /// The set of RAID types supported by the storage controller.
    ///
    /// This property shall contain an array of all the RAID types supported by this controller.
    SupportedRAIDTypes: ?[]const ?volume.RaidType = null,
    /// This property describes the various controller rates used for processes such as volume rebuild or consistency checks.
    ///
    /// This object shall contain all the rate settings available on the controller.
    ControllerRates: ?Rates = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_12_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// The link to the assembly associated with this storage controller.
    ///
    /// This property shall contain a link to a resource of type `Assembly`.
    Assembly: ?core.NavProperty(assembly.Assembly) = null,
    /// The link to the collection of ports that exist on the storage controller.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection`.
    Ports: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
};

test {
    std.testing.refAllDecls(@This());
}
