//! Namespace `MetricDefinition`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const physical_context = @import("physical_context.zig");
const resource = @import("resource.zig");

/// The types of calculations that can be applied to the metric reading.  Provides information to the client on the suitability of calculation using the metric reading.
///
/// The type shall describe the types of calculations that can be applied to the metric reading.
pub const Calculable = enum {
    /// No calculations should be performed on the metric reading.
    NonCalculatable,
    /// The sum of the metric reading across multiple instances is meaningful.
    Summable,
    /// The sum of the metric reading across multiple instances is not meaningful.
    NonSummable,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const CalculationAlgorithmEnum = enum {
    /// The metric is calculated as the average metric reading over a sliding time interval.
    ///
    /// The metric shall be calculated as the average metric reading over a sliding time interval.  The time interval shall contain the `CalculationTimeInterval` property value.
    Average,
    /// The metric is calculated as the maximum metric reading over during a time interval.
    ///
    /// The metric shall be calculated as the maximum metric reading over a sliding time interval.  The time interval shall contain the `CalculationTimeInterval` property value.
    Maximum,
    /// The metric is calculated as the minimum metric reading over a sliding time interval.
    ///
    /// The metric shall be calculated as the minimum metric reading over a sliding time interval.  The time interval shall contain the `CalculationTimeInterval` property value.
    Minimum,
    /// The metric is calculated as specified by an OEM.
    ///
    /// The metric shall be calculated as specified by an OEM.  The `OEMCalculationAlgorithm` property shall contain the specific OEM calculation algorithm.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ImplementationType = enum {
    /// The metric is implemented as a physical sensor.
    PhysicalSensor,
    /// The metric is implemented by applying a calculation on another metric property.  The calculation is specified in the `CalculationAlgorithm` property.
    Calculated,
    /// The metric is implemented by applying a calculation on one or more metric properties.  The calculation is not provided.
    Synthesized,
    /// The metric is implemented as digital meter.
    DigitalMeter,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The data type of the related metric values.  Provides information to the client on the nature of the metric reading.
///
/// This type shall describe the data type of the related metric values as defined by JSON data types.
pub const MetricDataType = enum {
    /// The JSON boolean definition.
    Boolean,
    /// The JSON string definition with the date-time format.
    DateTime,
    /// The JSON decimal definition.
    Decimal,
    /// The JSON integer definition.
    Integer,
    /// The JSON string definition.
    String,
    /// The JSON string definition with a set of defined enumerations.
    Enumeration,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The type of metric.  Provides information to the client on how the metric can be handled.
///
/// This property shall contain the type of metric.
pub const MetricType = enum {
    /// The metric is a numeric metric.  The metric value is any real number.
    Numeric,
    /// The metric is a discrete metric.  The metric value is discrete.  The possible values are listed in the `DiscreteValues` property.
    ///
    /// The metric values shall indicate discrete states.
    Discrete,
    /// The metric is a gauge metric.  The metric value is a real number.  When the metric value reaches the gauge's extrema, it stays at that value, until the reading falls within the extrema.
    Gauge,
    /// The metric is a counter metric.  The metric reading is a non-negative integer that increases monotonically.  When a counter reaches its maximum, the value resets to 0 and resumes counting.
    Counter,
    /// The metric is a countdown metric.  The metric reading is a non-negative integer that decreases monotonically.  When a counter reaches its minimum, the value resets to preset value and resumes counting down.
    Countdown,
    /// The metric is a non-discrete string metric.  The metric reading is a non-discrete string that displays some non-discrete, non-numeric data.
    String,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The parameters for a metric calculation.
///
/// This type shall contain the parameters for a metric calculation.
pub const CalculationParamsType = struct {
    /// The URI with wildcards and property identifiers of the metric property used as the input into the calculation.  If the URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a URI with wildcards and property identifiers of the metric property used as the input into the calculation.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall identify a resource property to which the metric definition applies.  The property identifiers portion of the URI shall follow RFC6901-defined JSON pointer notation rules.
    SourceMetric: ?[]const u8 = null,
    /// The URI with wildcards and property identifiers of the metric property that stores the result of the calculation.  If the URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a URI with wildcards and property identifiers of the metric property that stores the result of the calculation.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall identify a resource property to which the metric definition applies.  The property identifiers portion of the URI shall follow RFC6901-defined JSON pointer notation rules.
    ResultMetric: ?[]const u8 = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The wildcard and its substitution values.
///
/// This property shall contain a wildcard and its substitution values.
pub const Wildcard = struct {
    /// The string used as a wildcard.
    ///
    /// This property shall contain the string used as a wildcard.
    Name: ?[]const u8 = null,
    /// An array of values to substitute for the wildcard.  `*` indicates all possible values for the wildcard.
    ///
    /// This property shall contain the list of values to substitute for the wildcard.  The value `*` shall indicate all possible values for the wildcard.
    Values: ?[]const ?[]const u8 = null,
};

/// The `MetricDefinition` schema describes the metadata information for a metric.
///
/// This resource shall contain the metadata information for a metric in a Redfish implementation.
pub const MetricDefinition = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The type of metric.
    ///
    /// This property shall specify the type of metric.
    MetricType: ?MetricType = null,
    /// The data type of the metric.
    ///
    /// This property shall specify the data-type of the metric.
    MetricDataType: ?MetricDataType = null,
    /// The units of measure for this metric.
    ///
    /// This property shall specify the units of the metric.  This property shall be consistent with the case-sensitive ('C/s' column) Unified Code for Units of Measure.  Note: Not all units of measure are covered by UCUM.
    Units: ?[]const u8 = null,
    /// The implementation of the metric.
    ///
    /// This property shall specify the implementation of the metric.
    Implementation: ?ImplementationType = null,
    /// An indication of whether the metric can be used in a calculation.
    ///
    /// This property shall specify whether the metric can be used in a calculation.
    Calculable: ?Calculable = null,
    /// An indication of whether the metric values are linear versus non-linear.
    ///
    /// This property shall indicate whether the metric values are linear versus non-linear.  Linear metrics can use a greater than relation to compared them.  An example of linear metrics include performance metrics.  Examples of non-linear metrics include error codes.
    IsLinear: ?bool = null,
    /// The wildcards and their substitution values for the entries in the `MetricProperties` array property.
    ///
    /// The property shall contain a list of wildcards and their replacement strings, which are applied to the `MetricProperties` array property.  Each wildcard shall have a corresponding entry in this array property.
    Wildcards: ?[]const Wildcard = null,
    /// The list of URIs with wildcards and property identifiers that this metric definition defines.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This array property shall list the URIs with wildcards and property identifiers that this metric defines.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall identify a resource property to which the metric definition applies.  The property identifiers portion of the URI shall follow RFC6901-defined JSON pointer notation rules.  This property should not be present if `ImplementationType` contains `Synthesized` or `Calculated`.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The metric properties that are part of a calculation that this metric definition defines.
    ///
    /// This property shall list the metric properties that are part of a calculation that this metric definition defines.  This property should be present if `ImplementationType` contains `Synthesized` or `Calculated`.
    CalculationParameters: ?[]const ?CalculationParamsType = null,
    /// The physical context of the metric.
    ///
    /// This property shall contain the physical context of the metric.
    PhysicalContext: ?physical_context.PhysicalContext = null,
    /// The time interval between when a metric is updated.
    ///
    /// This property shall specify the frequency at which metric values are included in any `MetricReport` referencing this metric.
    SensingInterval: ?core.Duration = null,
    /// This array property specifies possible values of a discrete metric.
    ///
    /// The values of the property shall specify the possible values of the discrete metric.  This property shall have values when the `MetricType` property is `Discrete`.
    DiscreteValues: ?[]const ?[]const u8 = null,
    /// Number of significant digits in the metric reading.
    ///
    /// This property shall specify the number of significant digits in the metric reading.  The property is not meaningful when the `MetricType` property is `Discrete`.
    Precision: ?i64 = null,
    /// The estimated percent error of measured versus actual values.
    ///
    /// This property shall contain the percent error +/- of the measured versus actual values.  The property is not meaningful when the `MetricType` property is `Discrete`.
    Accuracy: ?core.Decimal = null,
    /// The calibration offset added to the metric reading.
    ///
    /// This property shall contain the calibration offset added to the metric reading.  The value shall have the units specified in the `Units` property.  The property is not meaningful when the `MetricType` property is `Discrete`.
    Calibration: ?core.Decimal = null,
    /// The accuracy of the timestamp.
    ///
    /// This property shall specify the expected + or - variability of the timestamp.
    TimestampAccuracy: ?core.Duration = null,
    /// Minimum value for metric reading.
    ///
    /// This property shall contain the lowest possible value for the metric reading.  The value shall have the units specified in the property Units.  The property is not meaningful when the `MetricType` property is `Discrete`.
    MinReadingRange: ?core.Decimal = null,
    /// Maximum value for metric reading.
    ///
    /// The value shall indicate the highest possible value for a related MetricValue.  The value shall have the units specified in the property Units.  The property is not meaningful when the `MetricType` property is `Discrete`.
    MaxReadingRange: ?core.Decimal = null,
    /// The calculation that is performed on a source metric to obtain the metric being defined.
    ///
    /// This property shall contain the calculation performed to obtain the metric.
    CalculationAlgorithm: ?CalculationAlgorithmEnum = null,
    /// The time interval over which the metric calculation is performed.
    ///
    /// This property shall specify the time interval over the metric calculation is performed.
    CalculationTimeInterval: ?core.Duration = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The OEM-defined calculation that is performed on a source metric to obtain the metric being defined.
    ///
    /// This property shall contain the OEM-defined calculation performed to obtain the metric.  This property shall be present if `CalculationAlgorithm` is `OEM`.
    OEMCalculationAlgorithm: ?[]const u8 = null,
    /// The logical contexts related to the metric.
    ///
    /// This property shall contain the logical contexts related to the metric.  This property should be present when the `PhysicalContext` property does not provide complete information and additional context information is needed.  For example, if the metric refers to capacity or performance.
    LogicalContexts: ?[]const physical_context.LogicalContext = null,
};

/// What a client may change of `MetricDefinition.MetricDefinition`.
///
/// The `MetricDefinition` schema describes the metadata information for a metric.
pub const MetricDefinitionUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The type of metric.
    ///
    /// This property shall specify the type of metric.
    MetricType: core.Nullable(MetricType) = .absent,
    /// The data type of the metric.
    ///
    /// This property shall specify the data-type of the metric.
    MetricDataType: core.Nullable(MetricDataType) = .absent,
    /// The units of measure for this metric.
    ///
    /// This property shall specify the units of the metric.  This property shall be consistent with the case-sensitive ('C/s' column) Unified Code for Units of Measure.  Note: Not all units of measure are covered by UCUM.
    Units: core.Nullable([]const u8) = .absent,
    /// An indication of whether the metric can be used in a calculation.
    ///
    /// This property shall specify whether the metric can be used in a calculation.
    Calculable: core.Nullable(Calculable) = .absent,
    /// An indication of whether the metric values are linear versus non-linear.
    ///
    /// This property shall indicate whether the metric values are linear versus non-linear.  Linear metrics can use a greater than relation to compared them.  An example of linear metrics include performance metrics.  Examples of non-linear metrics include error codes.
    IsLinear: core.Nullable(bool) = .absent,
    /// The list of URIs with wildcards and property identifiers that this metric definition defines.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This array property shall list the URIs with wildcards and property identifiers that this metric defines.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall identify a resource property to which the metric definition applies.  The property identifiers portion of the URI shall follow RFC6901-defined JSON pointer notation rules.  This property should not be present if `ImplementationType` contains `Synthesized` or `Calculated`.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The time interval between when a metric is updated.
    ///
    /// This property shall specify the frequency at which metric values are included in any `MetricReport` referencing this metric.
    SensingInterval: core.Nullable(core.Duration) = .absent,
    /// This array property specifies possible values of a discrete metric.
    ///
    /// The values of the property shall specify the possible values of the discrete metric.  This property shall have values when the `MetricType` property is `Discrete`.
    DiscreteValues: ?[]const ?[]const u8 = null,
    /// The time interval over which the metric calculation is performed.
    ///
    /// This property shall specify the time interval over the metric calculation is performed.
    CalculationTimeInterval: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `MetricDefinition.MetricDefinition`.
///
/// The `MetricDefinition` schema describes the metadata information for a metric.
pub const MetricDefinitionCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The type of metric.
    ///
    /// This property shall specify the type of metric.
    MetricType: core.Nullable(MetricType) = .absent,
    /// The data type of the metric.
    ///
    /// This property shall specify the data-type of the metric.
    MetricDataType: core.Nullable(MetricDataType) = .absent,
    /// The units of measure for this metric.
    ///
    /// This property shall specify the units of the metric.  This property shall be consistent with the case-sensitive ('C/s' column) Unified Code for Units of Measure.  Note: Not all units of measure are covered by UCUM.
    Units: core.Nullable([]const u8) = .absent,
    /// An indication of whether the metric can be used in a calculation.
    ///
    /// This property shall specify whether the metric can be used in a calculation.
    Calculable: core.Nullable(Calculable) = .absent,
    /// An indication of whether the metric values are linear versus non-linear.
    ///
    /// This property shall indicate whether the metric values are linear versus non-linear.  Linear metrics can use a greater than relation to compared them.  An example of linear metrics include performance metrics.  Examples of non-linear metrics include error codes.
    IsLinear: core.Nullable(bool) = .absent,
    /// The list of URIs with wildcards and property identifiers that this metric definition defines.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This array property shall list the URIs with wildcards and property identifiers that this metric defines.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall identify a resource property to which the metric definition applies.  The property identifiers portion of the URI shall follow RFC6901-defined JSON pointer notation rules.  This property should not be present if `ImplementationType` contains `Synthesized` or `Calculated`.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The time interval between when a metric is updated.
    ///
    /// This property shall specify the frequency at which metric values are included in any `MetricReport` referencing this metric.
    SensingInterval: core.Nullable(core.Duration) = .absent,
    /// This array property specifies possible values of a discrete metric.
    ///
    /// The values of the property shall specify the possible values of the discrete metric.  This property shall have values when the `MetricType` property is `Discrete`.
    DiscreteValues: ?[]const ?[]const u8 = null,
    /// The time interval over which the metric calculation is performed.
    ///
    /// This property shall specify the time interval over the metric calculation is performed.
    CalculationTimeInterval: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
