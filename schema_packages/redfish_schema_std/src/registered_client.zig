//! Namespace `RegisteredClient`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");
const settings = @import("settings.zig");

pub const ClientType = enum {
    /// The registered client only performs read operations on this service.
    Monitor,
    /// The registered client performs update, create, and delete operations on the resources listed in the `ManagedResources` property as well as read operations on the service.
    Configure,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// A resource managed by a client.
///
/// This object shall contain information about a resource managed by a client.  The managed resource may specify subordinate resources.
pub const ManagedResource = struct {
    /// The URI of the resource or resource collection managed by the registered client.
    ///
    /// This property shall contain the URI of the Redfish resource or Redfish resource collection managed by the registered client.  When the URI references a resource collection, all members of the resource collection may be monitored or configured by the client, and the `IncludesSubordinates` property shall contain `true`.
    ManagedResourceURI: ?[]const u8 = null,
    /// Indicates whether the registered client expects to have exclusive access to the managed resource.
    ///
    /// This property shall indicate whether the registered client expects to have exclusive access to the managed resource referenced by the `ManagedResourceURI` property, and also its subordinate resources if `IncludesSubordinates` contains `true`.  If not specified, the value is assumed to be `false`.
    PreferExclusive: ?bool = null,
    /// Indicates whether the subordinate resources of the managed resource are also managed by the registered client.
    ///
    /// This property shall indicate whether the subordinate resources of the managed resource referenced by the `ManagedResourceURI` property are also managed by the registered client.  If not specified, the value is assumed to be `false` unless `ManagedResourceURI` references a resource collection.
    IncludesSubordinates: ?bool = null,
};

/// What a client may change of `RegisteredClient.ManagedResource`.
///
/// A resource managed by a client.
pub const ManagedResourceUpdate = struct {
    /// The URI of the resource or resource collection managed by the registered client.
    ///
    /// This property shall contain the URI of the Redfish resource or Redfish resource collection managed by the registered client.  When the URI references a resource collection, all members of the resource collection may be monitored or configured by the client, and the `IncludesSubordinates` property shall contain `true`.
    ManagedResourceURI: core.Nullable([]const u8) = .absent,
    /// Indicates whether the registered client expects to have exclusive access to the managed resource.
    ///
    /// This property shall indicate whether the registered client expects to have exclusive access to the managed resource referenced by the `ManagedResourceURI` property, and also its subordinate resources if `IncludesSubordinates` contains `true`.  If not specified, the value is assumed to be `false`.
    PreferExclusive: core.Nullable(bool) = .absent,
    /// Indicates whether the subordinate resources of the managed resource are also managed by the registered client.
    ///
    /// This property shall indicate whether the subordinate resources of the managed resource referenced by the `ManagedResourceURI` property are also managed by the registered client.  If not specified, the value is assumed to be `false` unless `ManagedResourceURI` references a resource collection.
    IncludesSubordinates: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `RegisteredClient` schema defines the record format for a registered client.  It is designed to allow well-behaved clients to register with a Redfish service such that other clients are aware the service might be configured or monitored by the client.
///
/// This resource shall represent a registered client for a Redfish implementation.  It is not expected that transient tools, such as a short-lived CLI tool, register.  Clients and management tools that live for long periods of time can create `RegisteredClient` resources so that other clients are aware the service might be configured or monitored by the client.
pub const RegisteredClient = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The date and time when the client entry was created.
    ///
    /// This property shall contain the date and time when the client entry was created.
    CreatedDate: ?core.DateTimeOffset = null,
    /// The date and time when the client entry will expire.
    ///
    /// This property shall contain the date and time when the client entry expires.  Registered clients that are actively managing or monitoring should periodically update this value.  The value should not be more than 7 days after the date when it was last set.  If the current date is beyond this date, the service may delete this client entry.
    ExpirationDate: ?core.DateTimeOffset = null,
    /// The URI of the registered client.
    ///
    /// This property shall contain the URI of the registered client.
    ClientURI: ?[]const u8 = null,
    /// The type of registered client.
    ///
    /// This property shall contain the type of registered client.
    ClientType: ?ClientType = null,
    /// An array of resources that the registered client monitors or configures.
    ///
    /// This property shall contain an array of resources that the registered client monitors or configures.  Other clients can use this property to understand which resources are monitored or configured by the registered client.
    ManagedResources: ?[]const ?ManagedResource = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// A client-supplied data for providing context for its own use.
    ///
    /// This property shall contain data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    Context: ?[]const u8 = null,
    /// Additional client-supplied data for providing contextual information for its own use.
    ///
    /// This property shall contain additional data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    SubContext: ?[]const u8 = null,

    const closed = core.ClosedStruct(@This());
    pub const jsonParse = closed.jsonParse;
    pub const jsonParseFromValue = closed.jsonParseFromValue;
};

/// What a client may change of `RegisteredClient.RegisteredClient`.
///
/// The `RegisteredClient` schema defines the record format for a registered client.  It is designed to allow well-behaved clients to register with a Redfish service such that other clients are aware the service might be configured or monitored by the client.
pub const RegisteredClientUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The date and time when the client entry will expire.
    ///
    /// This property shall contain the date and time when the client entry expires.  Registered clients that are actively managing or monitoring should periodically update this value.  The value should not be more than 7 days after the date when it was last set.  If the current date is beyond this date, the service may delete this client entry.
    ExpirationDate: ?core.DateTimeOffset = null,
    /// The URI of the registered client.
    ///
    /// This property shall contain the URI of the registered client.
    ClientURI: ?[]const u8 = null,
    /// The type of registered client.
    ///
    /// This property shall contain the type of registered client.
    ClientType: ?ClientType = null,
    /// An array of resources that the registered client monitors or configures.
    ///
    /// This property shall contain an array of resources that the registered client monitors or configures.  Other clients can use this property to understand which resources are monitored or configured by the registered client.
    ManagedResources: ?[]const ?ManagedResourceUpdate = null,
    /// A client-supplied data for providing context for its own use.
    ///
    /// This property shall contain data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    Context: ?[]const u8 = null,
    /// Additional client-supplied data for providing contextual information for its own use.
    ///
    /// This property shall contain additional data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    SubContext: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `RegisteredClient.RegisteredClient`.
///
/// The `RegisteredClient` schema defines the record format for a registered client.  It is designed to allow well-behaved clients to register with a Redfish service such that other clients are aware the service might be configured or monitored by the client.
pub const RegisteredClientCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The date and time when the client entry will expire.
    ///
    /// This property shall contain the date and time when the client entry expires.  Registered clients that are actively managing or monitoring should periodically update this value.  The value should not be more than 7 days after the date when it was last set.  If the current date is beyond this date, the service may delete this client entry.
    ExpirationDate: ?core.DateTimeOffset = null,
    /// The URI of the registered client.
    ///
    /// This property shall contain the URI of the registered client.
    ClientURI: ?[]const u8 = null,
    /// The type of registered client.
    ///
    /// This property shall contain the type of registered client.
    ClientType: ClientType,
    /// An array of resources that the registered client monitors or configures.
    ///
    /// This property shall contain an array of resources that the registered client monitors or configures.  Other clients can use this property to understand which resources are monitored or configured by the registered client.
    ManagedResources: ?[]const ?ManagedResourceUpdate = null,
    /// A client-supplied data for providing context for its own use.
    ///
    /// This property shall contain data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    Context: ?[]const u8 = null,
    /// Additional client-supplied data for providing contextual information for its own use.
    ///
    /// This property shall contain additional data provided by the owning client used to identify the service, provide context about its state, or other information.  The value of this property shall not contain unencrypted sensitive data such as user credentials.  Services shall support values of at least 256 bytes in length.
    SubContext: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
