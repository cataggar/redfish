//! Namespace `ManagerAccount`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const account_service = @import("account_service.zig");
const certificate_collection = @import("certificate_collection.zig");
const key_collection = @import("key_collection.zig");
const resource = @import("resource.zig");
const role = @import("role.zig");
const settings = @import("settings.zig");

pub const AccountTypes = enum {
    /// Allow access to the Redfish service.
    ///
    /// This value shall indicate the account is allowed to access Redfish services.  If the version of the `ManagerAccount` resource is lower than the schema version when another enumeration value in this list was added, the implementation may include that functionality as part of the `Redfish` value.
    Redfish,
    /// Allow access to SNMP services.
    ///
    /// This value shall indicate the account is allowed to access SNMP services.
    SNMP,
    /// OEM account type.  See the `OEMAccountTypes` property.
    ///
    /// This value shall indicate the account is allowed to access the services listed in the `OEMAccountTypes` property.
    OEM,
    /// Allow access to the host's console, which could be connected through Telnet, SSH, or another protocol.
    ///
    /// This value shall indicate the account is allowed to access the host console.
    HostConsole,
    /// Allow access to the manager's console, which could be connected through Telnet, SSH, SM CLP, or another protocol.
    ///
    /// This value shall indicate the account is allowed to access the manager console.
    ManagerConsole,
    /// Allow access to the Intelligent Platform Management Interface service.
    ///
    /// This value shall indicate the account is allowed to access the Intelligent Platform Management Interface service.
    IPMI,
    /// Allow access to a Keyboard-Video-Mouse over IP session.
    ///
    /// This value shall indicate the account is allowed to access the Keyboard-Video-Mouse over IP session service.
    KVMIP,
    /// Allow access to control virtual media.
    ///
    /// This value shall indicate the account is allowed to control virtual media.
    VirtualMedia,
    /// Allow access to a web user interface session, such as a graphical interface or another web-based protocol.
    ///
    /// This value shall indicate the account is allowed to access the web interface.
    WebUI,
    /// Allow PIN-based access via an external control panel, such as a keypad, touchscreen, or other human interface.
    ///
    /// This value shall indicate the account is used to allow PIN-based access via an external control panel.  If this value is specified, the `AccountTypes` property should not contain other values for the same `ManagerAccount` resource.  If this value is specified, `Password` shall contain the PIN to enable access, and may not follow other password-related rules.  The `ChangePassword` action, if supported, may be used to update the PIN.
    ControlPanel,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpAuthenticationProtocols = enum {
    /// No authentication.
    ///
    /// This value shall indicate authentication is not required.
    None,
    /// HMAC-MD5-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-MD5-96 authentication protocol.
    HMAC_MD5,
    /// HMAC-SHA-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-SHA-96 authentication protocol.
    HMAC_SHA96,
    /// HMAC-128-SHA-224 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC128SHA224AuthProtocol.
    HMAC128_SHA224,
    /// HMAC-192-SHA-256 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC192SHA256AuthProtocol.
    HMAC192_SHA256,
    /// HMAC-256-SHA-384 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC256SHA384AuthProtocol.
    HMAC256_SHA384,
    /// HMAC-384-SHA-512 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC384SHA512AuthProtocol.
    HMAC384_SHA512,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpEncryptionProtocols = enum {
    /// No encryption.
    ///
    /// This value shall indicate there is no encryption.
    None,
    /// CBC-DES encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3414-defined CBC-DES encryption protocol.
    CBC_DES,
    /// CFB128-AES-128 encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3826-defined CFB128-AES-128 encryption protocol.
    CFB128_AES128,
    /// CFB128-AES-192 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-192 encryption protocol, extended from RFC3826.
    CFB128_AES192,
    /// CFB128-AES-256 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-256 encryption protocol, extended from RFC3826.
    CFB128_AES256,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `ManagerAccount.ChangePassword`.
///
/// This action changes the account password.
pub const ManagerAccountChangePasswordAction = struct {
    /// The password of the account tied to the current session.
    ///
    /// This parameter shall contain the password of the current session's account.  A user changing their own password shall provide their current password for this parameter.  An administrator changing the password for a different user shall provide their own password for this parameter.  If the request is performed with HTTP Basic authentication, this parameter shall contain the same password encoded in the `Authorization` header.
    SessionAccountPassword: ?[]const u8 = null,
    /// The new account password.
    ///
    /// This parameter shall contain the new password.
    NewPassword: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `ManagerAccount.ClearSecretKey`.
///
/// This action clears the secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
pub const ManagerAccountClearSecretKeyAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `ManagerAccount.GenerateSecretKey`.
///
/// This action randomly generates a new secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
pub const ManagerAccountGenerateSecretKeyAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `ManagerAccount.VerifyTimeBasedOneTimePassword`.
///
/// This action verifies a user-provided Time-based One-Time Password (TOTP).  This is to ensure the client's copy of the secret key is aligned with the secret key stored by the service.
pub const ManagerAccountVerifyTimeBasedOneTimePasswordAction = struct {
    /// The Time-based One-Time Password (TOTP) to verify.
    ///
    /// This parameter shall contain the Time-based One-Time Password (TOTP) to verify.  If the Time-based One-Time Password (TOTP) is not valid, the service shall return the HTTP `400 Bad Request` status code.
    TimeBasedOneTimePassword: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action changes the account password.
    ///
    /// This action shall change the account password while requiring password for the current session.  This action prevents session hijacking.
    @"#ManagerAccount.ChangePassword": ?core.Action(ManagerAccountChangePasswordAction, std.json.Value) = null,
    /// This action clears the secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// This action shall clear the secret key for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    @"#ManagerAccount.ClearSecretKey": ?core.Action(ManagerAccountClearSecretKeyAction, std.json.Value) = null,
    /// This action randomly generates a new secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// This action shall randomly generate a new secret key for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    @"#ManagerAccount.GenerateSecretKey": ?core.Action(ManagerAccountGenerateSecretKeyAction, GenerateSecretKeyResponse) = null,
    /// This action verifies a user-provided Time-based One-Time Password (TOTP).  This is to ensure the client's copy of the secret key is aligned with the secret key stored by the service.
    ///
    /// This action shall verify a user-provided RFC6238-defined Time-based One-Time Password (TOTP).
    @"#ManagerAccount.VerifyTimeBasedOneTimePassword": ?core.Action(ManagerAccountVerifyTimeBasedOneTimePasswordAction, std.json.Value) = null,

    /// This action changes the account password.
    ///
    /// This action shall change the account password while requiring password for the current session.  This action prevents session hijacking.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn changePassword(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerAccountChangePasswordAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#ManagerAccount.ChangePassword" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action clears the secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// This action shall clear the secret key for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn clearSecretKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerAccountClearSecretKeyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#ManagerAccount.ClearSecretKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action randomly generates a new secret key for Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// This action shall randomly generate a new secret key for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication for this account.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn generateSecretKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerAccountGenerateSecretKeyAction,
    ) !core.Owned(core.ModificationResponse(GenerateSecretKeyResponse)) {
        const target = self.@"#ManagerAccount.GenerateSecretKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action verifies a user-provided Time-based One-Time Password (TOTP).  This is to ensure the client's copy of the secret key is aligned with the secret key stored by the service.
    ///
    /// This action shall verify a user-provided RFC6238-defined Time-based One-Time Password (TOTP).
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn verifyTimeBasedOneTimePassword(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerAccountVerifyTimeBasedOneTimePasswordAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#ManagerAccount.VerifyTimeBasedOneTimePassword" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The response body for the `GenerateSecretKey` action.
///
/// This type shall contain the properties found in the response body for the `GenerateSecretKey` action.
pub const GenerateSecretKeyResponse = struct {
    /// The secret key generated for Time-based One-Time Password (TOTP) multi-factor authentication.
    ///
    /// This property shall contain secret key generated for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication.  Clients shall retain the value of this property to generate tokens for future session creation requests.
    SecretKey: ?[]const u8 = null,
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The link to the Redfish role that defines the privileges for this account.
    ///
    /// This property shall contain a link to a resource of type `Role`, and should link to the resource identified by the `RoleId` property.
    Role: ?core.NavProperty(role.Role) = null,
};

/// What a client may change of `ManagerAccount.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const SnmpUserInfo = struct {
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: ?SnmpAuthenticationProtocols = null,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: ?SnmpEncryptionProtocols = null,
    /// Indicates if the `AuthenticationKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `AuthenticationKey` property.  Otherwise, the property shall contain `false`.
    AuthenticationKeySet: ?bool = null,
    /// Indicates if the `EncryptionKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `EncryptionKey` property.  Otherwise, the property shall contain `false`.
    EncryptionKeySet: ?bool = null,
};

/// What a client may change of `ManagerAccount.SNMPUserInfo`.
pub const SnmpUserInfoUpdate = struct {
    /// The secret authentication key for SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 authentication.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    AuthenticationKey: core.Nullable([]const u8) = .absent,
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: core.Nullable(SnmpAuthenticationProtocols) = .absent,
    /// The secret encryption key used in SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 encryption.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    EncryptionKey: core.Nullable([]const u8) = .absent,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: core.Nullable(SnmpEncryptionProtocols) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `ManagerAccount` schema defines the user accounts that are owned by a manager.  Changes to a manager account might affect the current Redfish service connection if this manager is responsible for the Redfish service.
///
/// This resource shall represent a user account for the manager in a Redfish implementation.  The account shall indicate the allowed access to one of more services in the manager.
pub const ManagerAccount = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The username for the account.
    ///
    /// This property shall contain the username for this account.
    UserName: ?[]const u8 = null,
    /// The role for this account.
    ///
    /// This property shall contain the `RoleId` of the role resource configured for this account.  The service shall reject `POST`, `PATCH`, or `PUT` operations that provide a `RoleId` that does not exist by returning the HTTP `400 Bad Request` status code.
    RoleId: ?[]const u8 = null,
    /// An indication of whether the account service automatically locked the account because the lockout threshold was exceeded.  To manually unlock the account before the lockout duration period, an administrator can change the property to `false` to clear the lockout condition.
    ///
    /// This property shall indicate whether the account service automatically locked the account because the `AccountLockoutThreshold` was exceeded.  To manually unlock the account before the lockout duration period, an administrator shall be able to change the property to `false` to clear the lockout condition.
    Locked: ?bool = null,
    /// An indication of whether an account is enabled.  An administrator can disable it without deleting the user information.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    ///
    /// This property shall indicate whether an account is enabled.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    Enabled: ?bool = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// An indication of whether the service requires that the password for this account be changed before further access to the account is allowed.
    ///
    /// This property shall indicate whether the service requires that the password for this account be changed before further access to the account is allowed.  The implementation may deny access to the service if the password has not been changed.  A manager account created with an initial `PasswordChangeRequired` value of `true` may force a password change before first access of the account.  When the `Password` property for this account is updated, the service shall set this property to `false`.
    PasswordChangeRequired: ?bool = null,
    /// The SNMP settings for this account.
    ///
    /// This property shall contain the SNMP settings for this account when `AccountTypes` contains `SNMP`.
    SNMP: ?SnmpUserInfo = null,
    /// The list of services in the manager that the account is allowed to access.
    ///
    /// This property shall contain an array of the various manager services that the account is allowed to access.  This shall not include functionality for receiving events or other notifications.  If this property is not provided by the client, the default value shall be an array that contains the value `Redfish`.  The service may add additional values when this property is set or updated if allowed by the value of the `StrictAccountTypes` property.
    AccountTypes: ?[]const ?AccountTypes = null,
    /// The OEM account types.
    ///
    /// This property shall contain an array of the OEM account types for this account.  This property shall be valid when `AccountTypes` contains `OEM`.
    OEMAccountTypes: ?[]const ?[]const u8 = null,
    /// Indicates the date and time when this account password expires.  If `null`, the account password never expires.
    ///
    /// This property shall contain the date and time when this account password expires.  If the value is `null`, the account password never expires.  If provided during account creation or password modification, and allowed by the service, this value shall override the value of the `PasswordExpirationDays` property in the `AccountService` resource.
    PasswordExpiration: ?core.DateTimeOffset = null,
    /// Indicates if the service needs to use the account types exactly as specified when the account is created or updated.
    ///
    /// This property shall indicate if the service needs to use the value of `AccountTypes` and `OEMAccountTypes` values exactly as specified.  A `true` value shall indicate the service needs to either accept the value without changes or reject the request.  A `false` value shall indicate the service may add additional `AccountTypes` and `OEMAccountTypes` values as needed to support limitations it has in separately controlling access to individual services.  If this property is not present, the value shall be assumed to be `false`.  An update of the service can cause account types to be added to or removed from the `AccountTypes` and `OEMAccountTypes` properties, regardless of the value of this property.  After a service update, clients should inspect all accounts where the value of this property is `true` and perform maintenance as needed.
    StrictAccountTypes: ?bool = null,
    /// Indicates the date and time when this account expires.  If `null`, the account never expires.
    ///
    /// This property shall contain the date and time when this account expires.  The service shall disable or delete an account that has expired.  This property shall not apply to accounts created by the Redfish Host Interface Specification-defined credential bootstrapping.  If the value is `null`, or the property is not present, the account never expires.
    AccountExpiration: ?core.DateTimeOffset = null,
    /// An indication of whether this account is a bootstrap account for the host interface.
    ///
    /// This property shall indicate whether this account is a bootstrap account created by the Redfish Host Interface Specification-defined credential bootstrapping.
    HostBootstrapAccount: ?bool = null,
    /// The multi-factor authentication bypass settings for this account.
    ///
    /// This property shall contain the multi-factor authentication bypass settings for this account.
    MFABypass: ?account_service.MfaBypass = null,
    /// The email address associated with this account.
    ///
    /// This property shall contain the email address associated with this account.
    EmailAddress: ?[]const u8 = null,
    /// The contact phone number associated with this account.
    ///
    /// This property shall contain the contact phone number associated with this account.
    PhoneNumber: ?[]const u8 = null,
    /// The address used to receive one-time passcode messages for multi-factor authentication.
    ///
    /// This property shall contain the contact address for receiving one-time passcode messages for multi-factor authentication for this account when the `Enabled` property in the `OneTimePasscode` property in `AccountService` resource contains `true`.  This is typically the contact email address associated with the account, but may be a separate, relay email address for delivery via SMS or other contact method.
    OneTimePasscodeDeliveryAddress: ?[]const u8 = null,
    /// Indicates if the secret key for Time-based One-Time Password (TOTP) multi-factor authentication is set.
    ///
    /// This property shall indicate if the secret key for RFC6238-defined Time-based One-Time Password (TOTP) multi-factor authentication is set.
    SecretKeySet: ?bool = null,
    /// The link to a collection of user identity certificates for this account.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the user identity certificates for this account.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to the collection of keys that can be used to authenticate this account.  For example, an SSH public key could be added to this collection to allow for SSH public key authentication.
    ///
    /// This property shall contain a link to a resource collection of type `KeyCollection` that contains the keys that can be used to authenticate this account.
    Keys: ?core.NavProperty(key_collection.KeyCollection) = null,

    const closed = core.ClosedStruct(@This());
    pub const jsonParse = closed.jsonParse;
    pub const jsonParseFromValue = closed.jsonParseFromValue;
};

/// What a client may change of `ManagerAccount.ManagerAccount`.
///
/// The `ManagerAccount` schema defines the user accounts that are owned by a manager.  Changes to a manager account might affect the current Redfish service connection if this manager is responsible for the Redfish service.
pub const ManagerAccountUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The password.  Use this property with a `PATCH` or `PUT` to write the password for the account.  This property is `null` in responses.
    ///
    /// This property shall contain the password for this account.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,
    /// The username for the account.
    ///
    /// This property shall contain the username for this account.
    UserName: ?[]const u8 = null,
    /// The role for this account.
    ///
    /// This property shall contain the `RoleId` of the role resource configured for this account.  The service shall reject `POST`, `PATCH`, or `PUT` operations that provide a `RoleId` that does not exist by returning the HTTP `400 Bad Request` status code.
    RoleId: ?[]const u8 = null,
    /// An indication of whether the account service automatically locked the account because the lockout threshold was exceeded.  To manually unlock the account before the lockout duration period, an administrator can change the property to `false` to clear the lockout condition.
    ///
    /// This property shall indicate whether the account service automatically locked the account because the `AccountLockoutThreshold` was exceeded.  To manually unlock the account before the lockout duration period, an administrator shall be able to change the property to `false` to clear the lockout condition.
    Locked: ?bool = null,
    /// An indication of whether an account is enabled.  An administrator can disable it without deleting the user information.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    ///
    /// This property shall indicate whether an account is enabled.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    Enabled: ?bool = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// An indication of whether the service requires that the password for this account be changed before further access to the account is allowed.
    ///
    /// This property shall indicate whether the service requires that the password for this account be changed before further access to the account is allowed.  The implementation may deny access to the service if the password has not been changed.  A manager account created with an initial `PasswordChangeRequired` value of `true` may force a password change before first access of the account.  When the `Password` property for this account is updated, the service shall set this property to `false`.
    PasswordChangeRequired: core.Nullable(bool) = .absent,
    /// The SNMP settings for this account.
    ///
    /// This property shall contain the SNMP settings for this account when `AccountTypes` contains `SNMP`.
    SNMP: core.Nullable(SnmpUserInfoUpdate) = .absent,
    /// The list of services in the manager that the account is allowed to access.
    ///
    /// This property shall contain an array of the various manager services that the account is allowed to access.  This shall not include functionality for receiving events or other notifications.  If this property is not provided by the client, the default value shall be an array that contains the value `Redfish`.  The service may add additional values when this property is set or updated if allowed by the value of the `StrictAccountTypes` property.
    AccountTypes: ?[]const ?AccountTypes = null,
    /// The OEM account types.
    ///
    /// This property shall contain an array of the OEM account types for this account.  This property shall be valid when `AccountTypes` contains `OEM`.
    OEMAccountTypes: ?[]const ?[]const u8 = null,
    /// Indicates the date and time when this account password expires.  If `null`, the account password never expires.
    ///
    /// This property shall contain the date and time when this account password expires.  If the value is `null`, the account password never expires.  If provided during account creation or password modification, and allowed by the service, this value shall override the value of the `PasswordExpirationDays` property in the `AccountService` resource.
    PasswordExpiration: core.Nullable(core.DateTimeOffset) = .absent,
    /// Indicates if the service needs to use the account types exactly as specified when the account is created or updated.
    ///
    /// This property shall indicate if the service needs to use the value of `AccountTypes` and `OEMAccountTypes` values exactly as specified.  A `true` value shall indicate the service needs to either accept the value without changes or reject the request.  A `false` value shall indicate the service may add additional `AccountTypes` and `OEMAccountTypes` values as needed to support limitations it has in separately controlling access to individual services.  If this property is not present, the value shall be assumed to be `false`.  An update of the service can cause account types to be added to or removed from the `AccountTypes` and `OEMAccountTypes` properties, regardless of the value of this property.  After a service update, clients should inspect all accounts where the value of this property is `true` and perform maintenance as needed.
    StrictAccountTypes: core.Nullable(bool) = .absent,
    /// Indicates the date and time when this account expires.  If `null`, the account never expires.
    ///
    /// This property shall contain the date and time when this account expires.  The service shall disable or delete an account that has expired.  This property shall not apply to accounts created by the Redfish Host Interface Specification-defined credential bootstrapping.  If the value is `null`, or the property is not present, the account never expires.
    AccountExpiration: core.Nullable(core.DateTimeOffset) = .absent,
    /// The multi-factor authentication bypass settings for this account.
    ///
    /// This property shall contain the multi-factor authentication bypass settings for this account.
    MFABypass: core.Nullable(account_service.MfaBypassUpdate) = .absent,
    /// The email address associated with this account.
    ///
    /// This property shall contain the email address associated with this account.
    EmailAddress: core.Nullable([]const u8) = .absent,
    /// The contact phone number associated with this account.
    ///
    /// This property shall contain the contact phone number associated with this account.
    PhoneNumber: core.Nullable([]const u8) = .absent,
    /// The address used to receive one-time passcode messages for multi-factor authentication.
    ///
    /// This property shall contain the contact address for receiving one-time passcode messages for multi-factor authentication for this account when the `Enabled` property in the `OneTimePasscode` property in `AccountService` resource contains `true`.  This is typically the contact email address associated with the account, but may be a separate, relay email address for delivery via SMS or other contact method.
    OneTimePasscodeDeliveryAddress: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `ManagerAccount.ManagerAccount`.
///
/// The `ManagerAccount` schema defines the user accounts that are owned by a manager.  Changes to a manager account might affect the current Redfish service connection if this manager is responsible for the Redfish service.
pub const ManagerAccountCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The password.  Use this property with a `PATCH` or `PUT` to write the password for the account.  This property is `null` in responses.
    ///
    /// This property shall contain the password for this account.  The value shall be `null` in responses.
    Password: []const u8,
    /// The username for the account.
    ///
    /// This property shall contain the username for this account.
    UserName: []const u8,
    /// The role for this account.
    ///
    /// This property shall contain the `RoleId` of the role resource configured for this account.  The service shall reject `POST`, `PATCH`, or `PUT` operations that provide a `RoleId` that does not exist by returning the HTTP `400 Bad Request` status code.
    RoleId: []const u8,
    /// An indication of whether the account service automatically locked the account because the lockout threshold was exceeded.  To manually unlock the account before the lockout duration period, an administrator can change the property to `false` to clear the lockout condition.
    ///
    /// This property shall indicate whether the account service automatically locked the account because the `AccountLockoutThreshold` was exceeded.  To manually unlock the account before the lockout duration period, an administrator shall be able to change the property to `false` to clear the lockout condition.
    Locked: ?bool = null,
    /// An indication of whether an account is enabled.  An administrator can disable it without deleting the user information.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    ///
    /// This property shall indicate whether an account is enabled.  If `true`, the account is enabled and the user can log in.  If `false`, the account is disabled and, in the future, the user cannot log in.
    Enabled: ?bool = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// An indication of whether the service requires that the password for this account be changed before further access to the account is allowed.
    ///
    /// This property shall indicate whether the service requires that the password for this account be changed before further access to the account is allowed.  The implementation may deny access to the service if the password has not been changed.  A manager account created with an initial `PasswordChangeRequired` value of `true` may force a password change before first access of the account.  When the `Password` property for this account is updated, the service shall set this property to `false`.
    PasswordChangeRequired: core.Nullable(bool) = .absent,
    /// The SNMP settings for this account.
    ///
    /// This property shall contain the SNMP settings for this account when `AccountTypes` contains `SNMP`.
    SNMP: core.Nullable(SnmpUserInfoUpdate) = .absent,
    /// The list of services in the manager that the account is allowed to access.
    ///
    /// This property shall contain an array of the various manager services that the account is allowed to access.  This shall not include functionality for receiving events or other notifications.  If this property is not provided by the client, the default value shall be an array that contains the value `Redfish`.  The service may add additional values when this property is set or updated if allowed by the value of the `StrictAccountTypes` property.
    AccountTypes: ?[]const ?AccountTypes = null,
    /// The OEM account types.
    ///
    /// This property shall contain an array of the OEM account types for this account.  This property shall be valid when `AccountTypes` contains `OEM`.
    OEMAccountTypes: ?[]const ?[]const u8 = null,
    /// Indicates the date and time when this account password expires.  If `null`, the account password never expires.
    ///
    /// This property shall contain the date and time when this account password expires.  If the value is `null`, the account password never expires.  If provided during account creation or password modification, and allowed by the service, this value shall override the value of the `PasswordExpirationDays` property in the `AccountService` resource.
    PasswordExpiration: core.Nullable(core.DateTimeOffset) = .absent,
    /// Indicates if the service needs to use the account types exactly as specified when the account is created or updated.
    ///
    /// This property shall indicate if the service needs to use the value of `AccountTypes` and `OEMAccountTypes` values exactly as specified.  A `true` value shall indicate the service needs to either accept the value without changes or reject the request.  A `false` value shall indicate the service may add additional `AccountTypes` and `OEMAccountTypes` values as needed to support limitations it has in separately controlling access to individual services.  If this property is not present, the value shall be assumed to be `false`.  An update of the service can cause account types to be added to or removed from the `AccountTypes` and `OEMAccountTypes` properties, regardless of the value of this property.  After a service update, clients should inspect all accounts where the value of this property is `true` and perform maintenance as needed.
    StrictAccountTypes: core.Nullable(bool) = .absent,
    /// Indicates the date and time when this account expires.  If `null`, the account never expires.
    ///
    /// This property shall contain the date and time when this account expires.  The service shall disable or delete an account that has expired.  This property shall not apply to accounts created by the Redfish Host Interface Specification-defined credential bootstrapping.  If the value is `null`, or the property is not present, the account never expires.
    AccountExpiration: core.Nullable(core.DateTimeOffset) = .absent,
    /// The multi-factor authentication bypass settings for this account.
    ///
    /// This property shall contain the multi-factor authentication bypass settings for this account.
    MFABypass: core.Nullable(account_service.MfaBypassUpdate) = .absent,
    /// The email address associated with this account.
    ///
    /// This property shall contain the email address associated with this account.
    EmailAddress: core.Nullable([]const u8) = .absent,
    /// The contact phone number associated with this account.
    ///
    /// This property shall contain the contact phone number associated with this account.
    PhoneNumber: core.Nullable([]const u8) = .absent,
    /// The address used to receive one-time passcode messages for multi-factor authentication.
    ///
    /// This property shall contain the contact address for receiving one-time passcode messages for multi-factor authentication for this account when the `Enabled` property in the `OneTimePasscode` property in `AccountService` resource contains `true`.  This is typically the contact email address associated with the account, but may be a separate, relay email address for delivery via SMS or other contact method.
    OneTimePasscodeDeliveryAddress: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
