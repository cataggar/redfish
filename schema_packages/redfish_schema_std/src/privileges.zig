//! Namespace `Privileges`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");

pub const PrivilegeType = enum {
    /// Can log in to the service and read resources.
    Login,
    /// Can configure managers.
    ConfigureManager,
    /// Can configure users and their accounts.
    ConfigureUsers,
    /// Can change the password for the current user account, log out of their own sessions, and perform operations on resources they created.  Services will need to be aware of resource ownership to map this privilege to an operation from a particular user.
    ConfigureSelf,
    /// Can configure components that this service manages.
    ConfigureComponents,
    /// Authentication is not required.
    ///
    /// This value shall be used to indicate an operation does not require authentication.  This privilege shall not be used in Redfish roles.
    NoAuth,
    /// Can view and configure composition service resources.
    ///
    /// This value shall be used to indicate the user can view and configure composition service resources without matching the `Client` property in the `ResourceBlock` or `CompositionReservation` resources.
    ConfigureCompositionInfrastructure,
    /// Administrator for systems found in the systems collection.  Able to manage boot configuration, keys, and certificates for systems.
    AdministrateSystems,
    /// Operator for systems found in the systems collection.  Able to perform resets and configure interfaces.
    OperateSystems,
    /// Administrator for storage subsystems and storage systems found in the storage collection and storage system collection respectively.
    AdministrateStorage,
    /// Operator for storage backup functionality for storage subsystems and storage systems found in the storage collection and storage system collection respectively.
    OperateStorageBackup,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

test {
    std.testing.refAllDecls(@This());
}
