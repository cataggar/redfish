//! Namespace `Drive`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const assembly = @import("assembly.zig");
const certificate_collection = @import("certificate_collection.zig");
const chassis = @import("chassis.zig");
const drive_metrics = @import("drive_metrics.zig");
const endpoint = @import("endpoint.zig");
const environment_metrics = @import("environment_metrics.zig");
const network_device_function = @import("network_device_function.zig");
const pcie_function = @import("pcie_function.zig");
const protocol = @import("protocol.zig");
const resource = @import("resource.zig");
const software_inventory = @import("software_inventory.zig");
const storage = @import("storage.zig");
const storage_pool = @import("storage_pool.zig");
const task = @import("task.zig");
const volume = @import("volume.zig");

pub const ConfigLockOptions = enum {
    /// The command is supported, able to be locked, and is currently unlocked.
    Unlocked,
    /// The command is supported and is currently locked.
    Locked,
    /// The command is supported but is not able to be locked.
    LockdownUnsupported,
    /// The command is not supported, therefore lockdown does not apply.
    CommandUnsupported,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ConfigurationLock = enum {
    /// In-band configuration requests are locked as specified by `TargetConfigurationLockLevel`.
    ///
    /// This value shall indicate in-band configuration requests are locked as specified by `TargetConfigurationLockLevel`.
    Enabled,
    /// In-band configuration requests are not locked.
    ///
    /// This value shall indicate in-band configuration requests are not locked.
    Disabled,
    /// Some in-band configuration requests are not locked while others are locked.  This value is used for status reporting to indicate that the drive is partially locked and client action is recommended.
    ///
    /// This value shall indicate some in-band configuration requests are not locked while others are locked.
    Partial,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const DataSanitizationType = enum {
    /// Delete all logical block addresses, including those that are not currently mapping to active addresses, but leaving the data on the drive.
    ///
    /// This value shall indicate sanitization is performed by deleting all logical block addresses, including those that are not currently mapping to active addresses, but leaving the data on the drive.
    BlockErase,
    /// Erase the target data's encryption key leaving only the ciphertext on the drive.  For more information, see NIST800-88 and ISO/IEC 27040.
    ///
    /// This value shall indicate sanitization is performed by erasing the target data's encryption key leaving only the ciphertext on the drive.  For more information, see NIST800-88 and ISO/IEC 27040.
    CryptographicErase,
    /// Overwrite data by writing an implementation-specific pattern onto all sectors of the drive.
    ///
    /// This value shall indicate sanitization is performed by overwriting data by writing an implementation-specific pattern onto all sectors of the drive.
    Overwrite,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EncryptionAbility = enum {
    /// The drive is not capable of self-encryption.
    None,
    /// The drive is capable of self-encryption per the Trusted Computing Group's Self Encrypting Drive Standard.
    SelfEncryptingDrive,
    /// The drive is capable of self-encryption through some other means.
    Other,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EncryptionStatus = enum {
    /// The drive is not currently encrypted.
    ///
    /// Deprecated in v1_1_0.
    /// This value has been deprecated in favor of Unencrypted.
    Unecrypted,
    /// The drive is currently encrypted but the data is accessible to the user in unencrypted form.
    Unlocked,
    /// The drive is currently encrypted and the data is not accessible to the user.  However, the system can unlock the drive automatically.
    Locked,
    /// The drive is currently encrypted, the data is not accessible to the user, and the system requires user intervention to expose the data.
    Foreign,
    /// The drive is not currently encrypted.
    Unencrypted,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const FormFactor = enum {
    /// A 3.5 inch drive.
    ///
    /// This value shall indicate the drive is approximately 3.5 inches in width and no more than 1.1 inches in height.
    Drive3_5,
    /// A 2.5 inch drive.
    ///
    /// This value shall indicate the drive is approximately 2.5 inches in width and no more than 0.8 inches in height and is not a U.2 drive.
    Drive2_5,
    /// An EDSFF drive.
    ///
    /// This value shall indicate the drive corresponds to one of SNIA's SFF specifications with an unspecified form factor.  The `SlotFormFactor` property should not contain this value.
    EDSFF,
    /// An EDSFF 1U Long (E1.L) drive.
    ///
    /// This value shall indicate the drive corresponds to the SFF-TA-1007 Specification.
    EDSFF_1U_Long,
    /// An EDSFF 1U Short (E1.S) drive.
    ///
    /// This value shall indicate the drive corresponds to the SFF-TA-1006 Specification.
    EDSFF_1U_Short,
    /// An EDSFF E3 Short (E3.S) drive.
    ///
    /// This value shall indicate the drive corresponds to the SFF-TA-1008 Specification and is approximately 112.75 mm in length.
    EDSFF_E3_Short,
    /// An EDSFF E3 Long (E3.L) drive.
    ///
    /// This value shall indicate the drive corresponds to the SFF-TA-1008 Specification and is approximately 142.2 mm in length.
    EDSFF_E3_Long,
    /// An M.2 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification with an unspecified form factor.  The `SlotFormFactor` property should not contain this value.
    M2,
    /// An M.2 2230 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification and is approximately 22 mm in width and 30 mm in length.
    M2_2230,
    /// An M.2 2242 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification and is approximately 22 mm in width and 42 mm in length.
    M2_2242,
    /// An M.2 2260 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification and is approximately 22 mm in width and 60 mm in length.
    M2_2260,
    /// An M.2 2280 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification and is approximately 22 mm in width and 80 mm in length.
    M2_2280,
    /// An M.2 22110 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express M.2 Specification and is approximately 22 mm in width and 110 mm in length.
    M2_22110,
    /// A U.2 drive.
    ///
    /// This value shall indicate the drive corresponds to the PCI Express SFF-8639 Module Specification.
    U2,
    /// A full-length PCIe add-in card.
    ///
    /// This value shall indicate the drive is an add-in card greater than 7 inches in length.
    PCIeSlotFullLength,
    /// A low-profile PCIe add-in card.
    ///
    /// This value shall indicate the drive is an add-in card less than 2.5 inches in height.
    PCIeSlotLowProfile,
    /// A half-length PCIe add-in card.
    ///
    /// This value shall indicate the drive is an add-in card less than 7 inches in length.
    PCIeHalfLength,
    /// An OEM-defined form factor.
    ///
    /// This value shall indicate the drive is an OEM-defined form factor.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const HotspareReplacementModeType = enum {
    /// The hot spare drive that is commissioned due to a drive failure reverts to a hot spare after the failed drive is replaced and rebuilt.
    Revertible,
    /// The hot spare drive that is commissioned due to a drive failure remains as a data drive and does not revert to a hot spare if the failed drive is replaced.
    NonRevertible,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const HotspareType = enum {
    /// The drive is not a hot spare.
    None,
    /// The drive is serving as a hot spare for all other drives in this storage domain.
    Global,
    /// The drive is serving as a hot spare for all other drives in this storage domain that are contained in the same chassis.
    Chassis,
    /// The drive is serving as a hot spare for a user-defined set of drives or volumes.  Clients cannot specify this value when modifying the `HotspareType` property.  This value is reported as a result of configuring the spare drives within a volume.
    Dedicated,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const MediaType = enum {
    /// The drive media type is traditional magnetic platters.
    HDD,
    /// The drive media type is solid state or flash memory.
    SSD,
    /// The drive media type is shingled magnetic recording.
    SMR,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmveMinimumRequiredResetType = enum {
    /// No reset required.
    NoReset,
    /// A Controller Level Reset is required.
    ControllerLevel,
    /// A Controller Level Reset other than a Controller Level Reset initiated by a Controller Reset is required.
    LimitedControllerLevel,
    /// An NVM Subsystem Reset is required.
    NVMSubsystemReset,
    /// A power cycle is required.
    PowerCycle,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmvePersonalityKeyAlgorithm = enum {
    /// HMAC-SHA-384.
    HMAC_SHA384,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NmveUnfreezeAuthMode = enum {
    /// Programmable Key Authentication.
    ProgrammedKey,
    /// Physical Credential Authentication.
    PhysicalId,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const StatusIndicator = enum {
    /// The drive is OK.
    OK,
    /// The drive has failed.
    Fail,
    /// The drive is being rebuilt.
    Rebuild,
    /// The drive still works but is predicted to fail soon.
    PredictiveFailureAnalysis,
    /// The drive has been marked to automatically rebuild and replace a failed drive.
    Hotspare,
    /// The array to which this drive belongs has been degraded.
    InACriticalArray,
    /// The array to which this drive belongs has failed.
    InAFailedArray,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TargetConfigurationLockLevel = enum {
    /// The standard configuration lock level, corresponding to applying firmware, updating security keys, and modifying other hardware settings.  It does not include managing the volumes or data on the drive.
    Baseline,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Drive.FreezePersonality`.
///
/// This action freezes a personality for the NVMe subsystem.
pub const DriveFreezePersonalityAction = struct {
    /// The personality to freeze.  `255` indicates all personalities.
    ///
    /// This parameter shall contain the personality to freeze as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.  The value `255` shall indicate all personalities.
    Personality: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.GetPersonalityNonce`.
///
/// This action obtains a nonce required to unfreeze personalities with a programmed key.
pub const DriveGetPersonalityNonceAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.Reset`.
///
/// This action resets this drive.
pub const DriveResetAction = struct {
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and perform an implementation-specific default reset.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.RevertPersonalitiesToDefaults`.
///
/// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
pub const DriveRevertPersonalitiesToDefaultsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.RevertToOriginalFactoryState`.
///
/// This action reverts a self-encrypting drive (SED) to the original factory state.
pub const DriveRevertToOriginalFactoryStateAction = struct {
    /// The physical secure ID (PSID).  The PSID is generally printed on the drive label and used to revert an encrypted SED.
    ///
    /// This parameter shall contain the physical secure ID (PSID).  The PSID is generally printed on the drive label and used to revert an encrypted SED.
    PhysicalSecureID: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.SecureErase`.
///
/// This action securely erases the contents of the drive.
pub const DriveSecureEraseAction = struct {
    /// The type of data sanitization to perform.
    ///
    /// This parameter shall contain the type of data sanitization to perform for the secure erase request.  The service can accept a request without the parameter and perform an implementation-specific default secure erase.
    SanitizationType: ?DataSanitizationType = null,
    /// The number of times to overwrite the drive if performing an overwrite type of sanitization.
    ///
    /// This parameter shall contain the number of times to overwrite the drive if the `SanitizationType` parameter contains the value `Overwrite`.  This parameter shall be ignored if the `SanitizationType` parameter does not contain the value `Overwrite`.  If the client does not provide this parameter, the service shall perform an implementation-specific number of passes.
    OverwritePasses: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.SetPersonalityKey`.
///
/// This action sets the personality key for the NVMe subsystem.
pub const DriveSetPersonalityKeyAction = struct {
    /// The type of authentication algorithm for the key.
    ///
    /// This parameter shall contain the type of authentication algorithm for the key as defined by the 'CDP Authentication Algorithm' field in the NVMe Base Specification.
    Algorithm: ?NmvePersonalityKeyAlgorithm = null,
    /// The personality key to set on the NVMe subsystem.
    ///
    /// This parameter shall contain the personality key to set on the NVMe subsystem as a hex-encoded string.
    Key: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Drive.UnfreezePersonality`.
///
/// This action unfreezes a personality for the NVMe subsystem.
pub const DriveUnfreezePersonalityAction = struct {
    /// The personality to freeze.  `255` indicates all personalities.
    ///
    /// This parameter shall contain the personality to freeze as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.  The value `255` shall indicate all personalities.
    Personality: ?i64 = null,
    /// The authentication method for the unfreeze operation.
    ///
    /// This parameter shall contain the authentication method for the unfreeze operation as defined by the 'Authenticated Unfreeze Support' field in the NVMe Base Specification.
    AuthenticationMethod: ?NmveUnfreezeAuthMode = null,
    /// The authentication key for the unfreeze operation.
    ///
    /// This parameter shall contain the authentication key for the unfreeze operation as a hex-encoded string.  If `AuthenticationMethod` contains `ProgrammedKey`, this parameter contains a key that is derived from the original key provided in the `SetPersonalityKey` action.  If `AuthenticationMethod` contains `PhysicalId`, this parameter contains the physical secure ID (PSID) of the drive converted to a hex-encoded string.
    Key: ?[]const u8 = null,
    /// The personality nonce provided by the `GetPersonalityNonce` action.
    ///
    /// This parameter shall contain the personality nonce provided by the `GetPersonalityNonce` action.  This parameter shall be required if `AuthenticationMethod` contains `ProgrammedKey`.
    Nonce: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action freezes a personality for the NVMe subsystem.
    ///
    /// This action shall freeze a personality for the NVMe subsystem as defined by the 'Configurable Device Personality' feature in the NVMe Base Specification.
    @"#Drive.FreezePersonality": ?core.Action(DriveFreezePersonalityAction, std.json.Value) = null,
    /// This action obtains a nonce required to unfreeze personalities with a programmed key.
    ///
    /// This action shall obtain a nonce required to unfreeze personalities with a programmed key as defined by the 'CDP Random Nonce Data' clause in the NVMe Base Specification.
    @"#Drive.GetPersonalityNonce": ?core.Action(DriveGetPersonalityNonceAction, GetPersonalityNonceResponse) = null,
    /// This action resets this drive.
    ///
    /// This action shall reset this drive.
    @"#Drive.Reset": ?core.Action(DriveResetAction, std.json.Value) = null,
    /// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
    ///
    /// This action shall revert all personalities to their manufacturing default state for the NVMe subsystem as defined by the 'Manufacturing Default Personality' clause of the NVMe Base Specification.
    @"#Drive.RevertPersonalitiesToDefaults": ?core.Action(DriveRevertPersonalitiesToDefaultsAction, std.json.Value) = null,
    /// This action reverts a self-encrypting drive (SED) to the original factory state.
    ///
    /// This action shall revert a self-encrypting drive (SED) to the original factory state.  Upon successful completion of this action, the drive data shall be securely erased and the `EncryptionStatus` property shall contain `Unencrypted`.
    @"#Drive.RevertToOriginalFactoryState": ?core.Action(DriveRevertToOriginalFactoryStateAction, std.json.Value) = null,
    /// This action securely erases the contents of the drive.
    ///
    /// This action shall securely erase the drive.
    @"#Drive.SecureErase": ?core.Action(DriveSecureEraseAction, std.json.Value) = null,
    /// This action sets the personality key for the NVMe subsystem.
    ///
    /// This action shall set the personality key for the NVMe subsystem as defined by the 'Programmable Key Authentication Considerations' clause in the NVMe Base Specification.
    @"#Drive.SetPersonalityKey": ?core.Action(DriveSetPersonalityKeyAction, std.json.Value) = null,
    /// This action unfreezes a personality for the NVMe subsystem.
    ///
    /// This action shall unfreeze a personality for the NVMe subsystem as defined by the 'Authenticated Unfreeze Operation' clause in the NVMe Base Specification.
    @"#Drive.UnfreezePersonality": ?core.Action(DriveUnfreezePersonalityAction, std.json.Value) = null,

    /// This action freezes a personality for the NVMe subsystem.
    ///
    /// This action shall freeze a personality for the NVMe subsystem as defined by the 'Configurable Device Personality' feature in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn freezePersonality(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveFreezePersonalityAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.FreezePersonality" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action obtains a nonce required to unfreeze personalities with a programmed key.
    ///
    /// This action shall obtain a nonce required to unfreeze personalities with a programmed key as defined by the 'CDP Random Nonce Data' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn getPersonalityNonce(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveGetPersonalityNonceAction,
    ) !core.Owned(core.ModificationResponse(GetPersonalityNonceResponse)) {
        const target = self.@"#Drive.GetPersonalityNonce" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action resets this drive.
    ///
    /// This action shall reset this drive.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action reverts all personalities to their manufacturing default state for the NVMe subsystem.
    ///
    /// This action shall revert all personalities to their manufacturing default state for the NVMe subsystem as defined by the 'Manufacturing Default Personality' clause of the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn revertPersonalitiesToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveRevertPersonalitiesToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.RevertPersonalitiesToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action reverts a self-encrypting drive (SED) to the original factory state.
    ///
    /// This action shall revert a self-encrypting drive (SED) to the original factory state.  Upon successful completion of this action, the drive data shall be securely erased and the `EncryptionStatus` property shall contain `Unencrypted`.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn revertToOriginalFactoryState(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveRevertToOriginalFactoryStateAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.RevertToOriginalFactoryState" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action securely erases the contents of the drive.
    ///
    /// This action shall securely erase the drive.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn secureErase(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveSecureEraseAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.SecureErase" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action sets the personality key for the NVMe subsystem.
    ///
    /// This action shall set the personality key for the NVMe subsystem as defined by the 'Programmable Key Authentication Considerations' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setPersonalityKey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveSetPersonalityKeyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.SetPersonalityKey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action unfreezes a personality for the NVMe subsystem.
    ///
    /// This action shall unfreeze a personality for the NVMe subsystem as defined by the 'Authenticated Unfreeze Operation' clause in the NVMe Base Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn unfreezePersonality(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: DriveUnfreezePersonalityAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Drive.UnfreezePersonality" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The personality nonce from a drive.
///
/// This object shall contain the personality nonce from a drive.
pub const GetPersonalityNonceResponse = struct {
    /// The personality nonce.
    ///
    /// This property shall contain the personality nonce as a hex-encoded string as defined by the 'CDP Random Nonce Data' clause of the NVMe Base Specification.
    Nonce: ?[]const u8 = null,
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the volumes that this drive either wholly or only partially contains.
    ///
    /// This property shall contain an array of links to resources of type `Volume` with which this drive is associated.  This property shall include all volume resources of which this drive is a member and all volumes for which this drive acts as a spare if the hot spare type is `Dedicated`.
    Volumes: ?[]const core.NavProperty(volume.Volume) = null,
    /// An array of links to the endpoints that connect to this drive.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` with which this drive is associated.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// The link to the chassis that contains this drive.
    ///
    /// This property shall contain a link to a resource of type `Chassis` that represents the physical container associated with this drive.
    Chassis: ?core.NavProperty(chassis.Chassis) = null,
    /// An array of links to the PCIe functions that the drive produces.
    ///
    /// This property shall link to a resource of type `PCIeFunction` that represents the PCIe functions associated with this resource.
    PCIeFunctions: ?[]const core.NavProperty(pcie_function.PcieFunction) = null,
    /// An array of links to the storage pools to which this drive belongs.
    ///
    /// This property shall contain an array of links of type `StoragePool` to which this drive belongs.
    StoragePools: ?[]const core.NavProperty(storage_pool.StoragePool) = null,
    /// A link to the storage subsystem to which this drive belongs.
    ///
    /// This property shall contain a link to a resource of type `Storage` that represents the storage subsystem to which this drive belongs.
    Storage: ?core.NavProperty(storage.Storage) = null,
    /// An array of links to the network device functions that provide network connectivity for this drive.
    ///
    /// This property shall contain the array of links to resources of type `NetworkDeviceFunction`.  This property should only be present for drives with network connectivity, such as Ethernet attached drives.
    NetworkDeviceFunctions: ?[]const core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// The link to the software inventory that represents the active drive firmware image.
    ///
    /// This property shall contain a link a resource of type `SoftwareInventory` that represents the active drive firmware image.
    ActiveSoftwareImage: ?core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// The images that are associated with this drive.
    ///
    /// This property shall contain an array of links to resources of type `SoftwareInventory` that represent the firmware images that apply to this drive.
    SoftwareImages: ?[]const core.NavProperty(software_inventory.SoftwareInventory) = null,
};

/// What a client may change of `Drive.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Nvme = struct {
    /// The configurable features that are able to be locked from in-band usage on an NVMe subsystem and their current lock state.
    ///
    /// This property shall contain the configurable features that are able to be locked from in-band usage on an NVMe subsystem and their current lock state.
    ConfigurationLockState: ?NvmeConfigurationLockState = null,
    /// The active personality settings of the NVMe subsystem.
    ///
    /// This property shall contain the active personality settings of the NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
    ActivePersonalities: ?[]const ?NvmePersonality = null,
};

/// What a client may change of `Drive.NVMe`.
pub const NvmeUpdate = struct {
    /// The active personality settings of the NVMe subsystem.
    ///
    /// This property shall contain the active personality settings of the NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
    ActivePersonalities: ?[]const ?NvmePersonalityUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The configurable features that are able to be locked on an NVMe subsystem and their current lock state.
///
/// This type shall contain the configurable features that are able to be locked on an NVMe subsystem and their current lock state.
pub const NvmeConfigurationLockState = struct {
    /// The lock state of the NVMe-defined Firmware Commit command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Firmware Commit command.
    FirmwareCommit: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Lockdown command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Lockdown command.
    Lockdown: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Security Send command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Security Send command.
    SecuritySend: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-defined Firmware Image Download command.
    ///
    /// This property shall contain the lock state of the NVMe-defined Firmware Image Download command.
    FirmwareImageDownload: ?ConfigLockOptions = null,
    /// The lock state of the NVMe-MI-defined VPD Write command.
    ///
    /// This property shall contain the lock state of the NVMe-MI-defined VPD Write command.
    VPDWrite: ?ConfigLockOptions = null,
};

/// An active personality setting of an NVMe subsystem.
///
/// This type shall contain an active personality setting of an NVMe subsystem as defined by the NVMe Device Personalities log page and NVMe Configurable Device Personality feature.
pub const NvmePersonality = struct {
    /// The personality identifier.
    ///
    /// This property shall contain the personality identifier as defined by the 'Personality Identifier List' figure in the NVMe Base Specification.
    Identifier: ?i64 = null,
    /// An indication of whether this personality is set to the manufacturing default values.
    ///
    /// This property shall indicate whether this personality is set to the manufacturing default values as defined by the 'Personality Manufacturing Default Settings State' field in the NVMe Base Specification.
    Default: ?bool = null,
    /// An indication of whether this personality is frozen.
    ///
    /// This property shall indicate whether this personality is frozen as defined by the 'Personality Freeze State' field in the NVMe Base Specification.
    Frozen: ?bool = null,
    /// An indication of whether this personality contains pending changes.
    ///
    /// This property shall indicate whether this personality contains pending changes as defined by the 'Pending Personality Settings Change' field in the NVMe Base Specification.
    Pending: ?bool = null,
    /// The minimum reset type required to change the personality settings.
    ///
    /// This property shall contain the minimum reset type required to change the personality settings as defined by the 'Minimum Required Reset Type' field in the NVMe Base Specification.
    MinimumRequiredResetType: ?NmveMinimumRequiredResetType = null,
    /// An indication of whether changing this personality affects user data.
    ///
    /// This property shall indicate whether changing this personality affects user data as defined by the 'Personality Settings Change User Data Effect' field in the NVMe Base Specification.
    ChangeAffectsUserData: ?bool = null,
    /// The authentication modes supported to unfreeze this personality.
    ///
    /// This property shall contain the authentication modes supported to unfreeze this personality as defined by the 'Authenticated Unfreeze Support' field in the NVMe Base Specification.
    UnfreezeAuthenticationModes: ?[]const ?NmveUnfreezeAuthMode = null,
    /// The personality data.
    ///
    /// This property shall contain the personality data as a hex-encoded string.
    Data: ?[]const u8 = null,
};

/// What a client may change of `Drive.NVMePersonality`.
///
/// An active personality setting of an NVMe subsystem.
pub const NvmePersonalityUpdate = struct {
    /// The personality data.
    ///
    /// This property shall contain the personality data as a hex-encoded string.
    Data: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const Operations = struct {
    /// The name of the operation.
    ///
    /// This property shall contain a string of the name of the operation.
    ///
    /// Deprecated in v1_17_0.
    /// This property is deprecated in favor of the `Operation` property using the `OperationType` enumeration defined in the `Volume` schema.
    OperationName: ?[]const u8 = null,
    /// The percentage of the operation that has been completed.
    ///
    /// This property shall contain an integer of the percentage, `0` to `100`, of the operation that has been completed.
    PercentageComplete: ?i64 = null,
    /// The type of the operation.
    ///
    /// This property shall contain the type of the operation.
    Operation: ?volume.OperationType = null,
    /// The link to the task associated with the operation, if any.
    ///
    /// This property shall contain a link to a resource of type `Task` that represents the task associated with the operation.
    AssociatedTask: ?core.NavProperty(task.Task) = null,
};

/// The `Drive` schema represents a single physical drive for a system, including links to associated volumes.  It also describes the location, such as a slot, socket, or bay, where a unit can be installed, by populating a resource instance with an absent state if a unit is not present.
///
/// This resource shall represent a drive or other physical storage medium for a Redfish implementation.  It may also represent a location, such as a slot, socket, or bay, where a unit may be installed, but the `State` property within the `Status` property contains `Absent`.
pub const Drive = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The state of the status indicator, which communicates status information about this drive.
    ///
    /// This property shall contain the status indicator state for the status indicator associated with this drive.  The `@Redfish.AllowableValues` annotation specifies the valid values for this property.
    StatusIndicator: ?StatusIndicator = null,
    /// The state of the indicator LED, that identifies the drive.
    ///
    /// This property shall contain the state for the indicator light associated with this drive.
    ///
    /// Deprecated in v1_11_0.
    /// This property has been deprecated in favor of the `LocationIndicatorActive` property.
    IndicatorLED: ?resource.IndicatorLed = null,
    /// The model number for the drive.
    ///
    /// This property shall contain the name by which the manufacturer generally refers to the drive.
    Model: ?[]const u8 = null,
    /// The revision of a SCSI-based drive, as returned from the 'Inquiry' command, which can be the firmware or hardware version.  For other types of drives, the `HardwareVersion` and `FirmwareVersion` properties are recommended in favor of this property.
    ///
    /// This property shall contain the manufacturer-defined revision for a SCSI-based drive, as returned in the 'Product Revision Level' field from the 'Inquiry' command, which can be the firmware or hardware version.  For other types of drives, this property should not be present and services should support the `HardwareVersion` and `FirmwareVersion` properties in favor of this property.  If this property is present for drives that are not SCSI-based, the value may contain a firmware version, hardware version, or a combination.
    Revision: ?[]const u8 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The size, in bytes, of this drive.
    ///
    /// This property shall contain the raw size, in bytes, of the associated drive.
    CapacityBytes: ?i64 = null,
    /// An indication of whether this drive currently predicts a failure in the near future.
    ///
    /// This property shall indicate whether this drive currently predicts a manufacturer-defined failure.
    FailurePredicted: ?bool = null,
    /// The protocol that this drive currently uses to communicate to the storage controller.
    ///
    /// This property shall contain the protocol that the associated drive currently uses to communicate to the storage controller for this system.
    Protocol: ?protocol.Protocol = null,
    /// The type of media contained in this drive.
    ///
    /// This property shall contain the type of media contained in the associated drive.
    MediaType: ?MediaType = null,
    /// The manufacturer of this drive.
    ///
    /// This property shall contain the name of the organization responsible for producing the drive.  This organization may be the entity from whom the drive is purchased, but this is not necessarily true.
    Manufacturer: ?[]const u8 = null,
    /// The SKU for this drive.
    ///
    /// This property shall contain the stock-keeping unit (SKU) number for this drive.
    SKU: ?[]const u8 = null,
    /// The serial number for this drive.
    ///
    /// This property shall contain the manufacturer-allocated number that identifies the drive.
    SerialNumber: ?[]const u8 = null,
    /// The part number for this drive.
    ///
    /// This property shall contain the part number assigned by the organization that is responsible for producing or manufacturing the drive.
    PartNumber: ?[]const u8 = null,
    /// The user-assigned asset tag for this drive.
    ///
    /// This property shall track the drive for inventory purposes.
    AssetTag: ?[]const u8 = null,
    /// The durable names for the drive.
    ///
    /// This property shall contain a list of all known durable names for the associated drive.
    Identifiers: ?[]const resource.Identifier = null,
    /// The location of the drive.
    ///
    /// This property shall contain the location information of the associated drive.
    ///
    /// Deprecated in v1_4_0.
    /// This property has been deprecated in favor of the singular `PhysicalLocation` property.
    Location: ?[]const resource.Location = null,
    /// The type of hot spare that this drive serves as.
    ///
    /// This property shall contain the hot spare type for the associated drive.  If the drive currently serves as a hot spare, the `State` property in `Status` shall contain `StandbySpare` and `Enabled` when it is part of a volume.
    HotspareType: ?HotspareType = null,
    /// The encryption ability of this drive.
    ///
    /// This property shall contain the encryption ability for the associated drive.
    EncryptionAbility: ?EncryptionAbility = null,
    /// The status of the encryption of this drive.
    ///
    /// This property shall contain the encryption status for the associated drive.
    EncryptionStatus: ?EncryptionStatus = null,
    /// The rotation speed of this drive, in revolutions per minute (RPM) units.
    ///
    /// This property shall contain the rotation speed, in revolutions per minute (RPM) units, of the associated drive.
    RotationSpeedRPM: ?core.Decimal = null,
    /// The size, in bytes, of the smallest addressable unit, or block.
    ///
    /// This property shall contain the size of the smallest addressable unit of the associated drive.
    BlockSizeBytes: ?i64 = null,
    /// The speed, in gigabits per second (Gbit/s) units, at which this drive can communicate to a storage controller in ideal conditions.
    ///
    /// This property shall contain fastest capable bus speed, in gigabits per second (Gbit/s) units, of the associated drive.
    CapableSpeedGbs: ?core.Decimal = null,
    /// The speed, in gigabits per second (Gbit/s) units, at which this drive currently communicates to the storage controller.
    ///
    /// This property shall contain current bus speed, in gigabits per second (Gbit/s) units, of the associated drive.
    NegotiatedSpeedGbs: ?core.Decimal = null,
    /// The percentage of reads and writes that are predicted to be available for the media.
    ///
    /// This property shall contain an indicator of the percentage, typically `0` to `100`, of life remaining in the drive's media.
    PredictedMediaLifeLeftPercent: ?core.Decimal = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The operations currently running on the drive.
    ///
    /// This property shall contain a list of all operations currently running on the drive.
    Operations: ?[]const Operations = null,
    /// The location of the drive.  This property is equivalent to the `Location` property common to other standard Redfish resources.
    ///
    /// This property shall contain the location information of the associated drive.
    PhysicalLocation: ?resource.Location = null,
    /// The replacement mode for the hot spare drive.
    ///
    /// This property shall indicate whether a commissioned hot spare continues to serve as a hot spare after the failed drive is replaced.
    HotspareReplacementMode: ?HotspareReplacementModeType = null,
    /// An indication of whether the drive write cache is enabled.
    ///
    /// This property shall indicate whether the drive write cache is enabled.
    WriteCacheEnabled: ?bool = null,
    /// An indication of whether the drive is accessible from multiple paths.
    ///
    /// This property shall indicate whether the drive is accessible by an initiator from multiple paths allowing for failover capabilities upon a path failure.
    Multipath: ?bool = null,
    /// An indication of whether the drive is prepared by the system for removal.
    ///
    /// This property shall indicate whether the system is prepared for the removal of this drive.
    ReadyToRemove: ?bool = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: ?bool = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_14_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// The form factor of the slot.
    ///
    /// This property shall contain the form factor of the slot.
    SlotFormFactor: ?FormFactor = null,
    /// The form factor of the drive inserted in this slot.
    ///
    /// This property shall contain the form factor of the drive inserted in this slot.
    DriveFormFactor: ?FormFactor = null,
    /// The drive protocols capable in this slot.
    ///
    /// This property shall contain the drive protocols capable in this slot.  The value of this property depends upon the connector in this slot, the storage controllers connected to this slot, the configuration of the system, and other constraints that determine if a particular protocol is capable at a given time.
    SlotCapableProtocols: ?[]const ?protocol.Protocol = null,
    /// The firmware version for this drive.
    ///
    /// This property shall contain the firmware version as defined by the manufacturer for this drive.
    FirmwareVersion: ?[]const u8 = null,
    /// The spare part number of the drive.
    ///
    /// This property shall contain the spare part number of the drive.
    SparePartNumber: ?[]const u8 = null,
    /// Indicates whether in-band configuration requests to the drive are locked.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall indicate whether configuration requests to the drive are locked.  Services shall reject modification requests that contain the value `Partial`.
    ConfigurationLock: ?ConfigurationLock = null,
    /// Indicates if establishment of a TCG-defined security ID (SID) on the drive is blocked.
    ///
    /// This property shall indicate if establishment of a TCG-defined security ID (SID) on the drive is blocked.  The value `true` shall indicate the TCG-defined 'Block SID' command is sent to the drive during each drive boot sequence.
    BlockSecurityIDEnabled: ?bool = null,
    /// Indicates the target configuration lock level for the drive based upon the state of the `ConfigurationLock` property.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall contain the target configuration lock level for the drive.  For NVMe drives, services shall implement the locking requirements specified by SNIA's Swordfish NVMe Model Overview and Mapping Guide.
    TargetConfigurationLockLevel: ?TargetConfigurationLockLevel = null,
    /// NVMe properties for this drive.
    ///
    /// This property shall contain NVMe-specific properties of this drive.
    NVMe: ?Nvme = null,
    /// The hardware version of this drive.
    ///
    /// This property shall contain the hardware version of this drive as determined by the vendor or supplier.
    HardwareVersion: ?[]const u8 = null,
    /// Indicates if the TCG-defined security ID (SID) was changed from its default value, flagging the drive as owned.
    ///
    /// This property shall indicate if a TCG-defined security ID (SID) differs from the TCG-defined manufacturing security ID (MSID) on the drive, flagging the drive as owned.  The value `true` shall indicate that the MSID and SID differ.  This property should not be present for non-self-encrypting (SED) drives.
    SecurityIDOwned: ?bool = null,
    /// The link to the assembly associated with this drive.
    ///
    /// This property shall contain a link to a resource of type `Assembly`.
    Assembly: ?core.NavProperty(assembly.Assembly) = null,
    /// The link to the environment metrics for this drive.
    ///
    /// This property shall contain a link to a resource of type `EnvironmentMetrics` that specifies the environment metrics for this drive.
    EnvironmentMetrics: ?core.NavProperty(environment_metrics.EnvironmentMetrics) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to the metrics associated with this drive.
    ///
    /// This property shall contain a link to the metrics associated with this drive.
    Metrics: ?core.NavProperty(drive_metrics.DriveMetrics) = null,
};

/// What a client may change of `Drive.Drive`.
///
/// The `Drive` schema represents a single physical drive for a system, including links to associated volumes.  It also describes the location, such as a slot, socket, or bay, where a unit can be installed, by populating a resource instance with an absent state if a unit is not present.
pub const DriveUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The state of the status indicator, which communicates status information about this drive.
    ///
    /// This property shall contain the status indicator state for the status indicator associated with this drive.  The `@Redfish.AllowableValues` annotation specifies the valid values for this property.
    StatusIndicator: core.Nullable(StatusIndicator) = .absent,
    /// The state of the indicator LED, that identifies the drive.
    ///
    /// This property shall contain the state for the indicator light associated with this drive.
    ///
    /// Deprecated in v1_11_0.
    /// This property has been deprecated in favor of the `LocationIndicatorActive` property.
    IndicatorLED: core.Nullable(resource.IndicatorLed) = .absent,
    /// The user-assigned asset tag for this drive.
    ///
    /// This property shall track the drive for inventory purposes.
    AssetTag: core.Nullable([]const u8) = .absent,
    /// The location of the drive.
    ///
    /// This property shall contain the location information of the associated drive.
    ///
    /// Deprecated in v1_4_0.
    /// This property has been deprecated in favor of the singular `PhysicalLocation` property.
    Location: ?[]const resource.LocationUpdate = null,
    /// The type of hot spare that this drive serves as.
    ///
    /// This property shall contain the hot spare type for the associated drive.  If the drive currently serves as a hot spare, the `State` property in `Status` shall contain `StandbySpare` and `Enabled` when it is part of a volume.
    HotspareType: core.Nullable(HotspareType) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The location of the drive.  This property is equivalent to the `Location` property common to other standard Redfish resources.
    ///
    /// This property shall contain the location information of the associated drive.
    PhysicalLocation: ?resource.LocationUpdate = null,
    /// The replacement mode for the hot spare drive.
    ///
    /// This property shall indicate whether a commissioned hot spare continues to serve as a hot spare after the failed drive is replaced.
    HotspareReplacementMode: core.Nullable(HotspareReplacementModeType) = .absent,
    /// An indication of whether the drive write cache is enabled.
    ///
    /// This property shall indicate whether the drive write cache is enabled.
    WriteCacheEnabled: core.Nullable(bool) = .absent,
    /// An indication of whether the drive is prepared by the system for removal.
    ///
    /// This property shall indicate whether the system is prepared for the removal of this drive.
    ReadyToRemove: core.Nullable(bool) = .absent,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: core.Nullable(bool) = .absent,
    /// Indicates whether in-band configuration requests to the drive are locked.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall indicate whether configuration requests to the drive are locked.  Services shall reject modification requests that contain the value `Partial`.
    ConfigurationLock: core.Nullable(ConfigurationLock) = .absent,
    /// Indicates if establishment of a TCG-defined security ID (SID) on the drive is blocked.
    ///
    /// This property shall indicate if establishment of a TCG-defined security ID (SID) on the drive is blocked.  The value `true` shall indicate the TCG-defined 'Block SID' command is sent to the drive during each drive boot sequence.
    BlockSecurityIDEnabled: core.Nullable(bool) = .absent,
    /// Indicates the target configuration lock level for the drive based upon the state of the `ConfigurationLock` property.  Other properties, such as `ConfigurationLockState`, contain additional information regarding the status of the configuration lock.
    ///
    /// This property shall contain the target configuration lock level for the drive.  For NVMe drives, services shall implement the locking requirements specified by SNIA's Swordfish NVMe Model Overview and Mapping Guide.
    TargetConfigurationLockLevel: core.Nullable(TargetConfigurationLockLevel) = .absent,
    /// NVMe properties for this drive.
    ///
    /// This property shall contain NVMe-specific properties of this drive.
    NVMe: core.Nullable(NvmeUpdate) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
