//! Namespace `TelemetryService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const log_service = @import("log_service.zig");
const metric_definition = @import("metric_definition.zig");
const metric_definition_collection = @import("metric_definition_collection.zig");
const metric_report_collection = @import("metric_report_collection.zig");
const metric_report_definition_collection = @import("metric_report_definition_collection.zig");
const resource = @import("resource.zig");
const telemetry_data = @import("telemetry_data.zig");
const telemetry_data_collection = @import("telemetry_data_collection.zig");
const triggers_collection = @import("triggers_collection.zig");

/// An operation to perform over the sample.
///
/// If present, the metric value shall be computed according to this function.
pub const CollectionFunction = enum {
    /// An averaging function.
    Average,
    /// A maximum function.
    Maximum,
    /// A minimum function.
    Minimum,
    /// A summation function.
    Summation,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `TelemetryService.ClearMetricReports`.
///
/// The action to clear the metric reports for this telemetry service.
pub const TelemetryServiceClearMetricReportsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `TelemetryService.ClearTelemetryData`.
///
/// The action to clear the telemetry data for this telemetry service.
pub const TelemetryServiceClearTelemetryDataAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `TelemetryService.CollectTelemetryData`.
///
/// The action to collect the telemetry data from a device or service.  When the telemetry data is collected, a new telemetry data resource will be created.
pub const TelemetryServiceCollectTelemetryDataAction = struct {
    /// The type of telemetry data to collect.
    ///
    /// This parameter shall contain the type of telemetry data to collect.
    TelemetryDataType: ?telemetry_data.TelemetryDataTypes = null,
    /// The OEM-defined type of telemetry data to collect.
    ///
    /// This parameter shall contain the OEM-defined type of telemetry data to collect.  This parameter shall be required if `TelemetryDataType` is `OEM`.
    OEMTelemetryDataType: ?[]const u8 = null,
    /// An array of devices from which to collect telemetry data.
    ///
    /// This parameter shall contain an array of devices from which to collect telemetry data.
    TargetDevices: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `TelemetryService.ResetMetricReportDefinitionsToDefaults`.
///
/// The action to reset the metric report definitions to factory defaults.
pub const TelemetryServiceResetMetricReportDefinitionsToDefaultsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `TelemetryService.ResetTriggersToDefaults`.
///
/// The action to reset the triggers to factory defaults.
pub const TelemetryServiceResetTriggersToDefaultsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `TelemetryService.SubmitTestMetricReport`.
///
/// This action generates a metric report.
pub const TelemetryServiceSubmitTestMetricReportAction = struct {
    /// The name of the metric report in generated metric report.
    ///
    /// This parameter shall contain the name of the generated metric report.
    MetricReportName: ?[]const u8 = null,
    /// The contents of the `MetricReportValues` array in the generated metric report.
    ///
    /// This parameter shall contain the contents of the `MetricReportValues` array property in the generated metric report.
    ///
    /// Deprecated in v1_1_0.
    /// This property has been deprecated in favor of using the property `GeneratedMetricReportValues`.
    MetricReportValues: ?[]const u8 = null,
    /// The contents of the `MetricReportValues` in the generated metric report.
    ///
    /// This parameter shall contain the contents of the `MetricReportValues` array property in the generated metric report.
    GeneratedMetricReportValues: ?[]const MetricValue = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// The action to clear the metric reports for this telemetry service.
    ///
    /// This action shall delete all entries found in the metric report collection for this telemetry service.
    @"#TelemetryService.ClearMetricReports": ?core.Action(TelemetryServiceClearMetricReportsAction, std.json.Value) = null,
    /// The action to clear the telemetry data for this telemetry service.
    ///
    /// This action shall delete all entries found in the telemetry data collection for this telemetry service.
    @"#TelemetryService.ClearTelemetryData": ?core.Action(TelemetryServiceClearTelemetryDataAction, std.json.Value) = null,
    /// The action to collect the telemetry data from a device or service.  When the telemetry data is collected, a new telemetry data resource will be created.
    ///
    /// This action shall collect the telemetry data from a device or service.  The `Location` header in the response shall contain a URI to a resource of type `TelemetryData` that contains the telemetry data.  The `AdditionalDataURI` property in the referenced `TelemetryData` resource shall contain the URI to download the telemetry data.
    @"#TelemetryService.CollectTelemetryData": ?core.Action(TelemetryServiceCollectTelemetryDataAction, CollectTelemetryDataResponse) = null,
    /// The action to reset the metric report definitions to factory defaults.
    ///
    /// This action shall reset all entries found in the metric report definition collection to factory defaults.  This action may delete members of the metric report definition collection.
    @"#TelemetryService.ResetMetricReportDefinitionsToDefaults": ?core.Action(TelemetryServiceResetMetricReportDefinitionsToDefaultsAction, std.json.Value) = null,
    /// The action to reset the triggers to factory defaults.
    ///
    /// This action shall reset all entries found in the triggers collection to factory defaults.  This action may delete members of the triggers collection.
    @"#TelemetryService.ResetTriggersToDefaults": ?core.Action(TelemetryServiceResetTriggersToDefaultsAction, std.json.Value) = null,
    /// This action generates a metric report.
    ///
    /// This action shall cause the event service to immediately generate the metric report as an alert event.  Then, this message should be sent to any appropriate event destinations.
    @"#TelemetryService.SubmitTestMetricReport": ?core.Action(TelemetryServiceSubmitTestMetricReportAction, std.json.Value) = null,

    /// The action to clear the metric reports for this telemetry service.
    ///
    /// This action shall delete all entries found in the metric report collection for this telemetry service.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn clearMetricReports(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceClearMetricReportsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#TelemetryService.ClearMetricReports" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The action to clear the telemetry data for this telemetry service.
    ///
    /// This action shall delete all entries found in the telemetry data collection for this telemetry service.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn clearTelemetryData(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceClearTelemetryDataAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#TelemetryService.ClearTelemetryData" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The action to collect the telemetry data from a device or service.  When the telemetry data is collected, a new telemetry data resource will be created.
    ///
    /// This action shall collect the telemetry data from a device or service.  The `Location` header in the response shall contain a URI to a resource of type `TelemetryData` that contains the telemetry data.  The `AdditionalDataURI` property in the referenced `TelemetryData` resource shall contain the URI to download the telemetry data.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn collectTelemetryData(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceCollectTelemetryDataAction,
    ) !core.Owned(core.ModificationResponse(CollectTelemetryDataResponse)) {
        const target = self.@"#TelemetryService.CollectTelemetryData" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The action to reset the metric report definitions to factory defaults.
    ///
    /// This action shall reset all entries found in the metric report definition collection to factory defaults.  This action may delete members of the metric report definition collection.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetMetricReportDefinitionsToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceResetMetricReportDefinitionsToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#TelemetryService.ResetMetricReportDefinitionsToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The action to reset the triggers to factory defaults.
    ///
    /// This action shall reset all entries found in the triggers collection to factory defaults.  This action may delete members of the triggers collection.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetTriggersToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceResetTriggersToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#TelemetryService.ResetTriggersToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action generates a metric report.
    ///
    /// This action shall cause the event service to immediately generate the metric report as an alert event.  Then, this message should be sent to any appropriate event destinations.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn submitTestMetricReport(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: TelemetryServiceSubmitTestMetricReportAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#TelemetryService.SubmitTestMetricReport" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The response body for the `CollectTelemetryData` action.
///
/// This type shall contain the properties found in the response body for the `CollectTelemetryData` action.
pub const CollectTelemetryDataResponse = struct {
    /// An array of links to the collected telemetry data.
    ///
    /// This property shall contain an array of links to resources of type `TelemetryData` that represent the collected telemetry data.
    TelemetryData: ?[]const []const u8 = null,
};

/// Properties that capture a metric value and other associated information.
///
/// This type shall contain properties that capture a metric value and other associated information.
pub const MetricValue = struct {
    /// The metric definitions identifier for this metric.
    ///
    /// This property shall contain the same value as the `Id` property of the source metric within the associated metric definition.
    MetricId: ?[]const u8 = null,
    /// The metric value, as a string.
    ///
    /// This property shall contain the metric value, as a string.
    MetricValue: ?[]const u8 = null,
    /// The date and time when the metric is obtained.  A management application can establish a time series of metric data by retrieving the instances of metric value and sorting them according to their timestamp.
    ///
    /// The value shall time when the metric value was obtained.  Note that this value may be different from the time when this instance is created.
    Timestamp: ?core.DateTimeOffset = null,
    /// The URI for the property from which this metric is derived.
    ///
    /// The value shall be the URI to the property following the JSON fragment notation, as defined by RFC6901, to identify an individual property in a Redfish resource.
    MetricProperty: ?[]const u8 = null,
    /// The link to the metric definition for this metric.
    ///
    /// This property shall contain a link to a resource of type `MetricDefinition` that describes what this metric value captures.
    MetricDefinition: ?core.NavProperty(metric_definition.MetricDefinition) = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `TelemetryService` schema describes a telemetry service.  The telemetry service is used for collecting and reporting metric data within the Redfish service.
///
/// This resource contains a telemetry service for a Redfish implementation.
pub const TelemetryService = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The maximum number of metric reports that this service supports.
    ///
    /// This property shall contain the maximum number of metric reports that this service supports.
    MaxReports: ?i64 = null,
    /// The minimum time interval between gathering metric data that this service allows.
    ///
    /// This property shall contain the minimum time interval between gathering metric data that this service allows.
    MinCollectionInterval: ?core.Duration = null,
    /// The functions that can be performed over each metric.
    ///
    /// This property shall contain the function to apply over the collection duration.
    SupportedCollectionFunctions: ?[]const ?CollectionFunction = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: ?bool = null,
    /// A list of supported telemetry data types.
    ///
    /// This property shall contain a list of supported telemetry data types.
    SupportedTelemetryDataTypes: ?[]const ?telemetry_data.TelemetryDataTypes = null,
    /// A list of supported OEM-defined telemetry data types.
    ///
    /// This property shall contain a list of supported OEM-defined telemetry data types.
    SupportedOEMTelemetryDataTypes: ?[]const ?[]const u8 = null,
    /// The link to the collection of metric definitions.
    ///
    /// This property shall contain a link to a resource collection of type `MetricDefinitionCollection`.
    MetricDefinitions: ?core.NavProperty(metric_definition_collection.MetricDefinitionCollection) = null,
    /// The link to the collection of metric report definitions.
    ///
    /// This property shall contain a link to a resource collection of type `MetricReportDefinitionCollection`.
    MetricReportDefinitions: ?core.NavProperty(metric_report_definition_collection.MetricReportDefinitionCollection) = null,
    /// The link to the collection of metric reports.
    ///
    /// This property shall contain a link to a resource collection of type `MetricReportCollection`.
    MetricReports: ?core.NavProperty(metric_report_collection.MetricReportCollection) = null,
    /// The link to the collection of triggers that apply to metrics.
    ///
    /// This property shall contain a link to a resource collection of type `TriggersCollection`.
    Triggers: ?core.NavProperty(triggers_collection.TriggersCollection) = null,
    /// The link to a log service that the telemetry service uses.  This service can be a dedicated log service or a pointer to a log service under another resource, such as a manager.
    ///
    /// This property shall contain a link to a resource of type `LogService` that this telemetry service uses.
    LogService: ?core.NavProperty(log_service.LogService) = null,
    /// The link to the collection of data telemetry.
    ///
    /// This property shall contain a link to a resource collection of type `TelemetryDataCollection`.
    TelemetryData: ?core.NavProperty(telemetry_data_collection.TelemetryDataCollection) = null,
};

/// What a client may change of `TelemetryService.TelemetryService`.
///
/// The `TelemetryService` schema describes a telemetry service.  The telemetry service is used for collecting and reporting metric data within the Redfish service.
pub const TelemetryServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
