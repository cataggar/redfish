//! Namespace `LicenseService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const license_collection = @import("license_collection.zig");
const resource = @import("resource.zig");

pub const TransferProtocolType = enum {
    /// Common Internet File System (CIFS).
    CIFS,
    /// File Transfer Protocol (FTP).
    FTP,
    /// SSH File Transfer Protocol (SFTP).
    SFTP,
    /// Hypertext Transfer Protocol (HTTP).
    HTTP,
    /// Hypertext Transfer Protocol Secure (HTTPS).
    HTTPS,
    /// Secure Copy Protocol (SCP).
    SCP,
    /// Trivial File Transfer Protocol (TFTP).
    TFTP,
    /// A manufacturer-defined protocol.
    OEM,
    /// Network File System (NFS).
    NFS,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `LicenseService.Install`.
///
/// This action installs one or more licenses from a remote file.
pub const LicenseServiceInstallAction = struct {
    /// The URI of the license file to install.
    ///
    /// This parameter shall contain an RFC3986-defined URI that links to a file that the license service retrieves to install the license in that file.  This URI should contain a scheme that describes the transfer protocol.  If the `TransferProtocol` parameter is absent or not supported, and a transfer protocol is not specified by a scheme contained within this URI, the service shall use HTTP to get the file.
    LicenseFileURI: ?[]const u8 = null,
    /// The network protocol that the license service uses to retrieve the license file located at the URI provided in `LicenseFileURI`.  This parameter is ignored if the URI provided in `LicenseFileURI` contains a scheme.
    ///
    /// This parameter shall contain the network protocol that the license service shall use to retrieve the license file located at the `LicenseFileURI`.  Services should ignore this parameter if the URI provided in `LicenseFileURI` contains a scheme.  If this parameter is not provided or supported, and if a transfer protocol is not specified by a scheme contained within this URI, the service shall use HTTP to retrieve the file.
    TransferProtocol: ?TransferProtocolType = null,
    /// The username to access the URI specified by the `LicenseFileURI` parameter.
    ///
    /// This parameter shall contain the username to access the URI specified by the `LicenseFileURI` parameter.
    Username: ?[]const u8 = null,
    /// The password to access the URI specified by the `LicenseFileURI` parameter.
    ///
    /// This parameter shall contain the password to access the URI specified by the `LicenseFileURI` parameter.
    Password: ?[]const u8 = null,
    /// An array of links to the devices to be authorized by the license.
    ///
    /// This parameter shall contain an array of links to the devices to be authorized by the license.  Clients can provide this parameter when installing a license to apply the license to specific devices.  If not provided when installing a license, the service may determine the devices to which the license applies.  This parameter shall not be present if the `AuthorizationScope` property contains the value `Service`.
    AuthorizedDevices: ?[]const core.Reference = null,
    /// An array of links to the managers where the license will be installed.
    ///
    /// This property shall contain an array of links to resources of type `Manager` that represent the services where the license will be installed, such as remote Redfish services.  This parameter shall only be present in aggregators when the `AuthorizationScope` property contains `Service` or `Capacity`.
    TargetServices: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action installs one or more licenses from a remote file.
    ///
    /// This action shall install one or more licenses from a remote file.  The service may update an existing `License` resource.  The `Location` header in the response shall contain the URI of the new or updated `License` resource.
    @"#LicenseService.Install": ?core.Action(LicenseServiceInstallAction, std.json.Value) = null,

    /// This action installs one or more licenses from a remote file.
    ///
    /// This action shall install one or more licenses from a remote file.  The service may update an existing `License` resource.  The `Location` header in the response shall contain the URI of the new or updated `License` resource.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn install(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: LicenseServiceInstallAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#LicenseService.Install" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `LicenseService` schema describes the license service and the properties for the service itself with a link to the collection of licenses.  The license service also provides methods for installing licenses in a Redfish service.
///
/// This resource shall represent a license service and the properties that affect the service itself for a Redfish implementation.
pub const LicenseService = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: ?bool = null,
    /// The number of days prior to a license expiration that a warning message is sent.  A value of zero indicates no warning message is sent.
    ///
    /// This property shall contain the number of days prior to a license expiration that the service shall send the `DaysBeforeExpiration` message from the License Message Registry at least once.  A value of zero shall indicate that no warning messages are sent prior to license expiration.
    LicenseExpirationWarningDays: ?i64 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The link to the collection of licenses.
    ///
    /// This property shall contain a link to a resource collection of type `LicenseCollection`.  When installing a license with a `POST` operation to this collection, the service may update an existing License resource instead of creating a new resource.  In these cases, the service shall respond with the HTTP `200 OK` status code or HTTP `204 No Content` status code and the `Location` header in the response shall contain the URI of the updated License resource.
    Licenses: ?core.NavProperty(license_collection.LicenseCollection) = null,
};

/// What a client may change of `LicenseService.LicenseService`.
///
/// The `LicenseService` schema describes the license service and the properties for the service itself with a link to the collection of licenses.  The license service also provides methods for installing licenses in a Redfish service.
pub const LicenseServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,
    /// The number of days prior to a license expiration that a warning message is sent.  A value of zero indicates no warning message is sent.
    ///
    /// This property shall contain the number of days prior to a license expiration that the service shall send the `DaysBeforeExpiration` message from the License Message Registry at least once.  A value of zero shall indicate that no warning messages are sent prior to license expiration.
    LicenseExpirationWarningDays: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
