//! Namespace `Bios`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");
const software_inventory = @import("software_inventory.zig");

/// Arguments to `Bios.ChangePassword`.
///
/// This action changes a BIOS password.
pub const BiosChangePasswordAction = struct {
    /// The name of the BIOS password to change.
    ///
    /// This parameter shall contain the name of the BIOS password to change.  For instance, `AdminPassword` or `UserPassword`.
    PasswordName: ?[]const u8 = null,
    /// The existing BIOS password.  This parameter is not needed if the password is currently not set.
    ///
    /// This parameter shall contain the existing BIOS password to change.  Services may reject the action request if this parameter is not provided or the value supplied does not match the current password.
    OldPassword: ?[]const u8 = null,
    /// The new BIOS password.
    ///
    /// This parameter shall contain the new BIOS password.
    NewPassword: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Bios.ResetBios`.
///
/// This action resets the BIOS attributes to default.
pub const BiosResetBiosAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action changes a BIOS password.
    ///
    /// This action shall change the selected BIOS password.
    @"#Bios.ChangePassword": ?core.Action(BiosChangePasswordAction, std.json.Value) = null,
    /// This action resets the BIOS attributes to default.
    ///
    /// This action shall reset the BIOS attributes to their default values.  To apply the default values, a system reset may be required.  This action can impact other resources.  This action may clear pending values in the settings resource.
    @"#Bios.ResetBios": ?core.Action(BiosResetBiosAction, std.json.Value) = null,

    /// This action changes a BIOS password.
    ///
    /// This action shall change the selected BIOS password.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn changePassword(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: BiosChangePasswordAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Bios.ChangePassword" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action resets the BIOS attributes to default.
    ///
    /// This action shall reset the BIOS attributes to their default values.  To apply the default values, a system reset may be required.  This action can impact other resources.  This action may clear pending values in the settings resource.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetBios(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: BiosResetBiosAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Bios.ResetBios" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The list of BIOS attributes and their values as determined by the manufacturer or provider.
///
/// This property shall contain the list of BIOS attributes and their values as determined by the manufacturer or provider.  This object shall describe BIOS attribute settings as additional properties.  If the object specifies a BIOS attribute registry, attributes shall be looked up in that attribute registry by their attribute name.  Attributes in this attribute registry with the `AttributeType` of `Enumeration` shall use valid `ValueName` values in this object, as listed in that attribute registry.
pub const Attributes = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The link to the software inventory that represents the active BIOS firmware image.
    ///
    /// This property shall contain a link a resource of type `SoftwareInventory` that represents the active BIOS firmware image.
    ActiveSoftwareImage: ?core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// The images that are associated with this BIOS.
    ///
    /// This property shall contain an array of links to resources of type `SoftwareInventory` that represent the firmware images that apply to this BIOS.
    SoftwareImages: ?[]const core.NavProperty(software_inventory.SoftwareInventory) = null,
};

/// What a client may change of `Bios.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// Information about a BIOS password specific to the manufacturer or provider.
///
/// This type shall contain information about a BIOS password specific to the manufacturer or provider.
pub const Password = struct {
    /// Indicates if this password is set.
    ///
    /// This property shall contain `true` if a valid value was provided for this password.  Otherwise, the property shall contain `false`.
    Set: ?bool = null,
    /// The name of the password specific to the manufacturer or provider.  The value is used with the `PasswordName` parameter for the `ChangePassword` action.
    ///
    /// This property shall contain the name of the password specific to the manufacturer or provider.
    Name: ?[]const u8 = null,
};

/// The list of BIOS passwords specific to the manufacturer or provider.
///
/// This property shall contain the list of BIOS passwords specific to the manufacturer or provider.
pub const Passwords = struct {
    /// Information about the password to enter the setup menu.
    ///
    /// This property shall contain information about the password to enter the setup menu.
    Setup: ?Password = null,
    /// Information about the password that allows the system to boot to an operating system.
    ///
    /// This property shall contain information about the password that allows the system to boot to an operating system.
    Boot: ?Password = null,
};

/// The `Bios` schema contains properties related to the BIOS attribute registry.  The attribute registry describes the system-specific BIOS attributes and actions for changing to BIOS settings.  Changes to the BIOS typically require a system reset before they take effect.  It is likely that a client finds the `@Redfish.Settings` term in this resource, and if it is found, the client makes requests to change BIOS settings by modifying the resource identified by the `@Redfish.Settings` term.
///
/// This resource shall represent BIOS attributes for a Redfish implementation.
pub const Bios = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The resource ID of the attribute registry that has the system-specific information about a BIOS resource.
    ///
    /// The link to the attribute registry that lists the metadata describing the BIOS attribute settings in this resource.
    AttributeRegistry: ?[]const u8 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The list of BIOS attributes specific to the manufacturer or provider.
    ///
    /// This property shall contain the list of BIOS attributes specific to the manufacturer or provider.  BIOS attribute settings appear as additional properties in this object and can be looked up in the attribute registry by their `AttributeName`.
    Attributes: ?Attributes = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// An indication of whether there is a pending request to reset the BIOS attributes to default values.
    ///
    /// This property shall indicate whether there is a pending request to reset the BIOS attributes to default values.  A successful completion of the `ResetBios` action shall set this property to `true`.  Applying the default attribute values to this resource shall set this property to `false`.  Services may reject modification requests to the settings resource if this property contains `true`.
    ResetBiosToDefaultsPending: ?bool = null,
    /// The list of BIOS passwords specific to the manufacturer or provider.
    ///
    /// This property shall contain the list of BIOS passwords specific to the manufacturer or provider.
    Passwords: ?Passwords = null,
};

/// What a client may change of `Bios.Bios`.
///
/// The `Bios` schema contains properties related to the BIOS attribute registry.  The attribute registry describes the system-specific BIOS attributes and actions for changing to BIOS settings.  Changes to the BIOS typically require a system reset before they take effect.  It is likely that a client finds the `@Redfish.Settings` term in this resource, and if it is found, the client makes requests to change BIOS settings by modifying the resource identified by the `@Redfish.Settings` term.
pub const BiosUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
