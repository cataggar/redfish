//! Namespace `OperatingConfig`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The clock speed for a set of cores.
///
/// This type shall specify the clock speed for a set of cores.
pub const BaseSpeedPrioritySettings = struct {
    /// The number of cores to configure with a specified speed.
    ///
    /// This property shall contain the number of cores to configure with the speed specified by the `BaseSpeedMHz` property.  The sum of all `CoreCount` properties shall equal the value of the `TotalAvailableCoreCount` property.
    CoreCount: ?i64 = null,
    /// The identifier of the cores to configure with the specified speed.
    ///
    /// This property shall contain an array identifying the cores to configure with the speed specified by the `BaseSpeedMHz` property.  The length of the array shall equal the value of the `CoreCount` property.
    CoreIDs: ?[]const i64 = null,
    /// The clock speed to configure the set of cores in MHz.
    ///
    /// This property shall contain the clock speed to configure the set of cores in MHz.
    BaseSpeedMHz: ?i64 = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The turbo profile for a set of active cores.
///
/// This type shall specify the turbo profile for a set of active cores.
pub const TurboProfileDatapoint = struct {
    /// The number of active cores to be configured with the specified maximum clock speed.
    ///
    /// This property shall contain the number of cores to be configured with the maximum turbo clock speed.  The value shall be less than or equal to the `TotalAvailableCoreCount` property.
    ActiveCoreCount: ?i64 = null,
    /// The maximum turbo clock speed that correspond to the number of active cores in MHz.
    ///
    /// This property shall contain the maximum turbo clock speed that correspond to the number of active cores in MHz.
    MaxSpeedMHz: ?i64 = null,
};

/// The `OperatingConfig` schema specifies a configuration that can be used when the processor is operational.
///
/// This resource shall represent an operational configuration for a processor in the Redfish Specification.
pub const OperatingConfig = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The number of cores in the processor that can be configured.
    ///
    /// This property shall contain the number of cores in the processor that can be configured.
    TotalAvailableCoreCount: ?i64 = null,
    /// The thermal design point of the processor in watt units.
    ///
    /// This property shall contain the thermal design point of the processor in watt units.
    TDPWatts: ?i64 = null,
    /// The base (nominal) clock speed of the processor in MHz.
    ///
    /// This property shall contain the base (nominal) clock speed of the processor in MHz.
    BaseSpeedMHz: ?i64 = null,
    /// The maximum clock speed to which the processor can be configured in MHz.
    ///
    /// This property shall contain the maximum clock speed to which the processor can be configured in MHz.
    MaxSpeedMHz: ?i64 = null,
    /// The maximum temperature of the junction in degree Celsius units.
    ///
    /// This property shall contain the maximum temperature of the junction in degree Celsius units.
    MaxJunctionTemperatureCelsius: ?i64 = null,
    /// The clock speed for sets of cores when the configuration is operational.
    ///
    /// This property shall contain an array of objects that specify the clock speed for sets of cores when the configuration is operational.
    BaseSpeedPrioritySettings: ?[]const BaseSpeedPrioritySettings = null,
    /// The turbo profiles for the processor.  A turbo profile is the maximum turbo clock speed as a function of the number of active cores.
    ///
    /// The property shall contain an array of objects that specify the turbo profile for a set of active cores.
    TurboProfile: ?[]const TurboProfileDatapoint = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
};

test {
    std.testing.refAllDecls(@This());
}
