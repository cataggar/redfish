//! Namespace `OutboundConnection`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const certificate_collection = @import("certificate_collection.zig");
const resource = @import("resource.zig");
const session = @import("session.zig");

pub const AuthenticationType = enum {
    /// Mutual TLS.
    ///
    /// This value shall indicate the service will exchange and verify certificates during TLS handshaking when establishing the outbound connecting.
    MTLS,
    /// JSON Web Token.
    ///
    /// This value shall indicate an RFC7519-defined JSON Web Token (JWT) is specified in one of the HTTP headers in the `PreUpgradeHTTPHeaders` property.  This is typically encoded in the `Authorization` header with the scheme `Bearer`.
    JWT,
    /// No authentication.
    ///
    /// This value shall indicate the service does not provide any authentication information to the remote client.
    None,
    /// OEM-specific.
    ///
    /// This value shall indicate an OEM-specific authentication mechanism.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const OutboundConnectionRetryPolicyType = enum {
    /// No retries.
    ///
    /// This value shall indicate the service will not attempt to re-establish the outbound connection if the connection is dropped or not established.  If the connection is dropped or not established, the service shall set the `ConnectionEnabled` property to `false`.
    None,
    /// Retry forever.
    ///
    /// This value shall indicate the service will attempt to re-establish the outbound connection at the interval specified by the `RetryIntervalMinutes` property regardless of the number of retries.
    RetryForever,
    /// Retry until a maximum count is reached.
    ///
    /// This value shall indicate the service will attempt to re-establish the outbound connection at the interval specified by the `RetryIntervalMinutes` property until the number of retries reaches the count specified by the `RetryCount` property.  If the limit is reached, the service shall set the `ConnectionEnabled` property to `false`.  If a connection is established, the service shall reset the count.
    RetryCount,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The HTTP header value is the property value.  The header name is the property name.
///
/// This type shall contain the HTTP header name and value to add to the Redfish responses over the WebSocket.
pub const HttpHeaderProperty = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The link to the session for this outbound connection.
    ///
    /// This property shall contain the link to a resource of type `Session` that represents the active connection for this outbound connection.
    Session: ?core.NavProperty(session.Session) = null,
};

/// What a client may change of `OutboundConnection.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The retry policy for an outbound connection.
///
/// This type shall contain the retry policy for an outbound connection.
pub const RetryPolicyType = struct {
    /// The type of retry policy for this outbound connection.
    ///
    /// This property shall contain the type of retry policy for this outbound connection.
    ConnectionRetryPolicy: ?OutboundConnectionRetryPolicyType = null,
    /// The retry interval in minutes.
    ///
    /// This property shall contain the interval for the service to retry connecting to remote client in minutes.
    RetryIntervalMinutes: ?i64 = null,
    /// The number of retries to attempt if the retry policy specifies a maximum number of retries.
    ///
    /// This property shall contain the number of retries to attempt if the retry policy specifies a maximum number of retries.
    RetryCount: ?i64 = null,
};

/// What a client may change of `OutboundConnection.RetryPolicyType`.
///
/// The retry policy for an outbound connection.
pub const RetryPolicyTypeUpdate = struct {
    /// The retry interval in minutes.
    ///
    /// This property shall contain the interval for the service to retry connecting to remote client in minutes.
    RetryIntervalMinutes: core.Nullable(i64) = .absent,
    /// The number of retries to attempt if the retry policy specifies a maximum number of retries.
    ///
    /// This property shall contain the number of retries to attempt if the retry policy specifies a maximum number of retries.
    RetryCount: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `OutboundConnection` schema defines how the Redfish service connects to a remote client over a WebSocket connection.  This allows a service behind a firewall to establish a connection to a remote client outside of the firewall.
///
/// This resource shall represent the connection configuration necessary to connect to a remote client.  Services shall initiate the outbound connection over a WebSocket defined in the 'Outbound connections' clause of the Redfish Specification.
pub const OutboundConnection = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The authentication mechanism for the WebSocket connection.
    ///
    /// This property shall contain the authentication mechanism for the WebSocket connection.
    Authentication: ?AuthenticationType = null,
    /// Indicates if the outbound connection is enabled.
    ///
    /// This property shall indicate if the outbound connection is enabled.  If `true`, the service shall attempt to establish an outbound connection to the remote client specified by the `EndpointURI` property.  If `false`, the service shall not attempt to establish a connection to the remote client and shall close the connection if one is already established.  When a connection is established, the service shall create a Session resource to represent the active connection.  When a connection is closed, the service shall delete the connection's respective Session resource.  If the client does not provide this property, the service shall default this value to `true`.
    ConnectionEnabled: ?bool = null,
    /// The URI of the WebSocket connection to the remote client.
    ///
    /// This property shall contain the WebSocket URI to the external web service of the remote client.  The value shall follow the URI format defined in RFC6455.  Services shall reject URIs that do not contain the scheme `wss`.
    EndpointURI: ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The HTTP headers to send to the remote client during the initial connection prior to the WebSocket upgrade.  This property is an empty object in responses.
    ///
    /// This property shall contain an object consisting of the names and values of HTTP headers to send to the remote client during the initial connection prior to the WebSocket upgrade.  This property shall be an empty object in responses.
    PreUpgradeHTTPHeaders: ?HttpHeaderProperty = null,
    /// The retry policy for this outbound connection.
    ///
    /// This property shall contain the retry policy for this outbound connection.  If not specified by the client in the create request, the service shall assume ConnectionRetryPolicy contains `None`.
    RetryPolicy: ?RetryPolicyType = null,
    /// The Redfish roles that contain the privileges of the remote client for the outbound connection.
    ///
    /// This property shall contain the Redfish roles that contain the privileges of the remote client for the outbound connection.
    Roles: ?[]const []const u8 = null,
    /// Interval for sending the WebSocket ping opcode in minutes.  The value `0` indicates the ping opcode is not sent.
    ///
    /// This property shall contain the interval for the service to send the WebSocket ping opcode to the remote client in minutes.  If `0`, the service shall not send the WebSocket ping opcode to the remote client.
    WebSocketPingIntervalMinutes: ?i64 = null,
    /// The link to a collection of server certificates for the remote client referenced by the `EndpointURI` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the server certificates for the remote client referenced by the `EndpointURI` property.  If the `Authentication` property contains `MTLS`, the service shall compare the certificates in this collection with the certificate obtained during handshaking with the WebSocket service to verify the identity of the remote client prior to completing the connection.  If the remote client cannot be verified, the service shall not complete the connection.  Regardless of the contents of this collection, services may perform additional verification based on other factors, such as the configuration of the `SecurityPolicy` resource.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to a collection of client identity certificates provided to the remote client referenced by the `EndpointURI` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the client identity certificates for the service.  If the `Authentication` property contains `MTLS`, these certificates are provided to the remote client referenced by the `EndpointURI` property as part of TLS handshaking.
    ClientCertificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
};

/// What a client may change of `OutboundConnection.OutboundConnection`.
///
/// The `OutboundConnection` schema defines how the Redfish service connects to a remote client over a WebSocket connection.  This allows a service behind a firewall to establish a connection to a remote client outside of the firewall.
pub const OutboundConnectionUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Indicates if the outbound connection is enabled.
    ///
    /// This property shall indicate if the outbound connection is enabled.  If `true`, the service shall attempt to establish an outbound connection to the remote client specified by the `EndpointURI` property.  If `false`, the service shall not attempt to establish a connection to the remote client and shall close the connection if one is already established.  When a connection is established, the service shall create a Session resource to represent the active connection.  When a connection is closed, the service shall delete the connection's respective Session resource.  If the client does not provide this property, the service shall default this value to `true`.
    ConnectionEnabled: core.Nullable(bool) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The retry policy for this outbound connection.
    ///
    /// This property shall contain the retry policy for this outbound connection.  If not specified by the client in the create request, the service shall assume ConnectionRetryPolicy contains `None`.
    RetryPolicy: ?RetryPolicyTypeUpdate = null,
    /// Interval for sending the WebSocket ping opcode in minutes.  The value `0` indicates the ping opcode is not sent.
    ///
    /// This property shall contain the interval for the service to send the WebSocket ping opcode to the remote client in minutes.  If `0`, the service shall not send the WebSocket ping opcode to the remote client.
    WebSocketPingIntervalMinutes: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `OutboundConnection.OutboundConnection`.
///
/// The `OutboundConnection` schema defines how the Redfish service connects to a remote client over a WebSocket connection.  This allows a service behind a firewall to establish a connection to a remote client outside of the firewall.
pub const OutboundConnectionCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The authentication mechanism for the WebSocket connection.
    ///
    /// This property shall contain the authentication mechanism for the WebSocket connection.
    Authentication: AuthenticationType,
    /// Indicates if the outbound connection is enabled.
    ///
    /// This property shall indicate if the outbound connection is enabled.  If `true`, the service shall attempt to establish an outbound connection to the remote client specified by the `EndpointURI` property.  If `false`, the service shall not attempt to establish a connection to the remote client and shall close the connection if one is already established.  When a connection is established, the service shall create a Session resource to represent the active connection.  When a connection is closed, the service shall delete the connection's respective Session resource.  If the client does not provide this property, the service shall default this value to `true`.
    ConnectionEnabled: core.Nullable(bool) = .absent,
    /// The URI of the WebSocket connection to the remote client.
    ///
    /// This property shall contain the WebSocket URI to the external web service of the remote client.  The value shall follow the URI format defined in RFC6455.  Services shall reject URIs that do not contain the scheme `wss`.
    EndpointURI: []const u8,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The retry policy for this outbound connection.
    ///
    /// This property shall contain the retry policy for this outbound connection.  If not specified by the client in the create request, the service shall assume ConnectionRetryPolicy contains `None`.
    RetryPolicy: ?RetryPolicyTypeUpdate = null,
    /// The Redfish roles that contain the privileges of the remote client for the outbound connection.
    ///
    /// This property shall contain the Redfish roles that contain the privileges of the remote client for the outbound connection.
    Roles: []const []const u8,
    /// Interval for sending the WebSocket ping opcode in minutes.  The value `0` indicates the ping opcode is not sent.
    ///
    /// This property shall contain the interval for the service to send the WebSocket ping opcode to the remote client in minutes.  If `0`, the service shall not send the WebSocket ping opcode to the remote client.
    WebSocketPingIntervalMinutes: i64,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
