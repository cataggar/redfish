//! Namespace `AllowDeny`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");

pub const AllowType = enum {
    /// Indicates that traffic that matches the criteria in this resource is permitted.
    Allow,
    /// Indicates that traffic that matches the criteria in this resource is not permitted.
    Deny,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const DataDirection = enum {
    /// Indicates that this limit is enforced on packets and bytes received by the network device function.
    Ingress,
    /// Indicates that this limit is enforced on packets and bytes transmitted by the network device function.
    Egress,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const IpAddressType = enum {
    /// IPv4 addressing is used for all IP-fields in this object.
    IPv4,
    /// IPv6 addressing is used for all IP-fields in this object.
    IPv6,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `AllowDeny` schema represents a set of allow or deny configurations.
///
/// This resource shall represent an AllowDeny resource in a Redfish implementation.
pub const AllowDeny = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// Indicates the direction of the data to which this permission applies.
    ///
    /// This value shall indicate the direction of the data to which this permission applies for this network device function.
    Direction: ?DataDirection = null,
    /// Indicates the type of permission.
    ///
    /// This property shall indicate the type of permission.
    AllowType: ?AllowType = null,
    /// Indicates if this is a permission that only applies to stateful connections.
    ///
    /// This property shall indicate if this permission only applies to stateful connections, which are those using SYN, ACK, and FIN.
    StatefulSession: ?bool = null,
    /// The type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.
    ///
    /// This property shall contain the type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.  Services shall not permit mixing IPv6 and IPv4 addresses on the same resource.
    IPAddressType: ?IpAddressType = null,
    /// The lower IP address to which this permission applies.
    ///
    /// This property shall contain the lower IP address to which this permission applies.
    IPAddressLower: ?[]const u8 = null,
    /// The upper IP address to which this permission applies.
    ///
    /// This property shall contain the upper IP address to which this permission applies.
    IPAddressUpper: ?[]const u8 = null,
    /// The IANA protocol number to which this permission applies.  For TCP, this is `6`.  For UDP, this is `17`.
    ///
    /// This property shall contain the IANA protocol number to which this permission applies.
    IANAProtocolNumber: ?i64 = null,
    /// The TCP, UDP, or other source port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule begins application, inclusive.
    SourcePortLower: ?i64 = null,
    /// The TCP, UDP or other source port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule ends application, inclusive.
    SourcePortUpper: ?i64 = null,
    /// The TCP, UDP, or other destination port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule begins application, inclusive.
    DestinationPortLower: ?i64 = null,
    /// The TCP, UDP, or other destination port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule ends application, inclusive.
    DestinationPortUpper: ?i64 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
};

/// What a client may change of `AllowDeny.AllowDeny`.
///
/// The `AllowDeny` schema represents a set of allow or deny configurations.
pub const AllowDenyUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Indicates the direction of the data to which this permission applies.
    ///
    /// This value shall indicate the direction of the data to which this permission applies for this network device function.
    Direction: core.Nullable(DataDirection) = .absent,
    /// Indicates the type of permission.
    ///
    /// This property shall indicate the type of permission.
    AllowType: core.Nullable(AllowType) = .absent,
    /// Indicates if this is a permission that only applies to stateful connections.
    ///
    /// This property shall indicate if this permission only applies to stateful connections, which are those using SYN, ACK, and FIN.
    StatefulSession: core.Nullable(bool) = .absent,
    /// The type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.
    ///
    /// This property shall contain the type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.  Services shall not permit mixing IPv6 and IPv4 addresses on the same resource.
    IPAddressType: core.Nullable(IpAddressType) = .absent,
    /// The lower IP address to which this permission applies.
    ///
    /// This property shall contain the lower IP address to which this permission applies.
    IPAddressLower: core.Nullable([]const u8) = .absent,
    /// The upper IP address to which this permission applies.
    ///
    /// This property shall contain the upper IP address to which this permission applies.
    IPAddressUpper: core.Nullable([]const u8) = .absent,
    /// The IANA protocol number to which this permission applies.  For TCP, this is `6`.  For UDP, this is `17`.
    ///
    /// This property shall contain the IANA protocol number to which this permission applies.
    IANAProtocolNumber: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other source port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule begins application, inclusive.
    SourcePortLower: core.Nullable(i64) = .absent,
    /// The TCP, UDP or other source port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule ends application, inclusive.
    SourcePortUpper: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other destination port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule begins application, inclusive.
    DestinationPortLower: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other destination port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule ends application, inclusive.
    DestinationPortUpper: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `AllowDeny.AllowDeny`.
///
/// The `AllowDeny` schema represents a set of allow or deny configurations.
pub const AllowDenyCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Indicates the direction of the data to which this permission applies.
    ///
    /// This value shall indicate the direction of the data to which this permission applies for this network device function.
    Direction: core.Nullable(DataDirection) = .absent,
    /// Indicates the type of permission.
    ///
    /// This property shall indicate the type of permission.
    AllowType: core.Nullable(AllowType) = .absent,
    /// Indicates if this is a permission that only applies to stateful connections.
    ///
    /// This property shall indicate if this permission only applies to stateful connections, which are those using SYN, ACK, and FIN.
    StatefulSession: core.Nullable(bool) = .absent,
    /// The type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.
    ///
    /// This property shall contain the type of IP address populated in the `IPAddressLower` and `IPAddressUpper` properties.  Services shall not permit mixing IPv6 and IPv4 addresses on the same resource.
    IPAddressType: core.Nullable(IpAddressType) = .absent,
    /// The lower IP address to which this permission applies.
    ///
    /// This property shall contain the lower IP address to which this permission applies.
    IPAddressLower: core.Nullable([]const u8) = .absent,
    /// The upper IP address to which this permission applies.
    ///
    /// This property shall contain the upper IP address to which this permission applies.
    IPAddressUpper: core.Nullable([]const u8) = .absent,
    /// The IANA protocol number to which this permission applies.  For TCP, this is `6`.  For UDP, this is `17`.
    ///
    /// This property shall contain the IANA protocol number to which this permission applies.
    IANAProtocolNumber: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other source port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule begins application, inclusive.
    SourcePortLower: core.Nullable(i64) = .absent,
    /// The TCP, UDP or other source port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other source port to which this rule ends application, inclusive.
    SourcePortUpper: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other destination port to which this rule begins application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule begins application, inclusive.
    DestinationPortLower: core.Nullable(i64) = .absent,
    /// The TCP, UDP, or other destination port to which this rule ends application, inclusive.
    ///
    /// This property shall contain the TCP, UDP, or other destination port to which this rule ends application, inclusive.
    DestinationPortUpper: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
