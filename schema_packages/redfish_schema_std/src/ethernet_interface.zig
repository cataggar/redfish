//! Namespace `EthernetInterface`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const chassis = @import("chassis.zig");
const endpoint = @import("endpoint.zig");
const host_interface = @import("host_interface.zig");
const ip_addresses = @import("ip_addresses.zig");
const network_device_function = @import("network_device_function.zig");
const port = @import("port.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");
const vlan_network_interface = @import("vlan_network_interface.zig");
const vlan_network_interface_collection = @import("vlan_network_interface_collection.zig");

pub const MacAddress = []const u8;

pub const DhcpFallback = enum {
    /// Fall back to a static address specified by `IPv4StaticAddresses`.
    ///
    /// DHCP shall fall back to a static address specified by `IPv4StaticAddresses`.
    Static,
    /// Fall back to an autoconfigured address.
    ///
    /// DHCP shall fall back to an address generated by the implementation.
    AutoConfig,
    /// Continue attempting DHCP without a fallback address.
    ///
    /// DHCP shall continue trying to obtain an address without falling back to a fixed address.
    None,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const Dhcpv6operatingMode = enum {
    /// DHCPv6 stateful mode.
    ///
    /// DHCPv6 shall operate in stateful mode on this interface.  DHCPv6 stateful mode configures addresses, and when it is enabled, stateless mode is also implicitly enabled.  Services may replace this value with `Enabled`.
    ///
    /// Deprecated in v1_8_0.
    /// This property has been deprecated in favor of `Enabled`.  The control between 'stateful' and 'stateless' is managed by the DHCP server and not the client.
    Stateful,
    /// DHCPv6 stateless mode.
    ///
    /// DHCPv6 shall operate in stateless mode on this interface.  DHCPv6 stateless mode allows configuring the interface using DHCP options but does not configure addresses.  It is always enabled by default whenever DHCPv6 stateful mode is also enabled.  Services may replace this value with `Enabled`.
    ///
    /// Deprecated in v1_8_0.
    /// This property has been deprecated in favor of `Enabled`.  The control between 'stateful' and 'stateless' is managed by the DHCP server and not the client.
    Stateless,
    /// DHCPv6 is disabled.
    ///
    /// DHCPv6 shall be disabled for this interface.
    Disabled,
    /// DHCPv6 is enabled.
    ///
    /// DHCPv6 shall be enabled for this interface.
    Enabled,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EthernetDeviceType = enum {
    /// A physical Ethernet interface.
    ///
    /// This value shall indicate a physical traditional network interface.
    Physical,
    /// A virtual Ethernet interface.
    ///
    /// This value shall indicate a network device function has multiple VLANs and is representing one of them as a virtual Ethernet interface.  The `NetworkDeviceFunction` property within Links shall contain the locator for the parent network device function.
    Virtual,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const LinkStatus = enum {
    /// The link is available for communication on this interface.
    LinkUp,
    /// No link or connection is detected on this interface.
    NoLink,
    /// No link is detected on this interface, but the interface is connected.
    LinkDown,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const RoutingScope = enum {
    /// Externally accessible.
    ///
    /// This value shall indicate this interface is externally accessible as if it were physically on the same network as the hosting system.
    External,
    /// Only accessible to a dedicated interface on the host.
    ///
    /// This value shall indicate this interface is only accessible to a dedicated interface on the hosting system.
    HostOnly,
    /// Only accessible to internal networking on the host, such as when virtual machines or containers are allowed to communicate with each other on the same host system as well as a dedicated interface on the hosting system.
    ///
    /// This value shall indicate this interface is only accessible to internal networking on the host, such as when virtual machines or containers are allowed to communicate with each other on the same host system as well as a dedicated interface on the hosting system.
    Internal,
    /// Accessible through IP translation provided by the hosting system.
    ///
    /// This value shall indicate this interface is accessible through IP translation provided by the hosting system, such as a NAT (network address translation).
    Limited,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TeamMode = enum {
    /// No teaming.
    None,
    /// Packets are transmitted in sequential order from the teamed interfaces.
    RoundRobin,
    /// One interface in the team is active and the others are kept in standby until a failure occurs.
    ActiveBackup,
    /// Transmitting is determined based upon a hash policy.
    XOR,
    /// Packets are transmitted on all interfaces in the team.
    Broadcast,
    /// The interfaces in the team create an IEEE802.3ad link aggregation group.
    IEEE802_3ad,
    /// Packets are transmitted based upon the current load of each interface in the team.
    AdaptiveTransmitLoadBalancing,
    /// Packets are transmitted and received based upon the current load of each interface in the team.
    AdaptiveLoadBalancing,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

pub const Dhcpv4configuration = struct {
    /// An indication of whether DHCP v4 is enabled on this Ethernet interface.
    ///
    /// This property shall indicate whether DHCP v4 is enabled for this Ethernet interface.
    DHCPEnabled: ?bool = null,
    /// An indication of whether this interface uses DHCP v4-supplied DNS servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v4-supplied DNS servers.
    UseDNSServers: ?bool = null,
    /// An indication of whether this interface uses a DHCP v4-supplied domain name.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied domain name.
    UseDomainName: ?bool = null,
    /// An indication of whether this interface uses a DHCP v4-supplied gateway.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied gateway.
    UseGateway: ?bool = null,
    /// An indication of whether the interface uses DHCP v4-supplied NTP servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v4-supplied NTP servers.
    UseNTPServers: ?bool = null,
    /// An indication of whether the interface uses DHCP v4-supplied static routes.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied static routes.
    UseStaticRoutes: ?bool = null,
    /// DHCPv4 fallback address method for this interface.
    ///
    /// This property shall contain the fallback address method of DHCPv4.
    FallbackAddress: ?DhcpFallback = null,
};

/// What a client may change of `EthernetInterface.DHCPv4Configuration`.
pub const Dhcpv4configurationUpdate = struct {
    /// An indication of whether DHCP v4 is enabled on this Ethernet interface.
    ///
    /// This property shall indicate whether DHCP v4 is enabled for this Ethernet interface.
    DHCPEnabled: core.Nullable(bool) = .absent,
    /// An indication of whether this interface uses DHCP v4-supplied DNS servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v4-supplied DNS servers.
    UseDNSServers: core.Nullable(bool) = .absent,
    /// An indication of whether this interface uses a DHCP v4-supplied domain name.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied domain name.
    UseDomainName: core.Nullable(bool) = .absent,
    /// An indication of whether this interface uses a DHCP v4-supplied gateway.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied gateway.
    UseGateway: core.Nullable(bool) = .absent,
    /// An indication of whether the interface uses DHCP v4-supplied NTP servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v4-supplied NTP servers.
    UseNTPServers: core.Nullable(bool) = .absent,
    /// An indication of whether the interface uses DHCP v4-supplied static routes.
    ///
    /// This property shall indicate whether the interface uses a DHCP v4-supplied static routes.
    UseStaticRoutes: core.Nullable(bool) = .absent,
    /// DHCPv4 fallback address method for this interface.
    ///
    /// This property shall contain the fallback address method of DHCPv4.
    FallbackAddress: core.Nullable(DhcpFallback) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// DHCPv6 configuration for this interface.
///
/// This type shall describe the configuration of DHCP v6.
pub const Dhcpv6configuration = struct {
    /// Determines the DHCPv6 operating mode for this interface.
    ///
    /// This property shall control the operating mode of DHCPv6 on this interface.
    OperatingMode: ?Dhcpv6operatingMode = null,
    /// An indication of whether the interface uses DHCP v6-supplied DNS servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v6-supplied DNS servers.
    UseDNSServers: ?bool = null,
    /// An indication of whether this interface uses a DHCP v6-supplied domain name.
    ///
    /// This property shall indicate whether the interface uses a DHCP v6-supplied domain name.
    UseDomainName: ?bool = null,
    /// An indication of whether the interface uses DHCP v6-supplied NTP servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v6-supplied NTP servers.
    UseNTPServers: ?bool = null,
    /// An indication of whether the interface uses DHCP v6 rapid commit mode for stateful mode address assignments.  Do not enable this option in networks where more than one DHCP v6 server is configured to provide address assignments.
    ///
    /// This property shall indicate whether the interface uses DHCP v6 rapid commit mode for stateful mode address assignments.
    UseRapidCommit: ?bool = null,
};

/// What a client may change of `EthernetInterface.DHCPv6Configuration`.
///
/// DHCPv6 configuration for this interface.
pub const Dhcpv6configurationUpdate = struct {
    /// Determines the DHCPv6 operating mode for this interface.
    ///
    /// This property shall control the operating mode of DHCPv6 on this interface.
    OperatingMode: core.Nullable(Dhcpv6operatingMode) = .absent,
    /// An indication of whether the interface uses DHCP v6-supplied DNS servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v6-supplied DNS servers.
    UseDNSServers: core.Nullable(bool) = .absent,
    /// An indication of whether this interface uses a DHCP v6-supplied domain name.
    ///
    /// This property shall indicate whether the interface uses a DHCP v6-supplied domain name.
    UseDomainName: core.Nullable(bool) = .absent,
    /// An indication of whether the interface uses DHCP v6-supplied NTP servers.
    ///
    /// This property shall indicate whether the interface uses DHCP v6-supplied NTP servers.
    UseNTPServers: core.Nullable(bool) = .absent,
    /// An indication of whether the interface uses DHCP v6 rapid commit mode for stateful mode address assignments.  Do not enable this option in networks where more than one DHCP v6 server is configured to provide address assignments.
    ///
    /// This property shall indicate whether the interface uses DHCP v6 rapid commit mode for stateful mode address assignments.
    UseRapidCommit: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The entry in the RFC6724-defined address selection policy table.
///
/// This type shall describe an entry in the RFC6724-defined address selection policy table.
pub const Ipv6addressPolicyEntry = struct {
    /// The IPv6 address prefix, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 address prefix for this table entry, as defined in RFC6724, section 2.1.
    Prefix: ?[]const u8 = null,
    /// The IPv6 precedence, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 precedence value for this table entry, as defined in RFC6724, section 2.1.
    Precedence: ?i64 = null,
    /// The IPv6 label, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 label value for this table entry, as defined in RFC6724, section 2.1.
    Label: ?i64 = null,
};

/// What a client may change of `EthernetInterface.IPv6AddressPolicyEntry`.
///
/// The entry in the RFC6724-defined address selection policy table.
pub const Ipv6addressPolicyEntryUpdate = struct {
    /// The IPv6 address prefix, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 address prefix for this table entry, as defined in RFC6724, section 2.1.
    Prefix: core.Nullable([]const u8) = .absent,
    /// The IPv6 precedence, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 precedence value for this table entry, as defined in RFC6724, section 2.1.
    Precedence: core.Nullable(i64) = .absent,
    /// The IPv6 label, as defined in RFC6724, section 2.1.
    ///
    /// This property shall contain the IPv6 label value for this table entry, as defined in RFC6724, section 2.1.
    Label: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the endpoints that connect to this Ethernet interface.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` with which this Ethernet interface is associated.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `Endpoints` has in total, which is not how many this response carries.
    @"Endpoints@odata.count": ?i64 = null,
    /// The next page of `Endpoints`. Present only when this response left members out.
    @"Endpoints@odata.nextLink": ?core.ODataId = null,
    /// The link to a Host Interface that is associated with this Ethernet interface.
    ///
    /// This property shall contain a link to a resource of type `HostInterface` that represents the interface that a host uses to communicate with a manager.
    HostInterface: ?core.NavProperty(host_interface.HostInterface) = null,
    /// The link to the chassis that contains this Ethernet interface.
    ///
    /// This property shall contain a link to a resource of type `Chassis` that represent the physical container associated with this Ethernet interface.
    Chassis: ?core.NavProperty(chassis.Chassis) = null,
    /// The link to the parent network device function and is only used when representing one of the VLANs on that network device function, such as is done in Unix.
    ///
    /// This property shall contain a link to a resource of type `NetworkDeviceFunction` and only be populated with the `EthernetInterfaceType` property is `Virtual`.
    ///
    /// Deprecated in v1_7_0.
    /// This property has been deprecated in favor of `NetworkDeviceFunctions` as each `EthernetInterface` could represent more than one `NetworkDeviceFunction`.
    NetworkDeviceFunction: ?core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// The link to the network device functions that constitute this Ethernet interface.
    ///
    /// This property shall contain an array of links to resources of type `NetworkDeviceFunction`.
    NetworkDeviceFunctions: ?[]const core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// How many members `NetworkDeviceFunctions` has in total, which is not how many this response carries.
    @"NetworkDeviceFunctions@odata.count": ?i64 = null,
    /// The next page of `NetworkDeviceFunctions`. Present only when this response left members out.
    @"NetworkDeviceFunctions@odata.nextLink": ?core.ODataId = null,
    /// The links to the ports providing this Ethernet interface.
    ///
    /// This property shall contain an array of links to resources of type `Port` that represent the ports providing this Ethernet interface.  This property shall not be present if the Ethernet interface is not directly associated to a physical port.
    Ports: ?[]const core.NavProperty(port.Port) = null,
    /// How many members `Ports` has in total, which is not how many this response carries.
    @"Ports@odata.count": ?i64 = null,
    /// The next page of `Ports`. Present only when this response left members out.
    @"Ports@odata.nextLink": ?core.ODataId = null,
    /// The links to the Ethernet interfaces that constitute this Ethernet interface.
    ///
    /// This property shall contain an array of links to resources of type `EthernetInterface`.  If `TeamMode` contains `None`, this property shall contain one member that represents the parent interface for the VLAN.  For other values of TeamMode, this property shall contain the members of the team.
    RelatedInterfaces: ?[]const core.NavProperty(EthernetInterface) = null,
    /// How many members `RelatedInterfaces` has in total, which is not how many this response carries.
    @"RelatedInterfaces@odata.count": ?i64 = null,
    /// The next page of `RelatedInterfaces`. Present only when this response left members out.
    @"RelatedInterfaces@odata.nextLink": ?core.ODataId = null,
    /// The links to the Ethernet interfaces that are affiliated with this interface, such as a VLAN or a team that uses this interface.
    ///
    /// This property shall contain an array of links to resources of type `EthernetInterface` that represent the Ethernet interfaces that are affiliated with this interface.  `EthernetInterface` resources referenced in this property shall reference this resource in their `RelatedInterfaces` property.
    AffiliatedInterfaces: ?[]const core.NavProperty(EthernetInterface) = null,
    /// How many members `AffiliatedInterfaces` has in total, which is not how many this response carries.
    @"AffiliatedInterfaces@odata.count": ?i64 = null,
    /// The next page of `AffiliatedInterfaces`. Present only when this response left members out.
    @"AffiliatedInterfaces@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `EthernetInterface.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// Stateless address autoconfiguration (SLAAC) parameters for this interface.
///
/// This type shall describe the IPv4 and IPv6 stateless address autoconfiguration (SLAAC) for this interface.
pub const StatelessAddressAutoConfiguration = struct {
    /// An indication of whether IPv4 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    ///
    /// This property shall indicate whether IPv4 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    IPv4AutoConfigEnabled: ?bool = null,
    /// An indication of whether IPv6 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    ///
    /// This property shall indicate whether IPv6 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    IPv6AutoConfigEnabled: ?bool = null,
};

/// What a client may change of `EthernetInterface.StatelessAddressAutoConfiguration`.
///
/// Stateless address autoconfiguration (SLAAC) parameters for this interface.
pub const StatelessAddressAutoConfigurationUpdate = struct {
    /// An indication of whether IPv4 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    ///
    /// This property shall indicate whether IPv4 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    IPv4AutoConfigEnabled: core.Nullable(bool) = .absent,
    /// An indication of whether IPv6 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    ///
    /// This property shall indicate whether IPv6 stateless address autoconfiguration (SLAAC) is enabled for this interface.
    IPv6AutoConfigEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `EthernetInterface` schema represents a single, logical Ethernet interface or network interface controller (NIC).
///
/// This resource contains NIC resources as part of the Redfish Specification.
pub const EthernetInterface = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The UEFI device path for this interface.
    ///
    /// This property shall contain the UEFI device path to the device that implements this interface, or port.
    UefiDevicePath: ?[]const u8 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// An indication of whether this interface is enabled.
    ///
    /// This property shall indicate whether this interface is enabled.  If this interface is also a host interface, modifying this property may modify the `InterfaceEnabled` property in the `HostInterface` resource for this interface.
    InterfaceEnabled: ?bool = null,
    /// The permanent MAC address assigned to this interface, or port.
    ///
    /// This property shall contain the permanent MAC address of this interface, or port.  Typically, this value is programmed during manufacturing.  This address is not assignable.
    PermanentMACAddress: ?MacAddress = null,
    /// The currently configured MAC address of the interface, or logical port.
    ///
    /// This property shall contain the effective current MAC address of this interface.  If an assignable MAC address is not supported, this value is a read-only alias of the PermanentMACAddress.
    MACAddress: ?MacAddress = null,
    /// The current speed, in Mbit/s, of this interface.
    ///
    /// This property shall contain the link speed of the interface, in megabits per second (Mbit/s) units.  This property shall be writable only when the `AutoNeg` property is `false`.
    SpeedMbps: ?i64 = null,
    /// An indication of whether the speed and duplex are automatically negotiated and configured on this interface.
    ///
    /// This property shall indicate whether the speed and duplex are automatically negotiated and configured on this interface.
    AutoNeg: ?bool = null,
    /// An indication of whether full-duplex mode is enabled on the Ethernet connection for this interface.
    ///
    /// This property shall indicate whether full-duplex mode is enabled on the Ethernet connection for this interface.
    FullDuplex: ?bool = null,
    /// The currently configured maximum transmission unit (MTU), in bytes, on this interface.
    ///
    /// This property shall contain the size, in bytes, of the largest protocol data unit (PDU) that can be passed in an Ethernet (MAC) frame on this interface, as currently configured by the operating system or driver.
    MTUSize: ?i64 = null,
    /// The DNS host name, without any domain information.
    ///
    /// This property shall contain the DNS host name for this interface.  Modifying this property may modify the `HostName` in one or more `EthernetInterface` resources that belong to the same system, manager, or other device.  If this interface is subordinate to a `ComputerSystem` resource, modifying this property may modify the `HostName` of the `ComputerSystem` resource that contains this interface.  If this interface is subordinate to a `Manager` resource, modifying this property may modify the `HostName` of the `ManagerNetworkProtocol` resource of the `Manager` resource that contains this interface.  Modifying this property may modify the `FQDN` property.  Services should ignore this property in modification requests if `FQDN` is also provided in the same request.
    HostName: ?[]const u8 = null,
    /// The complete, fully qualified domain name that DNS obtains for this interface.
    ///
    /// This property shall contain the fully qualified domain name that DNS obtains for this interface.  Modifying this property may modify the `HostName` property.
    FQDN: ?[]const u8 = null,
    /// The maximum number of static IPv6 addresses that can be configured on this interface.
    ///
    /// This property shall indicate the number of array items supported by `IPv6StaticAddresses`, or the maximum number of static IPv6 addresses that can be configured on this interface.
    MaxIPv6StaticAddresses: ?i64 = null,
    /// If this network interface supports more than one VLAN, this property is absent.  VLAN collections appear in the `Links` property of this resource.
    ///
    /// This property shall contain the VLAN for this interface.  If this interface supports more than one VLAN, the `VLAN` property shall be absent and, instead, the VLAN collection link shall be present.
    VLAN: ?vlan_network_interface.Vlan = null,
    /// The IPv4 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv4 connection characteristics currently in use by this interface for any value of `AddressOrigin`.  It is recommended that this property be regarded as read-only with configuration of static addresses performed by updating the values within `IPv4StaticAddresses`.  Services may reject updates to this array for this reason.  This property should contain an empty array if there are no active IPv4 addresses.
    IPv4Addresses: ?[]const ip_addresses.Ipv4address = null,
    /// An array that represents the RFC6724-defined address selection policy table.
    ///
    /// This property shall contain an array of objects that represent the RFC6724-defined address selection policy table.
    IPv6AddressPolicyTable: ?[]const ?Ipv6addressPolicyEntry = null,
    /// The IPv6 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 connection characteristics for this interface for any value of  `AddressOrigin`.  This property should contain an empty array if there are no active IPv6 addresses.
    IPv6Addresses: ?[]const ip_addresses.Ipv6address = null,
    /// The IPv6 static addresses assigned to this interface.  See `IPv6Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 static connection characteristics for this interface.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `::`.
    IPv6StaticAddresses: ?[]const ?ip_addresses.Ipv6staticAddress = null,
    /// The IPv6 default gateway address in use on this interface.
    ///
    /// This property shall contain the current IPv6 default gateway address in use on this interface.
    IPv6DefaultGateway: ?[]const u8 = null,
    /// The DNS servers in use on this interface.
    ///
    /// This property shall contain the DNS servers in use on this interface.
    NameServers: ?[]const []const u8 = null,
    /// The link status of this interface, or port.
    ///
    /// This property shall contain the link status of this interface, or port.
    LinkStatus: ?LinkStatus = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// DHCPv4 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v4.
    DHCPv4: ?Dhcpv4configuration = null,
    /// DHCPv6 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v6.
    DHCPv6: ?Dhcpv6configuration = null,
    /// Stateless address autoconfiguration (SLAAC) parameters for this interface.
    ///
    /// This object shall contain the IPv4 and IPv6 stateless address automatic configuration (SLAAC) properties for this interface.
    StatelessAddressAutoConfig: ?StatelessAddressAutoConfiguration = null,
    /// The IPv6 static default gateways for this interface.
    ///
    /// The values in this array shall represent the IPv6 static default gateway addresses for this interface.
    IPv6StaticDefaultGateways: ?[]const ?ip_addresses.Ipv6gatewayStaticAddress = null,
    /// The statically-defined set of DNS server IPv4 and IPv6 addresses.
    ///
    /// This property shall contain the statically-defined set of DNS server IP addresses to use when DHCP provisioning is not enabled for name server configuration.  As an implementation option, they can be used in addition to DHCP-provided addresses, or in cases where the DHCP server provides no DNS assignments.
    StaticNameServers: ?[]const ?[]const u8 = null,
    /// The IPv4 static addresses assigned to this interface.  See `IPv4Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent all IPv4 static addresses assigned to, but not necessarily in use by, this interface.  The `IPv4Addresses` property shall also list the addresses that this interface uses.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `0.0.0.0`.
    IPv4StaticAddresses: ?[]const ?ip_addresses.Ipv4address = null,
    /// The type of interface.
    ///
    /// This property shall contain the type of interface.
    EthernetInterfaceType: ?EthernetDeviceType = null,
    /// The team mode for this interface.
    ///
    /// This property shall contain the team mode for this interface.  If this property is not present, the value shall be assumed to be `None`.
    TeamMode: ?TeamMode = null,
    /// The routing scope for this interface.
    ///
    /// This property shall contain the routing scope for this interface.  This property shall only be present if this interface belongs to a virtual machine or container.
    RoutingScope: ?RoutingScope = null,
    /// An indication of whether IPv6 is enabled on this interface.
    ///
    /// This property shall indicate whether IPv6 is enabled on this interface.  If this property contains `false`, the interface shall not contain any assigned IPv6 addresses, shall not initiate DHCPv6 requests, and shall not send or process ICMPv6 packets.  If this property is not present, but this interface contains other IPv6 properties, the value shall be assumed to be `true`.
    IPv6Enabled: ?bool = null,
    /// The link to a collection of VLANs, which applies only if the interface supports more than one VLAN.  If this property applies, the `VLANEnabled` and `VLANId` properties do not apply.
    ///
    /// This property shall contain a link to a resource collection of type `VLanNetworkInterfaceCollection`, which applies only if the interface supports more than one VLAN.  If this property is present, the `VLANEnabled` and `VLANId` properties shall not be present.
    ///
    /// Deprecated in v1_7_0.
    /// This property has been deprecated in favor of newer methods indicating multiple VLANs.
    VLANs: ?core.NavProperty(vlan_network_interface_collection.VlanNetworkInterfaceCollection) = null,
};

/// What a client may change of `EthernetInterface.EthernetInterface`.
///
/// The `EthernetInterface` schema represents a single, logical Ethernet interface or network interface controller (NIC).
pub const EthernetInterfaceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this interface is enabled.
    ///
    /// This property shall indicate whether this interface is enabled.  If this interface is also a host interface, modifying this property may modify the `InterfaceEnabled` property in the `HostInterface` resource for this interface.
    InterfaceEnabled: core.Nullable(bool) = .absent,
    /// The currently configured MAC address of the interface, or logical port.
    ///
    /// This property shall contain the effective current MAC address of this interface.  If an assignable MAC address is not supported, this value is a read-only alias of the PermanentMACAddress.
    MACAddress: core.Nullable(MacAddress) = .absent,
    /// The current speed, in Mbit/s, of this interface.
    ///
    /// This property shall contain the link speed of the interface, in megabits per second (Mbit/s) units.  This property shall be writable only when the `AutoNeg` property is `false`.
    SpeedMbps: core.Nullable(i64) = .absent,
    /// An indication of whether the speed and duplex are automatically negotiated and configured on this interface.
    ///
    /// This property shall indicate whether the speed and duplex are automatically negotiated and configured on this interface.
    AutoNeg: core.Nullable(bool) = .absent,
    /// An indication of whether full-duplex mode is enabled on the Ethernet connection for this interface.
    ///
    /// This property shall indicate whether full-duplex mode is enabled on the Ethernet connection for this interface.
    FullDuplex: core.Nullable(bool) = .absent,
    /// The currently configured maximum transmission unit (MTU), in bytes, on this interface.
    ///
    /// This property shall contain the size, in bytes, of the largest protocol data unit (PDU) that can be passed in an Ethernet (MAC) frame on this interface, as currently configured by the operating system or driver.
    MTUSize: core.Nullable(i64) = .absent,
    /// The DNS host name, without any domain information.
    ///
    /// This property shall contain the DNS host name for this interface.  Modifying this property may modify the `HostName` in one or more `EthernetInterface` resources that belong to the same system, manager, or other device.  If this interface is subordinate to a `ComputerSystem` resource, modifying this property may modify the `HostName` of the `ComputerSystem` resource that contains this interface.  If this interface is subordinate to a `Manager` resource, modifying this property may modify the `HostName` of the `ManagerNetworkProtocol` resource of the `Manager` resource that contains this interface.  Modifying this property may modify the `FQDN` property.  Services should ignore this property in modification requests if `FQDN` is also provided in the same request.
    HostName: core.Nullable([]const u8) = .absent,
    /// The complete, fully qualified domain name that DNS obtains for this interface.
    ///
    /// This property shall contain the fully qualified domain name that DNS obtains for this interface.  Modifying this property may modify the `HostName` property.
    FQDN: core.Nullable([]const u8) = .absent,
    /// If this network interface supports more than one VLAN, this property is absent.  VLAN collections appear in the `Links` property of this resource.
    ///
    /// This property shall contain the VLAN for this interface.  If this interface supports more than one VLAN, the `VLAN` property shall be absent and, instead, the VLAN collection link shall be present.
    VLAN: ?vlan_network_interface.VlanUpdate = null,
    /// The IPv4 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv4 connection characteristics currently in use by this interface for any value of `AddressOrigin`.  It is recommended that this property be regarded as read-only with configuration of static addresses performed by updating the values within `IPv4StaticAddresses`.  Services may reject updates to this array for this reason.  This property should contain an empty array if there are no active IPv4 addresses.
    IPv4Addresses: ?[]const ip_addresses.Ipv4addressUpdate = null,
    /// An array that represents the RFC6724-defined address selection policy table.
    ///
    /// This property shall contain an array of objects that represent the RFC6724-defined address selection policy table.
    IPv6AddressPolicyTable: ?[]const ?Ipv6addressPolicyEntryUpdate = null,
    /// The IPv6 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 connection characteristics for this interface for any value of  `AddressOrigin`.  This property should contain an empty array if there are no active IPv6 addresses.
    IPv6Addresses: ?[]const ip_addresses.Ipv6addressUpdate = null,
    /// The IPv6 static addresses assigned to this interface.  See `IPv6Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 static connection characteristics for this interface.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `::`.
    IPv6StaticAddresses: ?[]const ?ip_addresses.Ipv6staticAddressUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// DHCPv4 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v4.
    DHCPv4: ?Dhcpv4configurationUpdate = null,
    /// DHCPv6 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v6.
    DHCPv6: ?Dhcpv6configurationUpdate = null,
    /// Stateless address autoconfiguration (SLAAC) parameters for this interface.
    ///
    /// This object shall contain the IPv4 and IPv6 stateless address automatic configuration (SLAAC) properties for this interface.
    StatelessAddressAutoConfig: ?StatelessAddressAutoConfigurationUpdate = null,
    /// The IPv6 static default gateways for this interface.
    ///
    /// The values in this array shall represent the IPv6 static default gateway addresses for this interface.
    IPv6StaticDefaultGateways: ?[]const ?ip_addresses.Ipv6gatewayStaticAddressUpdate = null,
    /// The statically-defined set of DNS server IPv4 and IPv6 addresses.
    ///
    /// This property shall contain the statically-defined set of DNS server IP addresses to use when DHCP provisioning is not enabled for name server configuration.  As an implementation option, they can be used in addition to DHCP-provided addresses, or in cases where the DHCP server provides no DNS assignments.
    StaticNameServers: ?[]const ?[]const u8 = null,
    /// The IPv4 static addresses assigned to this interface.  See `IPv4Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent all IPv4 static addresses assigned to, but not necessarily in use by, this interface.  The `IPv4Addresses` property shall also list the addresses that this interface uses.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `0.0.0.0`.
    IPv4StaticAddresses: ?[]const ?ip_addresses.Ipv4addressUpdate = null,
    /// The team mode for this interface.
    ///
    /// This property shall contain the team mode for this interface.  If this property is not present, the value shall be assumed to be `None`.
    TeamMode: core.Nullable(TeamMode) = .absent,
    /// An indication of whether IPv6 is enabled on this interface.
    ///
    /// This property shall indicate whether IPv6 is enabled on this interface.  If this property contains `false`, the interface shall not contain any assigned IPv6 addresses, shall not initiate DHCPv6 requests, and shall not send or process ICMPv6 packets.  If this property is not present, but this interface contains other IPv6 properties, the value shall be assumed to be `true`.
    IPv6Enabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `EthernetInterface.EthernetInterface`.
///
/// The `EthernetInterface` schema represents a single, logical Ethernet interface or network interface controller (NIC).
pub const EthernetInterfaceCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this interface is enabled.
    ///
    /// This property shall indicate whether this interface is enabled.  If this interface is also a host interface, modifying this property may modify the `InterfaceEnabled` property in the `HostInterface` resource for this interface.
    InterfaceEnabled: core.Nullable(bool) = .absent,
    /// The currently configured MAC address of the interface, or logical port.
    ///
    /// This property shall contain the effective current MAC address of this interface.  If an assignable MAC address is not supported, this value is a read-only alias of the PermanentMACAddress.
    MACAddress: core.Nullable(MacAddress) = .absent,
    /// The current speed, in Mbit/s, of this interface.
    ///
    /// This property shall contain the link speed of the interface, in megabits per second (Mbit/s) units.  This property shall be writable only when the `AutoNeg` property is `false`.
    SpeedMbps: core.Nullable(i64) = .absent,
    /// An indication of whether the speed and duplex are automatically negotiated and configured on this interface.
    ///
    /// This property shall indicate whether the speed and duplex are automatically negotiated and configured on this interface.
    AutoNeg: core.Nullable(bool) = .absent,
    /// An indication of whether full-duplex mode is enabled on the Ethernet connection for this interface.
    ///
    /// This property shall indicate whether full-duplex mode is enabled on the Ethernet connection for this interface.
    FullDuplex: core.Nullable(bool) = .absent,
    /// The currently configured maximum transmission unit (MTU), in bytes, on this interface.
    ///
    /// This property shall contain the size, in bytes, of the largest protocol data unit (PDU) that can be passed in an Ethernet (MAC) frame on this interface, as currently configured by the operating system or driver.
    MTUSize: core.Nullable(i64) = .absent,
    /// The DNS host name, without any domain information.
    ///
    /// This property shall contain the DNS host name for this interface.  Modifying this property may modify the `HostName` in one or more `EthernetInterface` resources that belong to the same system, manager, or other device.  If this interface is subordinate to a `ComputerSystem` resource, modifying this property may modify the `HostName` of the `ComputerSystem` resource that contains this interface.  If this interface is subordinate to a `Manager` resource, modifying this property may modify the `HostName` of the `ManagerNetworkProtocol` resource of the `Manager` resource that contains this interface.  Modifying this property may modify the `FQDN` property.  Services should ignore this property in modification requests if `FQDN` is also provided in the same request.
    HostName: core.Nullable([]const u8) = .absent,
    /// The complete, fully qualified domain name that DNS obtains for this interface.
    ///
    /// This property shall contain the fully qualified domain name that DNS obtains for this interface.  Modifying this property may modify the `HostName` property.
    FQDN: core.Nullable([]const u8) = .absent,
    /// If this network interface supports more than one VLAN, this property is absent.  VLAN collections appear in the `Links` property of this resource.
    ///
    /// This property shall contain the VLAN for this interface.  If this interface supports more than one VLAN, the `VLAN` property shall be absent and, instead, the VLAN collection link shall be present.
    VLAN: ?vlan_network_interface.VlanUpdate = null,
    /// The IPv4 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv4 connection characteristics currently in use by this interface for any value of `AddressOrigin`.  It is recommended that this property be regarded as read-only with configuration of static addresses performed by updating the values within `IPv4StaticAddresses`.  Services may reject updates to this array for this reason.  This property should contain an empty array if there are no active IPv4 addresses.
    IPv4Addresses: ?[]const ip_addresses.Ipv4addressUpdate = null,
    /// An array that represents the RFC6724-defined address selection policy table.
    ///
    /// This property shall contain an array of objects that represent the RFC6724-defined address selection policy table.
    IPv6AddressPolicyTable: ?[]const ?Ipv6addressPolicyEntryUpdate = null,
    /// The IPv6 addresses currently in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 connection characteristics for this interface for any value of  `AddressOrigin`.  This property should contain an empty array if there are no active IPv6 addresses.
    IPv6Addresses: ?[]const ip_addresses.Ipv6addressUpdate = null,
    /// The IPv6 static addresses assigned to this interface.  See `IPv6Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent the IPv6 static connection characteristics for this interface.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `::`.
    IPv6StaticAddresses: ?[]const ?ip_addresses.Ipv6staticAddressUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: LinksUpdate,
    /// DHCPv4 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v4.
    DHCPv4: ?Dhcpv4configurationUpdate = null,
    /// DHCPv6 configuration for this interface.
    ///
    /// This property shall contain the configuration of DHCP v6.
    DHCPv6: ?Dhcpv6configurationUpdate = null,
    /// Stateless address autoconfiguration (SLAAC) parameters for this interface.
    ///
    /// This object shall contain the IPv4 and IPv6 stateless address automatic configuration (SLAAC) properties for this interface.
    StatelessAddressAutoConfig: ?StatelessAddressAutoConfigurationUpdate = null,
    /// The IPv6 static default gateways for this interface.
    ///
    /// The values in this array shall represent the IPv6 static default gateway addresses for this interface.
    IPv6StaticDefaultGateways: ?[]const ?ip_addresses.Ipv6gatewayStaticAddressUpdate = null,
    /// The statically-defined set of DNS server IPv4 and IPv6 addresses.
    ///
    /// This property shall contain the statically-defined set of DNS server IP addresses to use when DHCP provisioning is not enabled for name server configuration.  As an implementation option, they can be used in addition to DHCP-provided addresses, or in cases where the DHCP server provides no DNS assignments.
    StaticNameServers: ?[]const ?[]const u8 = null,
    /// The IPv4 static addresses assigned to this interface.  See `IPv4Addresses` for the addresses in use by this interface.
    ///
    /// This property shall contain an array of objects that represent all IPv4 static addresses assigned to, but not necessarily in use by, this interface.  The `IPv4Addresses` property shall also list the addresses that this interface uses.  Services should represent static addresses that are not configured as `null` values, as described by the 'PATCH on array properties' clause of the Redfish Specification.  Services should not represent static addresses that are not configured as invalid IP addresses, such as `0.0.0.0`.
    IPv4StaticAddresses: ?[]const ?ip_addresses.Ipv4addressUpdate = null,
    /// The team mode for this interface.
    ///
    /// This property shall contain the team mode for this interface.  If this property is not present, the value shall be assumed to be `None`.
    TeamMode: core.Nullable(TeamMode) = .absent,
    /// An indication of whether IPv6 is enabled on this interface.
    ///
    /// This property shall indicate whether IPv6 is enabled on this interface.  If this property contains `false`, the interface shall not contain any assigned IPv6 addresses, shall not initiate DHCPv6 requests, and shall not send or process ICMPv6 packets.  If this property is not present, but this interface contains other IPv6 properties, the value shall be assumed to be `true`.
    IPv6Enabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
