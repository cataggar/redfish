//! Namespace `Zone`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const address_pool = @import("address_pool.zig");
const endpoint = @import("endpoint.zig");
const resource = @import("resource.zig");
const resource_block = @import("resource_block.zig");
const settings = @import("settings.zig");
const @"switch" = @import("switch.zig");

pub const ExternalAccessibility = enum {
    /// Any external entity with the correct access details, which might include authorization information, can access the endpoints that this zone lists.
    ///
    /// This value shall indicate that any external entity with the correct access details, which may include authorization information, can access the endpoints that this zone lists, regardless of zone.
    GloballyAccessible,
    /// Any external entity that another zone does not explicitly list can access the endpoints that this zone lists.
    ///
    /// This value shall indicate that any external entity that another zone does not explicitly list can access the endpoints that this zone lists.
    NonZonedAccessible,
    /// Only accessible by endpoints that this zone explicitly lists.
    ///
    /// This value shall indicate that endpoints in this zone are only accessible by endpoints that this zone explicitly lists.
    ZoneOnly,
    /// Routing is not enabled within this zone.
    ///
    /// This value shall indicate that implicit routing within this zone is not defined.
    NoInternalRouting,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ZoneType = enum {
    /// The zone in which all endpoints are added by default when instantiated.
    ///
    /// This value shall indicate a zone in which all endpoints are added by default when instantiated.  This value shall only be used for zones subordinate to the fabric collection.
    Default,
    /// A zone that contains endpoints.
    ///
    /// This value shall indicate a zone that contains resources of type `Endpoint`.  This value shall only be used for zones subordinate to the fabric collection.
    ZoneOfEndpoints,
    /// A zone that contains zones.
    ///
    /// This value shall indicate a zone that contains resources of type `Zone`.  This value shall only be used for zones subordinate to the fabric collection.
    ZoneOfZones,
    /// A zone that contains resource blocks.
    ///
    /// This value shall indicate a zone that contains resources of type `ResourceBlock`.  This value shall only be used for zones subordinate to the composition service.
    ZoneOfResourceBlocks,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Zone.AddEndpoint`.
///
/// This action adds an endpoint to a zone.
pub const ZoneAddEndpointAction = struct {
    /// The endpoint to add to the zone.
    ///
    /// This parameter shall contain a link to the specified endpoint to add to the zone.
    Endpoint: ?core.Reference = null,
    /// The current ETag of the endpoint to add to the zone.
    ///
    /// This parameter shall contain the current ETag of the endpoint to add to the zone.  If the client-provided ETag does not match the current ETag of the endpoint that the `Endpoint` parameter specifies, the service shall return the HTTP `428 Precondition Required` status code to reject the request.
    EndpointETag: ?[]const u8 = null,
    /// The current ETag of the zone.
    ///
    /// This parameter shall contain the current ETag of the zone.  If the client-provided ETag does not match the current ETag of the zone, the service shall return the HTTP `428 Precondition Required` status code to reject the request.
    ZoneETag: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Zone.RemoveEndpoint`.
///
/// This action removes an endpoint from a zone.
pub const ZoneRemoveEndpointAction = struct {
    /// The endpoint to remove from the zone.
    ///
    /// This parameter shall contain a link to the specified endpoint to remove from the zone.
    Endpoint: ?core.Reference = null,
    /// The current ETag of the endpoint to remove from the system.
    ///
    /// This parameter shall contain the current ETag of the endpoint to remove from the system.  If the client-provided ETag does not match the current ETag of the endpoint that the `Endpoint` parameter specifies, the service shall return the HTTP `428 Precondition Required` status code to reject the request.
    EndpointETag: ?[]const u8 = null,
    /// The current ETag of the zone.
    ///
    /// This parameter shall contain the current ETag of the zone.  If the client-provided ETag does not match the current ETag of the zone, the service shall return the HTTP `428 Precondition Required` status code to reject the request.
    ZoneETag: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action adds an endpoint to a zone.
    ///
    /// This action shall add an endpoint to a zone.
    @"#Zone.AddEndpoint": ?core.Action(ZoneAddEndpointAction, std.json.Value) = null,
    /// This action removes an endpoint from a zone.
    ///
    /// This action shall remove an endpoint from a zone.
    @"#Zone.RemoveEndpoint": ?core.Action(ZoneRemoveEndpointAction, std.json.Value) = null,

    /// This action adds an endpoint to a zone.
    ///
    /// This action shall add an endpoint to a zone.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn addEndpoint(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ZoneAddEndpointAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Zone.AddEndpoint" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action removes an endpoint from a zone.
    ///
    /// This action shall remove an endpoint from a zone.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn removeEndpoint(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ZoneRemoveEndpointAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Zone.RemoveEndpoint" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The links to the endpoints that this zone contains.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` that this zone contains.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `Endpoints` has in total, which is not how many this response carries.
    @"Endpoints@odata.count": ?i64 = null,
    /// The next page of `Endpoints`. Present only when this response left members out.
    @"Endpoints@odata.nextLink": ?core.ODataId = null,
    /// The links to the collection of switches in this zone.
    ///
    /// This property shall contain an array of links to resources of type `Switch` in this zone.
    InvolvedSwitches: ?[]const core.NavProperty(@"switch".Switch) = null,
    /// How many members `InvolvedSwitches` has in total, which is not how many this response carries.
    @"InvolvedSwitches@odata.count": ?i64 = null,
    /// The next page of `InvolvedSwitches`. Present only when this response left members out.
    @"InvolvedSwitches@odata.nextLink": ?core.ODataId = null,
    /// The links to the resource blocks with which this zone is associated.
    ///
    /// This property shall contain an array of links to resources of type `ResourceBlock` with which this zone is associated.
    ResourceBlocks: ?[]const core.NavProperty(resource_block.ResourceBlock) = null,
    /// How many members `ResourceBlocks` has in total, which is not how many this response carries.
    @"ResourceBlocks@odata.count": ?i64 = null,
    /// The next page of `ResourceBlocks`. Present only when this response left members out.
    @"ResourceBlocks@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the address pools associated with this zone.
    ///
    /// This property shall contain an array of links to resources of type `AddressPool` with which this zone is associated.
    AddressPools: ?[]const core.NavProperty(address_pool.AddressPool) = null,
    /// How many members `AddressPools` has in total, which is not how many this response carries.
    @"AddressPools@odata.count": ?i64 = null,
    /// The next page of `AddressPools`. Present only when this response left members out.
    @"AddressPools@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the zone that contain this zone.
    ///
    /// This property shall contain an array of links to resources of type `Zone` that represent the zones that contain this zone.  The zones referenced by this property shall not be contained by other zones.
    ContainedByZones: ?[]const core.NavProperty(Zone) = null,
    /// How many members `ContainedByZones` has in total, which is not how many this response carries.
    @"ContainedByZones@odata.count": ?i64 = null,
    /// The next page of `ContainedByZones`. Present only when this response left members out.
    @"ContainedByZones@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the zones that are contained by this zone.
    ///
    /// This property shall contain an array of links to resources of type `Zone` that represent the zones that are contained by this zone.  The zones referenced by this property shall not contain other zones.
    ContainsZones: ?[]const core.NavProperty(Zone) = null,
    /// How many members `ContainsZones` has in total, which is not how many this response carries.
    @"ContainsZones@odata.count": ?i64 = null,
    /// The next page of `ContainsZones`. Present only when this response left members out.
    @"ContainsZones@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Zone.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `Zone` schema describes a simple fabric zone for a Redfish implementation.
///
/// This resource shall represent a simple fabric zone for a Redfish implementation.
pub const Zone = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The durable names for the zone.
    ///
    /// This property shall contain a list of all known durable names for the associated zone.
    Identifiers: ?[]const resource.Identifier = null,
    /// Indicates accessibility of endpoints in this zone to endpoints outside of this zone.
    ///
    /// This property shall contain and indication of accessibility of endpoints in this zone to endpoints outside of this zone.
    ExternalAccessibility: ?ExternalAccessibility = null,
    /// The type of zone.
    ///
    /// This property shall contain the type of zone that this zone represents.
    ZoneType: ?ZoneType = null,
    /// This property indicates whether routing within this zone is enabled.
    ///
    /// This property shall indicate whether routing within this zone is enabled.
    DefaultRoutingEnabled: ?bool = null,
};

/// What a client may change of `Zone.Zone`.
///
/// The `Zone` schema describes a simple fabric zone for a Redfish implementation.
pub const ZoneUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// Indicates accessibility of endpoints in this zone to endpoints outside of this zone.
    ///
    /// This property shall contain and indication of accessibility of endpoints in this zone to endpoints outside of this zone.
    ExternalAccessibility: core.Nullable(ExternalAccessibility) = .absent,
    /// The type of zone.
    ///
    /// This property shall contain the type of zone that this zone represents.
    ZoneType: core.Nullable(ZoneType) = .absent,
    /// This property indicates whether routing within this zone is enabled.
    ///
    /// This property shall indicate whether routing within this zone is enabled.
    DefaultRoutingEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `Zone.Zone`.
///
/// The `Zone` schema describes a simple fabric zone for a Redfish implementation.
pub const ZoneCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// Indicates accessibility of endpoints in this zone to endpoints outside of this zone.
    ///
    /// This property shall contain and indication of accessibility of endpoints in this zone to endpoints outside of this zone.
    ExternalAccessibility: core.Nullable(ExternalAccessibility) = .absent,
    /// The type of zone.
    ///
    /// This property shall contain the type of zone that this zone represents.
    ZoneType: core.Nullable(ZoneType) = .absent,
    /// This property indicates whether routing within this zone is enabled.
    ///
    /// This property shall indicate whether routing within this zone is enabled.
    DefaultRoutingEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
