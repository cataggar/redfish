//! Namespace `Volume`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const capacity = @import("capacity.zig");
const class_of_service = @import("class_of_service.zig");
const connection = @import("connection.zig");
const consistency_group = @import("consistency_group.zig");
const data_storage_lo_scapabilities = @import("data_storage_lo_scapabilities.zig");
const drive = @import("drive.zig");
const endpoint = @import("endpoint.zig");
const features_registry = @import("features_registry.zig");
const io_statistics = @import("io_statistics.zig");
const resource = @import("resource.zig");
const spare_resource_set = @import("spare_resource_set.zig");
const storage = @import("storage.zig");
const storage_controller = @import("storage_controller.zig");
const storage_group = @import("storage_group.zig");
const storage_group_collection = @import("storage_group_collection.zig");
const storage_pool = @import("storage_pool.zig");
const storage_pool_collection = @import("storage_pool_collection.zig");
const storage_replica_info = @import("storage_replica_info.zig");
const storage_service = @import("storage_service.zig");
const volume_metrics = @import("volume_metrics.zig");

pub const EncryptionTypes = enum {
    /// The volume is utilizing the native drive encryption capabilities of the drive hardware.
    NativeDriveEncryption,
    /// The volume is being encrypted by the storage controller entity.
    ControllerAssisted,
    /// The volume is being encrypted by software running on the system or the operating system.
    SoftwareAssisted,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const InitializeMethod = enum {
    /// The volume will be available for use immediately, with no preparation.
    Skip,
    /// The volume will be available for use immediately, with data erasure and preparation to happen as background tasks.
    Background,
    /// Data erasure and preparation tasks will complete before the volume is presented as available for use.
    Foreground,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Deprecated.
/// Deprecated in favor of the InitializeMethod enumerated type.
pub const InitializeType = enum {
    /// The volume is prepared for use quickly, typically by erasing just the beginning and end of the space so that partitioning can be performed.
    Fast,
    /// The volume is prepared for use slowly, typically by completely erasing the volume.
    Slow,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// LBAFormatType is defined in the NVMe specification set. This field indicates the LBA data size supported; implementations may report up to 16 values. For more details refer to the appropriate NVMe specification.
pub const LbaFormatType = enum {
    /// LBAFormat0 is a required type. Indicates the LBA data size supported.
    LBAFormat0,
    /// Indicates the LBA data size if supported.
    LBAFormat1,
    /// Indicates the LBA data size supported if supported.
    LBAFormat2,
    /// Indicates the LBA data size supported if supported.
    LBAFormat3,
    /// Indicates the LBA data size supported if supported.
    LBAFormat4,
    /// Indicates the LBA data size supported if supported.
    LBAFormat5,
    /// Indicates the LBA data size supported if supported.
    LBAFormat6,
    /// Indicates the LBA data size supported if supported.
    LBAFormat7,
    /// Indicates the LBA data size supported if supported.
    LBAFormat8,
    /// Indicates the LBA data size supported if supported.
    LBAFormat9,
    /// Indicates the LBA data size supported if supported.
    LBAFormat10,
    /// Indicates the LBA data size supported if supported.
    LBAFormat11,
    /// Indicates the LBA data size supported if supported.
    LBAFormat12,
    /// Indicates the LBA data size supported if supported.
    LBAFormat13,
    /// Indicates the LBA data size supported if supported.
    LBAFormat14,
    /// Indicates the LBA data size supported if supported.
    LBAFormat15,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Indicate the relative performance of the LBA format relative to other LBA formats supported by the controller.
pub const LbaRelativePerformanceType = enum {
    /// Best performance.
    Best,
    /// Better performance.
    Better,
    /// Good performance.
    Good,
    /// Degraded performance.
    Degraded,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NamespaceType = enum {
    /// The namespace is configured for use with a block storage interface.
    Block,
    /// The namespace is configured for use with a KeyValue interface.
    KeyValue,
    /// The namespace is configured for use with a zoned storage interface.
    ZNS,
    /// The namespace is configured for use with a computational storage interface.
    Computational,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const OperationType = enum {
    /// A Deduplicate operation is being performed.
    Deduplicate,
    /// A CheckConsistency operation is being performed.
    CheckConsistency,
    /// An Initialize operation is being performed.
    Initialize,
    /// A Replicate operation is being performed.
    Replicate,
    /// A Delete operation is being performed.
    Delete,
    /// A ChangeRAIDType operation is being performed.
    ChangeRAIDType,
    /// A Rebuild operation is being performed.
    Rebuild,
    /// An Encrypt operation is being performed.
    Encrypt,
    /// A Decrypt operation is being performed.
    Decrypt,
    /// A Resize operation is being performed.
    Resize,
    /// A Compress operation is being performed.
    Compress,
    /// A Sanitize operation is being performed.
    Sanitize,
    /// A Format operation is being performed.
    Format,
    /// A ChangeStripSize operation is being performed.
    ChangeStripSize,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const RaidType = enum {
    /// A placement policy where consecutive logical blocks of data are uniformly distributed across a set of independent storage devices without offering any form of redundancy.
    ///
    /// A placement policy where consecutive logical blocks of data are uniformly distributed across a set of independent storage devices without offering any form of redundancy. This is commonly referred to as data striping. This form of RAID will encounter data loss with the failure of any storage device in the set.
    RAID0,
    /// A placement policy where each logical block of data is stored on more than one independent storage device.
    ///
    /// A placement policy where each logical block of data is stored on more than one independent storage device. This is commonly referred to as mirroring. Data stored using this form of RAID is able to survive a single storage device failure without data loss.
    RAID1,
    /// A placement policy using parity-based protection where logical bytes of data are uniformly distributed across a set of independent storage devices and where the parity is stored on a dedicated independent storage device.
    ///
    /// A placement policy using parity-based protection where logical bytes of data are uniformly distributed across a set of independent storage devices and where the parity is stored on a dedicated independent storage device. Data stored using this form of RAID is able to survive a single storage device failure without data loss. If the storage devices use rotating media, they are assumed to be rotationally synchronized, and the data stripe size should be no larger than the exported block size.
    RAID3,
    /// A placement policy using parity-based protection where logical blocks of data are uniformly distributed across a set of independent storage devices and where the parity is stored on a dedicated independent storage device.
    ///
    /// A placement policy using parity-based protection where logical blocks of data are uniformly distributed across a set of independent storage devices and where the parity is stored on a dedicated independent storage device. Data stored using this form of RAID is able to survive a single storage device failure without data loss.
    RAID4,
    /// A placement policy using parity-based protection for storing stripes of 'n' logical blocks of data and one logical block of parity across a set of 'n+1' independent storage devices where the parity and data blocks are interleaved across the storage devices.
    ///
    /// A placement policy using parity-based protection for storing stripes of 'n' logical blocks of data and one logical block of parity across a set of 'n+1' independent storage devices where the parity and data blocks are interleaved across the storage devices. Data stored using this form of RAID is able to survive a single storage device failure without data loss.
    RAID5,
    /// A placement policy using parity-based protection for storing stripes of 'n' logical blocks of data and two logical blocks of independent parity across a set of 'n+2' independent storage devices where the parity and data blocks are interleaved across the storage devices.
    ///
    /// A placement policy using parity-based protection for storing stripes of 'n' logical blocks of data and two logical blocks of independent parity across a set of 'n+2' independent storage devices where the parity and data blocks are interleaved across the storage devices. Data stored using this form of RAID is able to survive any two independent storage device failures without data loss.
    RAID6,
    /// A placement policy that creates a striped device (RAID 0) over a set of mirrored devices (RAID 1).
    ///
    /// A placement policy that creates a striped device (RAID 0) over a set of mirrored devices (RAID 1). This is commonly referred to as RAID 1/0. Data stored using this form of RAID is able to survive storage device failures in each RAID 1 set without data loss.
    RAID10,
    /// A data placement policy that creates a mirrored device (RAID 1) over a set of striped devices (RAID 0).
    ///
    /// A data placement policy that creates a mirrored device (RAID 1) over a set of striped devices (RAID 0). This is commonly referred to as RAID 0+1 or RAID 0/1. Data stored using this form of RAID is able to survive a single RAID 0 data set failure without data loss.
    RAID01,
    /// A placement policy that uses parity-based protection for storing stripes of 'n' logical blocks of data and three logical blocks of independent parity across a set of 'n+3' independent storage devices where the parity and data blocks are interleaved across the storage devices.
    ///
    /// A placement policy that uses parity-based protection for storing stripes of 'n' logical blocks of data and three logical blocks of independent parity across a set of 'n+3' independent storage devices where the parity and data blocks are interleaved across the storage devices. This is commonly referred to as Triple Parity RAID. Data stored using this form of RAID is able to survive any three independent storage device failures without data loss.
    RAID6TP,
    /// A placement policy that uses a form of mirroring implemented over a set of independent storage devices where logical blocks are duplicated on a pair of independent storage devices so that data is uniformly distributed across the storage devices.
    ///
    /// A placement policy that uses a form of mirroring implemented over a set of independent storage devices where logical blocks are duplicated on a pair of independent storage devices so that data is uniformly distributed across the storage devices. This is commonly referred to as RAID 1 Enhanced. Data stored using this form of RAID is able to survive a single storage device failure without data loss.
    RAID1E,
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 5 sets of independent storage devices.
    ///
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 5 sets of independent storage devices. Data stored using this form of RAID is able to survive a single storage device failure within each RAID 5 set without data loss.
    RAID50,
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 6 sets of independent storage devices.
    ///
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 6 sets of independent storage devices. Data stored using this form of RAID is able to survive two device failures within each RAID 6 set without data loss.
    RAID60,
    /// A placement policy that creates a RAID 0 stripe set over two or more RAID 0 sets.
    ///
    /// A placement policy that creates a RAID 0 stripe set over two or more RAID 0 sets. This is commonly referred to as RAID 0+0. This form of data layout is not fault tolerant; if any storage device fails there will be data loss.
    RAID00,
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 10 sets.
    ///
    /// A placement policy that uses a RAID 0 stripe set over two or more RAID 10 sets. This is commonly referred to as Enhanced RAID 10. Data stored using this form of RAID is able to survive a single device failure within each nested RAID 1 set without data loss.
    RAID10E,
    /// A placement policy where each logical block of data is mirrored three times across a set of three independent storage devices.
    ///
    /// A placement policy where each logical block of data is mirrored three times across a set of three independent storage devices. This is commonly referred to as three-way mirroring. This form of RAID can survive two device failures without data loss.
    RAID1Triple,
    /// A placement policy that uses a striped device (RAID 0) over a set of triple mirrored devices (RAID 1Triple).
    ///
    /// A placement policy that uses a striped device (RAID 0) over a set of triple mirrored devices (RAID 1Triple). This form of RAID can survive up to two failures in each triple mirror set without data loss.
    RAID10Triple,
    /// A placement policy with no redundancy at the device level.
    ///
    /// A placement policy with no redundancy at the device level.
    None,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ReadCachePolicyType = enum {
    /// A caching technique in which the controller pre-fetches data anticipating future read requests.
    ReadAhead,
    /// A caching technique in which the controller dynamically determines whether to pre-fetch data anticipating future read requests, based on previous cache hit ratio.
    AdaptiveReadAhead,
    /// The read cache is disabled.
    Off,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const VolumeType = enum {
    /// The volume is a raw physical device without any RAID or other virtualization applied.
    RawDevice,
    /// The volume is a non-redundant storage device.
    NonRedundant,
    /// The volume is a mirrored device.
    Mirrored,
    /// The volume is a device which uses parity to retain redundant information.
    StripedWithParity,
    /// The volume is a spanned set of mirrored devices.
    SpannedMirrors,
    /// The volume is a spanned set of devices which uses parity to retain redundant information.
    SpannedStripesWithParity,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const VolumeUsageType = enum {
    /// The volume is allocated for use as a consumable data volume.
    ///
    /// The volume shall be allocated for use as a consumable data volume.
    Data,
    /// The volume is allocated for use as a consumable data volume reserved for system use.
    ///
    /// The volume shall be allocated for use as a consumable data volume reserved for system use.
    SystemData,
    /// The volume is allocated for use as a non-consumable cache only volume.
    ///
    /// The volume shall be allocated for use as a non-consumable cache only volume.
    CacheOnly,
    /// The volume is allocated for use as a non-consumable system reserved volume.
    ///
    /// The volume shall be allocated for use as a non-consumable system reserved volume.
    SystemReserve,
    /// The volume is allocated for use as a non-consumable reserved volume for replication use.
    ///
    /// The volume shall be allocated for use as a non-consumable reserved volume for replication use.
    ReplicationReserve,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const WriteCachePolicyType = enum {
    /// A caching technique in which the completion of a write request is not signaled until data is safely stored on non-volatile media.
    ///
    /// A caching technique in which the completion of a write request is not signaled until data is safely stored on non-volatile media.
    WriteThrough,
    /// A caching technique in which the completion of a write request is signaled as soon as the data is in cache, and actual writing to non-volatile media is guaranteed to occur at a later time.
    ///
    /// A caching technique in which the completion of a write request is signaled as soon as the data is in cache, and actual writing to non-volatile media is guaranteed to occur at a later time.
    ProtectedWriteBack,
    /// A caching technique in which the completion of a write request is signaled as soon as the data is in cache; actual writing to non-volatile media is not guaranteed to occur at a later time.
    ///
    /// A caching technique in which the completion of a write request is signaled as soon as the data is in cache; actual writing to non-volatile media is not guaranteed to occur at a later time.
    UnprotectedWriteBack,
    /// The write cache is disabled.
    ///
    /// Indicates that the write cache shall be disabled.
    Off,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const WriteCacheStateType = enum {
    /// Indicates that the cache state type in use generally does not protect write requests on non-volatile media.
    ///
    /// Indicates that the cache state type in use generally does not protect write requests on non-volatile media.
    Unprotected,
    /// Indicates that the cache state type in use generally protects write requests on non-volatile media.
    ///
    /// Indicates that the cache state type in use generally protects write requests on non-volatile media.
    Protected,
    /// Indicates an issue with the cache state in which the cache space is diminished or disabled due to a failure or an outside influence such as a discharged battery.
    ///
    /// Indicates an issue with the cache state in which the cache space is diminished or disabled due to a failure or an outside influence such as a discharged battery.
    Degraded,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const WriteHoleProtectionPolicyType = enum {
    /// The volume is not using any policy to address the write hole issue.
    ///
    /// The support for addressing the write hole issue is disabled. The volume is not performing any additional activities to close the RAID write hole.
    Off,
    /// The policy that uses separate block device for write-ahead logging to address write hole issue.
    ///
    /// The policy that uses separate block device for write-ahead logging to address write hole issue. All write operations on the RAID volume are first logged on dedicated journaling device that is not part of the volume.
    Journaling,
    /// The policy that distributes additional log among the volume's capacity sources to address write hole issue.
    ///
    /// The policy that distributes additional log (e.q. checksum of the parity) among the volume's capacity sources to address write hole issue. Additional data is used to detect data corruption on the volume.
    DistributedLog,
    /// The policy that is Oem specific.
    ///
    /// The policy that is Oem specific. The mechanism details are unknown unless provided separately by the Oem.
    Oem,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Volume.AssignReplicaTarget`.
///
/// This action is used to establish a replication relationship by assigning an existing volume to serve as a target replica for an existing source volume.
pub const VolumeAssignReplicaTargetAction = struct {
    /// The replica update mode (synchronous vs asynchronous).
    ///
    /// This parameter shall specify the replica update mode.
    ReplicaUpdateMode: ?storage_replica_info.ReplicaUpdateMode = null,
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,
    /// The type of replica relationship to be created.
    ///
    /// This parameter shall contain the type of replica relationship to be created (e.g., Clone, Mirror, Snap).
    ReplicaType: ?storage_replica_info.ReplicaType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.ChangeRAIDLayout`.
///
/// Request system change the RAID layout of the volume.
pub const VolumeChangeRaidLayoutAction = struct {
    /// The requested RAID type for the volume.
    ///
    /// This parameter shall contain the requested RAID type for the volume.
    RAIDType: ?RaidType = null,
    /// The number of blocks (bytes) requested for new strip size.
    ///
    /// This parameter shall contain the number of blocks (bytes) requested for the strip size.
    StripSizeBytes: ?i64 = null,
    /// The requested number of media elements used per span in the secondary RAID for a hierarchical RAID type.
    ///
    /// This parameter shall contain the requested number of media elements used per span in the secondary RAID for a hierarchical RAID type.
    MediaSpanCount: ?i64 = null,
    /// An array of the drives to be used by the volume.
    ///
    /// This parameter shall contain an array of the drives to be used by the volume.
    Drives: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.CheckConsistency`.
///
/// This action is used to force a check of the Volume's parity or redundant data to ensure it matches calculated values.
pub const VolumeCheckConsistencyAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.CreateReplicaTarget`.
///
/// This action is used to create a new volume resource to provide expanded data protection through a replica relationship with the specified source volume.
pub const VolumeCreateReplicaTargetAction = struct {
    /// The replica update mode (synchronous vs asynchronous).
    ///
    /// This parameter shall specify the replica update mode.
    ReplicaUpdateMode: ?storage_replica_info.ReplicaUpdateMode = null,
    /// The Name for the new target volume.
    ///
    /// This parameter shall contain the Name for the target volume.
    VolumeName: ?[]const u8 = null,
    /// The Uri to the existing target Storage Pool.
    ///
    /// This parameter shall contain the Uri to the existing StoragePool in which to create the target volume.
    TargetStoragePool: ?[]const u8 = null,
    /// The type of replica relationship to be created.
    ///
    /// This parameter shall contain the type of replica relationship to be created (e.g., Clone, Mirror, Snap).
    ReplicaType: ?storage_replica_info.ReplicaType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.ForceEnable`.
///
/// Request system force the volume to an enabled state regardless of data loss.
pub const VolumeForceEnableAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.Initialize`.
///
/// This action is used to prepare the contents of the volume for use by the system. If InitializeMethod is not specified in the request body, but the property InitializeMethod is specified, the property InitializeMethod value should be used. If neither is specified, the InitializeMethod should be Foreground.
pub const VolumeInitializeAction = struct {
    /// The type of initialization to be performed.
    ///
    /// This defines the property name for the action.
    InitializeMethod: ?InitializeMethod = null,
    /// The type of initialization to be performed.
    ///
    /// This defines the property name for the action.
    ///
    /// Deprecated.
    /// Deprecated in favor of the InitializeMethod property.
    InitializeType: ?InitializeType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.RemoveReplicaRelationship`.
///
/// This action is used to disable data synchronization between a source and target volume, remove the replication relationship, and optionally delete the target volume.
pub const VolumeRemoveReplicaRelationshipAction = struct {
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,
    /// Indicate whether or not to delete the target volume as part of the operation.
    ///
    /// This parameter shall indicate whether or not to delete the target volume as part of the operation. If not defined, the system should use its default behavior.
    DeleteTargetVolume: ?bool = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.ResumeReplication`.
///
/// This action is used to resume the active data synchronization between a source and target volume, without otherwise altering the replication relationship.
pub const VolumeResumeReplicationAction = struct {
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.ReverseReplicationRelationship`.
///
/// This action is used to reverse the replication relationship between a source and target volume.
pub const VolumeReverseReplicationRelationshipAction = struct {
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.SplitReplication`.
///
/// This action is used to split the replication relationship and suspend data synchronization between a source and target volume.
pub const VolumeSplitReplicationAction = struct {
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Volume.SuspendReplication`.
///
/// This action is used to suspend active data synchronization between a source and target volume, without otherwise altering the replication relationship.
pub const VolumeSuspendReplicationAction = struct {
    /// The Uri to the existing target volume.
    ///
    /// This parameter shall contain the Uri to the existing target volume.
    TargetVolume: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Alua = struct {
    /// The ANA group id for this volume.
    ///
    /// This shall contain the ANA group id for this volume.
    ANAGroupId: ?core.Decimal = null,
};

pub const Actions = struct {
    Oem: ?OemActions = null,
    /// This action is used to establish a replication relationship by assigning an existing volume to serve as a target replica for an existing source volume.
    ///
    /// This action shall be used to establish a replication relationship by assigning an existing volume to serve as a target replica for an existing source volume.
    @"#Volume.AssignReplicaTarget": ?core.Action(VolumeAssignReplicaTargetAction, std.json.Value) = null,
    /// Request system change the RAID layout of the volume.
    ///
    /// This action shall request the system to change the RAID layout of the volume.  Depending on the combination of the submitted parameters, this could be changing the RAID type, changing the span count, changing the number of drives used by the volume, or another configuration change supported by the system. Note that usage of this action while online may potentially cause data loss if the available capacity is reduced.
    @"#Volume.ChangeRAIDLayout": ?core.Action(VolumeChangeRaidLayoutAction, std.json.Value) = null,
    /// This action is used to force a check of the Volume's parity or redundant data to ensure it matches calculated values.
    ///
    /// This defines the name of the custom action supported on this resource.
    @"#Volume.CheckConsistency": ?core.Action(VolumeCheckConsistencyAction, std.json.Value) = null,
    /// This action is used to create a new volume resource to provide expanded data protection through a replica relationship with the specified source volume.
    ///
    /// This action shall be used to create a new volume resource to provide expanded data protection through a replica relationship with the specified source volume.
    @"#Volume.CreateReplicaTarget": ?core.Action(VolumeCreateReplicaTargetAction, std.json.Value) = null,
    /// Request system force the volume to an enabled state regardless of data loss.
    ///
    /// This action shall request the system to force the volume to enabled state regardless of data loss scenarios.
    @"#Volume.ForceEnable": ?core.Action(VolumeForceEnableAction, std.json.Value) = null,
    /// This action is used to prepare the contents of the volume for use by the system. If InitializeMethod is not specified in the request body, but the property InitializeMethod is specified, the property InitializeMethod value should be used. If neither is specified, the InitializeMethod should be Foreground.
    ///
    /// This defines the name of the custom action supported on this resource. If InitializeMethod is not specified in the request body, but the property InitializeMethod is specified, the property InitializeMethod value should be used. If neither is specified, the InitializeMethod should be Foreground.
    @"#Volume.Initialize": ?core.Action(VolumeInitializeAction, std.json.Value) = null,
    /// This action is used to disable data synchronization between a source and target volume, remove the replication relationship, and optionally delete the target volume.
    ///
    /// This action shall be used to disable data synchronization between a source and target volume, remove the replication relationship, and optionally delete the target volume.
    @"#Volume.RemoveReplicaRelationship": ?core.Action(VolumeRemoveReplicaRelationshipAction, std.json.Value) = null,
    /// This action is used to resume the active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// This action shall be used to resume the active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    @"#Volume.ResumeReplication": ?core.Action(VolumeResumeReplicationAction, std.json.Value) = null,
    /// This action is used to reverse the replication relationship between a source and target volume.
    ///
    /// This action shall be used to reverse the replication relationship between a source and target volume.
    @"#Volume.ReverseReplicationRelationship": ?core.Action(VolumeReverseReplicationRelationshipAction, std.json.Value) = null,
    /// This action is used to split the replication relationship and suspend data synchronization between a source and target volume.
    ///
    /// This action shall be used to split the replication relationship and suspend data synchronization between a source and target volume.
    @"#Volume.SplitReplication": ?core.Action(VolumeSplitReplicationAction, std.json.Value) = null,
    /// This action is used to suspend active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// This action shall be used to suspend active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    @"#Volume.SuspendReplication": ?core.Action(VolumeSuspendReplicationAction, std.json.Value) = null,

    /// This action is used to establish a replication relationship by assigning an existing volume to serve as a target replica for an existing source volume.
    ///
    /// This action shall be used to establish a replication relationship by assigning an existing volume to serve as a target replica for an existing source volume.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn assignReplicaTarget(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeAssignReplicaTargetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.AssignReplicaTarget" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Request system change the RAID layout of the volume.
    ///
    /// This action shall request the system to change the RAID layout of the volume.  Depending on the combination of the submitted parameters, this could be changing the RAID type, changing the span count, changing the number of drives used by the volume, or another configuration change supported by the system. Note that usage of this action while online may potentially cause data loss if the available capacity is reduced.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn changeRaidLayout(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeChangeRaidLayoutAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.ChangeRAIDLayout" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to force a check of the Volume's parity or redundant data to ensure it matches calculated values.
    ///
    /// This defines the name of the custom action supported on this resource.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn checkConsistency(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeCheckConsistencyAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.CheckConsistency" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to create a new volume resource to provide expanded data protection through a replica relationship with the specified source volume.
    ///
    /// This action shall be used to create a new volume resource to provide expanded data protection through a replica relationship with the specified source volume.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn createReplicaTarget(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeCreateReplicaTargetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.CreateReplicaTarget" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Request system force the volume to an enabled state regardless of data loss.
    ///
    /// This action shall request the system to force the volume to enabled state regardless of data loss scenarios.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn forceEnable(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeForceEnableAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.ForceEnable" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to prepare the contents of the volume for use by the system. If InitializeMethod is not specified in the request body, but the property InitializeMethod is specified, the property InitializeMethod value should be used. If neither is specified, the InitializeMethod should be Foreground.
    ///
    /// This defines the name of the custom action supported on this resource. If InitializeMethod is not specified in the request body, but the property InitializeMethod is specified, the property InitializeMethod value should be used. If neither is specified, the InitializeMethod should be Foreground.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn initialize(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeInitializeAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.Initialize" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to disable data synchronization between a source and target volume, remove the replication relationship, and optionally delete the target volume.
    ///
    /// This action shall be used to disable data synchronization between a source and target volume, remove the replication relationship, and optionally delete the target volume.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn removeReplicaRelationship(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeRemoveReplicaRelationshipAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.RemoveReplicaRelationship" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to resume the active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// This action shall be used to resume the active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resumeReplication(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeResumeReplicationAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.ResumeReplication" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to reverse the replication relationship between a source and target volume.
    ///
    /// This action shall be used to reverse the replication relationship between a source and target volume.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reverseReplicationRelationship(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeReverseReplicationRelationshipAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.ReverseReplicationRelationship" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to split the replication relationship and suspend data synchronization between a source and target volume.
    ///
    /// This action shall be used to split the replication relationship and suspend data synchronization between a source and target volume.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn splitReplication(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeSplitReplicationAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.SplitReplication" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to suspend active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// This action shall be used to suspend active data synchronization between a source and target volume, without otherwise altering the replication relationship.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn suspendReplication(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VolumeSuspendReplicationAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Volume.SuspendReplication" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

pub const LbaFormat = struct {
    /// The LBA format type.
    ///
    /// This shall be the LBA format type. This property is intended for capabilities instrumentation.
    LBAFormatType: ?LbaFormatType = null,
    /// The LBA Relative Performance type.
    ///
    /// This shall be the LBA Relative Performance type. This field indicates the relative performance of the LBA format indicated relative to other LBA formats supported by the controller. This property is intended for capabilities instrumentation.
    RelativePerformance: ?LbaRelativePerformanceType = null,
    /// The LBA data size in bytes.
    ///
    /// This shall be the LBA data size reported in bytes.
    LBADataSizeBytes: ?i64 = null,
    /// The LBA metadata size in bytes.
    ///
    /// This shall be the LBA metadata size reported in bytes.
    LBAMetadataSizeBytes: ?i64 = null,
};

/// Add the ProvidingStoragePool property.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of references to the drives which contain this volume. This will reference Drives that either wholly or only partly contain this volume.
    ///
    /// The value of this property shall be a reference to the resources that this volume is associated with and shall reference resources of type Drive. This property shall only contain references to Drive entities which are currently members of the Volume, not hot spare Drives which are not currently a member of the volume.
    Drives: ?[]const core.NavProperty(drive.Drive) = null,
    /// How many members `Drives` has in total, which is not how many this response carries.
    @"Drives@odata.count": ?i64 = null,
    /// The next page of `Drives`. Present only when this response left members out.
    @"Drives@odata.nextLink": ?core.ODataId = null,
    /// The ClassOfService that this storage volume conforms to.
    ///
    /// This property shall contain a reference to the ClassOfService that this storage volume conforms to.
    ClassOfService: ?core.NavProperty(class_of_service.ClassOfService) = null,
    /// An array of references to the drives which are dedicated spares for this volume.
    ///
    /// The value of this property shall be a reference to the resources that this volume is associated with and shall reference resources of type Drive. This property shall only contain references to Drive entities which are currently assigned as a dedicated spare and are able to support this Volume.
    DedicatedSpareDrives: ?[]const core.NavProperty(drive.Drive) = null,
    /// How many members `DedicatedSpareDrives` has in total, which is not how many this response carries.
    @"DedicatedSpareDrives@odata.count": ?i64 = null,
    /// The next page of `DedicatedSpareDrives`. Present only when this response left members out.
    @"DedicatedSpareDrives@odata.nextLink": ?core.ODataId = null,
    /// An array of references to SpareResourceSets.
    ///
    /// Each referenced SpareResourceSet shall contain resources that may be utilized to replace the capacity provided by a failed resource having a compatible type.
    SpareResourceSets: ?[]const core.NavProperty(spare_resource_set.SpareResourceSet) = null,
    /// How many members `SpareResourceSets` has in total, which is not how many this response carries.
    @"SpareResourceSets@odata.count": ?i64 = null,
    /// The next page of `SpareResourceSets`. Present only when this response left members out.
    @"SpareResourceSets@odata.nextLink": ?core.ODataId = null,
    /// An array of references to the client Endpoints associated with this volume.
    ///
    /// The value of this property shall be references to the client Endpoints this volume is associated with.
    ClientEndpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `ClientEndpoints` has in total, which is not how many this response carries.
    @"ClientEndpoints@odata.count": ?i64 = null,
    /// The next page of `ClientEndpoints`. Present only when this response left members out.
    @"ClientEndpoints@odata.nextLink": ?core.ODataId = null,
    /// An array of references to the server Endpoints associated with this volume.
    ///
    /// The value of this property shall be references to the server Endpoints this volume is associated with.
    ServerEndpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `ServerEndpoints` has in total, which is not how many this response carries.
    @"ServerEndpoints@odata.count": ?i64 = null,
    /// The next page of `ServerEndpoints`. Present only when this response left members out.
    @"ServerEndpoints@odata.nextLink": ?core.ODataId = null,
    /// An array of references to the StorageGroups associated with this volume.
    ///
    /// The value of this property shall be references to the StorageGroups this volume is associated with.
    StorageGroups: ?[]const core.NavProperty(storage_group.StorageGroup) = null,
    /// How many members `StorageGroups` has in total, which is not how many this response carries.
    @"StorageGroups@odata.count": ?i64 = null,
    /// The next page of `StorageGroups`. Present only when this response left members out.
    @"StorageGroups@odata.nextLink": ?core.ODataId = null,
    /// An array of references to the ConsistencyGroups associated with this volume.
    ///
    /// The value of this property shall be references to the ConsistencyGroups this volume is associated with.
    ConsistencyGroups: ?[]const core.NavProperty(consistency_group.ConsistencyGroup) = null,
    /// How many members `ConsistencyGroups` has in total, which is not how many this response carries.
    @"ConsistencyGroups@odata.count": ?i64 = null,
    /// The next page of `ConsistencyGroups`. Present only when this response left members out.
    @"ConsistencyGroups@odata.nextLink": ?core.ODataId = null,
    /// A pointer to the StorageService that owns or contains this volume.
    ///
    /// This shall be a pointer to the StorageService that owns or contains this volume.
    OwningStorageService: ?core.NavProperty(storage_service.StorageService) = null,
    /// A pointer to the Resource that serves as a journaling media for this volume.
    ///
    /// This shall be a pointer to the journaling media used for this Volume to address the write hole issue. Valid when WriteHoleProtectionPolicy property is set to 'Journaling'.
    JournalingMedia: ?core.NavProperty(resource.Resource) = null,
    /// A pointer to the Storage resource that owns or contains this volume.
    ///
    /// This shall be a pointer to the Storage resource that owns or contains this volume.
    OwningStorageResource: ?core.NavProperty(storage.Storage) = null,
    /// A pointer to the cache volume source for this volume.
    ///
    /// This shall be a pointer to the cache volume source for this volume. The corresponding VolumeUsage property shall be set to Data when this property is used.
    CacheVolumeSource: ?core.NavProperty(Volume) = null,
    /// A pointer to the data volumes this volume serves as a cache volume.
    ///
    /// This shall be a pointer to the cache data volumes this volume serves as a cache volume.  The corresponding VolumeUsage property shall be set to CacheOnly when this property is used.
    CacheDataVolumes: ?[]const core.NavProperty(Volume) = null,
    /// How many members `CacheDataVolumes` has in total, which is not how many this response carries.
    @"CacheDataVolumes@odata.count": ?i64 = null,
    /// The next page of `CacheDataVolumes`. Present only when this response left members out.
    @"CacheDataVolumes@odata.nextLink": ?core.ODataId = null,
    /// An array of the Controllers associated with this volume.
    ///
    /// This parameter shall contain an array of the controllers (of type StorageController) associated with this volume. When the volume is of type NVMe, these may be both the physical and logical controller representations.
    Controllers: ?[]const core.NavProperty(storage_controller.StorageController) = null,
    /// How many members `Controllers` has in total, which is not how many this response carries.
    @"Controllers@odata.count": ?i64 = null,
    /// The next page of `Controllers`. Present only when this response left members out.
    @"Controllers@odata.nextLink": ?core.ODataId = null,
    /// The StoragePool resource that provides this volume resource.
    ///
    /// This property shall contain a pointer to the StoragePool resource that provides this volume resource.
    ProvidingStoragePool: ?core.NavProperty(storage_pool.StoragePool) = null,
};

/// What a client may change of `Volume.Links`.
///
/// Add the ProvidingStoragePool property.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Add the SupportsMultipathandSharing, and ReservationCapabilities properties.
pub const NvmeNamespaceProperties = struct {
    /// The NVMe Namespace Identifier for this namespace.
    ///
    /// This property shall contain the NVMe Namespace Identifier for this namespace. This property shall be a hex value. Namespace identifiers are not durable and do not have meaning outside the scope of the NVMe subsystem. NSID 0x0, 0xFFFFFFFF, 0xFFFFFFFE are special purpose values.
    NamespaceId: ?[]const u8 = null,
    /// Indicates the namespace is shareable.
    ///
    /// This property shall indicate whether the namespace is shareable.
    IsShareable: ?bool = null,
    /// This property contains a set of Namespace Features.
    ///
    /// This property shall contain a set of Namespace Features.
    NamespaceFeatures: ?NamespaceFeatures = null,
    /// The number of LBA data size and metadata size combinations supported by this namespace. The value of this property is between 0 and 16.
    ///
    /// This property shall contain the number of LBA data size and metadata size combinations supported by this namespace. The value of this property is between 0 and 16. LBA formats with an index set beyond this value will not be supported.
    NumberLBAFormats: ?i64 = null,
    /// The LBA data size and metadata size combination that the namespace has been formatted with.
    ///
    /// This property shall contain the LBA data size and metadata size combination that the namespace has been formatted with. This is a 4-bit data structure.
    FormattedLBASize: ?[]const u8 = null,
    /// This property indicates whether or not the metadata is transferred at the end of the LBA creating an extended data LBA.
    ///
    /// This property shall indicate whether or not the metadata is transferred at the end of the LBA creating an extended data LBA.
    MetadataTransferredAtEndOfDataLBA: ?bool = null,
    /// The version of the NVMe Base Specification supported.
    ///
    /// This property shall contain the version of the NVMe Base Specification supported.
    NVMeVersion: ?[]const u8 = null,
    /// A list of the LBA format types supported for the namespace, or potential namespaces.
    ///
    /// This shall be a list of the LBA formats supported for the namespace, or potential namespaces.
    LBAFormatsSupported: ?[]const ?LbaFormatType = null,
    /// Identifies the type of namespace.
    ///
    /// This shall identify the type of namespace.
    Type: ?NamespaceType = null,
    /// Identifies the type of namespace.
    ///
    /// This shall identify the type of namespace.
    NamespaceType: ?NamespaceType = null,
    /// Describes the LBA format IDs and detailed properties.
    ///
    /// This property shall describe the LBA format IDs and corresponding detailed properties, such as the LBA data size and metadata size. This property is intended for use in a collection capabilities annotation. Use the LBAFormat property on an instance of a namespace.
    LBAFormats: ?[]const ?LbaFormat = null,
    /// Describes the current LBA format ID and detailed properties.
    ///
    /// This property shall describe the current LBA format ID and corresponding detailed properties, such as the LBA data size and metadata size. Use the LBAFormats property to describe namespace capabilities in a collection capabilities annotation.
    LBAFormat: ?LbaFormat = null,
    /// Indicates whether the namespace may be attached to two or more controllers.
    ///
    /// This property shall indicate whether the namespace may be attached to two or more controllers.
    SupportsMultipleNamespaceAttachments: ?bool = null,
    /// Indicates whether the namespace supports IO performance hints.
    ///
    /// This property shall indicate whether the namespace supports IO performance hints.
    SupportsIOPerformanceHints: ?bool = null,
};

/// What a client may change of `Volume.NVMeNamespaceProperties`.
///
/// Add the SupportsMultipathandSharing, and ReservationCapabilities properties.
pub const NvmeNamespacePropertiesUpdate = struct {
    /// Indicates the namespace is shareable.
    ///
    /// This property shall indicate whether the namespace is shareable.
    IsShareable: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const NamespaceFeatures = struct {
    /// This property indicates whether or not the NVMe Namespace supports thin provisioning.
    ///
    /// This property shall indicate whether or not the NVMe Namespace supports thin provisioning. Specifically, the namespace capacity reported may be less than the namespace size.
    SupportsThinProvisioning: ?bool = null,
    /// This property indicates that the controller supports deallocated or unwritten logical block error for this namespace.
    ///
    /// This property shall indicate that the controller supports deallocated or unwritten logical block error for this namespace.
    SupportsDeallocatedOrUnwrittenLBError: ?bool = null,
    /// This property indicates that the namespace supports the use of an NGUID (namespace globally unique identifier) value.
    ///
    /// This property shall indicate that the namespace supports the use of an NGUID (namespace globally unique identifier) value.
    SupportsNGUIDReuse: ?bool = null,
    /// Indicates that the NVM fields for Namespace preferred write granularity (NPWG), write alignment (NPWA), deallocate granularity (NPDG), deallocate alignment (NPDA) and optimal write size (NOWS)  are defined for this namespace and should be used by the host for I/O optimization.
    ///
    /// This property shall indicate whether or not the NVM fields for Namespace preferred write granularity (NPWG), write alignment (NPWA), deallocate granularity (NPDG), deallocate alignment (NPDA) and optimal write size (NOWS)  are defined for this namespace and should be used by the host for I/O optimization.
    SupportsAtomicTransactionSize: ?bool = null,
    /// Indicates that the Namespace Atomic Write Unit Normal (NAWUN), Namespace Atomic Write Unit Power Fail (NAWUPF), and Namespace Atomic Compare and Write Unit (NACWU) fields are defined for this namespace and should be used by the host for this namespace instead of the controller-level properties AWUN, AWUPF, and ACWU.
    ///
    /// This property shall indicate that the Namespace Atomic Write Unit Normal (NAWUN), Namespace Atomic Write Unit Power Fail (NAWUPF), and Namespace Atomic Compare and Write Unit (NACWU) fields are defined for this namespace and should be used by the host for this namespace instead of the controller-level properties AWUN, AWUPF, and ACWU.
    SupportsIOPerformanceHints: ?bool = null,
};

pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const Operation = struct {
    /// The name of the operation.
    ///
    /// Deprecated in v1_9_0.
    /// This property is deprecated in favor of the Operation property using the Operation enum.
    OperationName: ?[]const u8 = null,
    /// The percentage of the operation that has been completed.
    PercentageComplete: ?i64 = null,
    /// The type of the operation.
    ///
    /// This property shall contain the type of the operation.
    Operation: ?OperationType = null,
    /// A reference to the task associated with the operation if any.
    AssociatedFeaturesRegistry: ?core.NavProperty(features_registry.FeaturesRegistry) = null,
};

/// Add RAIDType property.
///
/// This resource shall be used to represent a volume, virtual disk, logical disk, LUN, or other logical storage for a Redfish implementation.
pub const Volume = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The property contains the status of the Volume.
    ///
    /// The property shall contain the status of the Volume.
    Status: ?resource.Status = null,
    /// The size in bytes of this Volume.
    ///
    /// This property shall contain the size in bytes of the associated volume.
    CapacityBytes: ?i64 = null,
    /// The type of this volume.
    ///
    /// This property shall contain the type of the associated Volume.
    ///
    /// Deprecated.
    /// Deprecated in favor of explicit use of RAIDType.
    VolumeType: ?VolumeType = null,
    /// Is this Volume encrypted.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing encryption or not.
    Encrypted: ?bool = null,
    /// The types of encryption used by this Volume.
    ///
    /// This property shall contain the types of encryption used by this Volume.
    EncryptionTypes: ?[]const EncryptionTypes = null,
    /// The Durable names for the volume.
    ///
    /// This property shall contain a list of all known durable names for the associated volume.
    Identifiers: ?[]const resource.Identifier = null,
    /// The size of the smallest addressable unit (Block) of this volume in bytes.
    ///
    /// This property shall contain size of the smallest addressable unit of the associated volume.
    BlockSizeBytes: ?i64 = null,
    /// The operations currently running on the Volume.
    ///
    /// This property shall contain a list of all currently running on the Volume.
    Operations: ?[]const Operation = null,
    /// The size in bytes of this Volume's optimum IO size.
    ///
    /// This property shall contain the optimum IO size to use when performing IO on this volume. For logical disks, this is the stripe size. For physical disks, this describes the physical sector size.
    OptimumIOSizeBytes: ?i64 = null,
    /// Contains references to other resources that are related to this resource.
    ///
    /// The Links property, as described by the Redfish Specification, shall contain references to resources that are related to, but not contained by (subordinate to), this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// The Actions property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// Supported IO access capabilities.
    ///
    /// Each entry shall specify a current storage access capability.
    AccessCapabilities: ?[]const ?data_storage_lo_scapabilities.StorageAccessCapability = null,
    /// Max Block size in bytes.
    ///
    /// This property shall contain size of the largest addressable unit of this storage volume.
    MaxBlockSizeBytes: ?i64 = null,
    /// Capacity utilization.
    ///
    /// Information about the utilization of capacity allocated to this storage volume.
    Capacity: ?capacity.Capacity = null,
    /// Low space warning.
    ///
    /// Each time the following value is less than one of the values in the array the LOW_SPACE_THRESHOLD_WARNING event shall be triggered: Across all CapacitySources entries, percent = (SUM(AllocatedBytes) - SUM(ConsumedBytes))/SUM(AllocatedBytes).
    LowSpaceWarningThresholdPercents: ?[]const ?i64 = null,
    /// The manufacturer or OEM of this storage volume.
    ///
    /// This property shall contain a value that represents the manufacturer or implementer of the storage volume.
    Manufacturer: ?[]const u8 = null,
    /// The model number for this storage volume.
    ///
    /// The value is assigned by the manufacturer and shall represents a specific storage volume implementation.
    Model: ?[]const u8 = null,
    /// Describes this storage volume in its role as a target replica.
    ///
    /// This property shall describe the replica relationship between this storage volume and a corresponding source volume.
    ReplicaInfo: ?storage_replica_info.ReplicaInfo = null,
    /// Statistics for this volume.
    ///
    /// The value shall represent IO statistics for this volume.
    ///
    /// Deprecated in v1_10_0.
    /// This property is deprecated in favor of the IOStatistics property in VolumeMetrics.
    IOStatistics: ?io_statistics.IoStatistics = null,
    /// The percentage of the capacity remaining in the Volume.
    ///
    /// If present, this value shall return  {[(SUM(AllocatedBytes) - SUM(ConsumedBytes)]/SUM(AllocatedBytes)}*100 represented as an integer value.
    RemainingCapacityPercent: ?i64 = null,
    /// Current number of capacity source resources that are available as replacements.
    ///
    /// The value is the number of available capacity source resources currently available in the event that an equivalent capacity source resource fails.
    RecoverableCapacitySourceCount: ?i64 = null,
    /// The RAID type of this volume.
    ///
    /// This property shall contain the RAID type of the associated Volume.
    RAIDType: ?RaidType = null,
    /// This property specifies the volume's storage allocation, or provisioning policy.
    ///
    /// This property shall specify the volume's supported storage allocation policy.
    ProvisioningPolicy: ?data_storage_lo_scapabilities.ProvisioningPolicy = null,
    /// The number of blocks (bytes) in a strip in a disk array that uses striped data mapping.
    ///
    /// The number of consecutively addressed virtual disk blocks (bytes) mapped to consecutively addressed blocks on a single member extent of a disk array. Synonym for stripe depth and chunk size.
    StripSizeBytes: ?i64 = null,
    /// Indicates the read cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the read cache policy for the Volume.
    ReadCachePolicy: ?ReadCachePolicyType = null,
    /// Indicates the Volume usage type setting for the Volume.
    ///
    /// This property shall contain the volume usage type for the Volume.
    VolumeUsage: ?VolumeUsageType = null,
    /// Indicates the write cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the write cache policy for the Volume.
    WriteCachePolicy: ?WriteCachePolicyType = null,
    /// Indicates the WriteCacheState policy setting for the Volume.
    ///
    /// This property shall contain the WriteCacheState policy setting for the Volume.
    WriteCacheState: ?WriteCacheStateType = null,
    /// Indicates the host-visible LogicalUnitNumber assigned to this Volume.
    ///
    /// This property shall contain host-visible LogicalUnitNumber assigned to this Volume. This property shall only be used when in a single connect configuration and no StorageGroup configuration is used.
    LogicalUnitNumber: ?i64 = null,
    /// Indicates the number of media elements used per span in the secondary RAID for a hierarchical RAID type.
    ///
    /// This property shall indicate the number of media elements used per span in the secondary RAID for a hierarchical RAID type.
    MediaSpanCount: ?i64 = null,
    /// A user-configurable string to name the volume.
    ///
    /// This property shall contain a user-configurable string to name the volume.
    DisplayName: ?[]const u8 = null,
    /// The policy that the RAID volume is using to address the write hole issue.
    ///
    /// This property specifies the policy that is enabled to address the write hole issue on the RAID volume. If no policy is enabled at the moment, this property shall be set to 'Off'.
    WriteHoleProtectionPolicy: ?WriteHoleProtectionPolicyType = null,
    /// Indicator of whether or not the Volume has deduplication enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing deduplication or not.
    Deduplicated: ?bool = null,
    /// Indicator of whether or not the Volume has compression enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing compression or not.
    Compressed: ?bool = null,
    /// Indicates the IO performance mode setting for the volume.
    ///
    /// This property shall indicate whether IO performance mode is enabled for the volume.
    IOPerfModeEnabled: ?bool = null,
    /// This property contains properties to use when Volume is used to describe an NVMe Namespace.
    ///
    /// This property shall contain properties to use when Volume is used to describe an NVMe Namespace.
    NVMeNamespaceProperties: ?NvmeNamespaceProperties = null,
    /// Indicates the Initialization Method used for this volume. If InitializeMethod is not specified, the InitializeMethod should be Foreground.
    ///
    /// This property shall indicate the initialization method used for this volume. If InitializeMethod is not specified, the InitializeMethod should be Foreground. This value reflects the most recently used Initialization Method, and may be changed using the Initialize Action.
    InitializeMethod: ?InitializeMethod = null,
    /// This property indicates whether or not the Volume contains a boot image and is capable of booting.
    ///
    /// This property shall indicate whether or not the Volume contains a boot image and is capable of booting.  This property may be settable by an admin or client with visibility into the contents of the volume. This property should only be set to true when VolumeUsage is either not specified, or when VolumeUsage is set to Data or SystemData.
    IsBootCapable: ?bool = null,
    /// URIs to the resources that are remote target replicas of this source.
    ///
    /// The value shall reference the URIs to the remote target replicas that are sourced by this replica. Remote indicates that the replica is managed by a separate Swordfish service instance.
    RemoteReplicaTargets: ?[]const ?[]const u8 = null,
    /// Indicates whether or not replication is enabled on the volume.
    ///
    /// The property shall indicate whether or not replication is enabled on the volume. This property shall be consistent with the state reflected at the storage pool level.
    ReplicationEnabled: ?bool = null,
    /// ALUA properties for this volume.
    ///
    /// This shall identify the ALUA properties for this volume.
    ALUA: ?Alua = null,
    /// An array of space allocations to this volume.
    ///
    /// Fully or partially consumed storage from a source resource. Each entry provides capacity allocation information from a named source resource.
    CapacitySources: ?[]const core.NavProperty(capacity.CapacitySource) = null,
    /// How many members `CapacitySources` has in total, which is not how many this response carries.
    @"CapacitySources@odata.count": ?i64 = null,
    /// The next page of `CapacitySources`. Present only when this response left members out.
    @"CapacitySources@odata.nextLink": ?core.ODataId = null,
    /// An array of references to Storage Groups that includes this volume.
    ///
    /// The value of this property shall contain references to all storage groups that include this volume.
    ///
    /// Deprecated in v1_9_0.
    /// This property is deprecated in favor of the Connections property.
    StorageGroups: ?core.NavProperty(storage_group_collection.StorageGroupCollection) = null,
    /// An array of references to StoragePools allocated from this Volume.
    ///
    /// The value of this property shall contain references to all storage pools allocated from this volume.
    AllocatedPools: ?core.NavProperty(storage_pool_collection.StoragePoolCollection) = null,
    /// The resources that are target replicas of this source.
    ///
    /// The value shall reference the target replicas that are sourced by this replica.
    ReplicaTargets: ?[]const core.NavProperty(resource.Item) = null,
    /// How many members `ReplicaTargets` has in total, which is not how many this response carries.
    @"ReplicaTargets@odata.count": ?i64 = null,
    /// The next page of `ReplicaTargets`. Present only when this response left members out.
    @"ReplicaTargets@odata.nextLink": ?core.ODataId = null,
    /// The link to the metrics for this volume.
    ///
    /// This property shall contain a link to a resource of type VolumeMetrics that specifies the metrics for this volume. IO metrics are reported in the IOStatistics property.
    Metrics: ?core.NavProperty(volume_metrics.VolumeMetrics) = null,
    /// An array of references to Connections that includes this volume.
    ///
    /// The value of this property shall contain references to all Connections that include this volume.
    Connections: ?[]const core.NavProperty(connection.Connection) = null,
    /// How many members `Connections` has in total, which is not how many this response carries.
    @"Connections@odata.count": ?i64 = null,
    /// The next page of `Connections`. Present only when this response left members out.
    @"Connections@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Volume.Volume`.
///
/// Add RAIDType property.
pub const VolumeUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The size in bytes of this Volume.
    ///
    /// This property shall contain the size in bytes of the associated volume.
    CapacityBytes: core.Nullable(i64) = .absent,
    /// Is this Volume encrypted.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing encryption or not.
    Encrypted: core.Nullable(bool) = .absent,
    /// The types of encryption used by this Volume.
    ///
    /// This property shall contain the types of encryption used by this Volume.
    EncryptionTypes: ?[]const EncryptionTypes = null,
    /// Contains references to other resources that are related to this resource.
    ///
    /// The Links property, as described by the Redfish Specification, shall contain references to resources that are related to, but not contained by (subordinate to), this resource.
    Links: ?LinksUpdate = null,
    /// Supported IO access capabilities.
    ///
    /// Each entry shall specify a current storage access capability.
    AccessCapabilities: ?[]const ?data_storage_lo_scapabilities.StorageAccessCapability = null,
    /// Capacity utilization.
    ///
    /// Information about the utilization of capacity allocated to this storage volume.
    Capacity: ?capacity.CapacityUpdate = null,
    /// Low space warning.
    ///
    /// Each time the following value is less than one of the values in the array the LOW_SPACE_THRESHOLD_WARNING event shall be triggered: Across all CapacitySources entries, percent = (SUM(AllocatedBytes) - SUM(ConsumedBytes))/SUM(AllocatedBytes).
    LowSpaceWarningThresholdPercents: ?[]const ?i64 = null,
    /// Statistics for this volume.
    ///
    /// The value shall represent IO statistics for this volume.
    ///
    /// Deprecated in v1_10_0.
    /// This property is deprecated in favor of the IOStatistics property in VolumeMetrics.
    IOStatistics: ?io_statistics.IoStatisticsUpdate = null,
    /// Current number of capacity source resources that are available as replacements.
    ///
    /// The value is the number of available capacity source resources currently available in the event that an equivalent capacity source resource fails.
    RecoverableCapacitySourceCount: core.Nullable(i64) = .absent,
    /// This property specifies the volume's storage allocation, or provisioning policy.
    ///
    /// This property shall specify the volume's supported storage allocation policy.
    ProvisioningPolicy: core.Nullable(data_storage_lo_scapabilities.ProvisioningPolicy) = .absent,
    /// The number of blocks (bytes) in a strip in a disk array that uses striped data mapping.
    ///
    /// The number of consecutively addressed virtual disk blocks (bytes) mapped to consecutively addressed blocks on a single member extent of a disk array. Synonym for stripe depth and chunk size.
    StripSizeBytes: core.Nullable(i64) = .absent,
    /// Indicates the read cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the read cache policy for the Volume.
    ReadCachePolicy: core.Nullable(ReadCachePolicyType) = .absent,
    /// Indicates the write cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the write cache policy for the Volume.
    WriteCachePolicy: core.Nullable(WriteCachePolicyType) = .absent,
    /// A user-configurable string to name the volume.
    ///
    /// This property shall contain a user-configurable string to name the volume.
    DisplayName: core.Nullable([]const u8) = .absent,
    /// The policy that the RAID volume is using to address the write hole issue.
    ///
    /// This property specifies the policy that is enabled to address the write hole issue on the RAID volume. If no policy is enabled at the moment, this property shall be set to 'Off'.
    WriteHoleProtectionPolicy: ?WriteHoleProtectionPolicyType = null,
    /// Indicator of whether or not the Volume has deduplication enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing deduplication or not.
    Deduplicated: core.Nullable(bool) = .absent,
    /// Indicator of whether or not the Volume has compression enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing compression or not.
    Compressed: core.Nullable(bool) = .absent,
    /// Indicates the IO performance mode setting for the volume.
    ///
    /// This property shall indicate whether IO performance mode is enabled for the volume.
    IOPerfModeEnabled: core.Nullable(bool) = .absent,
    /// This property contains properties to use when Volume is used to describe an NVMe Namespace.
    ///
    /// This property shall contain properties to use when Volume is used to describe an NVMe Namespace.
    NVMeNamespaceProperties: core.Nullable(NvmeNamespacePropertiesUpdate) = .absent,
    /// This property indicates whether or not the Volume contains a boot image and is capable of booting.
    ///
    /// This property shall indicate whether or not the Volume contains a boot image and is capable of booting.  This property may be settable by an admin or client with visibility into the contents of the volume. This property should only be set to true when VolumeUsage is either not specified, or when VolumeUsage is set to Data or SystemData.
    IsBootCapable: core.Nullable(bool) = .absent,
    /// Indicates whether or not replication is enabled on the volume.
    ///
    /// The property shall indicate whether or not replication is enabled on the volume. This property shall be consistent with the state reflected at the storage pool level.
    ReplicationEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `Volume.Volume`.
///
/// Add RAIDType property.
pub const VolumeCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The size in bytes of this Volume.
    ///
    /// This property shall contain the size in bytes of the associated volume.
    CapacityBytes: core.Nullable(i64) = .absent,
    /// Is this Volume encrypted.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing encryption or not.
    Encrypted: core.Nullable(bool) = .absent,
    /// The types of encryption used by this Volume.
    ///
    /// This property shall contain the types of encryption used by this Volume.
    EncryptionTypes: ?[]const EncryptionTypes = null,
    /// Contains references to other resources that are related to this resource.
    ///
    /// The Links property, as described by the Redfish Specification, shall contain references to resources that are related to, but not contained by (subordinate to), this resource.
    Links: ?LinksUpdate = null,
    /// Supported IO access capabilities.
    ///
    /// Each entry shall specify a current storage access capability.
    AccessCapabilities: ?[]const ?data_storage_lo_scapabilities.StorageAccessCapability = null,
    /// Capacity utilization.
    ///
    /// Information about the utilization of capacity allocated to this storage volume.
    Capacity: ?capacity.CapacityUpdate = null,
    /// Low space warning.
    ///
    /// Each time the following value is less than one of the values in the array the LOW_SPACE_THRESHOLD_WARNING event shall be triggered: Across all CapacitySources entries, percent = (SUM(AllocatedBytes) - SUM(ConsumedBytes))/SUM(AllocatedBytes).
    LowSpaceWarningThresholdPercents: ?[]const ?i64 = null,
    /// Statistics for this volume.
    ///
    /// The value shall represent IO statistics for this volume.
    ///
    /// Deprecated in v1_10_0.
    /// This property is deprecated in favor of the IOStatistics property in VolumeMetrics.
    IOStatistics: ?io_statistics.IoStatisticsUpdate = null,
    /// Current number of capacity source resources that are available as replacements.
    ///
    /// The value is the number of available capacity source resources currently available in the event that an equivalent capacity source resource fails.
    RecoverableCapacitySourceCount: core.Nullable(i64) = .absent,
    /// This property specifies the volume's storage allocation, or provisioning policy.
    ///
    /// This property shall specify the volume's supported storage allocation policy.
    ProvisioningPolicy: core.Nullable(data_storage_lo_scapabilities.ProvisioningPolicy) = .absent,
    /// The number of blocks (bytes) in a strip in a disk array that uses striped data mapping.
    ///
    /// The number of consecutively addressed virtual disk blocks (bytes) mapped to consecutively addressed blocks on a single member extent of a disk array. Synonym for stripe depth and chunk size.
    StripSizeBytes: core.Nullable(i64) = .absent,
    /// Indicates the read cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the read cache policy for the Volume.
    ReadCachePolicy: core.Nullable(ReadCachePolicyType) = .absent,
    /// Indicates the write cache policy setting for the Volume.
    ///
    /// This property shall contain a boolean indicator of the write cache policy for the Volume.
    WriteCachePolicy: core.Nullable(WriteCachePolicyType) = .absent,
    /// A user-configurable string to name the volume.
    ///
    /// This property shall contain a user-configurable string to name the volume.
    DisplayName: core.Nullable([]const u8) = .absent,
    /// The policy that the RAID volume is using to address the write hole issue.
    ///
    /// This property specifies the policy that is enabled to address the write hole issue on the RAID volume. If no policy is enabled at the moment, this property shall be set to 'Off'.
    WriteHoleProtectionPolicy: ?WriteHoleProtectionPolicyType = null,
    /// Indicator of whether or not the Volume has deduplication enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing deduplication or not.
    Deduplicated: core.Nullable(bool) = .absent,
    /// Indicator of whether or not the Volume has compression enabled.
    ///
    /// This property shall contain a boolean indicator if the Volume is currently utilizing compression or not.
    Compressed: core.Nullable(bool) = .absent,
    /// Indicates the IO performance mode setting for the volume.
    ///
    /// This property shall indicate whether IO performance mode is enabled for the volume.
    IOPerfModeEnabled: core.Nullable(bool) = .absent,
    /// This property contains properties to use when Volume is used to describe an NVMe Namespace.
    ///
    /// This property shall contain properties to use when Volume is used to describe an NVMe Namespace.
    NVMeNamespaceProperties: core.Nullable(NvmeNamespacePropertiesUpdate) = .absent,
    /// This property indicates whether or not the Volume contains a boot image and is capable of booting.
    ///
    /// This property shall indicate whether or not the Volume contains a boot image and is capable of booting.  This property may be settable by an admin or client with visibility into the contents of the volume. This property should only be set to true when VolumeUsage is either not specified, or when VolumeUsage is set to Data or SystemData.
    IsBootCapable: core.Nullable(bool) = .absent,
    /// Indicates whether or not replication is enabled on the volume.
    ///
    /// The property shall indicate whether or not replication is enabled on the volume. This property shall be consistent with the state reflected at the storage pool level.
    ReplicationEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
