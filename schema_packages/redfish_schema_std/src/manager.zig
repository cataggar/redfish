//! Namespace `Manager`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const account_service = @import("account_service.zig");
const certificate_collection = @import("certificate_collection.zig");
const chassis = @import("chassis.zig");
const computer_system = @import("computer_system.zig");
const ethernet_interface_collection = @import("ethernet_interface_collection.zig");
const fabric = @import("fabric.zig");
const host_interface_collection = @import("host_interface_collection.zig");
const log_service_collection = @import("log_service_collection.zig");
const manager_diagnostic_data = @import("manager_diagnostic_data.zig");
const manager_network_protocol = @import("manager_network_protocol.zig");
const port = @import("port.zig");
const port_collection = @import("port_collection.zig");
const redundancy = @import("redundancy.zig");
const resource = @import("resource.zig");
const security_policy = @import("security_policy.zig");
const serial_interface_collection = @import("serial_interface_collection.zig");
const settings = @import("settings.zig");
const software_inventory = @import("software_inventory.zig");
const @"switch" = @import("switch.zig");
const virtual_media_collection = @import("virtual_media_collection.zig");

pub const CommandConnectTypesSupported = enum {
    /// The controller supports a command shell connection through the SSH protocol.
    SSH,
    /// The controller supports a command shell connection through the Telnet protocol.
    Telnet,
    /// The controller supports a command shell connection through the IPMI Serial Over LAN (SOL) protocol.
    IPMI,
    /// The controller supports a command shell connection through an OEM-specific protocol.
    Oem,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const DateTimeSource = enum {
    /// The date and time is retrieved from the manager's real time clock (RTC).
    RTC,
    /// The date and time is set and held by firmware.
    Firmware,
    /// The date and time is retrieved from the host.
    Host,
    /// The date and time source is a Network Time Protocol (NTP) server.
    NTP,
    /// The date and time source is a Precision Time Protocol (PTP) server.
    PTP,
    /// The date and time source is a Network Time Protocol (NTP) server whose address was provided via DHCP server.
    DHCP,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const GraphicalConnectTypesSupported = enum {
    /// The controller supports a graphical console connection through a KVM-IP (redirection of Keyboard, Video, Mouse over IP) protocol.
    KVMIP,
    /// The controller supports a graphical console connection through an OEM-specific protocol.
    Oem,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ManagerType = enum {
    /// A controller that primarily monitors or manages the operation of a device or system.
    ManagementController,
    /// A controller that provides management functions for a chassis, group of devices, or group of systems with their own BMCs (baseboard management controllers).  An example of this is a manager that aggregates and orchestrates management functions across multiple BMCs in an enclosure.
    EnclosureManager,
    /// A controller that provides management functions for one or more computer systems.  Commonly known as a BMC (baseboard management controller).  Examples of this include a BMC dedicated to one system or a multi-host manager providing BMC capabilities to multiple systems.
    BMC,
    /// A controller that provides management functions for a whole or part of a rack.  An example of this is a manager that aggregates and orchestrates management functions across multiple managers, such as enclosure managers and BMCs (baseboard management controllers), in a rack.
    RackManager,
    /// A controller that provides management functions for a particular subsystem or group of devices as part of a larger system.
    AuxiliaryController,
    /// A software-based service that provides management functions.
    Service,
    /// A controller that primarily monitors or manages the operation of a group of fabric attached nodes and switches.
    FabricManager,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ResetToDefaultsType = enum {
    /// Reset all settings to factory defaults.
    ResetAll,
    /// Reset all settings except network and local usernames/passwords to factory defaults.
    PreserveNetworkAndUsers,
    /// Reset all settings except network settings to factory defaults.
    PreserveNetwork,
    /// Reset all sensor thresholds to factory defaults.
    ///
    /// This value shall reset the thresholds for all Sensor resources controlled by this manager to the factory default values.  This action shall be the equivalent of performing the `ResetThresholds` action on each relevant `Sensor` resources.
    SensorThresholds,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SecurityModeTypes = enum {
    /// This security mode indicates the implementation complies with FIPS 140-2.
    ///
    /// This value shall indicate that the implementation complies with FIPS 140-2.
    FIPS_140_2,
    /// This security mode indicates the implementation complies with FIPS 140-3.
    ///
    /// This value shall indicate that the implementation complies with FIPS 140-3.
    FIPS_140_3,
    /// This security mode indicates the implementation meets NSA Commercial National Security Algorithm Suite 1.0 requirements.  This mode requires FIPS 140-2 compliance.
    ///
    /// This value shall indicate that the implementation meets NSA Commercial National Security Algorithm Suite 1.0 requirements and FIPS 140-2 compliance.
    CNSA_1_0,
    /// This security mode indicates the implementation meets NSA Commercial National Security Algorithm Suite 2.0 requirements.  This mode requires FIPS 140-3 compliance.
    ///
    /// This value shall indicate that the implementation meets NSA Commercial National Security Algorithm Suite 2.0 requirements and FIPS 140-3 compliance.
    CNSA_2_0,
    /// This security mode indicates the implementation meets NSA Suite B cryptographic standards for Top Secret installations.  This mode requires FIPS 140-2 compliance.
    ///
    /// This value shall indicate that the implementation meets NSA Suite B cryptographic standards for Top Secret installations and FIPS 140-2 compliance.
    SuiteB,
    /// This security mode indicates the implementation is in an OEM-specific security state.
    ///
    /// This value shall indicate that the implementation is in an OEM-specific security state.
    OEM,
    /// This security mode indicates the implementation is in a vendor-specific default security state that does not match any other value.
    ///
    /// This value shall indicate that the implementation is in a vendor-specific default security state that does not match any other value.
    Default,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SerialConnectTypesSupported = enum {
    /// The controller supports a serial console connection through the SSH protocol.
    SSH,
    /// The controller supports a serial console connection through the Telnet protocol.
    Telnet,
    /// The controller supports a serial console connection through the IPMI Serial Over LAN (SOL) protocol.
    IPMI,
    /// The controller supports a serial console connection through an OEM-specific protocol.
    Oem,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Manager.ForceFailover`.
///
/// The `ForceFailover` action forces a failover of this manager to the manager used in the parameter.
pub const ManagerForceFailoverAction = struct {
    /// The manager to which to fail over.
    ///
    /// This parameter shall contain the manager to which to fail over.
    NewManager: ?core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Manager.ModifyRedundancySet`.
///
/// The `ModifyRedundancySet` operation adds members to or removes members from a redundant group of managers.
pub const ManagerModifyRedundancySetAction = struct {
    /// An array of managers to add to the redundancy set.
    ///
    /// This parameter shall contain an array of managers to add to the redundancy set.
    Add: ?[]const core.Reference = null,
    /// An array of managers to remove from the redundancy set.
    ///
    /// This parameter shall contain an array of managers to remove from the redundancy set.
    Remove: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Manager.Reset`.
///
/// The reset action resets/reboots the manager.
pub const ManagerResetAction = struct {
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and perform an implementation-specific default reset.  Services should include the `@Redfish.AllowableValues` annotation for this parameter to ensure compatibility with clients, even when `ActionInfo` has been implemented.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Manager.ResetToDefaults`.
///
/// The reset action resets the manager settings to factory defaults.  This can cause the manager to reset.
pub const ManagerResetToDefaultsAction = struct {
    /// The type of reset to defaults.
    ///
    /// This parameter shall contain the type of reset to defaults.
    ResetType: ?ResetToDefaultsType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Manager.UpdateSecurityMode`.
///
/// The update security mode action updates the security mode for the manager.  This can cause the manager to reset and reset other settings to factory defaults.  Services might require the `ResetToDefaults` action to clear the security mode.
pub const ManagerUpdateSecurityModeAction = struct {
    /// The security mode to apply to the manager.
    ///
    /// This parameter shall contain the security mode to apply to the manager.
    SecurityMode: ?SecurityModeTypes = null,
    /// The OEM-specific security mode to apply to the manager.
    ///
    /// This parameter shall contain the OEM-specific security mode to apply to the manager.  This parameter shall be required if `SecurityMode` is `OEM`.
    OEMSecurityMode: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// The `ForceFailover` action forces a failover of this manager to the manager used in the parameter.
    ///
    /// This action shall perform a forced failover of the manager's redundancy to the manager supplied as a parameter.
    @"#Manager.ForceFailover": ?core.Action(ManagerForceFailoverAction, std.json.Value) = null,
    /// The `ModifyRedundancySet` operation adds members to or removes members from a redundant group of managers.
    ///
    /// The `ModifyRedundancySet` operation shall add members to or remove members from a redundant group of managers.
    @"#Manager.ModifyRedundancySet": ?core.Action(ManagerModifyRedundancySetAction, std.json.Value) = null,
    /// The reset action resets/reboots the manager.
    ///
    /// This action shall reset the manager.  If this manager provides the Redfish service, the service shall send the action response before resetting to prevent client timeouts.
    @"#Manager.Reset": ?core.Action(ManagerResetAction, std.json.Value) = null,
    /// The reset action resets the manager settings to factory defaults.  This can cause the manager to reset.
    ///
    /// This action shall reset the manager settings.  This action may impact other resources.
    @"#Manager.ResetToDefaults": ?core.Action(ManagerResetToDefaultsAction, std.json.Value) = null,
    /// The update security mode action updates the security mode for the manager.  This can cause the manager to reset and reset other settings to factory defaults.  Services might require the `ResetToDefaults` action to clear the security mode.
    ///
    /// This action shall update the security mode for the manager.  Services may reset other settings to factory defaults.  Services may require the `ResetToDefaults` action to clear security settings.  This action may impact other resources.
    @"#Manager.UpdateSecurityMode": ?core.Action(ManagerUpdateSecurityModeAction, std.json.Value) = null,

    /// The `ForceFailover` action forces a failover of this manager to the manager used in the parameter.
    ///
    /// This action shall perform a forced failover of the manager's redundancy to the manager supplied as a parameter.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn forceFailover(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerForceFailoverAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Manager.ForceFailover" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The `ModifyRedundancySet` operation adds members to or removes members from a redundant group of managers.
    ///
    /// The `ModifyRedundancySet` operation shall add members to or remove members from a redundant group of managers.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn modifyRedundancySet(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerModifyRedundancySetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Manager.ModifyRedundancySet" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The reset action resets/reboots the manager.
    ///
    /// This action shall reset the manager.  If this manager provides the Redfish service, the service shall send the action response before resetting to prevent client timeouts.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Manager.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The reset action resets the manager settings to factory defaults.  This can cause the manager to reset.
    ///
    /// This action shall reset the manager settings.  This action may impact other resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerResetToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Manager.ResetToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The update security mode action updates the security mode for the manager.  This can cause the manager to reset and reset other settings to factory defaults.  Services might require the `ResetToDefaults` action to clear the security mode.
    ///
    /// This action shall update the security mode for the manager.  Services may reset other settings to factory defaults.  Services may require the `ResetToDefaults` action to clear security settings.  This action may impact other resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn updateSecurityMode(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ManagerUpdateSecurityModeAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Manager.UpdateSecurityMode" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The information about a command shell service that this manager provides.
///
/// This type shall describe a command shell service for a manager.
pub const CommandShell = struct {
    /// An indication of whether the service is enabled for this manager.
    ///
    /// This property shall indicate whether the protocol for the service is enabled.
    ServiceEnabled: ?bool = null,
    /// The maximum number of service sessions, regardless of protocol, that this manager can support.
    ///
    /// This property shall contain the maximum number of concurrent service sessions that this implementation supports.
    MaxConcurrentSessions: ?i64 = null,
    /// This property enumerates the command shell connection types that the implementation allows.
    ///
    /// This property shall contain an array of the enumerations.  SSH shall be included if the Secure Shell (SSH) protocol is supported.  Telnet shall be included if the Telnet protocol is supported.  IPMI shall be included if the IPMI Serial Over LAN (SOL) protocol is supported.
    ConnectTypesSupported: ?[]const CommandConnectTypesSupported = null,
};

/// The daylight saving time settings for a manager.
///
/// This type shall contain the daylight saving time settings for a manager.
pub const DaylightSavingTime = struct {
    /// The start date and time with UTC offset of daylight saving time.
    ///
    /// This property shall contain the start date and time with UTC offset of daylight saving time for this manager.  Services shall update the UTC offset based on changes made to `DateTimeLocalOffset`.  This property shall be read-only if the service contains time zone databases.
    StartDateTime: ?core.DateTimeOffset = null,
    /// The end date and time with UTC offset of daylight saving time.
    ///
    /// This property shall contain the end date and time with UTC offset of daylight saving time for this manager.  If daylight saving time is permanent, specify a sufficiently distant end date and time.  Services shall update the UTC offset based on changes made to `DateTimeLocalOffset`.  This property shall be read-only if the service contains time zone databases.
    EndDateTime: ?core.DateTimeOffset = null,
    /// The daylight saving time offset in minutes.
    ///
    /// This property shall contain the number of minutes added to the `DateTime` value when the `DateTime` value is between the values of StartDateTime and EndDateTime.  This offset shall be applied only if AutoDSTEnabled is `true`.  This property shall be read-only if the service contains time zone databases.
    OffsetMinutes: ?i64 = null,
    /// The time zone of the manager when daylight saving time is in effect.
    ///
    /// This property shall contain the time zone of the manager when daylight saving time is in effect.  When daylight saving time is in effect, the service shall update the `TimeZoneName` property in the root of the resource.  When daylight saving time is no longer in effect, the service shall restore the original value of the `TimeZoneName` property in the root of the resource.  The time zone shall be either the 'Name' or the 'Format' for the zone as defined in the IANA Time Zone Database.  The value of this property is used for display purposes, especially to enhance the display of time.  This property shall be read-only if the service contains time zone databases.
    TimeZoneName: ?[]const u8 = null,
};

/// What a client may change of `Manager.DaylightSavingTime`.
///
/// The daylight saving time settings for a manager.
pub const DaylightSavingTimeUpdate = struct {
    /// The start date and time with UTC offset of daylight saving time.
    ///
    /// This property shall contain the start date and time with UTC offset of daylight saving time for this manager.  Services shall update the UTC offset based on changes made to `DateTimeLocalOffset`.  This property shall be read-only if the service contains time zone databases.
    StartDateTime: ?core.DateTimeOffset = null,
    /// The end date and time with UTC offset of daylight saving time.
    ///
    /// This property shall contain the end date and time with UTC offset of daylight saving time for this manager.  If daylight saving time is permanent, specify a sufficiently distant end date and time.  Services shall update the UTC offset based on changes made to `DateTimeLocalOffset`.  This property shall be read-only if the service contains time zone databases.
    EndDateTime: ?core.DateTimeOffset = null,
    /// The daylight saving time offset in minutes.
    ///
    /// This property shall contain the number of minutes added to the `DateTime` value when the `DateTime` value is between the values of StartDateTime and EndDateTime.  This offset shall be applied only if AutoDSTEnabled is `true`.  This property shall be read-only if the service contains time zone databases.
    OffsetMinutes: ?i64 = null,
    /// The time zone of the manager when daylight saving time is in effect.
    ///
    /// This property shall contain the time zone of the manager when daylight saving time is in effect.  When daylight saving time is in effect, the service shall update the `TimeZoneName` property in the root of the resource.  When daylight saving time is no longer in effect, the service shall restore the original value of the `TimeZoneName` property in the root of the resource.  The time zone shall be either the 'Name' or the 'Format' for the zone as defined in the IANA Time Zone Database.  The value of this property is used for display purposes, especially to enhance the display of time.  This property shall be read-only if the service contains time zone databases.
    TimeZoneName: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The information about a graphical console service that this manager provides.
///
/// This type shall describe a graphical console service for a manager.
pub const GraphicalConsole = struct {
    /// An indication of whether the service is enabled for this manager.
    ///
    /// This property shall indicate whether the protocol for the service is enabled.
    ServiceEnabled: ?bool = null,
    /// The maximum number of service sessions, regardless of protocol, that this manager can support.
    ///
    /// This property shall contain the maximum number of concurrent service sessions that this implementation supports.
    MaxConcurrentSessions: ?i64 = null,
    /// This property enumerates the graphical console connection types that the implementation allows.
    ///
    /// This property shall contain an array of the enumerations.  RDP shall be included if the Remote Desktop (RDP) protocol is supported.  KVMIP shall be included if a vendor-defined KVM-IP protocol is supported.
    ConnectTypesSupported: ?[]const GraphicalConnectTypesSupported = null,
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the systems that this manager controls.
    ///
    /// This property shall contain an array of links to computer systems over which this manager instance has control.
    ManagerForServers: ?[]const core.NavProperty(computer_system.ComputerSystem) = null,
    /// How many members `ManagerForServers` has in total, which is not how many this response carries.
    @"ManagerForServers@odata.count": ?i64 = null,
    /// The next page of `ManagerForServers`. Present only when this response left members out.
    @"ManagerForServers@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the chassis this manager controls.
    ///
    /// This property shall contain an array of links to chassis over which this manager instance has control.
    ManagerForChassis: ?[]const core.NavProperty(chassis.Chassis) = null,
    /// How many members `ManagerForChassis` has in total, which is not how many this response carries.
    @"ManagerForChassis@odata.count": ?i64 = null,
    /// The next page of `ManagerForChassis`. Present only when this response left members out.
    @"ManagerForChassis@odata.nextLink": ?core.ODataId = null,
    /// The link to the chassis where this manager is located.
    ///
    /// This property shall contain a link to the chassis where this manager is located.
    ManagerInChassis: ?core.NavProperty(chassis.Chassis) = null,
    /// An array of links to the switches that this manager controls.
    ///
    /// This property shall contain an array of links to switches that this manager instance controls.
    ManagerForSwitches: ?[]const core.NavProperty(@"switch".Switch) = null,
    /// How many members `ManagerForSwitches` has in total, which is not how many this response carries.
    @"ManagerForSwitches@odata.count": ?i64 = null,
    /// The next page of `ManagerForSwitches`. Present only when this response left members out.
    @"ManagerForSwitches@odata.nextLink": ?core.ODataId = null,
    /// The link to the software inventory resource that represents the active firmware image for this manager.
    ///
    /// This property shall contain a link to a resource of type `SoftwareInventory` that represents the active firmware image for this manager.
    ActiveSoftwareImage: ?core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// The images that are associated with this manager.
    ///
    /// This property shall contain an array of links to resources of type `SoftwareInventory` that represent the firmware images that apply to this manager.
    SoftwareImages: ?[]const core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// How many members `SoftwareImages` has in total, which is not how many this response carries.
    @"SoftwareImages@odata.count": ?i64 = null,
    /// The next page of `SoftwareImages`. Present only when this response left members out.
    @"SoftwareImages@odata.nextLink": ?core.ODataId = null,
    /// The array of links to the managers responsible for managing this manager.
    ///
    /// This property shall contain an array of links to resources of type `Manager` that represent the managers for this manager.
    ManagedBy: ?[]const core.NavProperty(Manager) = null,
    /// How many members `ManagedBy` has in total, which is not how many this response carries.
    @"ManagedBy@odata.count": ?i64 = null,
    /// The next page of `ManagedBy`. Present only when this response left members out.
    @"ManagedBy@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the managers that are managed by this manager.
    ///
    /// This property shall contain an array of links to resources of type `Manager` that represent the managers being managed by this manager.
    ManagerForManagers: ?[]const core.NavProperty(Manager) = null,
    /// How many members `ManagerForManagers` has in total, which is not how many this response carries.
    @"ManagerForManagers@odata.count": ?i64 = null,
    /// The next page of `ManagerForManagers`. Present only when this response left members out.
    @"ManagerForManagers@odata.nextLink": ?core.ODataId = null,
    /// The network port currently used by this manager.  This allows selection of shared or dedicated ports for managers that support one or the other.  For managers that always have their dedicated port enabled, this allows the selection of which shared port to use.
    ///
    /// This property shall contain a link to a resource of type `Port` that represents the current network port used by this manager.
    SelectedNetworkPort: ?core.NavProperty(port.Port) = null,
    /// An array of links to the fabrics that this manager controls.
    ///
    /// This property shall contain an array of links to resources of type `Fabric` that represent the fabrics that this manager controls.
    ManagerForFabrics: ?[]const core.NavProperty(fabric.Fabric) = null,
    /// How many members `ManagerForFabrics` has in total, which is not how many this response carries.
    @"ManagerForFabrics@odata.count": ?i64 = null,
    /// The next page of `ManagerForFabrics`. Present only when this response left members out.
    @"ManagerForFabrics@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Manager.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The manager services, such as serial console, command shell, or graphical console service.
pub const ManagerService = struct {
    /// An indication of whether the service is enabled for this manager.
    ///
    /// This property shall indicate whether the protocol for the service is enabled.
    ServiceEnabled: ?bool = null,
    /// The maximum number of service sessions, regardless of protocol, that this manager can support.
    ///
    /// This property shall contain the maximum number of concurrent service sessions that this implementation supports.
    MaxConcurrentSessions: ?i64 = null,
};

/// What a client may change of `Manager.ManagerService`.
///
/// The manager services, such as serial console, command shell, or graphical console service.
pub const ManagerServiceUpdate = struct {
    /// An indication of whether the service is enabled for this manager.
    ///
    /// This property shall indicate whether the protocol for the service is enabled.
    ServiceEnabled: ?bool = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The information about a serial console service that this manager provides.
///
/// This type shall describe a serial console service for a manager.
pub const SerialConsole = struct {
    /// An indication of whether the service is enabled for this manager.
    ///
    /// This property shall indicate whether the protocol for the service is enabled.
    ServiceEnabled: ?bool = null,
    /// The maximum number of service sessions, regardless of protocol, that this manager can support.
    ///
    /// This property shall contain the maximum number of concurrent service sessions that this implementation supports.
    MaxConcurrentSessions: ?i64 = null,
    /// This property enumerates the serial console connection types that the implementation allows.
    ///
    /// This property shall contain an array of the enumerations.  SSH shall be included if the Secure Shell (SSH) protocol is supported.  Telnet shall be included if the Telnet protocol is supported.  IPMI shall be included if the IPMI Serial Over LAN (SOL) protocol is supported.
    ConnectTypesSupported: ?[]const SerialConnectTypesSupported = null,
};

/// In Redfish, a manager is a systems management entity that can implement or provide access to a Redfish service.  Examples of managers are BMCs (baseboard management controllers), enclosure managers, management controllers, and other subsystems that are assigned manageability functions.  An implementation can have multiple managers, which might be directly accessible through a Redfish-defined interface.
///
/// This resource shall represent a management subsystem for a Redfish implementation.
pub const Manager = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The type of manager that this resource represents.
    ///
    /// This property shall describe the function of this manager.  The `ManagementController` value shall be used if none of the other enumerations apply.
    ManagerType: ?ManagerType = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The UUID of the Redfish service that is hosted by this manager.
    ///
    /// This property shall contain the UUID of the Redfish service that is hosted by this manager.  Each manager providing an entry point to the same Redfish service shall report the same UUID value, even though the name of the property may imply otherwise.  This property shall not be present if this manager does not provide a Redfish service entry point.
    ServiceEntryPointUUID: ?resource.Uuid = null,
    /// The UUID for this manager.
    ///
    /// This property shall contain the UUID for the manager.
    UUID: ?resource.Uuid = null,
    /// The model information of this manager, as defined by the manufacturer.
    ///
    /// This property shall contain the information about how the manufacturer refers to this manager.
    Model: ?[]const u8 = null,
    /// The current date and time with UTC offset of the manager.
    ///
    /// This property shall contain the current date and time with UTC offset of the manager.
    DateTime: ?core.DateTimeOffset = null,
    /// The time offset from UTC that the `DateTime` property is in `+HH:MM` format.
    ///
    /// This property shall contain the offset from UTC time that the `DateTime` property contains.  If both `DateTime` and `DateTimeLocalOffset` are provided in modification requests, services shall apply `DateTimeLocalOffset` after `DateTime` is applied.
    DateTimeLocalOffset: ?[]const u8 = null,
    /// The firmware version of this manager.
    ///
    /// This property shall contain the firmware version as defined by the manufacturer for the associated manager.
    FirmwareVersion: ?[]const u8 = null,
    /// The serial console service that this manager provides.
    ///
    /// This property shall contain information about the serial console service of this manager.
    ///
    /// Deprecated in v1_10_0.
    /// This property has been deprecated in favor of the `SerialConsole` property in the `ComputerSystem` resource.
    SerialConsole: ?SerialConsole = null,
    /// The manager's command line user interface or command shell service.
    ///
    /// This property shall describe a command line user interface or command shell service provided by this manager.  The command shell refers to an interface used to interact with the manager itself, not a dedicated console session redirected from a host operating system.  For redirected serial or host operating system consoles, see the `SerialConsole` property in the `ComputerSystem` resource.
    CommandShell: ?CommandShell = null,
    /// The manager's graphical console service.
    ///
    /// This property shall contain the information about the graphical console (KVM-IP) service of this manager.  This property should be used to describe a service for the manager's console or operating system, not a service provided on behalf of a host operating system.  Implementations representing host OS consoles, known generally as a KVM-IP feature, should use the `GraphicalConsole` property in the `ComputerSystem` resource.
    GraphicalConsole: ?GraphicalConsole = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The current power state of the manager.
    ///
    /// This property shall contain the power state of the manager.
    PowerState: ?resource.PowerState = null,
    /// An indication of whether the manager is configured for automatic Daylight Saving Time (DST) adjustment.
    ///
    /// This property shall indicate whether the manager is configured for automatic Daylight Saving Time (DST) adjustment.
    AutoDSTEnabled: ?bool = null,
    /// The URI of the Redfish service root for the remote manager that this resource represents.
    ///
    /// This property shall contain the URI of the Redfish service root for the remote manager that this resource represents.  This property shall only be present when providing aggregation of Redfish services.
    RemoteRedfishServiceUri: ?[]const u8 = null,
    /// The manufacturer of this manager.
    ///
    /// This property shall contain the name of the organization responsible for producing the manager.  This organization may be the entity from whom the manager is purchased, but this is not necessarily true.
    Manufacturer: ?[]const u8 = null,
    /// The serial number of the manager.
    ///
    /// This property shall contain a manufacturer-allocated number that identifies the manager.
    SerialNumber: ?[]const u8 = null,
    /// The part number of the manager.
    ///
    /// This property shall contain a part number assigned by the organization that is responsible for producing or manufacturing the manager.
    PartNumber: ?[]const u8 = null,
    /// The date and time when the manager was last reset or rebooted.
    ///
    /// This property shall contain the date and time when the manager last came out of a reset or was rebooted.
    LastResetTime: ?core.DateTimeOffset = null,
    /// The time zone of the manager.
    ///
    /// This property shall contain the time zone of the manager.  The time zone shall be either the 'Name' or the 'Format' for the zone as defined in the IANA Time Zone Database.  The value of this property is used for display purposes, especially to enhance the display of time.  A Redfish service may not be able to ensure accuracy and consistency between the `DateTimeOffset` property and this property.  Therefore, to specify the correct time zone offset, see the `DateTimeOffset` property.
    TimeZoneName: ?[]const u8 = null,
    /// The location of the manager.
    ///
    /// This property shall contain the location information of the associated manager.
    Location: ?resource.Location = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: ?bool = null,
    /// The spare part number of the manager.
    ///
    /// This property shall contain the spare part number of the manager.
    SparePartNumber: ?[]const u8 = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_14_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// A product instance identifier displayed in the Redfish service root.
    ///
    /// This property shall contain a vendor-provided or user-provided value that identifies and associates a discovered Redfish service with a particular product instance.  If this manager provides the Redfish service, the `ServiceIdentification` property in the `ServiceRoot` resource shall contain the value of this property.  This property shall only be present if the manager provides the Redfish service.  The value of this property is used in conjunction with the `Product` and `Vendor` properties in `ServiceRoot` to match user credentials or other a priori product instance information necessary for initial deployment to the correct, matching Redfish service.
    ServiceIdentification: ?[]const u8 = null,
    /// The additional firmware versions of the manager.
    ///
    /// This property shall contain the additional firmware versions of the manager.
    AdditionalFirmwareVersions: ?software_inventory.AdditionalVersions = null,
    /// The hardware version of this manager.
    ///
    /// This property shall contain the hardware version of this manager as determined by the vendor or supplier.
    Version: ?[]const u8 = null,
    /// The daylight saving time settings for this manager.
    ///
    /// This property shall contain the daylight saving time settings for this manager.
    DaylightSavingTime: ?DaylightSavingTime = null,
    /// The source of the date and time of this manager, such as NTP, RTC, or firmware.
    ///
    /// This property shall contain the source of the `DateTime` property of this manager.  The service shall update this property if the source changes internally, for example if an NTP server is unavailable and the source falls back to the time stored by the RTC.
    DateTimeSource: ?DateTimeSource = null,
    /// The security compliance mode that the manager is currently configured to enforce.
    ///
    /// This property shall contain the security compliance mode that the manager is currently configured to enforce.
    SecurityMode: ?SecurityModeTypes = null,
    /// The OEM-specific security compliance mode(s) that the manager is currently configured to enforce.
    ///
    /// This property shall the OEM-specific security compliance mode(s) that the manager is currently configured to enforce.  This property shall only be present if `SecurityMode` contains `OEM`.
    OEMSecurityMode: ?[]const u8 = null,
    /// An indication of whether the manager is prepared by the system for removal.
    ///
    /// This property shall indicate whether the manager is ready for removal.  Setting the value to `true` shall cause the service to perform appropriate actions to quiesce the device.  A task may spawn while the device is quiescing.
    ReadyToRemove: ?bool = null,
    /// The usage notification message displayed in the Redfish service root.
    ///
    /// This property shall contain the usage notification message for this manager.  If this manager provides the Redfish service, the `ServiceUseNotification` property in the `ServiceRoot` resource shall contain the value of this property.  This property shall only be present if the manager provides the Redfish service.
    ServiceUseNotification: ?[]const u8 = null,
    /// The link to a collection of NICs that this manager uses for network communication.
    ///
    /// This property shall contain a link to a resource collection of type `EthernetInterfaceCollection`.
    EthernetInterfaces: ?core.NavProperty(ethernet_interface_collection.EthernetInterfaceCollection) = null,
    /// The link to a collection of serial interfaces that this manager uses for serial and console communication.
    ///
    /// This property shall contain a link to a resource collection of type `SerialInterfaceCollection`, which this manager uses.
    SerialInterfaces: ?core.NavProperty(serial_interface_collection.SerialInterfaceCollection) = null,
    /// The link to the network services and their settings that the manager controls.
    ///
    /// This property shall contain a link to a resource of type `ManagerNetworkProtocol`, which represents the network services for this manager.
    NetworkProtocol: ?core.NavProperty(manager_network_protocol.ManagerNetworkProtocol) = null,
    /// The link to a collection of logs that the manager uses.
    ///
    /// This property shall contain a link to a resource collection of type `LogServiceCollection` that this manager uses.
    LogServices: ?core.NavProperty(log_service_collection.LogServiceCollection) = null,
    /// The link to the virtual media services for this particular manager.
    ///
    /// This property shall contain a link to a resource collection of type `VirtualMediaCollection`, which this manager uses.
    ///
    /// Deprecated in v1_10_0.
    /// This property has been deprecated in favor of the `VirtualMedia` property in the `ComputerSystem` resource.
    VirtualMedia: ?core.NavProperty(virtual_media_collection.VirtualMediaCollection) = null,
    /// The redundancy information for the managers of this system.
    ///
    /// The properties in this array shall show how this manager is grouped with other managers for form redundancy sets.
    Redundancy: ?[]const core.NavProperty(redundancy.Redundancy) = null,
    /// How many members `Redundancy` has in total, which is not how many this response carries.
    @"Redundancy@odata.count": ?i64 = null,
    /// The next page of `Redundancy`. Present only when this response left members out.
    @"Redundancy@odata.nextLink": ?core.ODataId = null,
    /// The link to a collection of host interfaces that this manager uses for local host communication.  Clients can find host interface configuration options and settings in this navigation property.
    ///
    /// This property shall contain a link to a resource collection of type `HostInterfaceCollection`.
    HostInterfaces: ?core.NavProperty(host_interface_collection.HostInterfaceCollection) = null,
    /// The link to the account service resource for the remote manager that this resource represents.
    ///
    /// This property shall contain a link to the account service resource for the remote manager that this resource represents.  This property shall only be present when providing aggregation of a remote manager.
    RemoteAccountService: ?core.NavProperty(account_service.AccountService) = null,
    /// The USB ports of the manager.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection` that represent the USB ports of the manager.
    USBPorts: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The diagnostic data for this manager.
    ///
    /// This property shall contain a link to a resource of type `ManagerDiagnosticData` that represents the diagnostic data for this manager.
    ManagerDiagnosticData: ?core.NavProperty(manager_diagnostic_data.ManagerDiagnosticData) = null,
    /// The dedicated network ports of the manager.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection` that represent the dedicated network ports of the manager.
    DedicatedNetworkPorts: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The shared network ports of the manager.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection` that represent the shared network ports of the manager.  The members of this collection shall reference Port resources subordinate to NetworkAdapter resources.
    SharedNetworkPorts: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The security policy settings for this manager.
    ///
    /// This property shall contain a link to a resource of type `SecurityPolicy` that contains the security policy settings for this manager.
    SecurityPolicy: ?core.NavProperty(security_policy.SecurityPolicy) = null,
};

/// What a client may change of `Manager.Manager`.
///
/// In Redfish, a manager is a systems management entity that can implement or provide access to a Redfish service.  Examples of managers are BMCs (baseboard management controllers), enclosure managers, management controllers, and other subsystems that are assigned manageability functions.  An implementation can have multiple managers, which might be directly accessible through a Redfish-defined interface.
pub const ManagerUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The current date and time with UTC offset of the manager.
    ///
    /// This property shall contain the current date and time with UTC offset of the manager.
    DateTime: core.Nullable(core.DateTimeOffset) = .absent,
    /// The time offset from UTC that the `DateTime` property is in `+HH:MM` format.
    ///
    /// This property shall contain the offset from UTC time that the `DateTime` property contains.  If both `DateTime` and `DateTimeLocalOffset` are provided in modification requests, services shall apply `DateTimeLocalOffset` after `DateTime` is applied.
    DateTimeLocalOffset: core.Nullable([]const u8) = .absent,
    /// An indication of whether the manager is configured for automatic Daylight Saving Time (DST) adjustment.
    ///
    /// This property shall indicate whether the manager is configured for automatic Daylight Saving Time (DST) adjustment.
    AutoDSTEnabled: ?bool = null,
    /// The time zone of the manager.
    ///
    /// This property shall contain the time zone of the manager.  The time zone shall be either the 'Name' or the 'Format' for the zone as defined in the IANA Time Zone Database.  The value of this property is used for display purposes, especially to enhance the display of time.  A Redfish service may not be able to ensure accuracy and consistency between the `DateTimeOffset` property and this property.  Therefore, to specify the correct time zone offset, see the `DateTimeOffset` property.
    TimeZoneName: ?[]const u8 = null,
    /// The location of the manager.
    ///
    /// This property shall contain the location information of the associated manager.
    Location: ?resource.LocationUpdate = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: core.Nullable(bool) = .absent,
    /// A product instance identifier displayed in the Redfish service root.
    ///
    /// This property shall contain a vendor-provided or user-provided value that identifies and associates a discovered Redfish service with a particular product instance.  If this manager provides the Redfish service, the `ServiceIdentification` property in the `ServiceRoot` resource shall contain the value of this property.  This property shall only be present if the manager provides the Redfish service.  The value of this property is used in conjunction with the `Product` and `Vendor` properties in `ServiceRoot` to match user credentials or other a priori product instance information necessary for initial deployment to the correct, matching Redfish service.
    ServiceIdentification: core.Nullable([]const u8) = .absent,
    /// The additional firmware versions of the manager.
    ///
    /// This property shall contain the additional firmware versions of the manager.
    AdditionalFirmwareVersions: ?software_inventory.AdditionalVersionsUpdate = null,
    /// The daylight saving time settings for this manager.
    ///
    /// This property shall contain the daylight saving time settings for this manager.
    DaylightSavingTime: ?DaylightSavingTimeUpdate = null,
    /// The source of the date and time of this manager, such as NTP, RTC, or firmware.
    ///
    /// This property shall contain the source of the `DateTime` property of this manager.  The service shall update this property if the source changes internally, for example if an NTP server is unavailable and the source falls back to the time stored by the RTC.
    DateTimeSource: core.Nullable(DateTimeSource) = .absent,
    /// An indication of whether the manager is prepared by the system for removal.
    ///
    /// This property shall indicate whether the manager is ready for removal.  Setting the value to `true` shall cause the service to perform appropriate actions to quiesce the device.  A task may spawn while the device is quiescing.
    ReadyToRemove: core.Nullable(bool) = .absent,
    /// The usage notification message displayed in the Redfish service root.
    ///
    /// This property shall contain the usage notification message for this manager.  If this manager provides the Redfish service, the `ServiceUseNotification` property in the `ServiceRoot` resource shall contain the value of this property.  This property shall only be present if the manager provides the Redfish service.
    ServiceUseNotification: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
