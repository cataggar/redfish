//! Namespace `StorageController`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const assembly = @import("assembly.zig");
const battery = @import("battery.zig");
const certificate_collection = @import("certificate_collection.zig");
const endpoint = @import("endpoint.zig");
const environment_metrics = @import("environment_metrics.zig");
const network_device_function = @import("network_device_function.zig");
const pcie_device = @import("pcie_device.zig");
const pcie_function = @import("pcie_function.zig");
const port_collection = @import("port_collection.zig");
const protocol = @import("protocol.zig");
const resource = @import("resource.zig");
const software_inventory = @import("software_inventory.zig");
const storage = @import("storage.zig");
const storage_controller_metrics = @import("storage_controller_metrics.zig");
const volume = @import("volume.zig");

pub const AnaAccessState = enum {
    /// Commands processed by a controller provide optimized access to any namespace in the ANA group.
    Optimized,
    /// Commands processed by a controller that reports this state for an ANA group provide non-optimized access characteristics, such as lower performance or non-optimal use of subsystem resources, to any namespace in the ANA group.
    NonOptimized,
    /// Namespaces in this group are inaccessible.  Commands are not able to access user data of namespaces in the ANA group.
    Inaccessible,
    /// The group is persistently inaccessible.  Commands are persistently not able to access user data of namespaces in the ANA group.
    PersistentLoss,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NvmeControllerType = enum {
    /// The NVMe controller is an admin controller.
    Admin,
    /// The NVMe controller is a discovery controller.
    Discovery,
    /// The NVMe controller is an I/O controller.
    IO,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `StorageController.AttachNamespaces`.
///
/// This action attaches referenced namespaces or volumes to the storage controller.  Attached namespaces and volumes are added to the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
pub const StorageControllerAttachNamespacesAction = struct {
    /// The namespaces or volumes to attach to the storage controller.
    ///
    /// This parameter shall contain an array of links to resources of type `Volume` that represent the namespaces or volumes to attach to the storage controller.
    Namespaces: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `StorageController.DetachNamespaces`.
///
/// This action detaches referenced namespaces or volumes from the storage controller.  Detached namespaces and volumes are removed from the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
pub const StorageControllerDetachNamespacesAction = struct {
    /// The namespaces or volumes to detach from the storage controller.
    ///
    /// This parameter shall contain an array of links to resources of type `Volume` that represent the namespaces or volumes to detach from the storage controller.
    Namespaces: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `StorageController.Reset`.
///
/// This action resets this storage controller.
pub const StorageControllerResetAction = struct {
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without this parameter and can complete an implementation-specific default reset.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `StorageController.SecurityReceive`.
///
/// This action transfers security protocol data from the controller.  The data transferred from the controller contains the status and data result of one or more `SecuritySend` action requests that were previously submitted to the controller.
pub const StorageControllerSecurityReceiveAction = struct {
    /// The security protocol number.
    ///
    /// This parameter shall contain the numeric identifier of the security protocol, as defined by the 'SECURITY PROTOCOL field in SECURITY PROTOCOL IN command' table of the SPC-5 Specification, and possibly extended by transport-specific standards.  Services shall only accept the values `0`, `1`, or `2`.
    SecurityProtocol: ?i64 = null,
    /// The security protocol-specific parameter.
    ///
    /// This parameter shall contain the security protocol-specific data for the transfer operation.  The value is defined by the protocol specified by the SecurityProtocolSpecific parameter.
    SecurityProtocolSpecific: ?i64 = null,
    /// Allocated size for received data.
    ///
    /// This parameter shall contain the allocated size of the received data, which imposes a maximum length of the data.  The response may contain padding to meet this length.
    AllocationLength: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `StorageController.SecuritySend`.
///
/// This action transfers security protocol data to the controller.  The data transferred to the controller contains security protocol-specific commands to be performed by the controller.
pub const StorageControllerSecuritySendAction = struct {
    /// The security protocol number.
    ///
    /// This parameter shall contain the numeric identifier of the security protocol, as defined by the 'SECURITY PROTOCOL field in SECURITY PROTOCOL OUT command' table of the SPC-5 Specification, and possibly extended by transport-specific standards.  Services shall only accept the values `1` or `2`.
    SecurityProtocol: ?i64 = null,
    /// The security protocol-specific parameter.
    ///
    /// This parameter shall contain the security protocol-specific data for the transfer operation.  The value is defined by the protocol specified by the SecurityProtocolSpecific parameter.
    SecurityProtocolSpecific: ?i64 = null,
    /// The data to transfer.
    ///
    /// This parameter shall contain a Base64-encoded string, with padding characters, of the security protocol data to transfer.
    Data: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The ANA characteristics and volume information for a storage controller.
///
/// This type shall contain the ANA characteristics and volume information for a storage controller.
pub const AnaCharacteristics = struct {
    /// Reported ANA access state.
    ///
    /// This property shall contain the reported ANA access state.
    AccessState: ?AnaAccessState = null,
    /// The specified volume.
    ///
    /// This property shall contain a link to a resource of type `Volume`.
    Volume: ?core.NavProperty(volume.Volume) = null,
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action attaches referenced namespaces or volumes to the storage controller.  Attached namespaces and volumes are added to the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    ///
    /// This action shall attach referenced namespaces or volumes to the storage controller.  For NVMe, namespaces are modeled as `Volume` resources.  Services shall add the attached namespaces and volumes to the `AttachedVolumes` property in `Links`.
    @"#StorageController.AttachNamespaces": ?core.Action(StorageControllerAttachNamespacesAction, AttachDetachNamespacesResponse) = null,
    /// This action detaches referenced namespaces or volumes from the storage controller.  Detached namespaces and volumes are removed from the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    ///
    /// This action shall detach referenced namespaces or volumes from the storage controller.  Services shall remove the detached namespaces and volumes from the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    @"#StorageController.DetachNamespaces": ?core.Action(StorageControllerDetachNamespacesAction, AttachDetachNamespacesResponse) = null,
    /// This action resets this storage controller.
    ///
    /// This action shall reset this storage controller.
    @"#StorageController.Reset": ?core.Action(StorageControllerResetAction, std.json.Value) = null,
    /// This action transfers security protocol data from the controller.  The data transferred from the controller contains the status and data result of one or more `SecuritySend` action requests that were previously submitted to the controller.
    ///
    /// This action shall transfer security protocol data from the controller.  The contents of the request are specified by the 'SECURITY PROTOCOL IN command' section of the SPC-5 Specification.
    @"#StorageController.SecurityReceive": ?core.Action(StorageControllerSecurityReceiveAction, SecurityReceiveResponse) = null,
    /// This action transfers security protocol data to the controller.  The data transferred to the controller contains security protocol-specific commands to be performed by the controller.
    ///
    /// This action shall transfer security protocol data to the controller.  The contents of the request are specified by the 'SECURITY PROTOCOL OUT command' section of the SPC-5 Specification.
    @"#StorageController.SecuritySend": ?core.Action(StorageControllerSecuritySendAction, std.json.Value) = null,

    /// This action attaches referenced namespaces or volumes to the storage controller.  Attached namespaces and volumes are added to the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    ///
    /// This action shall attach referenced namespaces or volumes to the storage controller.  For NVMe, namespaces are modeled as `Volume` resources.  Services shall add the attached namespaces and volumes to the `AttachedVolumes` property in `Links`.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn attachNamespaces(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageControllerAttachNamespacesAction,
    ) !core.Owned(core.ModificationResponse(AttachDetachNamespacesResponse)) {
        const target = self.@"#StorageController.AttachNamespaces" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action detaches referenced namespaces or volumes from the storage controller.  Detached namespaces and volumes are removed from the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    ///
    /// This action shall detach referenced namespaces or volumes from the storage controller.  Services shall remove the detached namespaces and volumes from the `AttachedVolumes` property in `Links`.  For NVMe, namespaces are modeled as `Volume` resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn detachNamespaces(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageControllerDetachNamespacesAction,
    ) !core.Owned(core.ModificationResponse(AttachDetachNamespacesResponse)) {
        const target = self.@"#StorageController.DetachNamespaces" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action resets this storage controller.
    ///
    /// This action shall reset this storage controller.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageControllerResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#StorageController.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action transfers security protocol data from the controller.  The data transferred from the controller contains the status and data result of one or more `SecuritySend` action requests that were previously submitted to the controller.
    ///
    /// This action shall transfer security protocol data from the controller.  The contents of the request are specified by the 'SECURITY PROTOCOL IN command' section of the SPC-5 Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn securityReceive(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageControllerSecurityReceiveAction,
    ) !core.Owned(core.ModificationResponse(SecurityReceiveResponse)) {
        const target = self.@"#StorageController.SecurityReceive" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action transfers security protocol data to the controller.  The data transferred to the controller contains security protocol-specific commands to be performed by the controller.
    ///
    /// This action shall transfer security protocol data to the controller.  The contents of the request are specified by the 'SECURITY PROTOCOL OUT command' section of the SPC-5 Specification.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn securitySend(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: StorageControllerSecuritySendAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#StorageController.SecuritySend" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The response body for the `AttachNamespaces` and `DetachNamespaces` actions.
///
/// This type shall contain the properties found in the response body for the `AttachNamespaces` and `DetachNamespaces` actions.
pub const AttachDetachNamespacesResponse = struct {
    /// An array of links to volumes that are attached to this controller instance.
    ///
    /// This property shall contain an array of links to resources of type `Volume` that are attached to this instance of storage controller.
    AttachedVolumes: ?[]const core.NavProperty(volume.Volume) = null,
};

/// This type describes the cache memory of the storage controller in general detail.
///
/// This type shall contain properties that describe the cache memory for a storage controller.
pub const CacheSummary = struct {
    /// The total configured cache memory, measured in MiB.
    ///
    /// This property shall contain the amount of configured cache memory as measured in mebibytes.
    TotalCacheSizeMiB: ?i64 = null,
    /// The portion of the cache memory that is persistent, measured in MiB.
    ///
    /// This property shall contain the amount of cache memory that is persistent as measured in mebibytes.  This size shall be less than or equal to the `TotalCacheSizeMiB`.
    PersistentCacheSizeMiB: ?i64 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the endpoints that connect to this controller.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` with which this controller is associated.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// An array of links to the PCIe functions that the storage controller produces.
    ///
    /// This property shall contain an array of links to resources of type `PCIeFunction` that represent the PCIe functions associated with this resource.
    PCIeFunctions: ?[]const core.NavProperty(pcie_function.PcieFunction) = null,
    /// An array of links to volumes that are attached to this controller instance.
    ///
    /// This property shall contain an array of links to resources of type `Volume` that are attached to this instance of storage controller.
    AttachedVolumes: ?[]const core.NavProperty(volume.Volume) = null,
    /// The network device functions that provide connectivity to this controller.
    ///
    /// This property shall contain an array of links to resources of type `NetworkDeviceFunction` that represent the devices that provide connectivity to this controller.
    NetworkDeviceFunctions: ?[]const core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// The batteries that provide power to this storage controller during a power-loss event.
    ///
    /// This property shall contain an array of links to resources of type `Battery` that represent the batteries that provide power to this storage controller during a power-loss event, such as with battery-backed RAID controllers.  This property shall not be present if the batteries power the containing chassis as a whole rather than the individual storage controller.
    Batteries: ?[]const core.NavProperty(battery.Battery) = null,
    /// The NVMe subsystems discovered by this discovery controller.
    ///
    /// This property shall contain an array of links to resources of type `Storage` that represent the NVMe subsystems discovered by this discovery controller.  This property shall only be present if `ControllerType` in `NVMeControllerProperties` contains `Discovery`.
    NVMeDiscoveredSubsystems: ?[]const core.NavProperty(storage.Storage) = null,
};

/// What a client may change of `StorageController.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The physical function-related properties for a storage controller within a multiple physical function storage controller.
///
/// This type shall contain the physical function-related properties for a storage controller within a multiple physical function storage controller.
pub const MpfProperties = struct {
    /// Indicates whether this physical function is the supervisor, which manages the other physical functions, manages the configuration of the controller, and controls management access.
    ///
    /// This property shall indicate whether this physical function is the supervisor, which manages the other physical functions, manages the configuration of the controller, and controls management access.
    IsSupervisor: ?bool = null,
};

pub const NvmeControllerAttributes = struct {
    /// Indicates whether or not the controller supports reporting of a UUID list.
    ///
    /// This property shall indicate whether or not the controller supports reporting of a UUID list.
    ReportsUUIDList: ?bool = null,
    /// Indicates whether or not the controller supports SQ Associations.
    ///
    /// This property shall indicate whether or not the controller supports SQ Associations.
    SupportsSQAssociations: ?bool = null,
    /// Indicates whether or not the controller supports reporting of Namespace Granularity.
    ///
    /// This property shall indicate whether or not the controller supports reporting of Namespace Granularity.
    ReportsNamespaceGranularity: ?bool = null,
    /// Indicates whether or not the controller supports restarting the Keep Alive Timer if traffic is processed from an admin command or I/O during a Keep Alive Timeout interval.
    ///
    /// This property shall indicate whether or not the controller supports restarting the Keep Alive Timer if traffic is processed from an admin command or I/O during a Keep Alive Timeout interval.
    SupportsTrafficBasedKeepAlive: ?bool = null,
    /// Indicates whether or not the controller supports Predictable Latency Mode.
    ///
    /// This property shall indicate whether or not the controller supports Predictable Latency Mode.
    SupportsPredictableLatencyMode: ?bool = null,
    /// Indicates whether or not the controller supports Endurance Groups.
    ///
    /// This property shall indicate whether or not the controller supports Endurance Groups.
    SupportsEnduranceGroups: ?bool = null,
    /// Indicates whether or not the controller supports Read Recovery Levels.
    ///
    /// This property shall indicate whether or not the controller supports Read Recovery Levels.
    SupportsReadRecoveryLevels: ?bool = null,
    /// Indicates whether or not the controller supports NVM Sets.
    ///
    /// This property shall indicate whether or not the controller supports NVM Sets.
    SupportsNVMSets: ?bool = null,
    /// Indicates whether or not the controller supports exceeding Power of Non-Operational State in order to execute controller-initiated background operations in a non-operational power state.
    ///
    /// This property shall indicate whether or not the controller supports exceeding Power of Non-Operational State in order to execute controller-initiated background operations in a non-operational power state.
    SupportsExceedingPowerOfNonOperationalState: ?bool = null,
    /// Indicates whether or not the controller supports a 128-bit Host Identifier.
    ///
    /// This property shall indicate whether or not the controller supports a 128-bit Host Identifier.
    Supports128BitHostId: ?bool = null,
    /// Indicates if the controller supports reservations.
    ///
    /// This property shall indicate if the controller supports reservations.
    SupportsReservations: ?bool = null,
};

pub const NvmeControllerProperties = struct {
    /// The type of NVMe controller.
    ///
    /// This property shall contain the type of NVMe controller.
    ControllerType: ?NvmeControllerType = null,
    /// The version of the NVMe Base Specification supported.
    ///
    /// This property shall contain the version of the NVMe Base Specification supported.
    NVMeVersion: ?[]const u8 = null,
    /// The NVMe controller attributes.
    ///
    /// This property shall contain NVMe controller attributes.
    NVMeControllerAttributes: ?NvmeControllerAttributes = null,
    /// The maximum individual queue size that an NVMe I/O controller supports.
    ///
    /// This property shall contain the maximum individual queue entry size supported per queue.  This is a zero-based value, where the minimum value is one, indicating two entries.  For PCIe, this applies to both submission and completion queues.  For NVMe-oF, this applies only to submission queues.
    MaxQueueSize: ?i64 = null,
    /// The ANA characteristics and volume information.
    ///
    /// This property shall contain the ANA characteristics and volume information.
    ANACharacteristics: ?[]const ?AnaCharacteristics = null,
    /// The NVMe SMART Critical Warnings for this storage controller.  This property contains possible triggers for the predictive drive failure warning for the corresponding drive.
    ///
    /// This property shall contain the NVMe SMART Critical Warnings for this storage controller.  This property can contain possible triggers for the predictive drive failure warning for the corresponding drive.
    NVMeSMARTCriticalWarnings: ?NvmeSmartCriticalWarnings = null,
    /// The number of I/O submission queues allocated to this NVMe I/O controller.
    ///
    /// This property shall contain the number of I/O submission queues allocated to this NVMe I/O controller.
    AllocatedSubmissionQueues: ?i64 = null,
    /// The number of I/O completion queues allocated to this NVMe I/O controller.
    ///
    /// This property shall contain the number of I/O completion queues allocated to this NVMe I/O controller.
    AllocatedCompletionQueues: ?i64 = null,
    /// The maximum number of attached namespaces allowed by this NVMe I/O controller.
    ///
    /// This property shall contain the maximum number of attached namespaces allowed by this NVMe I/O controller.
    MaxAttachedNamespaces: ?i64 = null,
    /// The NVMe discovery transport service identifier for the discovery controller.
    ///
    /// This property shall contain the NVMe discovery transport service identifier for the discovery controller.  This property shall only be present if `ControllerType` contains `Discovery`.  For NVMe/TCP, the default value should be `8009`.
    DiscoveryTransportServiceId: ?i64 = null,
};

pub const NvmeSmartCriticalWarnings = struct {
    /// The Persistent Memory Region has become read-only or unreliable.
    ///
    /// This property shall indicate that the Persistent Memory Region has become read-only or unreliable, as defined by the 'Persistent Memory Region Read-Only (PMRRO)' flag in the NVMe Base Specification.  PCIe memory reads can return invalid data or generate poisoned PCIe TLP(s).  Persistent Memory Region memory writes might not update memory or might update memory with undefined data.  The Persistent Memory Region might also have become non-persistent.
    PMRUnreliable: ?bool = null,
    /// Indicates that the volatile memory backup device has failed.
    ///
    /// This property shall indicate that the volatile memory backup device has failed, as defined by the 'Volatile Memory Backup Failed (VMBF)' flag in the NVMe Base Specification.
    PowerBackupFailed: ?bool = null,
    /// Indicates the media has been placed in read-only mode.
    ///
    /// This property shall indicate the media has been placed in read-only mode, as defined by the 'All Media Read-Only (AMRO)' flag in the NVMe Base Specification.  This is not set when the read-only condition of the media is a result of a change in the write protection state of a namespace.
    MediaInReadOnly: ?bool = null,
    /// Indicates that the NVM subsystem reliability has been compromised.
    ///
    /// This property shall indicate that the NVM subsystem reliability has been compromised, as defined by the 'NVM Subsystem Degraded Reliability (NDR)' flag in the NVMe Base Specification.
    OverallSubsystemDegraded: ?bool = null,
    /// Indicates that the available spare capacity has fallen below the threshold.
    ///
    /// This property shall indicate that the available spare capacity has fallen below the threshold, as defined by the 'Available Spare Capacity Below Threshold (ASCBT)' flag in the NVMe Base Specification.
    SpareCapacityWornOut: ?bool = null,
    /// A temperature in the NVM subsystem is either above a upper temperature limit or below a lower temperature limit.
    ///
    /// This property shall indicate that a temperature in the NVM subsystem is either above a upper temperature limit or below a lower temperature limit, as defined by the 'Temperature Threshold Condition (TTC)' flag in the NVMe Base Specification.
    TemperatureThresholdCondition: ?bool = null,
    /// A request to change the settings of a personality did not complete successfully and the NVM subsystem was not able to revert to the previous personality.
    ///
    /// This property shall indicate that a request to change the settings of a personality did not complete successfully and the NVM subsystem was not able to revert to the previous personality, as defined by the 'Indeterminate Personality State (IPS)' flag in the NVMe Base Specification.
    IndeterminatePersonalityStatus: ?bool = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// This type describes the various controller rates used for processes such as volume rebuild or consistency checks.
///
/// This type shall contain all the rate settings available on the controller.
pub const Rates = struct {
    /// The percentage of controller resources used for rebuilding/repairing volumes.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for rebuilding volumes.
    RebuildRatePercent: ?i64 = null,
    /// The percentage of controller resources used for transforming volumes from one configuration to another.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for transforming volumes.
    TransformationRatePercent: ?i64 = null,
    /// The percentage of controller resources used for performing a data consistency check on volumes.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for checking data consistency on volumes.
    ConsistencyCheckRatePercent: ?i64 = null,
};

/// What a client may change of `StorageController.Rates`.
///
/// This type describes the various controller rates used for processes such as volume rebuild or consistency checks.
pub const RatesUpdate = struct {
    /// The percentage of controller resources used for rebuilding/repairing volumes.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for rebuilding volumes.
    RebuildRatePercent: core.Nullable(i64) = .absent,
    /// The percentage of controller resources used for transforming volumes from one configuration to another.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for transforming volumes.
    TransformationRatePercent: core.Nullable(i64) = .absent,
    /// The percentage of controller resources used for performing a data consistency check on volumes.
    ///
    /// This property shall contain the percentage, `0` to `100`, of controller resources used for checking data consistency on volumes.
    ConsistencyCheckRatePercent: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The security data transferred from a controller.
///
/// This object shall contain the security data transferred from a controller.
pub const SecurityReceiveResponse = struct {
    /// The Base64-encoded security protocol data.
    ///
    /// This property shall contain a Base64-encoded string, with padding characters, of the security protocol data transferred from a controller.
    Data: ?[]const u8 = null,
};

/// The `StorageController` schema describes a storage controller and its properties.  A storage controller represents a physical or virtual storage device that produces volumes.
///
/// This resource shall represent a storage controller in the Redfish Specification.
pub const StorageController = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The maximum speed of the storage controller's device interface.
    ///
    /// This property shall represent the maximum supported speed of the storage bus interface, in Gbit/s.  The specified interface connects the controller to the storage devices, not the controller to a host.  For example, SAS bus not PCIe host bus.
    SpeedGbps: ?core.Decimal = null,
    /// The firmware version of this storage controller.
    ///
    /// This property shall contain the firmware version as defined by the manufacturer for the associated storage controller.
    FirmwareVersion: ?[]const u8 = null,
    /// The manufacturer of this storage controller.
    ///
    /// This property shall contain the name of the organization responsible for producing the storage controller.  This organization may be the entity from which the storage controller is purchased, but this is not necessarily true.
    Manufacturer: ?[]const u8 = null,
    /// The model number for the storage controller.
    ///
    /// This property shall contain the name by which the manufacturer generally refers to the storage controller.
    Model: ?[]const u8 = null,
    /// The SKU for this storage controller.
    ///
    /// This property shall contain the stock-keeping unit number for this storage controller.
    SKU: ?[]const u8 = null,
    /// The serial number for this storage controller.
    ///
    /// This property shall contain a manufacturer-allocated number that identifies the storage controller.
    SerialNumber: ?[]const u8 = null,
    /// The part number for this storage controller.
    ///
    /// This property shall contain a part number assigned by the organization that is responsible for producing or manufacturing the storage controller.
    PartNumber: ?[]const u8 = null,
    /// The user-assigned asset tag for this storage controller.
    ///
    /// This property shall track the storage controller for inventory purposes.
    AssetTag: ?[]const u8 = null,
    /// The supported set of protocols for communicating with this storage controller.
    ///
    /// This property shall contain the supported set of protocols for communicating with this storage controller.
    SupportedControllerProtocols: ?[]const protocol.Protocol = null,
    /// The protocols that the storage controller can use to communicate with attached devices.
    ///
    /// This property shall contain the set of protocols this storage controller can use to communicate with attached devices.
    SupportedDeviceProtocols: ?[]const protocol.Protocol = null,
    /// The durable names for the storage controller.
    ///
    /// This property shall contain a list of all known durable names for the associated storage controller.
    Identifiers: ?[]const resource.Identifier = null,
    /// The location of the storage controller.
    ///
    /// This property shall contain the location information of the associated storage controller.
    Location: ?resource.Location = null,
    /// The cache memory of the storage controller in general detail.
    ///
    /// This property shall contain properties that describe the cache memory for this resource.
    CacheSummary: ?CacheSummary = null,
    /// The PCIe interface details for this controller.
    ///
    /// This property shall contain details on the PCIe interface that connects this PCIe-based controller to its host.
    PCIeInterface: ?pcie_device.PcieInterface = null,
    /// The set of RAID types supported by the storage controller.
    ///
    /// This property shall contain an array of all the RAID types supported by this controller.
    SupportedRAIDTypes: ?[]const ?volume.RaidType = null,
    /// This property describes the various controller rates used for processes such as volume rebuild or consistency checks.
    ///
    /// This object shall contain all the rate settings available on the controller.
    ControllerRates: ?Rates = null,
    /// The NVMe-related properties for this storage controller.
    ///
    /// This property shall contain NVMe-related properties for this storage controller.
    NVMeControllerProperties: ?NvmeControllerProperties = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_5_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// Indicates whether this is a logical storage controller.
    ///
    /// This property shall indicate whether this is a logical storage controller.
    IsLogical: ?bool = null,
    /// The physical function-related properties for this storage controller within a multiple physical function storage controller.
    ///
    /// This property shall contain the physical function-related properties for this storage controller within a multiple physical function storage controller.  This property should only be present if `IsLogical` contains `true`.
    MPFProperties: ?MpfProperties = null,
    /// The link to the assembly associated with this storage controller.
    ///
    /// This property shall contain a link to a resource of type `Assembly`.
    Assembly: ?core.NavProperty(assembly.Assembly) = null,
    /// The link to the collection of ports that exist on the storage controller.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection`.
    Ports: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to the environment metrics for this storage controller.
    ///
    /// This property shall contain a link to a resource of type `EnvironmentMetrics` that represents the environment metrics for this storage controller.
    EnvironmentMetrics: ?core.NavProperty(environment_metrics.EnvironmentMetrics) = null,
    /// The link to the metrics associated with this storage controller.
    ///
    /// This property shall contain a link to the metrics associated with this storage controller.
    Metrics: ?core.NavProperty(storage_controller_metrics.StorageControllerMetrics) = null,
};

/// What a client may change of `StorageController.StorageController`.
///
/// The `StorageController` schema describes a storage controller and its properties.  A storage controller represents a physical or virtual storage device that produces volumes.
pub const StorageControllerUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The user-assigned asset tag for this storage controller.
    ///
    /// This property shall track the storage controller for inventory purposes.
    AssetTag: core.Nullable([]const u8) = .absent,
    /// The location of the storage controller.
    ///
    /// This property shall contain the location information of the associated storage controller.
    Location: ?resource.LocationUpdate = null,
    /// The PCIe interface details for this controller.
    ///
    /// This property shall contain details on the PCIe interface that connects this PCIe-based controller to its host.
    PCIeInterface: ?pcie_device.PcieInterfaceUpdate = null,
    /// This property describes the various controller rates used for processes such as volume rebuild or consistency checks.
    ///
    /// This object shall contain all the rate settings available on the controller.
    ControllerRates: ?RatesUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
