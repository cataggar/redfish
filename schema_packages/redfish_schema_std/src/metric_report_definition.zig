//! Namespace `MetricReportDefinition`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const metric_report = @import("metric_report.zig");
const resource = @import("resource.zig");
const schedule = @import("schedule.zig");
const settings = @import("settings.zig");
const triggers = @import("triggers.zig");

/// The function to apply to the list of metric properties.
///
/// This type shall specify the function to apply to the list of metric properties.
pub const CalculationAlgorithmEnum = enum {
    /// The metric is calculated as the average metric reading over a duration.
    ///
    /// This value shall indicate the metric is calculated as the average metric reading over a duration.  The duration shall be the `CollectionDuration` property value.
    Average,
    /// The metric is calculated as the maximum metric reading over a duration.
    ///
    /// This value shall indicate the metric is calculated as the maximum metric reading over a duration.  The duration shall be the `CollectionDuration` property value.
    Maximum,
    /// The metric is calculated as the minimum metric reading over a duration.
    ///
    /// This value shall indicate the metric is calculated as the minimum metric reading over a duration.  The duration shall be the `CollectionDuration` property value.
    Minimum,
    /// The metric is calculated as the sum of the values over a duration.
    ///
    /// This value shall indicate the metric is calculated as the sum of the specified metric reading over a duration.  The duration shall be the `CollectionDuration` property value.
    Summation,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The time scope of the related metric values.
///
/// This type shall specify the time scope of the corresponding metric values.
pub const CollectionTimeScope = enum {
    /// The corresponding metric values apply to a point in time.  On the corresponding metric value instances, the `Timestamp` property value in the metric report specifies the point in time.
    ///
    /// This value shall indicate the corresponding metric values apply to a point in time.  On the corresponding metric value instances, the `Timestamp` property value in the metric report shall specify the point in time.
    Point,
    /// The corresponding metric values apply to a time interval.  On the corresponding metric value instances, the `Timestamp` property value in the metric report specifies the end of the time interval and the `CollectionDuration` property specifies its duration.
    ///
    /// This value shall indicate the corresponding metric values apply to a time interval.  On the corresponding metric value instances, the `Timestamp` property value in the metric report shall specify the end of the time interval and the `CollectionDuration` property shall specify its duration.
    Interval,
    /// The corresponding metric values apply to a time interval that began at the startup of the measured resource.  On the corresponding metric value instances, the `Timestamp` property value in the metric report specifies the end of the time interval.  The `CollectionDuration` property value specifies the duration between the startup of the resource and timestamp.
    ///
    /// This value shall indicate the corresponding metric values apply to a time interval that began at the startup of the measured resource.  On the corresponding metric value instances, the `Timestamp` property value in the metric report shall specify the end of the time interval.  The `CollectionDuration` property value shall specify the duration between the startup of the resource and timestamp.
    StartupInterval,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Indicates when the metric report is generated.
///
/// This type shall specify when the metric report is generated.
pub const MetricReportDefinitionType = enum {
    /// The metric report is generated at a periodic time interval, specified in the `Schedule` property.
    Periodic,
    /// The metric report is generated when any of the metric values change.
    OnChange,
    /// The metric report is generated when an HTTP `GET` is performed on the specified metric report.
    OnRequest,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Actions to perform when a metric report is generated.
///
/// This type shall specify the actions to perform when a metric report is generated.
pub const ReportActionsEnum = enum {
    /// Record the occurrence to the metric report collection.
    ///
    /// This value shall indicate the service records the occurrence to the metric report collection found under the telemetry service.  The service shall update the metric report based on the setting of the `ReportUpdates` property.
    LogToMetricReportsCollection,
    /// Send a Redfish event message containing the metric report.
    ///
    /// This value shall indicate the service sends a Redfish event of type `MetricReport` to subscribers in the event subscription collection of the event service.
    RedfishEvent,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Handling of subsequent metric reports when a metric report exists.
///
/// This type shall indicate how the service handles subsequent metric reports when a metric report exists.
pub const ReportUpdatesEnum = enum {
    /// Overwrite the metric report.
    ///
    /// This value shall indicate the service overwrites the metric report referenced by the `MetricReport` property.
    Overwrite,
    /// New information is appended to the metric report.  The metric report entries are overwritten with new entries when the metric report has reached its maximum capacity.
    ///
    /// This value shall indicate the service appends new information to the metric report referenced by the `MetricReport` property.  The service shall overwrite entries in the metric report with new entries when the metric report has reached its maximum capacity.
    AppendWrapsWhenFull,
    /// New information is appended to the metric report.  The service stops adding entries when the metric report has reached its maximum capacity.
    ///
    /// This value shall indicate the service appends new information to the metric report referenced by the `MetricReport` property.  The service shall stop adding entries when the metric report has reached its maximum capacity.  The `State` property within `Status` should be set to `Disabled` and the `MetricReportDefinitionEnabled` property should be set to `false` when the append limit is reached.
    AppendStopsWhenFull,
    /// A new metric report is created, whose identifier is a service-defined identifier concatenated with the timestamp.
    ///
    /// This value shall indicate the service creates a new metric report resource, whose `Id` property is a service-defined identifier concatenated with the timestamp.  The metric report referenced by the `MetricReport` property shall reference the metric report most recently created by this metric report definition.
    NewReport,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The triggers that cause this metric report definition to generate a new metric report upon a trigger occurrence when the `TriggerActions` property contains `RedfishMetricReport`.
    ///
    /// This property shall contain a set of triggers that cause this metric report to generate a new metric report upon a trigger occurrence when the `TriggerActions` property contains `RedfishMetricReport`.
    Triggers: ?[]const core.NavProperty(triggers.Triggers) = null,
    /// How many members `Triggers` has in total, which is not how many this response carries.
    @"Triggers@odata.count": ?i64 = null,
    /// The next page of `Triggers`. Present only when this response left members out.
    @"Triggers@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `MetricReportDefinition.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Metric = struct {
    /// The metric definition identifier that contains the metric properties to include in the metric report.
    ///
    /// This property shall contain the value of the `Id` property of the `MetricDefinition` resource that contains the metric properties to include in the metric report.  This property should not be present if `MetricProperties` is present.
    MetricId: ?[]const u8 = null,
    /// The list of URIs with wildcards and property identifiers to include in the metric report.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a list of URIs with wildcards and property identifiers to include in the metric report.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall describe a resource property to include in the metric report.  The property identifiers portion of the URI shall follow RFC6901-specified JSON pointer notation rules.  This property should not be present if `MetricId` is present.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// Specifies the function to perform on each of the metric properties listed in the `MetricProperties` property or the metric properties specified in the `MetricDefinition` referenced by the `MetricId` property.  If not specified, calculations are not performed on the metric properties.
    ///
    /// The property shall specify the function to perform on each of the metric properties listed in the `MetricProperties` property or the metric properties specified in the `MetricDefinition` referenced by the `MetricId` property.  If not specified, calculations shall not be performed on the metric properties.
    CollectionFunction: ?CalculationAlgorithmEnum = null,
    /// The duration over which the function is computed.
    ///
    /// This property shall specify the duration over which the function is computed.
    CollectionDuration: ?core.Duration = null,
    /// The scope of time over which the function is applied.
    ///
    /// This property shall specify the scope of time over which the function is applied.
    CollectionTimeScope: ?CollectionTimeScope = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
};

/// What a client may change of `MetricReportDefinition.Metric`.
pub const MetricUpdate = struct {
    /// The metric definition identifier that contains the metric properties to include in the metric report.
    ///
    /// This property shall contain the value of the `Id` property of the `MetricDefinition` resource that contains the metric properties to include in the metric report.  This property should not be present if `MetricProperties` is present.
    MetricId: core.Nullable([]const u8) = .absent,
    /// The list of URIs with wildcards and property identifiers to include in the metric report.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a list of URIs with wildcards and property identifiers to include in the metric report.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall describe a resource property to include in the metric report.  The property identifiers portion of the URI shall follow RFC6901-specified JSON pointer notation rules.  This property should not be present if `MetricId` is present.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// Specifies the function to perform on each of the metric properties listed in the `MetricProperties` property or the metric properties specified in the `MetricDefinition` referenced by the `MetricId` property.  If not specified, calculations are not performed on the metric properties.
    ///
    /// The property shall specify the function to perform on each of the metric properties listed in the `MetricProperties` property or the metric properties specified in the `MetricDefinition` referenced by the `MetricId` property.  If not specified, calculations shall not be performed on the metric properties.
    CollectionFunction: core.Nullable(CalculationAlgorithmEnum) = .absent,
    /// The duration over which the function is computed.
    ///
    /// This property shall specify the duration over which the function is computed.
    CollectionDuration: core.Nullable(core.Duration) = .absent,
    /// The scope of time over which the function is applied.
    ///
    /// This property shall specify the scope of time over which the function is applied.
    CollectionTimeScope: core.Nullable(CollectionTimeScope) = .absent,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const Wildcard = struct {
    /// The string used as a wildcard.
    ///
    /// This property shall contain the string used as a wildcard.
    Name: ?[]const u8 = null,
    /// An array of values to substitute for the wildcard.
    ///
    /// This property shall contain the list of values to substitute for the wildcard.
    ///
    /// Deprecated in v1_1_0.
    /// This property has been deprecated in favor of using the property Values.
    Keys: ?[]const ?[]const u8 = null,
    /// An array of values to substitute for the wildcard.  `*` indicates all possible values for the wildcard.
    ///
    /// This property shall contain the list of values to substitute for the wildcard.  The value `*` shall indicate all possible values for the wildcard.
    Values: ?[]const ?[]const u8 = null,
};

/// What a client may change of `MetricReportDefinition.Wildcard`.
pub const WildcardUpdate = struct {
    /// The string used as a wildcard.
    ///
    /// This property shall contain the string used as a wildcard.
    Name: core.Nullable([]const u8) = .absent,
    /// An array of values to substitute for the wildcard.
    ///
    /// This property shall contain the list of values to substitute for the wildcard.
    ///
    /// Deprecated in v1_1_0.
    /// This property has been deprecated in favor of using the property Values.
    Keys: ?[]const ?[]const u8 = null,
    /// An array of values to substitute for the wildcard.  `*` indicates all possible values for the wildcard.
    ///
    /// This property shall contain the list of values to substitute for the wildcard.  The value `*` shall indicate all possible values for the wildcard.
    Values: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `MetricReportDefinition` schema describes set of metrics that are collected into a metric report.
///
/// This resource shall specify a set of metrics that shall be collected into a metric report in a Redfish implementation.
pub const MetricReportDefinition = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// Specifies when the metric report is generated.
    ///
    /// This property shall specify when the metric report is generated.  If the value is `Periodic`, the `Schedule` property shall be present.
    MetricReportDefinitionType: ?MetricReportDefinitionType = null,
    /// The schedule for generating the metric report.
    ///
    /// This property shall contain the schedule of the metric report.  The metric report shall be generated at an interval specified by the `RecurrenceInterval` property within `Schedule`.  If the `MaxOccurrences` property within `Schedule` is specified, the metric report shall no longer be generated after the specified number of occurrences.  The `State` property within `Status` should be set to `Disabled` and the `MetricReportDefinitionEnabled` property should be set to `false` when the specified number of occurrences is reached.
    Schedule: ?schedule.Schedule = null,
    /// The set of actions to perform when a metric report is generated.
    ///
    /// This property shall contain the set of actions to perform when the metric report is generated.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportActions: ?[]const ReportActionsEnum = null,
    /// The behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  Namely, whether to overwrite, append, or create a metric report.
    ///
    /// This property shall contain the behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportUpdates: ?ReportUpdatesEnum = null,
    /// The maximum number of entries that can be appended to a metric report.  When the metric report reaches its limit, its behavior is dictated by the `ReportUpdates` property.
    ///
    /// This property shall contain a number that indicates the maximum number of entries that can be appended to a metric report.  When the metric report reaches its limit, its behavior shall be dictated by the `ReportUpdates` property.  This property shall be required if `ReportUpdates` contains `AppendWrapsWhenFull` or `AppendStopsWhenFull`.
    AppendLimit: ?i64 = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The set of wildcards and their substitution values for the entries in the `MetricProperties` property.
    ///
    /// The property shall contain a set of wildcards and their replacement strings, which are applied to the `MetricProperties` property.  Each wildcard expressed in the `MetricProperties` property shall have a corresponding entry in this property.
    Wildcards: ?[]const Wildcard = null,
    /// The list of URIs with wildcards and property identifiers to include in the metric report.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a list of URIs with wildcards and property identifiers to include in the metric report.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall describe a resource property to include in the metric report.  The property identifiers portion of the URI shall follow RFC6901-specified JSON pointer notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The list of metrics to include in the metric report.  The metrics might include calculations to apply to metric properties.
    ///
    /// The property shall contain a list of metrics to include in the metric report.  The metrics may include calculations to apply to metric properties.
    Metrics: ?[]const Metric = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// An indication of whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report might contain no metrics if all metrics equal the values in the previously generated metric report.
    ///
    /// This property shall indicate whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report may contain no metrics if all metrics equal the values in the previously generated metric report.
    SuppressRepeatedMetricValue: ?bool = null,
    /// The interval at which to send the complete metric report because the Redfish client wants refreshed metric data even when the data has not changed.  This property value is always greater than the recurrence interval of a metric report, and it only applies when the `SuppressRepeatedMetricValue` property is `true`.
    ///
    /// The property value shall contain a Redfish duration that describes the time interval between generations of the unsuppressed metric report.  It shall always be a value greater than the `RecurrenceInterval` property within `Schedule` and should only apply when the `SuppressRepeatedMetricValue` property is `true`.
    MetricReportHeartbeatInterval: ?core.Duration = null,
    /// An indication of whether the generation of new metric reports is enabled.
    ///
    /// This property shall indicate whether the generation of new metric reports is enabled.
    MetricReportDefinitionEnabled: ?bool = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The maximum timespan that a metric report can cover.
    ///
    /// This property shall contain the maximum timespan that a metric report can cover.
    ReportTimespan: ?core.Duration = null,
    /// The most recent metric report produced by this metric report definition.
    ///
    /// This property shall contain a link to a resource of type `MetricReport` that represents the most recent metric report produced by this metric report definition.
    MetricReport: ?core.NavProperty(metric_report.MetricReport) = null,
};

/// What a client may change of `MetricReportDefinition.MetricReportDefinition`.
///
/// The `MetricReportDefinition` schema describes set of metrics that are collected into a metric report.
pub const MetricReportDefinitionUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Specifies when the metric report is generated.
    ///
    /// This property shall specify when the metric report is generated.  If the value is `Periodic`, the `Schedule` property shall be present.
    MetricReportDefinitionType: core.Nullable(MetricReportDefinitionType) = .absent,
    /// The schedule for generating the metric report.
    ///
    /// This property shall contain the schedule of the metric report.  The metric report shall be generated at an interval specified by the `RecurrenceInterval` property within `Schedule`.  If the `MaxOccurrences` property within `Schedule` is specified, the metric report shall no longer be generated after the specified number of occurrences.  The `State` property within `Status` should be set to `Disabled` and the `MetricReportDefinitionEnabled` property should be set to `false` when the specified number of occurrences is reached.
    Schedule: ?schedule.ScheduleUpdate = null,
    /// The set of actions to perform when a metric report is generated.
    ///
    /// This property shall contain the set of actions to perform when the metric report is generated.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportActions: ?[]const ReportActionsEnum = null,
    /// The behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  Namely, whether to overwrite, append, or create a metric report.
    ///
    /// This property shall contain the behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportUpdates: ?ReportUpdatesEnum = null,
    /// The set of wildcards and their substitution values for the entries in the `MetricProperties` property.
    ///
    /// The property shall contain a set of wildcards and their replacement strings, which are applied to the `MetricProperties` property.  Each wildcard expressed in the `MetricProperties` property shall have a corresponding entry in this property.
    Wildcards: ?[]const WildcardUpdate = null,
    /// The list of URIs with wildcards and property identifiers to include in the metric report.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a list of URIs with wildcards and property identifiers to include in the metric report.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall describe a resource property to include in the metric report.  The property identifiers portion of the URI shall follow RFC6901-specified JSON pointer notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The list of metrics to include in the metric report.  The metrics might include calculations to apply to metric properties.
    ///
    /// The property shall contain a list of metrics to include in the metric report.  The metrics may include calculations to apply to metric properties.
    Metrics: ?[]const MetricUpdate = null,
    /// An indication of whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report might contain no metrics if all metrics equal the values in the previously generated metric report.
    ///
    /// This property shall indicate whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report may contain no metrics if all metrics equal the values in the previously generated metric report.
    SuppressRepeatedMetricValue: core.Nullable(bool) = .absent,
    /// The interval at which to send the complete metric report because the Redfish client wants refreshed metric data even when the data has not changed.  This property value is always greater than the recurrence interval of a metric report, and it only applies when the `SuppressRepeatedMetricValue` property is `true`.
    ///
    /// The property value shall contain a Redfish duration that describes the time interval between generations of the unsuppressed metric report.  It shall always be a value greater than the `RecurrenceInterval` property within `Schedule` and should only apply when the `SuppressRepeatedMetricValue` property is `true`.
    MetricReportHeartbeatInterval: core.Nullable(core.Duration) = .absent,
    /// An indication of whether the generation of new metric reports is enabled.
    ///
    /// This property shall indicate whether the generation of new metric reports is enabled.
    MetricReportDefinitionEnabled: core.Nullable(bool) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The maximum timespan that a metric report can cover.
    ///
    /// This property shall contain the maximum timespan that a metric report can cover.
    ReportTimespan: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `MetricReportDefinition.MetricReportDefinition`.
///
/// The `MetricReportDefinition` schema describes set of metrics that are collected into a metric report.
pub const MetricReportDefinitionCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// Specifies when the metric report is generated.
    ///
    /// This property shall specify when the metric report is generated.  If the value is `Periodic`, the `Schedule` property shall be present.
    MetricReportDefinitionType: core.Nullable(MetricReportDefinitionType) = .absent,
    /// The schedule for generating the metric report.
    ///
    /// This property shall contain the schedule of the metric report.  The metric report shall be generated at an interval specified by the `RecurrenceInterval` property within `Schedule`.  If the `MaxOccurrences` property within `Schedule` is specified, the metric report shall no longer be generated after the specified number of occurrences.  The `State` property within `Status` should be set to `Disabled` and the `MetricReportDefinitionEnabled` property should be set to `false` when the specified number of occurrences is reached.
    Schedule: ?schedule.ScheduleUpdate = null,
    /// The set of actions to perform when a metric report is generated.
    ///
    /// This property shall contain the set of actions to perform when the metric report is generated.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportActions: ?[]const ReportActionsEnum = null,
    /// The behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  Namely, whether to overwrite, append, or create a metric report.
    ///
    /// This property shall contain the behavior for how subsequent metric reports are handled in relationship to an existing metric report created from the metric report definition.  This property should be ignored if `MetricReportDefinitionType` contains the value `OnRequest`.
    ReportUpdates: ?ReportUpdatesEnum = null,
    /// The set of wildcards and their substitution values for the entries in the `MetricProperties` property.
    ///
    /// The property shall contain a set of wildcards and their replacement strings, which are applied to the `MetricProperties` property.  Each wildcard expressed in the `MetricProperties` property shall have a corresponding entry in this property.
    Wildcards: ?[]const WildcardUpdate = null,
    /// The list of URIs with wildcards and property identifiers to include in the metric report.  If a URI has wildcards, the wildcards are substituted as specified in the `Wildcards` property.
    ///
    /// This property shall contain a list of URIs with wildcards and property identifiers to include in the metric report.  A set of curly braces shall delimit each wildcard in the URI.  The corresponding entry in the `Wildcard` property shall replace each wildcard.  After each wildcard is replaced, it shall describe a resource property to include in the metric report.  The property identifiers portion of the URI shall follow RFC6901-specified JSON pointer notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The list of metrics to include in the metric report.  The metrics might include calculations to apply to metric properties.
    ///
    /// The property shall contain a list of metrics to include in the metric report.  The metrics may include calculations to apply to metric properties.
    Metrics: ?[]const MetricUpdate = null,
    /// An indication of whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report might contain no metrics if all metrics equal the values in the previously generated metric report.
    ///
    /// This property shall indicate whether any metrics are suppressed from the generated metric report.  If `true`, any metric that equals the same value in the previously generated metric report is suppressed from the current report.  Also, duplicate metrics are suppressed.  If `false`, no metrics are suppressed from the current report.  The current report may contain no metrics if all metrics equal the values in the previously generated metric report.
    SuppressRepeatedMetricValue: core.Nullable(bool) = .absent,
    /// The interval at which to send the complete metric report because the Redfish client wants refreshed metric data even when the data has not changed.  This property value is always greater than the recurrence interval of a metric report, and it only applies when the `SuppressRepeatedMetricValue` property is `true`.
    ///
    /// The property value shall contain a Redfish duration that describes the time interval between generations of the unsuppressed metric report.  It shall always be a value greater than the `RecurrenceInterval` property within `Schedule` and should only apply when the `SuppressRepeatedMetricValue` property is `true`.
    MetricReportHeartbeatInterval: core.Nullable(core.Duration) = .absent,
    /// An indication of whether the generation of new metric reports is enabled.
    ///
    /// This property shall indicate whether the generation of new metric reports is enabled.
    MetricReportDefinitionEnabled: core.Nullable(bool) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The maximum timespan that a metric report can cover.
    ///
    /// This property shall contain the maximum timespan that a metric report can cover.
    ReportTimespan: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
