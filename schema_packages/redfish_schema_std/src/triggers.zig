//! Namespace `Triggers`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const metric_report_definition = @import("metric_report_definition.zig");
const resource = @import("resource.zig");

/// The condition, in relationship to the discrete trigger values, which constitutes a trigger.
///
/// This type shall specify the condition, in relationship to the discrete trigger values, which constitutes a trigger.
pub const DiscreteTriggerConditionEnum = enum {
    /// A discrete trigger condition is met when the metric value becomes one of the values that the `DiscreteTriggers` property lists.
    Specified,
    /// A discrete trigger condition is met whenever the metric value changes.
    Changed,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The type of metric for which the trigger is configured.
///
/// This type shall specify the type of metric for which the trigger is configured.
pub const MetricTypeEnum = enum {
    /// A numeric value trigger.
    Numeric,
    /// A discrete value trigger.
    Discrete,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ThresholdActivation = enum {
    /// Value increases above the threshold.
    ///
    /// This threshold is activated when the reading changes from a value lower than the threshold to a value higher than the threshold.
    Increasing,
    /// Value decreases below the threshold.
    ///
    /// This threshold is activated when the reading changes from a value higher than the threshold to a value lower than the threshold.
    Decreasing,
    /// Value crosses the threshold in either direction.
    ///
    /// This threshold is activated when either the `Increasing` or `Decreasing` conditions are met.
    Either,
    /// The threshold is disabled.
    ///
    /// This value shall indicate the threshold is disabled and no actions shall be taken as a result of the reading crossing the threshold value.
    Disabled,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The actions to perform when a trigger condition is met.
///
/// This type shall specify the actions to perform when a trigger condition is met.
pub const TriggerActionEnum = enum {
    /// When a trigger condition is met, record in a log.
    ///
    /// This value indicates that when a trigger condition is met, the service shall log the occurrence of the condition to the log that the `LogService` property in the telemetry service resource describes.  The message for the created log entry shall follow the guidance specified by the `TriggerActionMessage` property.
    LogToLogService,
    /// When a trigger condition is met, the service sends an event to subscribers.
    ///
    /// This value indicates that when a trigger condition is met, the service shall send an event to subscribers.  The message key for the event shall follow the guidance specified by TriggerActionMessage.
    RedfishEvent,
    /// When a trigger condition is met, force an update of the specified metric reports.
    ///
    /// This value indicates that when a trigger condition is met, the service shall force the metric reports managed by the metric report definitions specified by the `MetricReportDefinitions` property to be updated, regardless of the `MetricReportDefinitionType` property value.  The actions specified in the `ReportActions` property of each `MetricReportDefinition` resource shall be performed.
    RedfishMetricReport,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The message used for events or log entries when the trigger is activated.
///
/// The value shall indicate the message used to complete the specified trigger actions.
pub const TriggerActionMessage = enum {
    /// Messages from the Telemetry Message Registry.
    ///
    /// This value shall indicate that messages generated in response to a trigger action shall utilize messages from the Telemetry Message Registry.  If this property is not supplied or supported, this value should be used as the default for this trigger.
    Telemetry,
    /// `MediaLifeLeftLow` message from the Storage Device Message Registry.
    ///
    /// This value shall indicate that messages generated in response to a trigger action shall utilize the `MediaLifeLeftLow` message from the Storage Device Message Registry.
    DriveMediaLife,
    /// `ConnectionSpeedLow` message from the Network Device Message Registry.
    ///
    /// This value shall indicate that messages generated in response to a trigger action shall utilize the `ConnectionSpeedLow` message from the Network Device Message Registry.
    ConnectionSpeed,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The characteristics of the discrete trigger.
///
/// This object shall contain the characteristics of the discrete trigger.
pub const DiscreteTrigger = struct {
    /// The name of the trigger.
    ///
    /// This property shall contain a name for the trigger.
    Name: ?[]const u8 = null,
    /// The discrete metric value that constitutes a trigger event.
    ///
    /// This property shall contain the value discrete metric that constitutes a trigger event.  The `DwellTime` shall be measured from this point in time.
    Value: ?[]const u8 = null,
    /// The amount of time that a trigger event persists before the metric action is performed.
    ///
    /// This property shall contain the amount of time that a trigger event persists before the `TriggerActions` are performed.
    DwellTime: ?core.Duration = null,
    /// The severity of the event message.
    ///
    /// This property shall contain the `Severity` property to be used in the event message.
    Severity: ?resource.Health = null,
};

/// What a client may change of `Triggers.DiscreteTrigger`.
///
/// The characteristics of the discrete trigger.
pub const DiscreteTriggerUpdate = struct {
    /// The discrete metric value that constitutes a trigger event.
    ///
    /// This property shall contain the value discrete metric that constitutes a trigger event.  The `DwellTime` shall be measured from this point in time.
    Value: core.Nullable([]const u8) = .absent,
    /// The amount of time that a trigger event persists before the metric action is performed.
    ///
    /// This property shall contain the amount of time that a trigger event persists before the `TriggerActions` are performed.
    DwellTime: core.Nullable(core.Duration) = .absent,
    /// The severity of the event message.
    ///
    /// This property shall contain the `Severity` property to be used in the event message.
    Severity: core.Nullable(resource.Health) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The metric report definitions that generate new metric reports when a trigger condition is met and when the `TriggerActions` property contains `RedfishMetricReport`.
    ///
    /// This property shall contain a set of links to metric report definitions that generate new metric reports when a trigger condition is met and when the `TriggerActions` property contains `RedfishMetricReport`.
    MetricReportDefinitions: ?[]const core.NavProperty(metric_report_definition.MetricReportDefinition) = null,
};

/// What a client may change of `Triggers.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// A threshold definition for a metric.
///
/// This type shall contain the properties for an individual threshold for this metric.
pub const Threshold = struct {
    /// The threshold value.
    ///
    /// This property shall indicate the reading for this metric that activates the threshold.  The value of the property shall use the same units as the `MetricProperties` property.
    Reading: ?core.Decimal = null,
    /// The direction of crossing that activates this threshold.
    ///
    /// This property shall indicate the direction of crossing of the reading for this metric that activates the threshold.
    Activation: ?ThresholdActivation = null,
    /// The duration the metric value must violate the threshold before the threshold is activated.
    ///
    /// This property shall indicate the duration the metric value violates the threshold before the threshold is activated.
    DwellTime: ?core.Duration = null,
};

/// What a client may change of `Triggers.Threshold`.
///
/// A threshold definition for a metric.
pub const ThresholdUpdate = struct {
    /// The threshold value.
    ///
    /// This property shall indicate the reading for this metric that activates the threshold.  The value of the property shall use the same units as the `MetricProperties` property.
    Reading: core.Nullable(core.Decimal) = .absent,
    /// The direction of crossing that activates this threshold.
    ///
    /// This property shall indicate the direction of crossing of the reading for this metric that activates the threshold.
    Activation: core.Nullable(ThresholdActivation) = .absent,
    /// The duration the metric value must violate the threshold before the threshold is activated.
    ///
    /// This property shall indicate the duration the metric value violates the threshold before the threshold is activated.
    DwellTime: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The set of thresholds for a metric.
///
/// This type shall contain a set of thresholds for a metric.
pub const Thresholds = struct {
    /// The value at which the reading is above normal range.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is above the normal range.  The value of the property shall use the same units as the `MetricProperties` property.
    UpperWarning: ?Threshold = null,
    /// The value at which the reading is above normal range and requires attention.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is above the normal range and may require attention.  The value of the property shall use the same units as the `MetricProperties` property.
    UpperCritical: ?Threshold = null,
    /// The value at which the reading is below normal range.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is below the normal range.  The value of the property shall use the same units as the `MetricProperties` property.
    LowerWarning: ?Threshold = null,
    /// The value at which the reading is below normal range and requires attention.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is below the normal range and may require attention.  The value of the property shall use the same units as the `MetricProperties` property.
    LowerCritical: ?Threshold = null,
};

/// What a client may change of `Triggers.Thresholds`.
///
/// The set of thresholds for a metric.
pub const ThresholdsUpdate = struct {
    /// The value at which the reading is above normal range.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is above the normal range.  The value of the property shall use the same units as the `MetricProperties` property.
    UpperWarning: ?ThresholdUpdate = null,
    /// The value at which the reading is above normal range and requires attention.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is above the normal range and may require attention.  The value of the property shall use the same units as the `MetricProperties` property.
    UpperCritical: ?ThresholdUpdate = null,
    /// The value at which the reading is below normal range.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is below the normal range.  The value of the property shall use the same units as the `MetricProperties` property.
    LowerWarning: ?ThresholdUpdate = null,
    /// The value at which the reading is below normal range and requires attention.
    ///
    /// This property shall contain the value at which the `MetricProperties` property is below the normal range and may require attention.  The value of the property shall use the same units as the `MetricProperties` property.
    LowerCritical: ?ThresholdUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The wildcard and its substitution values.
///
/// This property shall contain a wildcard and its substitution values.
pub const Wildcard = struct {
    /// The wildcard.
    ///
    /// This property shall contain the string used as a wildcard when contained by curly braces for a URI segment in `MetricProperties`.
    Name: ?[]const u8 = null,
    /// An array of values to substitute for the wildcard.  A single value of `*` matches all resources.
    ///
    /// This array property shall contain the list of values to substitute for the wildcard.  A single value of `*` shall indicate that the wildcard matches any available values when substituted for a URI segment.  If this property is not present, the value shall be assumed to be `*`.
    Values: ?[]const ?[]const u8 = null,
};

/// What a client may change of `Triggers.Wildcard`.
///
/// The wildcard and its substitution values.
pub const WildcardUpdate = struct {
    /// An array of values to substitute for the wildcard.  A single value of `*` matches all resources.
    ///
    /// This array property shall contain the list of values to substitute for the wildcard.  A single value of `*` shall indicate that the wildcard matches any available values when substituted for a URI segment.  If this property is not present, the value shall be assumed to be `*`.
    Values: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `Triggers` schema describes a trigger condition that applies to metrics.
///
/// This resource shall contain a trigger condition that applies to metrics.
pub const Triggers = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The metric type of the trigger.
    ///
    /// This property shall contain the metric type of the trigger.
    MetricType: ?MetricTypeEnum = null,
    /// The actions that the trigger initiates.
    ///
    /// This property shall contain the actions that the trigger initiates.
    TriggerActions: ?[]const TriggerActionEnum = null,
    /// The thresholds for a numeric metric trigger.
    ///
    /// This property shall contain the list of thresholds to which to compare a numeric metric value.
    NumericThresholds: ?Thresholds = null,
    /// The conditions for a discrete metric trigger.
    ///
    /// This property shall contain the conditions when a discrete metric triggers.
    DiscreteTriggerCondition: ?DiscreteTriggerConditionEnum = null,
    /// The list of discrete triggers.
    ///
    /// This property shall contain a list of values to which to compare a metric reading.  This property shall be present when the `DiscreteTriggerCondition` property is `Specified`.
    DiscreteTriggers: ?[]const DiscreteTrigger = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The wildcards and their substitution values for the entries in the `MetricProperties` array property.
    ///
    /// This property shall contain the wildcards and their substitution values for the entries in the `MetricProperties` array property.  Each wildcard shall have a corresponding entry in this array property.
    Wildcards: ?[]const Wildcard = null,
    /// An array of URIs with wildcards and property identifiers for this trigger.  Each wildcard, a name contained by a set of curly braces, is replaced with its corresponding entry in the `Wildcard` array property.
    ///
    /// This property shall contain an array of URIs with wildcards and property identifiers for this trigger.  Use a set of curly braces to delimit each wildcard in the URI.  Replace each wildcard with its corresponding entry in the `Wildcard` array property.  A URI that contains wildcards shall link to a resource property to which this trigger definition applies after all wildcards are replaced with their corresponding entries in the Wildcard property.  The property identifiers portion of the URI shall follow the RFC6901-defined JSON fragment notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The array of `MessageId` values that specify when a trigger condition is met based on an event.
    ///
    /// This property shall contain an array of `MessageId` values that specify when a trigger condition is met based on an event.  When the service generates an event and if it contains a `MessageId` within this array, a trigger condition shall be met.  The `MetricType` property should not be present if this resource is configured for event-based triggers.
    EventTriggers: ?[]const ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The label for the metric definitions that contain the property identifiers for this trigger.  It matches the `Id` property of the corresponding metric definition.
    ///
    /// This property shall contain the labels for the metric definitions that contain the property identifiers for this trigger.  This property shall match the value of the `Id` property of the corresponding metric definitions.
    MetricIds: ?[]const ?[]const u8 = null,
    /// The reading offset from the threshold value required to clear the threshold.
    ///
    /// This property shall indicate the offset from the reading for this sensor and the threshold value that deactivates the threshold.  For example, a value of `-2` indicates the metric reading shall fall 2 units below an upper threshold value to deactivate the threshold.  The value of the property shall use the same units as the `Reading` property.  A value of `0`, or if the property is not present in the resource, shall indicate the threshold is deactivated when the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisReading: ?core.Decimal = null,
    /// The duration the metric value must not violate the threshold before the threshold is deactivated.
    ///
    /// This property shall indicate the duration the metric value no longer violates the threshold before the threshold is deactivated.  A duration of zero seconds, or if the property is not present in the resource, shall indicate the threshold is deactivated immediately once the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisDuration: ?core.Duration = null,
    /// An indication of whether the trigger is enabled.
    ///
    /// This property shall indicate whether the trigger is enabled.  If `true`, it is enabled.  If `false`, it is disabled and none of the actions listed in `TriggerActions` will occur.
    TriggerEnabled: ?bool = null,
    /// The message issued as part of the trigger actions.
    ///
    /// This property shall contain the message definition used to generate a Redfish event or a log entry as requested by the values of `TriggerActions`.
    TriggerActionMessage: ?TriggerActionMessage = null,
};

/// What a client may change of `Triggers.Triggers`.
///
/// The `Triggers` schema describes a trigger condition that applies to metrics.
pub const TriggersUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The thresholds for a numeric metric trigger.
    ///
    /// This property shall contain the list of thresholds to which to compare a numeric metric value.
    NumericThresholds: ?ThresholdsUpdate = null,
    /// The list of discrete triggers.
    ///
    /// This property shall contain a list of values to which to compare a metric reading.  This property shall be present when the `DiscreteTriggerCondition` property is `Specified`.
    DiscreteTriggers: ?[]const DiscreteTriggerUpdate = null,
    /// The wildcards and their substitution values for the entries in the `MetricProperties` array property.
    ///
    /// This property shall contain the wildcards and their substitution values for the entries in the `MetricProperties` array property.  Each wildcard shall have a corresponding entry in this array property.
    Wildcards: ?[]const WildcardUpdate = null,
    /// An array of URIs with wildcards and property identifiers for this trigger.  Each wildcard, a name contained by a set of curly braces, is replaced with its corresponding entry in the `Wildcard` array property.
    ///
    /// This property shall contain an array of URIs with wildcards and property identifiers for this trigger.  Use a set of curly braces to delimit each wildcard in the URI.  Replace each wildcard with its corresponding entry in the `Wildcard` array property.  A URI that contains wildcards shall link to a resource property to which this trigger definition applies after all wildcards are replaced with their corresponding entries in the Wildcard property.  The property identifiers portion of the URI shall follow the RFC6901-defined JSON fragment notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The array of `MessageId` values that specify when a trigger condition is met based on an event.
    ///
    /// This property shall contain an array of `MessageId` values that specify when a trigger condition is met based on an event.  When the service generates an event and if it contains a `MessageId` within this array, a trigger condition shall be met.  The `MetricType` property should not be present if this resource is configured for event-based triggers.
    EventTriggers: ?[]const ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The label for the metric definitions that contain the property identifiers for this trigger.  It matches the `Id` property of the corresponding metric definition.
    ///
    /// This property shall contain the labels for the metric definitions that contain the property identifiers for this trigger.  This property shall match the value of the `Id` property of the corresponding metric definitions.
    MetricIds: ?[]const ?[]const u8 = null,
    /// The reading offset from the threshold value required to clear the threshold.
    ///
    /// This property shall indicate the offset from the reading for this sensor and the threshold value that deactivates the threshold.  For example, a value of `-2` indicates the metric reading shall fall 2 units below an upper threshold value to deactivate the threshold.  The value of the property shall use the same units as the `Reading` property.  A value of `0`, or if the property is not present in the resource, shall indicate the threshold is deactivated when the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisReading: core.Nullable(core.Decimal) = .absent,
    /// The duration the metric value must not violate the threshold before the threshold is deactivated.
    ///
    /// This property shall indicate the duration the metric value no longer violates the threshold before the threshold is deactivated.  A duration of zero seconds, or if the property is not present in the resource, shall indicate the threshold is deactivated immediately once the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisDuration: core.Nullable(core.Duration) = .absent,
    /// An indication of whether the trigger is enabled.
    ///
    /// This property shall indicate whether the trigger is enabled.  If `true`, it is enabled.  If `false`, it is disabled and none of the actions listed in `TriggerActions` will occur.
    TriggerEnabled: core.Nullable(bool) = .absent,
    /// The message issued as part of the trigger actions.
    ///
    /// This property shall contain the message definition used to generate a Redfish event or a log entry as requested by the values of `TriggerActions`.
    TriggerActionMessage: core.Nullable(TriggerActionMessage) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `Triggers.Triggers`.
///
/// The `Triggers` schema describes a trigger condition that applies to metrics.
pub const TriggersCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The thresholds for a numeric metric trigger.
    ///
    /// This property shall contain the list of thresholds to which to compare a numeric metric value.
    NumericThresholds: ?ThresholdsUpdate = null,
    /// The list of discrete triggers.
    ///
    /// This property shall contain a list of values to which to compare a metric reading.  This property shall be present when the `DiscreteTriggerCondition` property is `Specified`.
    DiscreteTriggers: ?[]const DiscreteTriggerUpdate = null,
    /// The wildcards and their substitution values for the entries in the `MetricProperties` array property.
    ///
    /// This property shall contain the wildcards and their substitution values for the entries in the `MetricProperties` array property.  Each wildcard shall have a corresponding entry in this array property.
    Wildcards: ?[]const WildcardUpdate = null,
    /// An array of URIs with wildcards and property identifiers for this trigger.  Each wildcard, a name contained by a set of curly braces, is replaced with its corresponding entry in the `Wildcard` array property.
    ///
    /// This property shall contain an array of URIs with wildcards and property identifiers for this trigger.  Use a set of curly braces to delimit each wildcard in the URI.  Replace each wildcard with its corresponding entry in the `Wildcard` array property.  A URI that contains wildcards shall link to a resource property to which this trigger definition applies after all wildcards are replaced with their corresponding entries in the Wildcard property.  The property identifiers portion of the URI shall follow the RFC6901-defined JSON fragment notation rules.
    MetricProperties: ?[]const ?[]const u8 = null,
    /// The array of `MessageId` values that specify when a trigger condition is met based on an event.
    ///
    /// This property shall contain an array of `MessageId` values that specify when a trigger condition is met based on an event.  When the service generates an event and if it contains a `MessageId` within this array, a trigger condition shall be met.  The `MetricType` property should not be present if this resource is configured for event-based triggers.
    EventTriggers: ?[]const ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The label for the metric definitions that contain the property identifiers for this trigger.  It matches the `Id` property of the corresponding metric definition.
    ///
    /// This property shall contain the labels for the metric definitions that contain the property identifiers for this trigger.  This property shall match the value of the `Id` property of the corresponding metric definitions.
    MetricIds: ?[]const ?[]const u8 = null,
    /// The reading offset from the threshold value required to clear the threshold.
    ///
    /// This property shall indicate the offset from the reading for this sensor and the threshold value that deactivates the threshold.  For example, a value of `-2` indicates the metric reading shall fall 2 units below an upper threshold value to deactivate the threshold.  The value of the property shall use the same units as the `Reading` property.  A value of `0`, or if the property is not present in the resource, shall indicate the threshold is deactivated when the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisReading: core.Nullable(core.Decimal) = .absent,
    /// The duration the metric value must not violate the threshold before the threshold is deactivated.
    ///
    /// This property shall indicate the duration the metric value no longer violates the threshold before the threshold is deactivated.  A duration of zero seconds, or if the property is not present in the resource, shall indicate the threshold is deactivated immediately once the metric value no longer violates the threshold.  The threshold shall not deactivate until the conditions of both `HysteresisReading` and `HysteresisDuration` are met.
    HysteresisDuration: core.Nullable(core.Duration) = .absent,
    /// An indication of whether the trigger is enabled.
    ///
    /// This property shall indicate whether the trigger is enabled.  If `true`, it is enabled.  If `false`, it is disabled and none of the actions listed in `TriggerActions` will occur.
    TriggerEnabled: core.Nullable(bool) = .absent,
    /// The message issued as part of the trigger actions.
    ///
    /// This property shall contain the message definition used to generate a Redfish event or a log entry as requested by the values of `TriggerActions`.
    TriggerActionMessage: core.Nullable(TriggerActionMessage) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
