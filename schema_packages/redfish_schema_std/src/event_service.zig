//! Namespace `EventService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const event = @import("event.zig");
const event_destination = @import("event_destination.zig");
const event_destination_collection = @import("event_destination_collection.zig");
const resource = @import("resource.zig");

pub const SmtpAuthenticationMethods = enum {
    /// No authentication.
    ///
    /// This value shall indicate authentication is not required.
    None,
    /// Auto-detect.
    ///
    /// This value shall indicate authentication is auto-detected.
    AutoDetect,
    /// PLAIN authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC4954-defined AUTH PLAIN mechanism.
    Plain,
    /// LOGIN authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC4954-defined AUTH LOGIN mechanism.
    ///
    /// Deprecated in v1_7_0.
    /// This value has been deprecated in favor of `Plain`, which supersedes the LOGIN authentication method for SASL.
    Login,
    /// CRAM-MD5 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC4954-defined AUTH CRAM-MD5 mechanism.
    CRAM_MD5,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SmtpConnectionProtocol = enum {
    /// Clear text.
    ///
    /// This value shall indicate the connection is in clear text.
    None,
    /// Auto-detect.
    ///
    /// This value shall indicate the connection is auto-detected.
    AutoDetect,
    /// StartTLS.
    ///
    /// This value shall indicate the connection conforms to the RFC3207-defined StartTLS extension.
    StartTLS,
    /// TLS/SSL.
    ///
    /// This value shall indicate the connection is TLS/SSL.
    TLS_SSL,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `EventService.SubmitTestEvent`.
///
/// This action generates a test event.
pub const EventServiceSubmitTestEventAction = struct {
    /// The type for the event to add.
    ///
    /// This parameter shall contain the property name for which the following allowable values apply.  If not provided by the client, the resulting event should not contain the `EventType` property.
    ///
    /// Deprecated in v1_3_0.
    /// This parameter has been deprecated.  Starting with Redfish Specification v1.6 (Event v1.3), subscriptions are based on the `RegistryPrefix` and `ResourceType` properties and not on the `EventType` property.
    EventType: ?event.EventType = null,
    /// The ID for the event to add.
    ///
    /// This parameter shall have the same semantics as the `EventId` property in the `Event` schema for Redfish.  A service can ignore this value and replace it with its own.  If not provided by the client, the resulting event may contain a service-defined `EventId` property.
    EventId: ?[]const u8 = null,
    /// The date and time for the event to add.
    ///
    /// This parameter shall contain the date and time for the event to add and have the same semantics as the `EventTimestamp` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `EventTimestamp` property.
    EventTimestamp: ?core.DateTimeOffset = null,
    /// The severity for the event to add.
    ///
    /// This parameter shall contain the severity for the event to add and have the same semantics as the `Severity` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `Severity` property.
    Severity: ?[]const u8 = null,
    /// The severity for the event to add.
    ///
    /// This property shall contain the severity for the event to add and have the same semantics as the `MessageSeverity` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `MessageSeverity` property.
    MessageSeverity: ?resource.Health = null,
    /// The human-readable message for the event to add.
    ///
    /// This parameter shall have the same semantics as the `Message` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `Message` property.
    Message: ?[]const u8 = null,
    /// The `MessageId` for the event to add.
    ///
    /// This parameter shall contain the `MessageId` for the event to add and have the same semantics as the `MessageId` property in the `Event` schema for Redfish.  Services should accept arbitrary values for this parameter that match that match the defined pattern.
    MessageId: ?[]const u8 = null,
    /// An array of message arguments for the event to add.
    ///
    /// This parameter shall have the same semantics as the `MessageArgs` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `MessageArgs` property.
    MessageArgs: ?[]const []const u8 = null,
    /// The URL in the `OriginOfCondition` property of the event to add.  It is not a reference object.
    ///
    /// This parameter shall be a string that represents the URL contained by the `OriginOfCondition` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `OriginOfCondition` property.
    OriginOfCondition: ?[]const u8 = null,
    /// The group identifier for the event.
    ///
    /// The parameter shall contain the group identifier for the event.  It has the same semantics as the `EventGroupId` property in the `Event` schema for Redfish.  If not provided by the client, the resulting event should not contain the `EventGroupId` property.
    EventGroupId: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `EventService.TestEventSubscription`.
///
/// This action generates a test event using the pre-defined test message.
pub const EventServiceTestEventSubscriptionAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action generates a test event.
    ///
    /// This action shall add a test event to the event service with the event data specified in the action parameters.  Then, this message should be sent to any appropriate event destinations.
    @"#EventService.SubmitTestEvent": ?core.Action(EventServiceSubmitTestEventAction, std.json.Value) = null,
    /// This action generates a test event using the pre-defined test message.
    ///
    /// This action shall send an event containing the `TestMessage` message from the Resource Event Message Registry to all appropriate event destinations.
    @"#EventService.TestEventSubscription": ?core.Action(EventServiceTestEventSubscriptionAction, std.json.Value) = null,

    /// This action generates a test event.
    ///
    /// This action shall add a test event to the event service with the event data specified in the action parameters.  Then, this message should be sent to any appropriate event destinations.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn submitTestEvent(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: EventServiceSubmitTestEventAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#EventService.SubmitTestEvent" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action generates a test event using the pre-defined test message.
    ///
    /// This action shall send an event containing the `TestMessage` message from the Resource Event Message Registry to all appropriate event destinations.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn testEventSubscription(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: EventServiceTestEventSubscriptionAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#EventService.TestEventSubscription" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const Smtp = struct {
    /// An indication if SMTP for event delivery is enabled.
    ///
    /// This property shall indicate if SMTP for event delivery is enabled.
    ServiceEnabled: ?bool = null,
    /// The destination SMTP port.
    ///
    /// This property shall contain the destination port for the SMTP server.
    Port: ?i64 = null,
    /// The address of the SMTP server.
    ///
    /// This property shall contain the address of the SMTP server for outgoing email.
    ServerAddress: ?[]const u8 = null,
    /// The 'from' email address of the outgoing email.
    ///
    /// This property shall contain the email address to use for the 'from' field in an outgoing email.
    FromAddress: ?[]const u8 = null,
    /// The connection type to the outgoing SMTP server.
    ///
    /// This property shall contain the connection type to the outgoing SMTP server.
    ConnectionProtocol: ?SmtpConnectionProtocol = null,
    /// The authentication method for the SMTP server.
    ///
    /// This property shall contain the authentication method for the SMTP server.
    Authentication: ?SmtpAuthenticationMethods = null,
    /// The username for authentication with the SMTP server.
    ///
    /// This property shall contain the username for authentication with the SMTP server.
    Username: ?[]const u8 = null,
    /// Indicates if the `Password` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `Password` property.  Otherwise, the property shall contain `false`.
    PasswordSet: ?bool = null,
};

/// What a client may change of `EventService.SMTP`.
pub const SmtpUpdate = struct {
    /// An indication if SMTP for event delivery is enabled.
    ///
    /// This property shall indicate if SMTP for event delivery is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,
    /// The destination SMTP port.
    ///
    /// This property shall contain the destination port for the SMTP server.
    Port: core.Nullable(i64) = .absent,
    /// The address of the SMTP server.
    ///
    /// This property shall contain the address of the SMTP server for outgoing email.
    ServerAddress: core.Nullable([]const u8) = .absent,
    /// The 'from' email address of the outgoing email.
    ///
    /// This property shall contain the email address to use for the 'from' field in an outgoing email.
    FromAddress: core.Nullable([]const u8) = .absent,
    /// The connection type to the outgoing SMTP server.
    ///
    /// This property shall contain the connection type to the outgoing SMTP server.
    ConnectionProtocol: core.Nullable(SmtpConnectionProtocol) = .absent,
    /// The authentication method for the SMTP server.
    ///
    /// This property shall contain the authentication method for the SMTP server.
    Authentication: core.Nullable(SmtpAuthenticationMethods) = .absent,
    /// The username for authentication with the SMTP server.
    ///
    /// This property shall contain the username for authentication with the SMTP server.
    Username: core.Nullable([]const u8) = .absent,
    /// The password for authentication with the SMTP server.  The value is `null` in responses.
    ///
    /// This property shall contain the password for authentication with the SMTP server.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const SseFilterPropertiesSupported = struct {
    /// An indication of whether the service supports filtering by the `EventTypes` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `EventTypes` property.
    ///
    /// Deprecated in v1_3_0.
    /// This property has been deprecated.  Starting with Redfish Specification v1.6 (Event v1.3), subscriptions are based on the `RegistryPrefix` and `ResourceType` properties and not on the `EventType` property.
    EventType: ?bool = null,
    /// An indication of whether the service supports filtering by the `MetricReportDefinitions` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `MetricReportDefinitions` property.
    MetricReportDefinition: ?bool = null,
    /// An indication of whether the service supports filtering by the `RegistryPrefixes` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `RegistryPrefixes` property.
    RegistryPrefix: ?bool = null,
    /// An indication of whether the service supports filtering by the `ResourceTypes` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `ResourceTypes` property.
    ResourceType: ?bool = null,
    /// An indication of whether the service supports filtering by the `EventFormatType` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `EventFormatType` property.
    EventFormatType: ?bool = null,
    /// An indication of whether the service supports filtering by the `MessageIds` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `MessageIds` property.
    MessageId: ?bool = null,
    /// An indication of whether the service supports filtering by the `OriginResources` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `OriginResources` property.
    OriginResource: ?bool = null,
    /// An indication of whether the service supports filtering by the `SubordinateResources` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `SubordinateResources` property.
    SubordinateResources: ?bool = null,
};

/// The `EventService` schema contains properties for managing event subscriptions and generates the events sent to subscribers.  The resource has links to the actual collection of subscriptions, which are called event destinations.
///
/// This resource shall represent an event service for a Redfish implementation.
pub const EventService = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// An indication of whether this service is enabled.  If `false`, events are no longer published, new SSE connections cannot be established, and existing SSE connections are terminated.
    ///
    /// This property shall indicate whether this service is enabled.  If `false`, events are no longer published, new SSE connections cannot be established, and existing SSE connections are terminated.
    ServiceEnabled: ?bool = null,
    /// The number of times that the `POST` of an event is retried before the subscription terminates or is suspended.  This retry occurs at the service level, which means that the HTTP `POST` to the event destination fails with an HTTP `4XX` or `5XX` status code or an HTTP timeout occurs this many times before the event destination subscription terminates or is suspended.
    ///
    /// This property shall contain the number of times that the `POST` of an event is retried before the subscription terminates or is suspended.  This retry occurs at the service level, which means that the HTTP `POST` to the event destination fails with an HTTP `4XX` or `5XX` status code or an HTTP timeout occurs this many times before the event destination subscription terminates or is suspended.  The service shall delete the `EventDestination` resource to terminate the subscription.  The service shall set the value of the `State` property within `Status` of the `EventDestination` resource to `Disabled` for a suspended subscription.
    DeliveryRetryAttempts: ?i64 = null,
    /// The interval, in seconds, between retry attempts for sending any event.
    ///
    /// This property shall contain the interval, in seconds, between the retry attempts for any event sent to the subscription destination.
    DeliveryRetryIntervalSeconds: ?i64 = null,
    /// The types of events to which a client can subscribe.
    ///
    /// This property shall contain the types of events to which a client can subscribe.  The semantics associated with the enumeration values are defined in the Redfish Specification.
    ///
    /// Deprecated in v1_3_0.
    /// This property has been deprecated.  Starting with Redfish Specification v1.6 (Event v1.3), subscriptions are based on the `RegistryPrefix` and `ResourceType` properties and not on the `EventType` property.
    EventTypesForSubscription: ?[]const event.EventType = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The link to a URI for receiving Server-Sent Event representations for the events that this service generates.
    ///
    /// This property shall contain a URI that specifies an HTML5 Server-Sent Event-conformant endpoint.
    ServerSentEventUri: ?[]const u8 = null,
    /// The list of the prefixes of the message registries that can be used for the `RegistryPrefixes` or `ExcludeRegistryPrefixes` properties on a subscription.  If this property is absent or contains an empty array, the service does not support registry prefix-based subscriptions.
    ///
    /// This property shall contain the array of the prefixes of the message registries that shall be allowed or excluded for an event subscription.
    RegistryPrefixes: ?[]const ?[]const u8 = null,
    /// The list of `@odata.type` values, or schema names, that can be specified in the `ResourceTypes` array in a subscription.  If this property is absent or contains an empty array, the service does not support resource type-based subscriptions.
    ///
    /// This property shall specify an array of the valid `@odata.type` values that can be used for an event subscription.
    ResourceTypes: ?[]const ?[]const u8 = null,
    /// An indication of whether the service supports the `SubordinateResources` property on both event subscriptions and generated events.
    ///
    /// This property shall indicate whether the service supports the `SubordinateResources` property on both event subscriptions and generated events.
    SubordinateResourcesSupported: ?bool = null,
    /// The content types of the message that this service can send to the event destination.
    ///
    /// This property shall contain the content types of the message that this service can send to the event destination.  If this property is not present, the `EventFormatType` shall be assumed to be `Event`.
    EventFormatTypes: ?[]const ?event_destination.EventFormatType = null,
    /// The set of properties that are supported in the `$filter` query parameter for the `ServerSentEventUri`.
    ///
    /// This property shall contain the properties that are supported in the `$filter` query parameter for the URI indicated by the `ServerSentEventUri` property, as described by the Redfish Specification.
    SSEFilterPropertiesSupported: ?SseFilterPropertiesSupported = null,
    /// Settings for SMTP event delivery.
    ///
    /// This property shall contain settings for SMTP event delivery.
    SMTP: ?Smtp = null,
    /// An indication of whether the service supports including the resource payload of the origin of condition in the event payload.
    ///
    /// This property shall indicate whether the service supports including the resource payload of the origin of condition in the event payload.  If `true`, event subscriptions are allowed to specify the `IncludeOriginOfCondition` property.
    IncludeOriginOfConditionSupported: ?bool = null,
    /// An indication of whether the service supports filtering by the `ExcludeRegistryPrefixes` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `ExcludeRegistryPrefixes` property.
    ExcludeRegistryPrefix: ?bool = null,
    /// An indication of whether the service supports filtering by the `ExcludeMessageIds` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `ExcludeMessageIds` property.
    ExcludeMessageId: ?bool = null,
    /// The list of severities that can be specified in the `Severities` array in a subscription.
    ///
    /// This property shall specify an array of the allowable severities that can be used for an event subscription.  If this property is absent or contains an empty array, the service does not support severity-based subscriptions.
    Severities: ?[]const ?resource.Health = null,
    /// An indication of whether the service supports the `includeoriginofcondition` query parameter for the `ServerSentEventUri`.
    ///
    /// This property shall indicate whether the service supports the `includeoriginofcondition` query parameter for the `ServerSentEventUri`, as described by the Redfish Specification.
    SSEIncludeOriginOfConditionSupported: ?bool = null,
    /// An indication of whether the service supports filtering by the `OriginResources` property.
    ///
    /// This property shall indicate whether this service supports filtering by the `OriginResources` property.
    OriginResourcesSupported: ?bool = null,
    /// The link to a collection of event destinations.
    ///
    /// This property shall contain the link to a resource collection of type `EventDestinationCollection`.
    Subscriptions: ?core.NavProperty(event_destination_collection.EventDestinationCollection) = null,
};

/// What a client may change of `EventService.EventService`.
///
/// The `EventService` schema contains properties for managing event subscriptions and generates the events sent to subscribers.  The resource has links to the actual collection of subscriptions, which are called event destinations.
pub const EventServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.  If `false`, events are no longer published, new SSE connections cannot be established, and existing SSE connections are terminated.
    ///
    /// This property shall indicate whether this service is enabled.  If `false`, events are no longer published, new SSE connections cannot be established, and existing SSE connections are terminated.
    ServiceEnabled: core.Nullable(bool) = .absent,
    /// The number of times that the `POST` of an event is retried before the subscription terminates or is suspended.  This retry occurs at the service level, which means that the HTTP `POST` to the event destination fails with an HTTP `4XX` or `5XX` status code or an HTTP timeout occurs this many times before the event destination subscription terminates or is suspended.
    ///
    /// This property shall contain the number of times that the `POST` of an event is retried before the subscription terminates or is suspended.  This retry occurs at the service level, which means that the HTTP `POST` to the event destination fails with an HTTP `4XX` or `5XX` status code or an HTTP timeout occurs this many times before the event destination subscription terminates or is suspended.  The service shall delete the `EventDestination` resource to terminate the subscription.  The service shall set the value of the `State` property within `Status` of the `EventDestination` resource to `Disabled` for a suspended subscription.
    DeliveryRetryAttempts: ?i64 = null,
    /// The interval, in seconds, between retry attempts for sending any event.
    ///
    /// This property shall contain the interval, in seconds, between the retry attempts for any event sent to the subscription destination.
    DeliveryRetryIntervalSeconds: ?i64 = null,
    /// Settings for SMTP event delivery.
    ///
    /// This property shall contain settings for SMTP event delivery.
    SMTP: ?SmtpUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
