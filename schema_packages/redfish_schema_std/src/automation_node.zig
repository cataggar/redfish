//! Namespace `AutomationNode`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const automation_instrumentation = @import("automation_instrumentation.zig");
const chassis = @import("chassis.zig");
const control = @import("control.zig");
const resource = @import("resource.zig");
const sensor = @import("sensor.zig");
const settings = @import("settings.zig");

pub const MotionAxisType = enum {
    /// X axis.
    X,
    /// Y axis.
    Y,
    /// Z axis.
    Z,
    /// 2-axis.
    TwoAxis,
    /// 3-axis.
    ThreeAxis,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const MotionProfileType = enum {
    /// Trapezoidal profile.
    Trapezoidal,
    /// S-curve profile.
    SCurve,
    /// No profile specified.
    None,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NodeState = enum {
    /// The node's controller is idle.
    Idle,
    /// The node's controller has reached its destination position.
    Done,
    /// The node's controller is waiting to start.
    Waiting,
    /// The node's controller has stopped due to a condition fault.
    ConditionStop,
    /// The node's controller has stopped due to an error.
    ErrorStop,
    /// The node's controller is running.
    Running,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NodeType = enum {
    /// A position-based profiled motion node where position, velocity, and acceleration are all controlled.
    MotionPosition,
    /// A velocity-based profiled motion node where position, velocity and acceleration are all controlled.
    MotionVelocity,
    /// A multi-axis position-based profiled motion node where position, velocity and acceleration are all controlled.
    MotionPositionGroup,
    /// A node that attempts to match a set point using a PID control algorithm.
    PID,
    /// A simple node that incorporates no automated control function.
    Simple,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `AutomationNode.Reset`.
///
/// Resets the node.
pub const AutomationNodeResetAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AutomationNode.SendTrigger`.
///
/// Sends a trigger to the node.
pub const AutomationNodeSendTriggerAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AutomationNode.Start`.
///
/// Starts the node.
pub const AutomationNodeStartAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AutomationNode.Stop`.
///
/// Stops the node.
pub const AutomationNodeStopAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AutomationNode.Wait`.
///
/// Signals the node to wait for the next instruction.
pub const AutomationNodeWaitAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// Resets the node.
    ///
    /// This action shall reset the node to its power-on state.
    @"#AutomationNode.Reset": ?core.Action(AutomationNodeResetAction, std.json.Value) = null,
    /// Sends a trigger to the node.
    ///
    /// This action shall pulse the trigger signal for the node.
    @"#AutomationNode.SendTrigger": ?core.Action(AutomationNodeSendTriggerAction, std.json.Value) = null,
    /// Starts the node.
    ///
    /// This action shall transition the node to the `Running` state.  This action shall pulse the trigger signal to other connected nodes that are in the `Waiting` state.
    @"#AutomationNode.Start": ?core.Action(AutomationNodeStartAction, std.json.Value) = null,
    /// Stops the node.
    ///
    /// This action shall transition the node to the `Idle` state.
    @"#AutomationNode.Stop": ?core.Action(AutomationNodeStopAction, std.json.Value) = null,
    /// Signals the node to wait for the next instruction.
    ///
    /// This action shall transition the node to the `Waiting` state.
    @"#AutomationNode.Wait": ?core.Action(AutomationNodeWaitAction, std.json.Value) = null,

    /// Resets the node.
    ///
    /// This action shall reset the node to its power-on state.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AutomationNodeResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AutomationNode.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Sends a trigger to the node.
    ///
    /// This action shall pulse the trigger signal for the node.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn sendTrigger(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AutomationNodeSendTriggerAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AutomationNode.SendTrigger" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Starts the node.
    ///
    /// This action shall transition the node to the `Running` state.  This action shall pulse the trigger signal to other connected nodes that are in the `Waiting` state.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn start(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AutomationNodeStartAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AutomationNode.Start" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Stops the node.
    ///
    /// This action shall transition the node to the `Idle` state.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn stop(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AutomationNodeStopAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AutomationNode.Stop" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// Signals the node to wait for the next instruction.
    ///
    /// This action shall transition the node to the `Waiting` state.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn wait(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AutomationNodeWaitAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AutomationNode.Wait" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the chassis that contain this equipment.
    ///
    /// This property shall contain an array of links to resources of type `Chassis` that represent the physical containers that contain this equipment.
    Chassis: ?[]const core.NavProperty(chassis.Chassis) = null,
    /// How many members `Chassis` has in total, which is not how many this response carries.
    @"Chassis@odata.count": ?i64 = null,
    /// The next page of `Chassis`. Present only when this response left members out.
    @"Chassis@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the automation nodes that are included in an automation node group.
    ///
    /// This property shall contain an array of links to resources of type `AutomationNode` that represent the nodes that provide the functionality of this group.
    AutomationNodeGroup: ?[]const core.NavProperty(AutomationNode) = null,
    /// How many members `AutomationNodeGroup` has in total, which is not how many this response carries.
    @"AutomationNodeGroup@odata.count": ?i64 = null,
    /// The next page of `AutomationNodeGroup`. Present only when this response left members out.
    @"AutomationNodeGroup@odata.nextLink": ?core.ODataId = null,
    /// A link to the position sensor.
    ///
    /// This property shall contain a link to a resource of type `Sensor` that represents the position sensor associated with this node.
    PositionSensor: ?core.NavProperty(sensor.Sensor) = null,
    /// A link to the velocity sensor.
    ///
    /// This property shall contain a link to a resource of type `Sensor` that represents the velocity sensor associated with this node.
    VelocitySensor: ?core.NavProperty(sensor.Sensor) = null,
    /// A link to the PID feedback sensor.
    ///
    /// This property shall contain a link to a resource of type `Sensor` that represents the feedback sensor associated with this node.  The feedback sensor is intended to return controlled value output in a PID system.
    PidFeedbackSensor: ?core.NavProperty(sensor.Sensor) = null,
    /// A link to the output control.
    ///
    /// This property shall contain a link to a resource of type `Control` that represent the output control associated with this node.
    OutputControl: ?core.NavProperty(control.Control) = null,
};

/// What a client may change of `AutomationNode.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `AutomationNode` schema describes a node that carries out instructions in industrial automation environments.
///
/// This resource shall represent an automation node for a Redfish implementation.
pub const AutomationNode = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The type of automation node.
    ///
    /// This property shall contain the type for the automation node.
    NodeType: ?NodeType = null,
    /// The current state of the node.
    ///
    /// This property shall contain the current state of the automation node.
    NodeState: ?NodeState = null,
    /// The motion profile type used for this node.
    ///
    /// This property shall contain the type of motion profile used for this motion-based node.
    MotionProfile: ?MotionProfileType = null,
    /// The primary axis of motion for this motion-related node.
    ///
    /// This property shall contain the primary axis of motion for this motion-related node.
    MotionAxis: ?MotionAxisType = null,
    /// The status and health of a resource and its children.
    ///
    /// This type shall contain any status or health properties of a resource.
    Status: ?resource.Status = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// A link to the instrumentation for this node.
    ///
    /// This property shall contain a link to a resource of type `AutomationInstrumentation` that represents the instrumentation for this automation node.
    Instrumentation: ?core.NavProperty(automation_instrumentation.AutomationInstrumentation) = null,
};

/// What a client may change of `AutomationNode.AutomationNode`.
///
/// The `AutomationNode` schema describes a node that carries out instructions in industrial automation environments.
pub const AutomationNodeUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The motion profile type used for this node.
    ///
    /// This property shall contain the type of motion profile used for this motion-based node.
    MotionProfile: core.Nullable(MotionProfileType) = .absent,
    /// The primary axis of motion for this motion-related node.
    ///
    /// This property shall contain the primary axis of motion for this motion-related node.
    MotionAxis: core.Nullable(MotionAxisType) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
