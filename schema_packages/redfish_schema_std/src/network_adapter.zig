//! Namespace `NetworkAdapter`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const assembly = @import("assembly.zig");
const certificate_collection = @import("certificate_collection.zig");
const environment_metrics = @import("environment_metrics.zig");
const network_adapter_metrics = @import("network_adapter_metrics.zig");
const network_device_function = @import("network_device_function.zig");
const network_device_function_collection = @import("network_device_function_collection.zig");
const network_port = @import("network_port.zig");
const network_port_collection = @import("network_port_collection.zig");
const pcie_device = @import("pcie_device.zig");
const port = @import("port.zig");
const port_collection = @import("port_collection.zig");
const processor_collection = @import("processor_collection.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");
const software_inventory = @import("software_inventory.zig");

/// Arguments to `NetworkAdapter.Reset`.
///
/// This action resets the network adapter.
pub const NetworkAdapterResetAction = struct {
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and shall perform a `GracefulRestart`.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `NetworkAdapter.ResetSettingsToDefault`.
///
/// This action is to clear the settings back to factory defaults.
pub const NetworkAdapterResetSettingsToDefaultAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action resets the network adapter.
    ///
    /// This action shall reset a network adapter.
    @"#NetworkAdapter.Reset": ?core.Action(NetworkAdapterResetAction, std.json.Value) = null,
    /// This action is to clear the settings back to factory defaults.
    ///
    /// This action shall reset of all active and pending settings back to factory default settings upon reset of the network adapter.
    @"#NetworkAdapter.ResetSettingsToDefault": ?core.Action(NetworkAdapterResetSettingsToDefaultAction, std.json.Value) = null,

    /// This action resets the network adapter.
    ///
    /// This action shall reset a network adapter.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: NetworkAdapterResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#NetworkAdapter.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is to clear the settings back to factory defaults.
    ///
    /// This action shall reset of all active and pending settings back to factory default settings upon reset of the network adapter.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetSettingsToDefault(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: NetworkAdapterResetSettingsToDefaultAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#NetworkAdapter.ResetSettingsToDefault" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

pub const ControllerCapabilities = struct {
    /// The number of physical ports on this controller.
    ///
    /// This property shall contain the number of physical ports on this controller.
    NetworkPortCount: ?i64 = null,
    /// The maximum number of physical functions available on this controller.
    ///
    /// This property shall contain the number of physical functions available on this controller.
    NetworkDeviceFunctionCount: ?i64 = null,
    /// Data center bridging (DCB) for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to data center bridging (DCB) for this controller.
    DataCenterBridging: ?DataCenterBridging = null,
    /// Virtualization offload for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to virtualization offload for this controller.
    VirtualizationOffload: ?VirtualizationOffload = null,
    /// N_Port ID Virtualization (NPIV) capabilities for this controller.
    ///
    /// This property shall contain N_Port ID Virtualization (NPIV) capabilities for this controller.
    NPIV: ?Npiv = null,
    /// NIC Partitioning (NPAR) capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to NIC partitioning for this controller.
    NPAR: ?NicPartitioning = null,
};

/// What a client may change of `NetworkAdapter.ControllerCapabilities`.
pub const ControllerCapabilitiesUpdate = struct {
    /// Virtualization offload for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to virtualization offload for this controller.
    VirtualizationOffload: ?VirtualizationOffloadUpdate = null,
    /// NIC Partitioning (NPAR) capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to NIC partitioning for this controller.
    NPAR: ?NicPartitioningUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const ControllerLinks = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// An array of links to the PCIe devices associated with this network controller.
    ///
    /// This property shall contain an array of links to resources of type `PCIeDevice` that represent the PCIe devices associated with this network controller.
    PCIeDevices: ?[]const core.NavProperty(pcie_device.PcieDevice) = null,
    /// How many members `PCIeDevices` has in total, which is not how many this response carries.
    @"PCIeDevices@odata.count": ?i64 = null,
    /// The next page of `PCIeDevices`. Present only when this response left members out.
    @"PCIeDevices@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the network ports associated with this network controller.
    ///
    /// This property shall contain an array of links to resources of type `NetworkPort` that represent the network ports associated with this network controller.
    ///
    /// Deprecated in v1_5_0.
    /// This property has been deprecated in favor of the `Ports` property.
    NetworkPorts: ?[]const core.NavProperty(network_port.NetworkPort) = null,
    /// How many members `NetworkPorts` has in total, which is not how many this response carries.
    @"NetworkPorts@odata.count": ?i64 = null,
    /// The next page of `NetworkPorts`. Present only when this response left members out.
    @"NetworkPorts@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the network device functions associated with this network controller.
    ///
    /// This property shall contain an array of links to resources of type `NetworkDeviceFunction` that represent the network device functions associated with this network controller.
    NetworkDeviceFunctions: ?[]const core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// How many members `NetworkDeviceFunctions` has in total, which is not how many this response carries.
    @"NetworkDeviceFunctions@odata.count": ?i64 = null,
    /// The next page of `NetworkDeviceFunctions`. Present only when this response left members out.
    @"NetworkDeviceFunctions@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the ports associated with this network controller.
    ///
    /// This property shall contain an array of links to resources of type `Port` that represent the ports associated with this network controller.
    Ports: ?[]const core.NavProperty(port.Port) = null,
    /// How many members `Ports` has in total, which is not how many this response carries.
    @"Ports@odata.count": ?i64 = null,
    /// The next page of `Ports`. Present only when this response left members out.
    @"Ports@odata.nextLink": ?core.ODataId = null,
    /// The link to the software inventory resource that represents the active firmware image for this controller.
    ///
    /// This property shall contain a link to a resource of type `SoftwareInventory` that represents the active firmware image for this controller.
    ActiveSoftwareImage: ?core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// The images that are associated with this controller.
    ///
    /// This property shall contain an array of links to resource of type `SoftwareInventory` that represent the firmware images that apply to this controller.
    SoftwareImages: ?[]const core.NavProperty(software_inventory.SoftwareInventory) = null,
    /// How many members `SoftwareImages` has in total, which is not how many this response carries.
    @"SoftwareImages@odata.count": ?i64 = null,
    /// The next page of `SoftwareImages`. Present only when this response left members out.
    @"SoftwareImages@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `NetworkAdapter.ControllerLinks`.
pub const ControllerLinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Controllers = struct {
    /// The version of the user-facing firmware package.
    ///
    /// This property shall contain the version number of the user-facing firmware package.
    FirmwarePackageVersion: ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?ControllerLinks = null,
    /// The capabilities of this controller.
    ///
    /// This property shall contain the capabilities of this controller.
    ControllerCapabilities: ?ControllerCapabilities = null,
    /// The location of the network adapter controller.
    ///
    /// This property shall contain the location information of the controller associated with the network adapter.
    Location: ?resource.Location = null,
    /// The PCIe interface details for this controller.
    ///
    /// This property shall contain details for the PCIe interface that connects this PCIe-based controller to its host.
    PCIeInterface: ?pcie_device.PcieInterface = null,
    /// The durable names for the network adapter controller.
    ///
    /// This property shall contain a list of all known durable names for the controller associated with the network adapter.
    Identifiers: ?[]const resource.Identifier = null,
};

/// What a client may change of `NetworkAdapter.Controllers`.
pub const ControllersUpdate = struct {
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?ControllerLinksUpdate = null,
    /// The capabilities of this controller.
    ///
    /// This property shall contain the capabilities of this controller.
    ControllerCapabilities: ?ControllerCapabilitiesUpdate = null,
    /// The location of the network adapter controller.
    ///
    /// This property shall contain the location information of the controller associated with the network adapter.
    Location: ?resource.LocationUpdate = null,
    /// The PCIe interface details for this controller.
    ///
    /// This property shall contain details for the PCIe interface that connects this PCIe-based controller to its host.
    PCIeInterface: ?pcie_device.PcieInterfaceUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Data center bridging (DCB) for capabilities of a controller.
///
/// This type shall describe the capability, status, and configuration values related to data center bridging (DCB) for a controller.
pub const DataCenterBridging = struct {
    /// An indication of whether this controller is capable of data center bridging (DCB).
    ///
    /// This property shall indicate whether this controller is capable of data center bridging (DCB).
    Capable: ?bool = null,
};

/// N_Port ID Virtualization (NPIV) capabilities for a controller.
///
/// This type shall contain N_Port ID Virtualization (NPIV) capabilities for a controller.
pub const Npiv = struct {
    /// The maximum number of N_Port ID Virtualization (NPIV) logins allowed simultaneously from all ports on this controller.
    ///
    /// This property shall contain the maximum number of N_Port ID Virtualization (NPIV) logins allowed simultaneously from all ports on this controller.
    MaxDeviceLogins: ?i64 = null,
    /// The maximum number of N_Port ID Virtualization (NPIV) logins allowed per physical port on this controller.
    ///
    /// This property shall contain the maximum number of N_Port ID Virtualization (NPIV) logins allowed per physical port on this controller.
    MaxPortLogins: ?i64 = null,
};

/// NIC Partitioning capability, status, and configuration for a controller.
///
/// This type shall contain the capability, status, and configuration values for a controller.
pub const NicPartitioning = struct {
    /// An indication of whether the controller supports NIC function partitioning.
    ///
    /// This property shall indicate whether the controller supports NIC function partitioning.
    NparCapable: ?bool = null,
    /// An indication of whether NIC function partitioning is active on this controller.
    ///
    /// This property shall indicate whether NIC function partitioning is active on this controller.
    NparEnabled: ?bool = null,
};

/// What a client may change of `NetworkAdapter.NicPartitioning`.
///
/// NIC Partitioning capability, status, and configuration for a controller.
pub const NicPartitioningUpdate = struct {
    /// An indication of whether NIC function partitioning is active on this controller.
    ///
    /// This property shall indicate whether NIC function partitioning is active on this controller.
    NparEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The port aggregation capabilities for a controller.
///
/// This type shall contain capability, status, and configuration values related to aggregating ports on a controller.
pub const PortAggregation = struct {
    /// The total number of physical ports.
    ///
    /// This property shall contain the total number of physical ports.
    TotalPhysicalPorts: ?i64 = null,
    /// The allowable number of physical ports per aggregation.
    ///
    /// This property shall contain the allowable number of physical ports per aggregation.
    AllowablePhysicalPortsPerAggregation: ?[]const ?i64 = null,
    /// The configured number of physical ports per aggregation.
    ///
    /// This property shall contain the configured number of physical ports per aggregation.
    ConfiguredPhysicalPortsPerAggregation: ?i64 = null,
    /// The configured number number of aggregated ports.
    ///
    /// This property shall contain the configured number of aggregated ports.
    ConfiguredAggregatedPorts: ?i64 = null,
    /// Indicates whether port aggregation is enabled.
    ///
    /// This property shall indicate whether port aggregation is enabled.
    AggregationEnabled: ?bool = null,
};

/// What a client may change of `NetworkAdapter.PortAggregation`.
///
/// The port aggregation capabilities for a controller.
pub const PortAggregationUpdate = struct {
    /// The configured number of physical ports per aggregation.
    ///
    /// This property shall contain the configured number of physical ports per aggregation.
    ConfiguredPhysicalPortsPerAggregation: core.Nullable(i64) = .absent,
    /// The configured number number of aggregated ports.
    ///
    /// This property shall contain the configured number of aggregated ports.
    ConfiguredAggregatedPorts: core.Nullable(i64) = .absent,
    /// Indicates whether port aggregation is enabled.
    ///
    /// This property shall indicate whether port aggregation is enabled.
    AggregationEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The port splitting capabilities for a controller.
///
/// This type shall contain capability, status, and configuration values related to physically subdividing the lanes of ports on a controller.
pub const PortSplitting = struct {
    /// The maximum number of subdivided ports that this controller supports.
    ///
    /// This property shall contain the maximum number of subdivided ports that this controller supports.
    MaximumSubports: ?i64 = null,
    /// The maximum number of subdivided ports split from a single physical port that this controller supports.
    ///
    /// This property shall contain the maximum number of subdivided ports split from a single physical port that this controller supports.
    MaximumSubportsPerPort: ?i64 = null,
    /// The port splitting configurations this controller supports.
    ///
    /// This property shall contain the port splitting configurations that this controller supports.  Properties contained in this property shall be read-only.
    SupportedConfigurations: ?[]const PortSplittingSubconfigurationList = null,
    /// The current port splitting configuration for this controller.
    ///
    /// This property shall contain the current port splitting configuration for this controller.
    CurrentConfiguration: ?[]const PortSplittingSubconfiguration = null,
};

/// What a client may change of `NetworkAdapter.PortSplitting`.
///
/// The port splitting capabilities for a controller.
pub const PortSplittingUpdate = struct {
    /// The port splitting configurations this controller supports.
    ///
    /// This property shall contain the port splitting configurations that this controller supports.  Properties contained in this property shall be read-only.
    SupportedConfigurations: ?[]const PortSplittingSubconfigurationListUpdate = null,
    /// The current port splitting configuration for this controller.
    ///
    /// This property shall contain the current port splitting configuration for this controller.
    CurrentConfiguration: ?[]const PortSplittingSubconfigurationUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// A port splitting subconfiguration for one or more physical ports on a controller.
///
/// This type shall contain a port splitting subconfiguration for one or more physical ports on a controller.
pub const PortSplittingSubconfiguration = struct {
    /// The first physical port to which this subconfiguration applies.
    ///
    /// This property shall contain the first physical port to which this subconfiguration applies.
    StartingPhysicalPort: ?i64 = null,
    /// The last physical port to which this subconfiguration applies.
    ///
    /// This property shall contain the last physical port to which this subconfiguration applies.  Specifically, the splitting for ports `StartPort` through `EndPort`, inclusive, is characterized by this subconfiguration.  Each set of subconfigurations shall cover all physical ports on the controller and shall describe splitting for each port exactly once.
    EndingPhysicalPort: ?i64 = null,
    /// The first identifier to assign to subports in this subconfiguration.  Subport identifiers are assigned sequentially to the subports starting with those for `StartPort` and working through to those for `EndPort`.
    ///
    /// This property shall contain the first identifier to assign to subports in this subconfiguration.  Subport identifiers shall be assigned sequentially to the subports starting with those for `StartPort` and working through to those for `EndPort`.
    FirstSubportId: ?i64 = null,
    /// The number of subports created from each port in this subconfiguration.
    ///
    /// This property shall contain the number of subports created from each port in this subconfiguration.
    SubportsPerPort: ?i64 = null,
    /// The number of lanes for each subport.
    ///
    /// This property shall contain the number of lanes for each subport.  The number of members in this array shall equal the value contained in `SubportsPerPort` in the enclosing subconfiguration.
    Lanes: ?[]const ?i64 = null,
    /// The configured link speed for each subport.
    ///
    /// This property shall contain the configured link speed for each subport.  The number of members in this array shall equal the value contained in `SubportsPerPort` in the enclosing subconfiguration.
    LinkSpeedGbps: ?[]const ?i64 = null,
};

/// What a client may change of `NetworkAdapter.PortSplittingSubconfiguration`.
///
/// A port splitting subconfiguration for one or more physical ports on a controller.
pub const PortSplittingSubconfigurationUpdate = struct {
    /// The first physical port to which this subconfiguration applies.
    ///
    /// This property shall contain the first physical port to which this subconfiguration applies.
    StartingPhysicalPort: core.Nullable(i64) = .absent,
    /// The last physical port to which this subconfiguration applies.
    ///
    /// This property shall contain the last physical port to which this subconfiguration applies.  Specifically, the splitting for ports `StartPort` through `EndPort`, inclusive, is characterized by this subconfiguration.  Each set of subconfigurations shall cover all physical ports on the controller and shall describe splitting for each port exactly once.
    EndingPhysicalPort: core.Nullable(i64) = .absent,
    /// The first identifier to assign to subports in this subconfiguration.  Subport identifiers are assigned sequentially to the subports starting with those for `StartPort` and working through to those for `EndPort`.
    ///
    /// This property shall contain the first identifier to assign to subports in this subconfiguration.  Subport identifiers shall be assigned sequentially to the subports starting with those for `StartPort` and working through to those for `EndPort`.
    FirstSubportId: core.Nullable(i64) = .absent,
    /// The number of subports created from each port in this subconfiguration.
    ///
    /// This property shall contain the number of subports created from each port in this subconfiguration.
    SubportsPerPort: core.Nullable(i64) = .absent,
    /// The number of lanes for each subport.
    ///
    /// This property shall contain the number of lanes for each subport.  The number of members in this array shall equal the value contained in `SubportsPerPort` in the enclosing subconfiguration.
    Lanes: ?[]const ?i64 = null,
    /// The configured link speed for each subport.
    ///
    /// This property shall contain the configured link speed for each subport.  The number of members in this array shall equal the value contained in `SubportsPerPort` in the enclosing subconfiguration.
    LinkSpeedGbps: ?[]const ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// A port splitting configuration for a controller.
///
/// This type shall contain a port splitting configuration for a controller.
pub const PortSplittingSubconfigurationList = struct {
    /// The set of subconfigurations that collectively define a port splitting configuration for this controller.
    ///
    /// This property shall contain the set of subconfigurations that collectively define a port splitting configuration for this controller.
    Subconfigurations: ?[]const PortSplittingSubconfiguration = null,
};

/// What a client may change of `NetworkAdapter.PortSplittingSubconfigurationList`.
///
/// A port splitting configuration for a controller.
pub const PortSplittingSubconfigurationListUpdate = struct {
    /// The set of subconfigurations that collectively define a port splitting configuration for this controller.
    ///
    /// This property shall contain the set of subconfigurations that collectively define a port splitting configuration for this controller.
    Subconfigurations: ?[]const PortSplittingSubconfigurationUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Sriov = struct {
    /// An indication of whether this controller supports single root input/output virtualization (SR-IOV) in Virtual Ethernet Port Aggregator (VEPA) mode.
    ///
    /// This property shall indicate whether this controller supports single root input/output virtualization (SR-IOV) in Virtual Ethernet Port Aggregator (VEPA) mode.
    SRIOVVEPACapable: ?bool = null,
    /// An indication of whether single root input/output virtualization (SR-IOV) is enabled for this controller.
    ///
    /// This property shall indicate whether single root input/output virtualization (SR-IOV) is enabled for this controller.
    SRIOVEnabled: ?bool = null,
};

/// What a client may change of `NetworkAdapter.SRIOV`.
pub const SriovUpdate = struct {
    /// An indication of whether single root input/output virtualization (SR-IOV) is enabled for this controller.
    ///
    /// This property shall indicate whether single root input/output virtualization (SR-IOV) is enabled for this controller.
    SRIOVEnabled: ?bool = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// A virtual function of a controller.
///
/// This type shall describe the capability, status, and configuration values related to a virtual function for a controller.
pub const VirtualFunction = struct {
    /// The maximum number of virtual functions supported by this controller.
    ///
    /// This property shall contain the maximum number of virtual functions supported by this controller.
    DeviceMaxCount: ?i64 = null,
    /// The maximum number of virtual functions supported per network port for this controller.
    ///
    /// This property shall contain the maximum number of virtual functions supported per network port for this controller.
    NetworkPortMaxCount: ?i64 = null,
    /// The minimum number of virtual functions that can be allocated or moved between physical functions for this controller.
    ///
    /// This property shall contain the minimum number of virtual functions that can be allocated or moved between physical functions for this controller.
    MinAssignmentGroupSize: ?i64 = null,
};

/// A Virtualization offload capability of a controller.
///
/// This type shall describe the capability, status, and configuration values related to a virtualization offload for a controller.
pub const VirtualizationOffload = struct {
    /// The virtual function of the controller.
    ///
    /// This property shall describe the capability, status, and configuration values related to the virtual function for this controller.
    VirtualFunction: ?VirtualFunction = null,
    /// Single-root input/output virtualization (SR-IOV) capabilities.
    ///
    /// This property shall contain single-root input/output virtualization (SR-IOV) capabilities.
    SRIOV: ?Sriov = null,
};

/// What a client may change of `NetworkAdapter.VirtualizationOffload`.
///
/// A Virtualization offload capability of a controller.
pub const VirtualizationOffloadUpdate = struct {
    /// Single-root input/output virtualization (SR-IOV) capabilities.
    ///
    /// This property shall contain single-root input/output virtualization (SR-IOV) capabilities.
    SRIOV: ?SriovUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `NetworkAdapter` schema represents a physical network adapter capable of connecting to a computer network.  Examples include but are not limited to Ethernet, Fibre Channel, and converged network adapters.
///
/// This resource shall represent a physical network adapter capable of connecting to a computer network in a Redfish implementation.  Services should represent adapters that contain multiple controllers with independent management interfaces as multiple `NetworkAdapter` resources.
pub const NetworkAdapter = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The manufacturer or OEM of this network adapter.
    ///
    /// This property shall contain a value that represents the manufacturer of the network adapter.
    Manufacturer: ?[]const u8 = null,
    /// The model string for this network adapter.
    ///
    /// This property shall contain the information about how the manufacturer refers to this network adapter.
    Model: ?[]const u8 = null,
    /// The manufacturer SKU for this network adapter.
    ///
    /// This property shall contain the SKU for the network adapter.
    SKU: ?[]const u8 = null,
    /// The serial number for this network adapter.
    ///
    /// This property shall contain the serial number for the network adapter.
    SerialNumber: ?[]const u8 = null,
    /// Part number for this network adapter.
    ///
    /// This property shall contain the part number for the network adapter as defined by the manufacturer.
    PartNumber: ?[]const u8 = null,
    /// The set of network controllers ASICs that make up this NetworkAdapter.
    ///
    /// This property shall contain the set of network controllers ASICs that make up this network adapter.
    Controllers: ?[]const Controllers = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The location of the network adapter.
    ///
    /// This property shall contain the location information of the network adapter.
    Location: ?resource.Location = null,
    /// The durable names for the network adapter.
    ///
    /// This property shall contain a list of all known durable names for the network adapter.
    Identifiers: ?[]const resource.Identifier = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_9_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// Enable or disable LLDP globally for an adapter.
    ///
    /// This property shall contain the state indicating whether LLDP is globally enabled on a network adapter.  If set to `false`, the `LLDPEnabled` value for the ports associated with this adapter shall be disregarded.
    LLDPEnabled: ?bool = null,
    /// The port splitting capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to physically subdividing the lanes of ports on this controller.
    PortSplitting: ?PortSplitting = null,
    /// The port aggregation capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to aggregating ports on this controller.
    PortAggregation: ?PortAggregation = null,
    /// The link to the collection of network ports associated with this network adapter.
    ///
    /// This property shall contain a link to a resource collection of type `NetworkPortCollection`.
    ///
    /// Deprecated in v1_5_0.
    /// This property has been deprecated in favor of the `Ports` property.
    NetworkPorts: ?core.NavProperty(network_port_collection.NetworkPortCollection) = null,
    /// The link to the collection of network device functions associated with this network adapter.
    ///
    /// This property shall contain a link to a resource collection of type `NetworkDeviceFunctionCollection`.
    NetworkDeviceFunctions: ?core.NavProperty(network_device_function_collection.NetworkDeviceFunctionCollection) = null,
    /// The link to the assembly resource associated with this adapter.
    ///
    /// This property shall contain a link to a resource of type `Assembly`.
    Assembly: ?core.NavProperty(assembly.Assembly) = null,
    /// The link to the collection of ports associated with this network adapter.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection`.
    Ports: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to the metrics associated with this adapter.
    ///
    /// This property shall contain a link to a resource of type `NetworkAdapterMetrics` that contains the metrics associated with this adapter.
    Metrics: ?core.NavProperty(network_adapter_metrics.NetworkAdapterMetrics) = null,
    /// The link to the environment metrics for this network adapter.
    ///
    /// This property shall contain a link to a resource of type `EnvironmentMetrics` that specifies the environment metrics for this network adapter.
    EnvironmentMetrics: ?core.NavProperty(environment_metrics.EnvironmentMetrics) = null,
    /// The link to the collection of offload processors contained in this network adapter.
    ///
    /// This property shall contain a link to a resource collection of type `ProcessorCollection` that represent the offload processors contained in this network adapter.
    Processors: ?core.NavProperty(processor_collection.ProcessorCollection) = null,
};

/// What a client may change of `NetworkAdapter.NetworkAdapter`.
///
/// The `NetworkAdapter` schema represents a physical network adapter capable of connecting to a computer network.  Examples include but are not limited to Ethernet, Fibre Channel, and converged network adapters.
pub const NetworkAdapterUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The set of network controllers ASICs that make up this NetworkAdapter.
    ///
    /// This property shall contain the set of network controllers ASICs that make up this network adapter.
    Controllers: ?[]const ControllersUpdate = null,
    /// The location of the network adapter.
    ///
    /// This property shall contain the location information of the network adapter.
    Location: ?resource.LocationUpdate = null,
    /// Enable or disable LLDP globally for an adapter.
    ///
    /// This property shall contain the state indicating whether LLDP is globally enabled on a network adapter.  If set to `false`, the `LLDPEnabled` value for the ports associated with this adapter shall be disregarded.
    LLDPEnabled: ?bool = null,
    /// The port splitting capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to physically subdividing the lanes of ports on this controller.
    PortSplitting: core.Nullable(PortSplittingUpdate) = .absent,
    /// The port aggregation capabilities for this controller.
    ///
    /// This property shall contain capability, status, and configuration values related to aggregating ports on this controller.
    PortAggregation: core.Nullable(PortAggregationUpdate) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
