//! Namespace `VirtualMedia`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const certificate_collection = @import("certificate_collection.zig");
const resource = @import("resource.zig");

pub const ConnectedVia = enum {
    /// No current connection.
    NotConnected,
    /// Connected to a URI location.
    URI,
    /// Connected to a client application.
    Applet,
    /// Connected through an OEM-defined method.
    Oem,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EjectPolicy = enum {
    /// The virtual media ejection occurs during a system power or reset event.
    OnPowerOff,
    /// The virtual media ejection occurs when a session is terminated.  The session might be outside the Redfish service.
    Session,
    /// The virtual media ejection occurs when a timer configured by the `EjectTimeout` property expires.
    Timed,
    /// The virtual media ejection occurs after the media is used.
    AfterUse,
    /// The virtual media mount information persists indefinitely.
    Persistent,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const MediaType = enum {
    /// A CD-ROM format (ISO) image.
    CD,
    /// A floppy disk image.
    Floppy,
    /// An emulation of a USB storage device.
    USBStick,
    /// A DVD-ROM format image.
    DVD,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TransferMethod = enum {
    /// Stream image file data from the source URI.
    Stream,
    /// Upload the entire image file from the source URI to the service.
    Upload,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TransferProtocolType = enum {
    /// Common Internet File System (CIFS).
    CIFS,
    /// File Transfer Protocol (FTP).
    FTP,
    /// SSH File Transfer Protocol (SFTP).
    SFTP,
    /// Hypertext Transfer Protocol (HTTP).
    HTTP,
    /// Hypertext Transfer Protocol Secure (HTTPS).
    HTTPS,
    /// Network File System (NFS).
    NFS,
    /// Secure Copy Protocol (SCP).
    SCP,
    /// Trivial File Transfer Protocol (TFTP).
    TFTP,
    /// A manufacturer-defined protocol.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `VirtualMedia.EjectMedia`.
///
/// This action detaches remote media from virtual media.
pub const VirtualMediaEjectMediaAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `VirtualMedia.InsertMedia`.
///
/// This action attaches remote media to virtual media.
pub const VirtualMediaInsertMediaAction = struct {
    /// The URI of the media to attach to the virtual media.
    ///
    /// This parameter shall contain the URI of the media to be attached to the virtual media.  This parameter may specify an absolute URI to remote media or a relative URI to media local to the implementation.  A service may allow a relative URI to reference a `SoftwareInventory` resource.  Services may reject this action if the URI is not accessible.
    Image: ?[]const u8 = null,
    /// An indication of whether the image is treated as mounted and visible to the host system upon completion of the action.  The default is `true`.
    ///
    /// This parameter shall contain whether the image is treated as mounted and visible to the host system upon completion of the action.  If the client does not provide this parameter, the service shall default this value to `true`.
    Inserted: ?bool = null,
    /// An indication of whether the remote media is treated as write-protected.  The default is `true`.
    ///
    /// This parameter shall contain whether the remote media is treated as write-protected.  If the client does not provide this parameter, the service shall default this value to `true`.
    WriteProtected: ?bool = null,
    /// The username to access the URI specified by the `Image` parameter.
    ///
    /// This parameter shall contain the username to access the URI specified by the `Image` parameter.
    UserName: ?[]const u8 = null,
    /// The password to access the URI specified by the `Image` parameter.
    ///
    /// This parameter shall contain the password to access the URI specified by the `Image` parameter.
    Password: ?[]const u8 = null,
    /// The network protocol to use with the URI specified by the `Image` parameter.
    ///
    /// This parameter shall contain the network protocol to use with the URI specified by the `Image` parameter.
    TransferProtocolType: ?TransferProtocolType = null,
    /// The transfer method to use with the image.
    ///
    /// This parameter shall contain the transfer method to use with the specified image URI.
    TransferMethod: ?TransferMethod = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action detaches remote media from virtual media.
    ///
    /// This action shall detach the remote media from the virtual media.  At the completion of the operation, inserted shall be set to `false` and the image name shall be cleared.
    @"#VirtualMedia.EjectMedia": ?core.Action(VirtualMediaEjectMediaAction, std.json.Value) = null,
    /// This action attaches remote media to virtual media.
    ///
    /// This action shall attach remote media to virtual media.  Service should reject the request if the `Image` property in the resource does not contain `null`.  Users are expected to eject media prior to inserting new media.
    @"#VirtualMedia.InsertMedia": ?core.Action(VirtualMediaInsertMediaAction, std.json.Value) = null,

    /// This action detaches remote media from virtual media.
    ///
    /// This action shall detach the remote media from the virtual media.  At the completion of the operation, inserted shall be set to `false` and the image name shall be cleared.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn ejectMedia(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VirtualMediaEjectMediaAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#VirtualMedia.EjectMedia" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action attaches remote media to virtual media.
    ///
    /// This action shall attach remote media to virtual media.  Service should reject the request if the `Image` property in the resource does not contain `null`.  Users are expected to eject media prior to inserting new media.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn insertMedia(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: VirtualMediaInsertMediaAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#VirtualMedia.InsertMedia" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `VirtualMedia` schema contains properties related to the monitor and control of an instance of virtual media, such as a remote CD, DVD, or USB device.  A manager for a system or device provides virtual media functionality.
///
/// This resource shall represent a virtual media service for a Redfish implementation.
pub const VirtualMedia = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The current image name.
    ///
    /// This property shall contain the name of the image.
    ImageName: ?[]const u8 = null,
    /// The URI of the location of the selected image.
    ///
    /// This property shall contain the URI of the media attached to the virtual media.  This value may specify an absolute URI to remote media or a relative URI to media local to the implementation.  A service may allow a relative URI to reference a SoftwareInventory resource.  The value `null` shall indicate no image connection.  If the URI specified by this property is not accessible, services should populate the `Conditions` property in `Status` with messages that describe the condition.
    Image: ?[]const u8 = null,
    /// The media types supported as virtual media.
    ///
    /// This property shall contain an array of the supported media types for this connection.
    MediaTypes: ?[]const MediaType = null,
    /// The current virtual media connection method.
    ///
    /// This property shall contain the current connection method from a client to the virtual media that this resource represents.
    ConnectedVia: ?ConnectedVia = null,
    /// An indication of whether the media is mounted and visible to the host system.
    ///
    /// This property shall indicate whether the media is mounted and visible to the host system.
    Inserted: ?bool = null,
    /// An indication of whether the media is write-protected.
    ///
    /// This property shall indicate whether the remote device media prevents writing to that media.
    WriteProtected: ?bool = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The username to access the URI specified by the `Image` property.
    ///
    /// This property shall contain the username to access the URI specified by the `Image` property.
    UserName: ?[]const u8 = null,
    /// The password to access the URI specified by the `Image` property.  The value is `null` in responses.
    ///
    /// This property shall contain the password to access the URI specified by the `Image` property.  The value shall be `null` in responses.
    Password: ?[]const u8 = null,
    /// The network protocol to use with the URI specified by the `Image` property.
    ///
    /// This property shall contain network protocol to use with the URI specified by the `Image` property.
    TransferProtocolType: ?TransferProtocolType = null,
    /// The transfer method to use with the image.
    ///
    /// This property shall describe how the image transfer occurs.
    TransferMethod: ?TransferMethod = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// An indication of whether the service will verify the certificate of the server referenced by the `Image` property prior to completing the remote media connection.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `Image` property prior to completing the remote media connection with the certificates found in the collection referenced by the `Certificates` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    VerifyCertificate: ?bool = null,
    /// The ejection policy for the virtual media.
    ///
    /// This property shall contain the ejection policy for the virtual media.
    EjectPolicy: ?EjectPolicy = null,
    /// Timeout value before the virtual media is automatically ejected.
    ///
    /// This property shall indicate the amount of time before virtual media is automatically ejected when `EjectPolicy` contains `Timed`.
    EjectTimeout: ?core.Duration = null,
    /// The link to a collection of server certificates for the server referenced by the `Image` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the server certificates for the server referenced by the `Image` property.  If `VerifyCertificate` is `true`, services shall compare the certificates in this collection with the certificate obtained during handshaking with the image server in order to verify the identity of the image server prior to completing the remote media connection.  If the server cannot be verified, the service shall not complete the remote media connection.  If `VerifyCertificate` is `false`, the service shall not perform certificate verification with certificates in this collection.  Regardless of the contents of this collection, services may perform additional verification based on other factors, such as the configuration of the `SecurityPolicy` resource.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to a collection of client identity certificates provided to the server referenced by the `Image` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the client identity certificates that are provided to the server referenced by the `Image` property as part of TLS handshaking.
    ClientCertificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
};

/// What a client may change of `VirtualMedia.VirtualMedia`.
///
/// The `VirtualMedia` schema contains properties related to the monitor and control of an instance of virtual media, such as a remote CD, DVD, or USB device.  A manager for a system or device provides virtual media functionality.
pub const VirtualMediaUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The URI of the location of the selected image.
    ///
    /// This property shall contain the URI of the media attached to the virtual media.  This value may specify an absolute URI to remote media or a relative URI to media local to the implementation.  A service may allow a relative URI to reference a SoftwareInventory resource.  The value `null` shall indicate no image connection.  If the URI specified by this property is not accessible, services should populate the `Conditions` property in `Status` with messages that describe the condition.
    Image: core.Nullable([]const u8) = .absent,
    /// An indication of whether the media is mounted and visible to the host system.
    ///
    /// This property shall indicate whether the media is mounted and visible to the host system.
    Inserted: core.Nullable(bool) = .absent,
    /// An indication of whether the media is write-protected.
    ///
    /// This property shall indicate whether the remote device media prevents writing to that media.
    WriteProtected: core.Nullable(bool) = .absent,
    /// The username to access the URI specified by the `Image` property.
    ///
    /// This property shall contain the username to access the URI specified by the `Image` property.
    UserName: core.Nullable([]const u8) = .absent,
    /// The password to access the URI specified by the `Image` property.  The value is `null` in responses.
    ///
    /// This property shall contain the password to access the URI specified by the `Image` property.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,
    /// The network protocol to use with the URI specified by the `Image` property.
    ///
    /// This property shall contain network protocol to use with the URI specified by the `Image` property.
    TransferProtocolType: core.Nullable(TransferProtocolType) = .absent,
    /// The transfer method to use with the image.
    ///
    /// This property shall describe how the image transfer occurs.
    TransferMethod: core.Nullable(TransferMethod) = .absent,
    /// An indication of whether the service will verify the certificate of the server referenced by the `Image` property prior to completing the remote media connection.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `Image` property prior to completing the remote media connection with the certificates found in the collection referenced by the `Certificates` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    VerifyCertificate: core.Nullable(bool) = .absent,
    /// The ejection policy for the virtual media.
    ///
    /// This property shall contain the ejection policy for the virtual media.
    EjectPolicy: core.Nullable(EjectPolicy) = .absent,
    /// Timeout value before the virtual media is automatically ejected.
    ///
    /// This property shall indicate the amount of time before virtual media is automatically ejected when `EjectPolicy` contains `Timed`.
    EjectTimeout: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
