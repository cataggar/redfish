//! Namespace `VCATEntry`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The Virtual Channel Action Table entry corresponding to a specific Virtual Channel.
///
/// This type shall contain a Virtual Channel entry definition that describes a specific Virtual Channel.
pub const VcaTableEntry = struct {
    /// The bits corresponding to the supported Virtual Channel.
    ///
    /// This property shall contain a 32-bit value where the bits correspond to a supported Virtual Channel.
    VCMask: ?[]const u8 = null,
    /// The configured threshold.
    ///
    /// This property shall contain the Gen-Z Core Specification-defined 'TH' 7-bit threshold.
    Threshold: ?[]const u8 = null,
};

/// What a client may change of `VCATEntry.VCATableEntry`.
///
/// The Virtual Channel Action Table entry corresponding to a specific Virtual Channel.
pub const VcaTableEntryUpdate = struct {
    /// The bits corresponding to the supported Virtual Channel.
    ///
    /// This property shall contain a 32-bit value where the bits correspond to a supported Virtual Channel.
    VCMask: core.Nullable([]const u8) = .absent,
    /// The configured threshold.
    ///
    /// This property shall contain the Gen-Z Core Specification-defined 'TH' 7-bit threshold.
    Threshold: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `VCATEntry` schema defines an entry in a Virtual Channel Action Table.  A Virtual Channel is a mechanism used to create multiple, logical communication streams across a physical link.
///
/// This resource shall represent an entry of Virtual Channel Action Table in a Redfish implementation.
pub const VcatEntry = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The hexadecimal value of the Virtual Channel Action Table entries.
    ///
    /// This property shall contain the hexadecimal value of the Virtual Channel Action Table entries.  The length of the hexadecimal value depends on the number of Virtual Channel Action entries supported by the component.
    RawEntryHex: ?[]const u8 = null,
    /// An array of entries of the Virtual Channel Action Table.
    ///
    /// This property shall contain an array of entries of the Virtual Channel Action Table.  The length of the array depends on the number of Virtual Channel Action entries supported by the component.
    VCEntries: ?[]const VcaTableEntry = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
};

/// What a client may change of `VCATEntry.VCATEntry`.
///
/// The `VCATEntry` schema defines an entry in a Virtual Channel Action Table.  A Virtual Channel is a mechanism used to create multiple, logical communication streams across a physical link.
pub const VcatEntryUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The hexadecimal value of the Virtual Channel Action Table entries.
    ///
    /// This property shall contain the hexadecimal value of the Virtual Channel Action Table entries.  The length of the hexadecimal value depends on the number of Virtual Channel Action entries supported by the component.
    RawEntryHex: core.Nullable([]const u8) = .absent,
    /// An array of entries of the Virtual Channel Action Table.
    ///
    /// This property shall contain an array of entries of the Virtual Channel Action Table.  The length of the array depends on the number of Virtual Channel Action entries supported by the component.
    VCEntries: ?[]const VcaTableEntryUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `VCATEntry.VCATEntry`.
///
/// The `VCATEntry` schema defines an entry in a Virtual Channel Action Table.  A Virtual Channel is a mechanism used to create multiple, logical communication streams across a physical link.
pub const VcatEntryCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The hexadecimal value of the Virtual Channel Action Table entries.
    ///
    /// This property shall contain the hexadecimal value of the Virtual Channel Action Table entries.  The length of the hexadecimal value depends on the number of Virtual Channel Action entries supported by the component.
    RawEntryHex: core.Nullable([]const u8) = .absent,
    /// An array of entries of the Virtual Channel Action Table.
    ///
    /// This property shall contain an array of entries of the Virtual Channel Action Table.  The length of the array depends on the number of Virtual Channel Action entries supported by the component.
    VCEntries: ?[]const VcaTableEntryUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
