//! Namespace `AggregationService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const aggregate_collection = @import("aggregate_collection.zig");
const aggregation_source_collection = @import("aggregation_source_collection.zig");
const connection_method_collection = @import("connection_method_collection.zig");
const resource = @import("resource.zig");

/// Arguments to `AggregationService.Reset`.
///
/// This action is used to reset a set of resources.  For example this could be a list of computer systems.
pub const AggregationServiceResetAction = struct {
    /// The delay of the batches of elements being reset in seconds.
    ///
    /// This parameter shall contain the delay of the batches of elements being reset in seconds.
    DelayBetweenBatchesInSeconds: ?i64 = null,
    /// The number of elements in each batch being reset.
    ///
    /// This parameter shall contain the number of elements in each batch simultaneously being issued a reset.
    BatchSize: ?i64 = null,
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and perform an implementation-specific default reset.
    ResetType: ?resource.ResetType = null,
    /// An array of links to the resources being reset.
    ///
    /// This parameter shall contain an array of links to the resources being reset.
    TargetURIs: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AggregationService.SetDefaultBootOrder`.
///
/// This action is used to restore the boot order to the default state for the specified computer systems.
pub const AggregationServiceSetDefaultBootOrderAction = struct {
    /// The computer systems to restore.
    ///
    /// This parameter shall contain an array of links to resources of type `ComputerSystem`.
    Systems: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action is used to reset a set of resources.  For example this could be a list of computer systems.
    ///
    /// This action shall perform a reset of a set of resources.
    @"#AggregationService.Reset": ?core.Action(AggregationServiceResetAction, std.json.Value) = null,
    /// This action is used to restore the boot order to the default state for the specified computer systems.
    ///
    /// This action shall restore the boot order to the default state for the specified computer systems.
    @"#AggregationService.SetDefaultBootOrder": ?core.Action(AggregationServiceSetDefaultBootOrderAction, std.json.Value) = null,

    /// This action is used to reset a set of resources.  For example this could be a list of computer systems.
    ///
    /// This action shall perform a reset of a set of resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregationServiceResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AggregationService.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to restore the boot order to the default state for the specified computer systems.
    ///
    /// This action shall restore the boot order to the default state for the specified computer systems.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setDefaultBootOrder(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregationServiceSetDefaultBootOrderAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AggregationService.SetDefaultBootOrder" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `AggregationService` schema contains properties for managing aggregation operations, either on ad hoc combinations of resources or on defined sets of resources called aggregates.  Access points define the properties needed to access the entity being aggregated and connection methods describe the protocol or other semantics of the connection.
///
/// This resource shall represent an aggregation service for a Redfish implementation.
pub const AggregationService = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// An indication of whether the aggregation service is enabled.
    ///
    /// This property shall indicate whether the aggregation service is enabled.
    ServiceEnabled: ?bool = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The link to the collection of aggregates associated with this service.
    ///
    /// This property shall contain a link to a resource collection of type `AggregateCollection`.
    Aggregates: ?core.NavProperty(aggregate_collection.AggregateCollection) = null,
    /// The link to the collection of aggregation sources associated with this service.
    ///
    /// This property shall contain a link to a resource collection of type `AggregationSourceCollection`.
    AggregationSources: ?core.NavProperty(aggregation_source_collection.AggregationSourceCollection) = null,
    /// The link to the collection of connection methods associated with this service.
    ///
    /// This property shall contain a link to a resource collection of type `ConnectionMethodCollection`.
    ConnectionMethods: ?core.NavProperty(connection_method_collection.ConnectionMethodCollection) = null,
};

/// What a client may change of `AggregationService.AggregationService`.
///
/// The `AggregationService` schema contains properties for managing aggregation operations, either on ad hoc combinations of resources or on defined sets of resources called aggregates.  Access points define the properties needed to access the entity being aggregated and connection methods describe the protocol or other semantics of the connection.
pub const AggregationServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether the aggregation service is enabled.
    ///
    /// This property shall indicate whether the aggregation service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
