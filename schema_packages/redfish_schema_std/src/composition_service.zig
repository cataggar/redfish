//! Namespace `CompositionService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const composition_reservation_collection = @import("composition_reservation_collection.zig");
const manifest = @import("manifest.zig");
const resource = @import("resource.zig");
const resource_block_collection = @import("resource_block_collection.zig");
const settings = @import("settings.zig");
const zone_collection = @import("zone_collection.zig");

pub const ComposeRequestFormat = enum {
    /// The request body contains a manifest.
    ///
    /// This value shall indicate that the request contains a manifest as defined by the Redfish `Manifest` schema.
    Manifest,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ComposeRequestType = enum {
    /// Preview the outcome of the operations specified by the manifest.
    ///
    /// This value shall indicate that the request is to preview the outcome of the operations specified by the manifest to show what the service will do based on the contents of the request, and not affect any resources within the service.
    Preview,
    /// Preview the outcome of the operations specified by the manifest and reserve resources.
    ///
    /// This value shall indicate that the request is to preview the outcome of the operations specified by the manifest to show what the service will do based on the contents of the request.  Resources that would have been affected by this request shall be marked as reserved but otherwise shall not be affected.
    PreviewReserve,
    /// Perform the requested operations specified by the manifest and modify resources as needed.
    ///
    /// This value shall indicate that the request is to apply the requested operations specified by the manifest and modify resources as needed.
    Apply,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `CompositionService.Compose`.
///
/// This action performs a set of operations specified by a manifest.
pub const CompositionServiceComposeAction = struct {
    /// The format of the request.
    ///
    /// This parameter shall contain the format of the request.
    RequestFormat: ?ComposeRequestFormat = null,
    /// The type of request.
    ///
    /// This parameter shall contain the type of request.
    RequestType: ?ComposeRequestType = null,
    /// The manifest containing the compose operation request.
    ///
    /// This parameter shall contain the manifest containing the compose operation request.  This parameter shall be required if `RequestFormat` contains the value `Manifest`.
    Manifest: ?manifest.ManifestUpdate = null,
    /// The identifier of the composition reservation if applying a reservation.  The value for this parameter is obtained from the response of a `Compose` action where the RequestType parameter contains the value `PreviewReserve`.
    ///
    /// This parameter shall contain the value of the `Id` property of the `CompositionReservation` resource for applying a reservation.
    ReservationId: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action performs a set of operations specified by a manifest.
    ///
    /// This action shall perform a set of operations specified by a manifest.  Services shall not apply any part of the manifest unless all operations specified by the manifest are successful.
    @"#CompositionService.Compose": ?core.Action(CompositionServiceComposeAction, ComposeResponse) = null,

    /// This action performs a set of operations specified by a manifest.
    ///
    /// This action shall perform a set of operations specified by a manifest.  Services shall not apply any part of the manifest unless all operations specified by the manifest are successful.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn compose(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: CompositionServiceComposeAction,
    ) !core.Owned(core.ModificationResponse(ComposeResponse)) {
        const target = self.@"#CompositionService.Compose" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The response body for the `Compose` action.
///
/// This type shall contain the properties found in the response body for the `Compose` action.
pub const ComposeResponse = struct {
    /// The format of the request.
    ///
    /// This property shall contain the format of the request.
    RequestFormat: ?ComposeRequestFormat = null,
    /// The type of request.
    ///
    /// This property shall contain the type of request.
    RequestType: ?ComposeRequestType = null,
    /// The manifest containing the compose operation response.
    ///
    /// This property shall contain the manifest containing the compose operation response.  This property shall be required if `RequestFormat` contains the value `Manifest`.
    Manifest: ?manifest.Manifest = null,
    /// The identifier of the composition reservation that was created.
    ///
    /// This property shall contain the value of the `Id` property of the `CompositionReservation` resource that was created.  This property shall be required if `RequestType` contains the value `PreviewReserve`.
    ReservationId: ?[]const u8 = null,
};

/// What a client may change of `CompositionService.ComposeResponse`.
///
/// The response body for the `Compose` action.
pub const ComposeResponseUpdate = struct {
    /// The manifest containing the compose operation response.
    ///
    /// This property shall contain the manifest containing the compose operation response.  This property shall be required if `RequestFormat` contains the value `Manifest`.
    Manifest: ?manifest.ManifestUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `CompositionService` schema describes a composition service and its properties and links to the resources available for composition.
///
/// This resource shall represent the composition service and its properties for a Redfish implementation.
pub const CompositionService = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: ?bool = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// An indication of whether this service is allowed to overprovision a composition relative to the composition request.
    ///
    /// This property shall indicate whether this service is allowed to overprovision a composition relative to the composition request.
    AllowOverprovisioning: ?bool = null,
    /// An indication of whether a client can request that a specific resource zone fulfill a composition request.
    ///
    /// This property shall indicate whether a client can request that a specific resource zone fulfill a composition request.
    AllowZoneAffinity: ?bool = null,
    /// The length of time a composition reservation is held before the service deletes the reservation and marks any related resource blocks as no longer reserved.
    ///
    /// This property shall contain the length of time a composition reservation is held before the service deletes the reservation and marks any related resource blocks as no longer reserved.
    ReservationDuration: ?core.Duration = null,
    /// The resource blocks available on the service.
    ///
    /// This property shall contain a link to a resource collection of type `ResourceBlockCollection`.
    ResourceBlocks: ?core.NavProperty(resource_block_collection.ResourceBlockCollection) = null,
    /// The resource zones available on the service.
    ///
    /// This property shall contain a link to a resource collection of type `ZoneCollection`.
    ResourceZones: ?core.NavProperty(zone_collection.ZoneCollection) = null,
    /// The link to the collection of resource blocks within the active pool.  Resource blocks in the active pool are contributing to at least one composed resource as a result of a composition request.
    ///
    /// This property shall contain a link to a resource collection of type `ResourceBlockCollection`.  The members of this collection shall represent the resource blocks in the active pool.  Services shall filter members of this collection based on the requesting client.
    ActivePool: ?core.NavProperty(resource_block_collection.ResourceBlockCollection) = null,
    /// The link to the collection of resource blocks within the free pool.  Resource blocks in the free pool are not contributing to any composed resources.
    ///
    /// This property shall contain a link to a resource collection of type `ResourceBlockCollection`.  The members of this collection shall represent the resource blocks in the free pool.  Services shall filter members of this collection based on the requesting client.
    FreePool: ?core.NavProperty(resource_block_collection.ResourceBlockCollection) = null,
    /// The link to the collection of reservations with the composition reservation collection.
    ///
    /// This property shall contain a link to a resource collection of type `CompositionReservationCollection`.  The members of this collection shall contain links to reserved resource blocks and the related document that caused the reservations.  Services shall filter members of this collection based on the requesting client.
    CompositionReservations: ?core.NavProperty(composition_reservation_collection.CompositionReservationCollection) = null,

    const closed = core.ClosedStruct(@This());
    pub const jsonParse = closed.jsonParse;
    pub const jsonParseFromValue = closed.jsonParseFromValue;
};

/// What a client may change of `CompositionService.CompositionService`.
///
/// The `CompositionService` schema describes a composition service and its properties and links to the resources available for composition.
pub const CompositionServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,
    /// An indication of whether this service is allowed to overprovision a composition relative to the composition request.
    ///
    /// This property shall indicate whether this service is allowed to overprovision a composition relative to the composition request.
    AllowOverprovisioning: core.Nullable(bool) = .absent,
    /// The length of time a composition reservation is held before the service deletes the reservation and marks any related resource blocks as no longer reserved.
    ///
    /// This property shall contain the length of time a composition reservation is held before the service deletes the reservation and marks any related resource blocks as no longer reserved.
    ReservationDuration: core.Nullable(core.Duration) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
