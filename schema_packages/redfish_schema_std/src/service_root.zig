//! Namespace `ServiceRoot`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const account_service = @import("account_service.zig");
const aggregation_service = @import("aggregation_service.zig");
const automation_node_collection = @import("automation_node_collection.zig");
const cable_collection = @import("cable_collection.zig");
const certificate_service = @import("certificate_service.zig");
const chassis_collection = @import("chassis_collection.zig");
const component_integrity_collection = @import("component_integrity_collection.zig");
const composition_service = @import("composition_service.zig");
const computer_system_collection = @import("computer_system_collection.zig");
const event_service = @import("event_service.zig");
const fabric_collection = @import("fabric_collection.zig");
const facility_collection = @import("facility_collection.zig");
const job_service = @import("job_service.zig");
const json_schema_file_collection = @import("json_schema_file_collection.zig");
const key_service = @import("key_service.zig");
const license_service = @import("license_service.zig");
const manager = @import("manager.zig");
const manager_collection = @import("manager_collection.zig");
const message_registry_file_collection = @import("message_registry_file_collection.zig");
const nvme_domain_collection = @import("nvme_domain_collection.zig");
const power_equipment = @import("power_equipment.zig");
const registered_client_collection = @import("registered_client_collection.zig");
const resource = @import("resource.zig");
const resource_block_collection = @import("resource_block_collection.zig");
const service_conditions = @import("service_conditions.zig");
const session_collection = @import("session_collection.zig");
const session_service = @import("session_service.zig");
const settings = @import("settings.zig");
const storage_collection = @import("storage_collection.zig");
const storage_service_collection = @import("storage_service_collection.zig");
const storage_system_collection = @import("storage_system_collection.zig");
const task_service = @import("task_service.zig");
const telemetry_service = @import("telemetry_service.zig");
const thermal_equipment = @import("thermal_equipment.zig");
const update_service = @import("update_service.zig");

/// The information about deep operations that the service supports.
///
/// This type shall contain information about deep operations that the service supports.
pub const DeepOperations = struct {
    /// An indication of whether the service supports the deep `PATCH` operation.
    ///
    /// This property shall indicate whether this service supports the Redfish Specification-defined deep `PATCH` operation.
    DeepPATCH: ?bool = null,
    /// An indication of whether the service supports the deep `POST` operation.
    ///
    /// This property shall indicate whether this service supports the Redfish Specification-defined deep `POST` operation.
    DeepPOST: ?bool = null,
    /// The maximum levels of resources allowed in deep operations.
    ///
    /// This property shall contain the maximum levels of resources allowed in deep operations.
    MaxLevels: ?i64 = null,
};

/// The information about the use of `$expand` in the service.
///
/// This type shall contain information about the support of the `$expand` query parameter by the service.
pub const Expand = struct {
    /// An indication of whether this service supports the tilde (`~`) option of the `$expand` query parameter.
    ///
    /// This property shall indicate whether this service supports the supports the tilde (~) option of the `$expand` query parameter.
    Links: ?bool = null,
    /// An indication of whether the service supports the period (`.`) option of the `$expand` query parameter.
    ///
    /// This property shall indicate whether the service supports the period (`.`) option of the `$expand` query parameter.
    NoLinks: ?bool = null,
    /// An indication of whether the service supports the asterisk (`*`) option of the `$expand` query parameter.
    ///
    /// This property shall indicate whether this service supports the asterisk (`*`) option of the `$expand` query parameter.
    ExpandAll: ?bool = null,
    /// An indication of whether the service supports the `$levels` option of the `$expand` query parameter.
    ///
    /// This property shall indicate whether the service supports the `$levels` option of the `$expand` query parameter.
    Levels: ?bool = null,
    /// The maximum `$levels` option value in the `$expand` query parameter.
    ///
    /// This property shall contain the maximum `$levels` option value in the `$expand` query parameter.  This property shall be present if the `Levels` property contains `true`.
    MaxLevels: ?i64 = null,
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The link to a collection of sessions.
    ///
    /// This property shall contain a link to a resource collection of type `SessionCollection`.
    Sessions: ?core.NavProperty(session_collection.SessionCollection) = null,
    /// The link to the manager that is providing this Redfish service.
    ///
    /// This property shall contain a link to a resource of type `Manager` that represents the manager providing this Redfish service.
    ManagerProvidingService: ?core.NavProperty(manager.Manager) = null,
};

/// What a client may change of `ServiceRoot.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const ProtocolFeaturesSupported = struct {
    /// The information about the use of `$expand` in the service.
    ///
    /// This property shall contain information about the support of the `$expand` query parameter by the service.
    ExpandQuery: ?Expand = null,
    /// An indication of whether the service supports the `$filter` query parameter.
    ///
    /// This property shall indicate whether this service supports the `$filter` query parameter.
    FilterQuery: ?bool = null,
    /// An indication of whether the service supports the `$select` query parameter.
    ///
    /// This property shall indicate whether this service supports the `$select` query parameter.
    SelectQuery: ?bool = null,
    /// An indication of whether the service supports the `excerpt` query parameter.
    ///
    /// This property shall indicate whether this service supports the `excerpt` query parameter.
    ExcerptQuery: ?bool = null,
    /// An indication of whether the service supports the `only` query parameter.
    ///
    /// This property shall indicate whether this service supports the `only` query parameter.
    OnlyMemberQuery: ?bool = null,
    /// The information about deep operations that the service supports.
    ///
    /// This property shall contain information about deep operations that the service supports.
    DeepOperations: ?DeepOperations = null,
    /// An indication of whether the service supports multiple outstanding HTTP requests.
    ///
    /// This property shall indicate whether this service supports multiple outstanding HTTP requests.
    MultipleHTTPRequests: ?bool = null,
    /// An indication of whether the service supports both the `$top` and `$skip` query parameters.
    ///
    /// This property shall indicate whether this service supports both the `$top` and `$skip` query parameters.
    TopSkipQuery: ?bool = null,
    /// An indication of whether the service supports the `eq`, `ge`, `gt`, `le`, `lt`, and `ne` options for the `$filter` query parameter.
    ///
    /// This property shall indicate whether the service supports the `eq`, `ge`, `gt`, `le`, `lt`, and `ne` options for the `$filter` query parameter.  This property shall not be present if `FilterQuery` contains `false`.
    FilterQueryComparisonOperations: ?bool = null,
    /// An indication of whether the service supports the `()`, `and`, `not`, and `or` options for the `$filter` query parameter.
    ///
    /// This property shall indicate whether the service supports the Redfish Specification-defined grouping operators `()`, `and`, `not`, and `or` options for the `$filter` query parameter.  This property shall not be present if `FilterQuery` contains `false`.
    FilterQueryCompoundOperations: ?bool = null,
    /// An indication of whether the service supports the `includeoriginofcondition` query parameter.
    ///
    /// This property shall indicate whether the service supports the `includeoriginofcondition` query parameter.
    IncludeOriginOfConditionQuery: ?bool = null,
    /// An indication of whether the service supports the `clientcontext` query parameter for incremental retrieval from resource collections.
    ///
    /// This property shall indicate whether this service supports the `clientcontext` query parameter for stateless incremental retrieval from resource collections, as described in the Redfish Specification.  This parameter is currently defined for `LogEntryCollection` resource collections and is designed to be extensible to other resource collections in future versions.
    ClientContextQuery: ?bool = null,
};

/// The `ServiceRoot` schema describes the root of the Redfish service, located at the '/redfish/v1' URI.  All other resources accessible through the Redfish interface on this device are linked directly or indirectly from the service root.
///
/// This resource shall represent the root of the Redfish service.
pub const ServiceRoot = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The version of the Redfish service.
    ///
    /// This property shall represent the Redfish protocol version, as specified in the 'Protocol version' clause of the Redfish Specification, to which this service conforms.
    RedfishVersion: ?[]const u8 = null,
    /// Unique identifier for a service instance.  When SSDP is used, this value contains the same UUID returned in an HTTP `200 OK` response from an SSDP `M-SEARCH` request during discovery.
    ///
    /// This property shall contain the identifier of the Redfish service instance.  If SSDP is used, this value shall contain the same UUID returned in an HTTP `200 OK` response from an SSDP `M-SEARCH` request during discovery.  RFC4122 describes methods to use to create a UUID value.  The value should be considered to be opaque.  Client software should only treat the overall value as a universally unique identifier and should not interpret any subfields within the UUID.
    UUID: ?core.Guid = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The product associated with this Redfish service.
    ///
    /// This property shall include the name of the product represented by this Redfish service.
    Product: ?[]const u8 = null,
    /// The information about protocol features that the service supports.
    ///
    /// This property shall contain information about protocol features that the service supports.
    ProtocolFeaturesSupported: ?ProtocolFeaturesSupported = null,
    /// The vendor or manufacturer associated with this Redfish service.
    ///
    /// This property shall include the name of the manufacturer or vendor represented by this Redfish service.  If this property is supported, the vendor name shall not be included in the `Product` property value.
    Vendor: ?[]const u8 = null,
    /// The vendor or user-provided product and service identifier.
    ///
    /// This property shall contain a vendor-provided or user-provided value that identifies and associates a discovered Redfish service with a particular product instance.  The value of the property shall contain the value of the `ServiceIdentification` property in the `Manager` resource providing the Redfish service root resource.  The value of this property is used in conjunction with the `Product` and `Vendor` properties to match user credentials or other a priori product instance information necessary for initial deployment to the correct, matching Redfish service.  This property shall not be present if the value of the `ServiceIdentification` property in the `Manager` resource providing the Redfish service root resource is an empty string or `null`.
    ServiceIdentification: ?[]const u8 = null,
    /// The usage notification message for this service.
    ///
    /// This property shall contain the usage notification message for this service.  The value of the property shall contain the value of the `ServiceUseNotification` property in the `Manager` resource providing the Redfish service root resource.  This property shall not be present if the value of the `ServiceUseNotification` property in the `Manager` resource providing the Redfish service root resource is an empty string or `null`.
    ServiceUseNotification: ?[]const u8 = null,
    /// The link to a collection of systems.
    ///
    /// This property shall contain a link to a resource collection of type `ComputerSystemCollection`.
    Systems: ?core.NavProperty(computer_system_collection.ComputerSystemCollection) = null,
    /// The link to a collection of chassis.
    ///
    /// This property shall contain a link to a resource collection of type `ChassisCollection`.
    Chassis: ?core.NavProperty(chassis_collection.ChassisCollection) = null,
    /// The link to a collection of managers.
    ///
    /// This property shall contain a link to a resource collection of type `ManagerCollection`.
    Managers: ?core.NavProperty(manager_collection.ManagerCollection) = null,
    /// The link to the task service.
    ///
    /// This property shall contain a link to a resource of type `TaskService`.
    Tasks: ?core.NavProperty(task_service.TaskService) = null,
    /// The link to the sessions service.
    ///
    /// This property shall contain a link to a resource of type `SessionService`.
    SessionService: ?core.NavProperty(session_service.SessionService) = null,
    /// The link to the account service.
    ///
    /// This property shall contain a link to a resource of type `AccountService`.
    AccountService: ?core.NavProperty(account_service.AccountService) = null,
    /// The link to the event service.
    ///
    /// This property shall contain a link to a resource of type `EventService`.
    EventService: ?core.NavProperty(event_service.EventService) = null,
    /// The link to a collection of registries.
    ///
    /// This property shall contain a link to a resource collection of type `MessageRegistryFileCollection`.
    Registries: ?core.NavProperty(message_registry_file_collection.MessageRegistryFileCollection) = null,
    /// The link to a collection of JSON Schema files.
    ///
    /// This property shall contain a link to a resource collection of type `JsonSchemaFileCollection`.
    JsonSchemas: ?core.NavProperty(json_schema_file_collection.JsonSchemaFileCollection) = null,
    /// The link to a collection of storage systems.
    ///
    /// This property shall contain a link to a resource collection of type `StorageSystemCollection`.  This collection shall contain computer systems that act as storage servers.  The `HostingRoles` property of each such computer system shall contain a `StorageServer` entry.
    StorageSystems: ?core.NavProperty(storage_system_collection.StorageSystemCollection) = null,
    /// The link to a collection of storage services.
    ///
    /// This property shall contain a link to a resource collection of type `StorageServiceCollection`.
    StorageServices: ?core.NavProperty(storage_service_collection.StorageServiceCollection) = null,
    /// The link to a collection of fabrics.
    ///
    /// This property shall contain a link to a resource collection of type `FabricCollection`.
    Fabrics: ?core.NavProperty(fabric_collection.FabricCollection) = null,
    /// The link to the update service.
    ///
    /// This property shall contain a link to a resource of type `UpdateService`.
    UpdateService: ?core.NavProperty(update_service.UpdateService) = null,
    /// The link to the composition service.
    ///
    /// This property shall contain a link to a resource of type `CompositionService`.
    CompositionService: ?core.NavProperty(composition_service.CompositionService) = null,
    /// The link to the job service.
    ///
    /// This property shall contain a link to a resource of type `JobService`.
    JobService: ?core.NavProperty(job_service.JobService) = null,
    /// The link to the telemetry service.
    ///
    /// This property shall contain a link to a resource of type `TelemetryService`.
    TelemetryService: ?core.NavProperty(telemetry_service.TelemetryService) = null,
    /// The link to the certificate service.
    ///
    /// This property shall contain a link to a resource of type `CertificateService`.
    CertificateService: ?core.NavProperty(certificate_service.CertificateService) = null,
    /// The link to a collection of resource blocks.  This collection is intended for implementations that do not contain a composition service but that expose resources to an orchestrator that implements a composition service.
    ///
    /// This property shall contain a link to a resource collection of type `ResourceBlockCollection`.
    ResourceBlocks: ?core.NavProperty(resource_block_collection.ResourceBlockCollection) = null,
    /// The link to a set of power equipment.
    ///
    /// This property shall contain a link to a resource of type `PowerEquipment`.
    PowerEquipment: ?core.NavProperty(power_equipment.PowerEquipment) = null,
    /// The link to a collection of facilities.
    ///
    /// This property shall contain a link to a resource collection of type `FacilityCollection`.
    Facilities: ?core.NavProperty(facility_collection.FacilityCollection) = null,
    /// The link to the aggregation service.
    ///
    /// This property shall contain a link to a resource of type `AggregationService`.
    AggregationService: ?core.NavProperty(aggregation_service.AggregationService) = null,
    /// The link to a collection of storage subsystems.
    ///
    /// This property shall contain a link to a resource collection of type `StorageCollection`.
    Storage: ?core.NavProperty(storage_collection.StorageCollection) = null,
    /// The link to a collection of NVMe domains.
    ///
    /// This property shall contain a link to a resource collection of type `NVMeDomainCollection`.
    NVMeDomains: ?core.NavProperty(nvme_domain_collection.NvmeDomainCollection) = null,
    /// The link to the key service.
    ///
    /// This property shall contain a link to a resource of type `KeyService`.
    KeyService: ?core.NavProperty(key_service.KeyService) = null,
    /// The link to a collection of cables.
    ///
    /// This property shall contain a link to a resource collection of type `CableCollection`.
    Cables: ?core.NavProperty(cable_collection.CableCollection) = null,
    /// The link to the license service.
    ///
    /// This property shall contain a link to a resource of type `LicenseService`.
    LicenseService: ?core.NavProperty(license_service.LicenseService) = null,
    /// The link to the service conditions.
    ///
    /// This property shall contain a link to a resource of type `ServiceConditions`.
    ServiceConditions: ?core.NavProperty(service_conditions.ServiceConditions) = null,
    /// The link to a collection of component integrity information.
    ///
    /// This property shall contain a link to a resource collection of type `ComponentIntegrityCollection`.
    ComponentIntegrity: ?core.NavProperty(component_integrity_collection.ComponentIntegrityCollection) = null,
    /// The link to a collection of registered clients.
    ///
    /// This property shall contain a link to a resource collection of type `RegisteredClientCollection`.
    RegisteredClients: ?core.NavProperty(registered_client_collection.RegisteredClientCollection) = null,
    /// The link to a set of cooling equipment.
    ///
    /// This property shall contain a link to a resource of type `ThermalEquipment`.
    ThermalEquipment: ?core.NavProperty(thermal_equipment.ThermalEquipment) = null,
    /// The link to a collection of automation nodes.
    ///
    /// This property shall contain a link to a resource collection of type `AutomationNodeCollection`.
    AutomationNodes: ?core.NavProperty(automation_node_collection.AutomationNodeCollection) = null,
};

test {
    std.testing.refAllDecls(@This());
}
