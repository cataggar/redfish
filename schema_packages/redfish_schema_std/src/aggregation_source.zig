//! Namespace `AggregationSource`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const connection_method = @import("connection_method.zig");
const key = @import("key.zig");
const key_collection = @import("key_collection.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");

pub const AggregationType = enum {
    /// Only notifications are aggregated.
    ///
    /// This value shall indicate that the aggregator is only aggregating notifications or events from the aggregation source according to the connection method used.  This value shall not be used with connection methods that do not include notifications.
    NotificationsOnly,
    /// Full aggregation according to connection method.
    ///
    /// This value shall indicate that the aggregator is performing full aggregation according to the connection method without any limitation such as only receiving notifications.
    Full,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpAuthenticationProtocols = enum {
    /// No authentication.
    ///
    /// This value shall indicate authentication is not required.
    None,
    /// Trap community string authentication.
    ///
    /// This value shall indicate authentication using SNMP community strings and the value of `TrapCommunity`.
    ///
    /// Deprecated in v1_6_0.
    /// This value has been deprecated to clarify that `AuthenticationProtocol` applies to SNMPv3 support, which does not support community string-based authentication.
    CommunityString,
    /// HMAC-MD5-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-MD5-96 authentication protocol.
    HMAC_MD5,
    /// HMAC-SHA-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-SHA-96 authentication protocol.
    HMAC_SHA96,
    /// HMAC-128-SHA-224 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC128SHA224AuthProtocol.
    HMAC128_SHA224,
    /// HMAC-192-SHA-256 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC192SHA256AuthProtocol.
    HMAC192_SHA256,
    /// HMAC-256-SHA-384 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC256SHA384AuthProtocol.
    HMAC256_SHA384,
    /// HMAC-384-SHA-512 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC384SHA512AuthProtocol.
    HMAC384_SHA512,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpEncryptionProtocols = enum {
    /// No encryption.
    ///
    /// This value shall indicate there is no encryption.
    None,
    /// CBC-DES encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3414-defined CBC-DES encryption protocol.
    CBC_DES,
    /// CFB128-AES-128 encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3826-defined CFB128-AES-128 encryption protocol.
    CFB128_AES128,
    /// CFB128-AES-192 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-192 encryption protocol, extended from RFC3826.
    CFB128_AES192,
    /// CFB128-AES-256 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-256 encryption protocol, extended from RFC3826.
    CFB128_AES256,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const UserAuthenticationMethod = enum {
    /// SSH user authentication with a public key.
    ///
    /// This value shall indicate SSH user authentication with a public key specified by the `PublicIdentityKey` property in `SSHSettings`.
    PublicKey,
    /// SSH user authentication with a password.
    ///
    /// This value shall indicate SSH user authentication with a password specified by the `Password` property.
    Password,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `AggregationSource.GenerateSSHIdentityKeyPair`.
///
/// This action generates a new SSH identity key-pair to be used with this aggregation source.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentityKey` property in `SSHSettings`.  Any existing key-pair is deleted and replaced by the new key-pair.
pub const AggregationSourceGenerateSshIdentityKeyPairAction = struct {
    /// The type of SSH key.
    ///
    /// This parameter shall contain the type of SSH key.
    KeyType: ?key.SshKeyType = null,
    /// The length of the SSH key, in bits, if the KeyType parameter contains `RSA`.
    ///
    /// This parameter shall contain the length of the SSH key, in bits.  This parameter shall be required if the `KeyType` parameter contains `RSA` and shall be rejected for other values.
    KeyLength: ?i64 = null,
    /// The curve to use with the SSH key if the KeyType parameter contains `ECDSA`.
    ///
    /// This parameter shall contain the curve to use with the SSH key.  This parameter shall be required if the `KeyType` parameter contains `ECDSA` and shall be rejected for other values.
    Curve: ?key.EcdsaCurveType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `AggregationSource.RemoveSSHIdentityKeyPair`.
///
/// This action removes the SSH identity key-pair used with this aggregation source.
pub const AggregationSourceRemoveSshIdentityKeyPairAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action generates a new SSH identity key-pair to be used with this aggregation source.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentityKey` property in `SSHSettings`.  Any existing key-pair is deleted and replaced by the new key-pair.
    ///
    /// This action shall generate a new SSH identity key-pair to be used with this aggregation source.  The service shall store the generated public key in the `Key` resource referenced by the `PublicIdentityKey` property in `SSHSettings`.  If the aggregation source already has an associated SSH identity key-pair, the service shall delete the key-pair and replace it with the new key-pair.
    @"#AggregationSource.GenerateSSHIdentityKeyPair": ?core.Action(AggregationSourceGenerateSshIdentityKeyPairAction, std.json.Value) = null,
    /// This action removes the SSH identity key-pair used with this aggregation source.
    ///
    /// This action shall remove the private SSH identity key-pair used with this aggregation source.
    @"#AggregationSource.RemoveSSHIdentityKeyPair": ?core.Action(AggregationSourceRemoveSshIdentityKeyPairAction, std.json.Value) = null,

    /// This action generates a new SSH identity key-pair to be used with this aggregation source.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentityKey` property in `SSHSettings`.  Any existing key-pair is deleted and replaced by the new key-pair.
    ///
    /// This action shall generate a new SSH identity key-pair to be used with this aggregation source.  The service shall store the generated public key in the `Key` resource referenced by the `PublicIdentityKey` property in `SSHSettings`.  If the aggregation source already has an associated SSH identity key-pair, the service shall delete the key-pair and replace it with the new key-pair.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn generateSshIdentityKeyPair(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregationSourceGenerateSshIdentityKeyPairAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AggregationSource.GenerateSSHIdentityKeyPair" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action removes the SSH identity key-pair used with this aggregation source.
    ///
    /// This action shall remove the private SSH identity key-pair used with this aggregation source.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn removeSshIdentityKeyPair(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregationSourceRemoveSshIdentityKeyPairAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#AggregationSource.RemoveSSHIdentityKeyPair" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// A link to the connection method used to contact this aggregation source.
    ///
    /// This property shall a link to a resource of type `ConnectionMethod` that represents the method used to connect to the aggregation source.
    ConnectionMethod: ?core.NavProperty(connection_method.ConnectionMethod) = null,
    /// An array links to the resources added to the service through this aggregation source.  It is recommended that this be the minimal number of properties needed to find the resources that would be lost when the aggregation source is deleted.
    ///
    /// This property shall contain an array of links to the resources added to the service through the aggregation source.  It is recommended that this be the minimal number of properties needed to find the resources that would be lost when the aggregation source is deleted.  For example, this could be the pointers to the members of the root-level collections or the manager of a BMC.
    ResourcesAccessed: ?[]const core.NavProperty(resource.Resource) = null,
    /// How many members `ResourcesAccessed` has in total, which is not how many this response carries.
    @"ResourcesAccessed@odata.count": ?i64 = null,
    /// The next page of `ResourcesAccessed`. Present only when this response left members out.
    @"ResourcesAccessed@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `AggregationSource.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const SnmpSettings = struct {
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: ?SnmpAuthenticationProtocols = null,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: ?SnmpEncryptionProtocols = null,
    /// Indicates if the `AuthenticationKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `AuthenticationKey` property.  Otherwise, the property shall contain `false`.
    AuthenticationKeySet: ?bool = null,
    /// Indicates if the `EncryptionKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `EncryptionKey` property.  Otherwise, the property shall contain `false`.
    EncryptionKeySet: ?bool = null,
    /// The SNMP community string.
    ///
    /// This property shall contain the SNMP community string used for accessing an SNMP service at the aggregation source.  This value shall be `null` in responses.
    CommunityString: ?[]const u8 = null,
};

/// What a client may change of `AggregationSource.SNMPSettings`.
pub const SnmpSettingsUpdate = struct {
    /// The secret authentication key for SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 authentication.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    AuthenticationKey: core.Nullable([]const u8) = .absent,
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: core.Nullable(SnmpAuthenticationProtocols) = .absent,
    /// The secret authentication key for SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 encryption.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    EncryptionKey: core.Nullable([]const u8) = .absent,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: core.Nullable(SnmpEncryptionProtocols) = .absent,
    /// The SNMP trap community string.
    ///
    /// This property shall contain the SNMP trap community string.  The value shall be `null` in responses.  Services may provide a common trap community if not specified by the client when creating the aggregation source.
    TrapCommunity: core.Nullable([]const u8) = .absent,
    /// The SNMP community string.
    ///
    /// This property shall contain the SNMP community string used for accessing an SNMP service at the aggregation source.  This value shall be `null` in responses.
    CommunityString: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Settings for an aggregation source using SSH as part of the associated connection method.
///
/// This type shall contain the settings for an aggregation source using SSH as part of the associated connection method.
pub const SshSettingsType = struct {
    /// The client user authentication method.
    ///
    /// This property shall contain the client user authentication method.
    UserAuthenticationMethod: ?UserAuthenticationMethod = null,
    /// The date and time when the key referenced by the `PresentedPublicHostKey` property was last updated.
    ///
    /// This property shall contain the date and time when the key referenced by the `PresentedPublicHostKey` property was last updated.
    PresentedPublicHostKeyTimestamp: ?core.DateTimeOffset = null,
    /// A link to the trusted public host keys of the remote service corresponding to the aggregation source.  These trusted public host keys are used for authentication of the remote service with SSH.  An SSH public host key of the remote service can be added to this collection to allow for public key-based SSH authentication.
    ///
    /// This property shall contain a link to a resource collection of type `KeyCollection` that represents the trusted public host keys of the remote service corresponding to the aggregation source.  If the associated connection method specifies SSH tunneling, the service shall compare the public host key presented by the remote service with members of this collection to determine if the remote service can be trusted.  If the remote service cannot be trusted, the `State` property within `Status` shall contain `Disabled` and the service shall not connect to the remote service.
    TrustedPublicHostKeys: ?core.NavProperty(key_collection.KeyCollection) = null,
    /// A link to the last public host key presented by the remote service corresponding to the aggregation source.  A client that trusts this public host key can add the public host key to the TrustedPublicHostKeys collection to allow SSH communication with the aggregation source.
    ///
    /// This property shall contain a link to a resource of type `Key` that represents the last public host key presented by the remote service corresponding to the aggregation source.  This property shall not be present if a public host key has not yet been presented by the remote service.
    PresentedPublicHostKey: ?core.NavProperty(key.Key) = null,
    /// A link to the public key that is used with the aggregation source when the authentication method is configured to use a public key.  The GenerateSSHIdentityKeyPair and RemoveSSHIdentityKeyPair are used to update the key for this aggregation source.
    ///
    /// This property shall contain a link to a resource of type `Key` that represents the public key that is used with the aggregation source when `UserAuthenticationMethod` contains `PublicKey`.  This property shall not be present if a key-pair is not available.  The `State` property within `Status` shall contain `Disabled` if a key-pair is not available and `UserAuthenticationMethod` contains `PublicKey`.
    PublicIdentityKey: ?core.NavProperty(key.Key) = null,

    const closed = core.ClosedStruct(@This());
    pub const jsonParse = closed.jsonParse;
    pub const jsonParseFromValue = closed.jsonParseFromValue;
};

/// What a client may change of `AggregationSource.SSHSettingsType`.
///
/// Settings for an aggregation source using SSH as part of the associated connection method.
pub const SshSettingsTypeUpdate = struct {
    /// The client user authentication method.
    ///
    /// This property shall contain the client user authentication method.
    UserAuthenticationMethod: core.Nullable(UserAuthenticationMethod) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `AggregationSource` schema is used to represent the source of information for a subset of the resources provided by a Redfish service.  It can be thought of as a provider of information.  As such, most such interfaces have requirements to support the gathering of information like address and account used to access the information.
///
/// This resource shall represent an aggregation source for a Redfish implementation.
pub const AggregationSource = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The URI of the system to be accessed.
    ///
    /// This property shall contain the URI of the system to be aggregated.  This property shall not be required when the aggregation source is configured to only receive notifications from the aggregated system and the `AggregationType` property contains the value `NotificationsOnly`.
    HostName: ?[]const u8 = null,
    /// The username for accessing the aggregation source.
    ///
    /// This property shall contain the username for accessing the aggregation source.
    UserName: ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// SNMP settings of the aggregation source.
    ///
    /// This property shall contain the SNMP settings of the aggregation source.
    SNMP: ?SnmpSettings = null,
    /// The type of aggregation used towards the aggregation source.
    ///
    /// This property shall contain the type of aggregation used for the connection method towards the aggregation source.  If this property is not present, the value shall be assumed to be `Full`.
    AggregationType: ?AggregationType = null,
    /// Settings for an aggregation source using SSH as part of the associated connection method.
    ///
    /// This property shall contain the settings for an aggregation source using SSH as part of the associated connection method.
    SSHSettings: ?SshSettingsType = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The Modbus identifier of this aggregation source.
    ///
    /// This property shall contain the Modbus identifier of this aggregation source.
    ModbusTargetServerId: ?i64 = null,
    /// The network port.
    ///
    /// This property shall contain the network port used to connect to this aggregation source.
    Port: ?i64 = null,
};

/// What a client may change of `AggregationSource.AggregationSource`.
///
/// The `AggregationSource` schema is used to represent the source of information for a subset of the resources provided by a Redfish service.  It can be thought of as a provider of information.  As such, most such interfaces have requirements to support the gathering of information like address and account used to access the information.
pub const AggregationSourceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The URI of the system to be accessed.
    ///
    /// This property shall contain the URI of the system to be aggregated.  This property shall not be required when the aggregation source is configured to only receive notifications from the aggregated system and the `AggregationType` property contains the value `NotificationsOnly`.
    HostName: core.Nullable([]const u8) = .absent,
    /// The username for accessing the aggregation source.
    ///
    /// This property shall contain the username for accessing the aggregation source.
    UserName: core.Nullable([]const u8) = .absent,
    /// The password for accessing the aggregation source.  The value is `null` in responses.
    ///
    /// This property shall contain a password for accessing the aggregation source.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// SNMP settings of the aggregation source.
    ///
    /// This property shall contain the SNMP settings of the aggregation source.
    SNMP: ?SnmpSettingsUpdate = null,
    /// The type of aggregation used towards the aggregation source.
    ///
    /// This property shall contain the type of aggregation used for the connection method towards the aggregation source.  If this property is not present, the value shall be assumed to be `Full`.
    AggregationType: ?AggregationType = null,
    /// Settings for an aggregation source using SSH as part of the associated connection method.
    ///
    /// This property shall contain the settings for an aggregation source using SSH as part of the associated connection method.
    SSHSettings: ?SshSettingsTypeUpdate = null,
    /// The Modbus identifier of this aggregation source.
    ///
    /// This property shall contain the Modbus identifier of this aggregation source.
    ModbusTargetServerId: core.Nullable(i64) = .absent,
    /// The network port.
    ///
    /// This property shall contain the network port used to connect to this aggregation source.
    Port: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `AggregationSource.AggregationSource`.
///
/// The `AggregationSource` schema is used to represent the source of information for a subset of the resources provided by a Redfish service.  It can be thought of as a provider of information.  As such, most such interfaces have requirements to support the gathering of information like address and account used to access the information.
pub const AggregationSourceCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The URI of the system to be accessed.
    ///
    /// This property shall contain the URI of the system to be aggregated.  This property shall not be required when the aggregation source is configured to only receive notifications from the aggregated system and the `AggregationType` property contains the value `NotificationsOnly`.
    HostName: core.Nullable([]const u8) = .absent,
    /// The username for accessing the aggregation source.
    ///
    /// This property shall contain the username for accessing the aggregation source.
    UserName: core.Nullable([]const u8) = .absent,
    /// The password for accessing the aggregation source.  The value is `null` in responses.
    ///
    /// This property shall contain a password for accessing the aggregation source.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// SNMP settings of the aggregation source.
    ///
    /// This property shall contain the SNMP settings of the aggregation source.
    SNMP: ?SnmpSettingsUpdate = null,
    /// The type of aggregation used towards the aggregation source.
    ///
    /// This property shall contain the type of aggregation used for the connection method towards the aggregation source.  If this property is not present, the value shall be assumed to be `Full`.
    AggregationType: ?AggregationType = null,
    /// Settings for an aggregation source using SSH as part of the associated connection method.
    ///
    /// This property shall contain the settings for an aggregation source using SSH as part of the associated connection method.
    SSHSettings: ?SshSettingsTypeUpdate = null,
    /// The Modbus identifier of this aggregation source.
    ///
    /// This property shall contain the Modbus identifier of this aggregation source.
    ModbusTargetServerId: core.Nullable(i64) = .absent,
    /// The network port.
    ///
    /// This property shall contain the network port used to connect to this aggregation source.
    Port: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
