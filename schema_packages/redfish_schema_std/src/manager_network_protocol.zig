//! Namespace `ManagerNetworkProtocol`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const certificate_collection = @import("certificate_collection.zig");
const resource = @import("resource.zig");

pub const Ipv6addressGenerationMode = enum {
    /// Stable privacy address generation.
    ///
    /// This value shall indicate stable privacy address generation.
    StablePrivacy,
    /// EUI-64 address generation.
    ///
    /// This value shall indicate EUI-64 address generation.
    EUI64,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NotifyIpv6scope = enum {
    /// SSDP NOTIFY messages are sent to addresses in the IPv6 local link scope.
    Link,
    /// SSDP NOTIFY messages are sent to addresses in the IPv6 local site scope.
    Site,
    /// SSDP NOTIFY messages are sent to addresses in the IPv6 local organization scope.
    Organization,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpAuthenticationProtocols = enum {
    /// Authentication is determined by account settings.
    ///
    /// This value shall indicate authentication for SNMPv3 access is determined based on the corresponding account settings.
    Account,
    /// SNMP community string authentication.
    ///
    /// This value shall indicate authentication uses SNMP community strings.
    ///
    /// Deprecated in v1_13_0.
    /// This value has been deprecated to clarify that `AuthenticationProtocol` applies to SNMPv3 support, which does not support community string-based authentication.
    CommunityString,
    /// HMAC-MD5-96 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC3414-defined HMAC-MD5-96 authentication protocol.
    HMAC_MD5,
    /// HMAC-SHA-96 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC3414-defined HMAC-SHA-96 authentication protocol.
    HMAC_SHA96,
    /// HMAC-128-SHA-224 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC128SHA224AuthProtocol.
    HMAC128_SHA224,
    /// HMAC-192-SHA-256 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC192SHA256AuthProtocol.
    HMAC192_SHA256,
    /// HMAC-256-SHA-384 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC256SHA384AuthProtocol.
    HMAC256_SHA384,
    /// HMAC-384-SHA-512 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC384SHA512AuthProtocol.
    HMAC384_SHA512,
    /// No authentication.
    ///
    /// This value shall indicate authentication is not required.
    None,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpCommunityAccessMode = enum {
    /// READ-WRITE access mode.
    ///
    /// This value shall indicate the RFC1157-defined READ-WRITE access mode.
    Full,
    /// READ-ONLY access mode.
    ///
    /// This value shall indicate the RFC1157-defined READ-ONLY access mode.
    Limited,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpEncryptionProtocols = enum {
    /// No encryption.
    ///
    /// This value shall indicate there is no encryption.
    None,
    /// Encryption is determined by account settings.
    ///
    /// This value shall indicate encryption is determined based on the corresponding account settings.
    Account,
    /// CBC-DES encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3414-defined CBC-DES encryption protocol.
    CBC_DES,
    /// CFB128-AES-128 encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3826-defined CFB128-AES-128 encryption protocol.
    CFB128_AES128,
    /// CFB128-AES-192 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-192 encryption protocol, extended from RFC3826.
    CFB128_AES192,
    /// CFB128-AES-256 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-256 encryption protocol, extended from RFC3826.
    CFB128_AES256,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SshPreferredAuthentication = enum {
    /// Password-based SSH authentication as defined in RFC4252.
    Password,
    /// Public key-based SSH authentication as defined in RFC4252.
    PublicKey,
    /// Keyboard-interactive authentication as defined in RFC4256.
    KeyboardInteractive,
    /// Generic Security Service Application Program Interface (GSS-API)-based authentication with a Message Integrity Code (MIC) as defined in RFC4462.
    GSSAPIWithMIC,
    /// Host-based authentication as defined in RFC4252.
    HostBased,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

pub const EngineId = struct {
    /// The private enterprise ID.
    ///
    /// This property shall contain an RFC3411-defined private enterprise ID.
    PrivateEnterpriseId: ?[]const u8 = null,
    /// The enterprise-specific method.
    ///
    /// This property shall contain the enterprise-specific method as described in item 2 of the snmpEngineID syntax of RFC3411.  The full RFC3411-defined snmpEngineID is formed from the concatenation of the value in the `PrivateEnterpriseId` property and the value in this property.  If the most significant bit in `PrivateEnterpriseId` is set to one, this property shall not be present.
    EnterpriseSpecificMethod: ?[]const u8 = null,
    /// The architecture identifier.
    ///
    /// This property shall contain the architecture identifier as described in item 3 of the snmpEngineID syntax of RFC3411.  The full RFC3411-defined snmpEngineID is formed from the concatenation of the value in the `PrivateEnterpriseId` property and the value in this property.  If the most significant bit in `PrivateEnterpriseId` is set to zero, this property shall not be present.
    ArchitectureId: ?[]const u8 = null,
};

/// What a client may change of `ManagerNetworkProtocol.EngineId`.
pub const EngineIdUpdate = struct {
    /// The enterprise-specific method.
    ///
    /// This property shall contain the enterprise-specific method as described in item 2 of the snmpEngineID syntax of RFC3411.  The full RFC3411-defined snmpEngineID is formed from the concatenation of the value in the `PrivateEnterpriseId` property and the value in this property.  If the most significant bit in `PrivateEnterpriseId` is set to one, this property shall not be present.
    EnterpriseSpecificMethod: core.Nullable([]const u8) = .absent,
    /// The architecture identifier.
    ///
    /// This property shall contain the architecture identifier as described in item 3 of the snmpEngineID syntax of RFC3411.  The full RFC3411-defined snmpEngineID is formed from the concatenation of the value in the `PrivateEnterpriseId` property and the value in this property.  If the most significant bit in `PrivateEnterpriseId` is set to zero, this property shall not be present.
    ArchitectureId: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const HttpsProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// The link to a collection of certificates used for HTTPS by this manager.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection`.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
};

/// What a client may change of `ManagerNetworkProtocol.HTTPSProtocol`.
pub const HttpsProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const ModbusProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// The server identifier for this Modbus TCP protocol interface.
    ///
    /// This property shall contain the Modbus Messaging On TCP/IP Implementation Guide-defined 'Unit Identifier' that identifies this Modbus TCP protocol server.
    ServerId: ?i64 = null,
    /// The maximum number of simultaneously connected clients.
    ///
    /// This property shall contain the maximum number of simultaneously connected clients to this Modbus TCP server.
    MaximumConnectedClients: ?i64 = null,
    /// The current number of connected clients.
    ///
    /// This property shall contain the current number of connected clients to this Modbus TCP server.
    NumberOfConnectedClients: ?i64 = null,
    /// Indicates if the interface is read only.
    ///
    /// This property shall indicate if the Modbus TCP server protocol interface is read only.  If `true`, the Modbus TCP server service on this manager shall reject or ignore Modbus requests that modify data.
    ReadOnly: ?bool = null,
    /// Indicates if access to this Modbus TCP server protocol interface is restricted to allowed clients.
    ///
    /// This property shall indicate if access to the Modbus TCP server protocol interface is restricted to allowed clients.  If `true`, the Modbus TCP server service on this manager shall restrict access to the list of clients defined by the `AllowedClients` property.
    RestrictAccessToAllowedClients: ?bool = null,
    /// Indicates the list of allowed clients for this Modbus TCP server interface.
    ///
    /// This property shall contain all the clients allowed to access this Modbus TCP server interface.  If `RestrictAccessToAllowedClients` contains `true`, the service shall enforce this restriction.  If `RestrictAccessToAllowedClients` contains `false`, the service shall not enforce this restriction.  This property may contain FQDN, IPv4 addresses, IPv6 addresses, or any combination of those formats.  Services may reject unsupported formats.
    AllowedClients: ?[]const ?[]const u8 = null,
};

/// What a client may change of `ManagerNetworkProtocol.ModbusProtocol`.
pub const ModbusProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,
    /// The server identifier for this Modbus TCP protocol interface.
    ///
    /// This property shall contain the Modbus Messaging On TCP/IP Implementation Guide-defined 'Unit Identifier' that identifies this Modbus TCP protocol server.
    ServerId: core.Nullable(i64) = .absent,
    /// The maximum number of simultaneously connected clients.
    ///
    /// This property shall contain the maximum number of simultaneously connected clients to this Modbus TCP server.
    MaximumConnectedClients: core.Nullable(i64) = .absent,
    /// Indicates if the interface is read only.
    ///
    /// This property shall indicate if the Modbus TCP server protocol interface is read only.  If `true`, the Modbus TCP server service on this manager shall reject or ignore Modbus requests that modify data.
    ReadOnly: core.Nullable(bool) = .absent,
    /// Indicates if access to this Modbus TCP server protocol interface is restricted to allowed clients.
    ///
    /// This property shall indicate if access to the Modbus TCP server protocol interface is restricted to allowed clients.  If `true`, the Modbus TCP server service on this manager shall restrict access to the list of clients defined by the `AllowedClients` property.
    RestrictAccessToAllowedClients: core.Nullable(bool) = .absent,
    /// Indicates the list of allowed clients for this Modbus TCP server interface.
    ///
    /// This property shall contain all the clients allowed to access this Modbus TCP server interface.  If `RestrictAccessToAllowedClients` contains `true`, the service shall enforce this restriction.  If `RestrictAccessToAllowedClients` contains `false`, the service shall not enforce this restriction.  This property may contain FQDN, IPv4 addresses, IPv6 addresses, or any combination of those formats.  Services may reject unsupported formats.
    AllowedClients: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const NtpProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// Indicates to which user-supplied NTP servers this manager is subscribed.
    ///
    /// This property shall contain all the user-supplied NTP servers for which this manager is using to obtain time.  `NetworkSuppliedServers` is used for NTP servers supplied by other network protocols such as DHCP.
    NTPServers: ?[]const ?[]const u8 = null,
    /// The NTP servers supplied by other network protocols to this manager.
    ///
    /// This property shall contain the NTP servers supplied by other network protocols to this manager.  DHCP is an example of a protocol that can supply NTP servers to this manager.
    NetworkSuppliedServers: ?[]const ?[]const u8 = null,
};

/// What a client may change of `ManagerNetworkProtocol.NTPProtocol`.
pub const NtpProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,
    /// Indicates to which user-supplied NTP servers this manager is subscribed.
    ///
    /// This property shall contain all the user-supplied NTP servers for which this manager is using to obtain time.  `NetworkSuppliedServers` is used for NTP servers supplied by other network protocols such as DHCP.
    NTPServers: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The settings for a network protocol associated with a manager.
///
/// This type shall describe information about a protocol setting for a manager.
pub const Protocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
};

/// What a client may change of `ManagerNetworkProtocol.Protocol`.
///
/// The settings for a network protocol associated with a manager.
pub const ProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The HTTP/HTTPS proxy information for a manager.
///
/// This property shall contain the HTTP/HTTPS proxy configuration for a manager.
pub const ProxyConfiguration = struct {
    /// Indicates if the manager uses the proxy server.
    ///
    /// This property shall indicate if the proxy server is used for communications.
    Enabled: ?bool = null,
    /// The URI of the proxy server, including the scheme and any non-default port value.
    ///
    /// This property shall contain the URI of the proxy server.  The value shall contain the scheme for accessing the server, and shall include the port if the value is not the default port for the specified scheme.
    ProxyServerURI: ?[]const u8 = null,
    /// The username for the proxy.
    ///
    /// This property shall contain the username for this proxy.
    Username: ?[]const u8 = null,
    /// Indicates if the `Password` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `Password` property.  Otherwise, the property shall contain `false`.
    PasswordSet: ?bool = null,
    /// Addresses that do not require the proxy server to access.
    ///
    /// This property shall contain a list of hostnames or IP addresses that do not require a connection through the proxy server to access.
    ExcludeAddresses: ?[]const ?[]const u8 = null,
    /// The URI used to access a proxy auto-configuration (PAC) file.
    ///
    /// This property shall contain the URI at which to access a proxy auto-configuration (PAC) file containing one or more JavaScript functions for configuring proxy usage for this manager.
    ProxyAutoConfigURI: ?[]const u8 = null,
};

/// What a client may change of `ManagerNetworkProtocol.ProxyConfiguration`.
///
/// The HTTP/HTTPS proxy information for a manager.
pub const ProxyConfigurationUpdate = struct {
    /// Indicates if the manager uses the proxy server.
    ///
    /// This property shall indicate if the proxy server is used for communications.
    Enabled: ?bool = null,
    /// The URI of the proxy server, including the scheme and any non-default port value.
    ///
    /// This property shall contain the URI of the proxy server.  The value shall contain the scheme for accessing the server, and shall include the port if the value is not the default port for the specified scheme.
    ProxyServerURI: ?[]const u8 = null,
    /// The username for the proxy.
    ///
    /// This property shall contain the username for this proxy.
    Username: ?[]const u8 = null,
    /// The password for the proxy.  The value is `null` in responses.
    ///
    /// This property shall contain the password for this proxy.  The value shall be `null` in responses.
    Password: core.Nullable([]const u8) = .absent,
    /// Addresses that do not require the proxy server to access.
    ///
    /// This property shall contain a list of hostnames or IP addresses that do not require a connection through the proxy server to access.
    ExcludeAddresses: ?[]const ?[]const u8 = null,
    /// The URI used to access a proxy auto-configuration (PAC) file.
    ///
    /// This property shall contain the URI at which to access a proxy auto-configuration (PAC) file containing one or more JavaScript functions for configuring proxy usage for this manager.
    ProxyAutoConfigURI: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const SnmpCommunity = struct {
    /// The name of the SNMP community.
    ///
    /// This property shall contain a display name describing the SNMP community.
    Name: ?[]const u8 = null,
    /// The SNMP community string.
    ///
    /// This property shall contain the SNMP community string used for accessing an SNMP service on this manager.  If `HideCommunityStrings` is `true`, this value shall be `null` in responses.
    CommunityString: ?[]const u8 = null,
    /// The access level of the SNMP community.
    ///
    /// This property shall contain the access/privilege level of the SNMP community used to access an SNMP manager.
    AccessMode: ?SnmpCommunityAccessMode = null,
    /// Indicates if this community is restricted to accessing the service from a range of IPv4 addresses.
    ///
    /// This property shall indicate if this community is restricted to accessing the service from a range of IPv4 addresses.  If `true`, SNMP access using this community string is restricted to the range of IPv4 addresses defined by the `IPv4AddressRangeLower` and `IPv4AddressRangeUpper` properties.
    RestrictCommunityToIPv4AddressRange: ?bool = null,
    /// The lowest IPv4 address in the range allowed to access the service.
    ///
    /// This property shall contain the lowest IPv4 address in the range allowed to access the SNMP service using this community string.  If `RestrictCommunityToIPv4AddressRange` contains `true`, the service shall enforce this range.  If `RestrictCommunityToIPv4AddressRange` contains `false`, the service shall not enforce this range.
    IPv4AddressRangeLower: ?[]const u8 = null,
    /// The highest IPv4 address in the range allowed to access the service.
    ///
    /// This property shall contain the upper or highest IPv4 address in the range allowed to access the SNMP service using this community string.  If `RestrictCommunityToIPv4AddressRange` contains `true`, the service shall enforce this range.  If `RestrictCommunityToIPv4AddressRange` contains `false`, the service shall not enforce this range.
    IPv4AddressRangeUpper: ?[]const u8 = null,
};

/// What a client may change of `ManagerNetworkProtocol.SNMPCommunity`.
pub const SnmpCommunityUpdate = struct {
    /// The name of the SNMP community.
    ///
    /// This property shall contain a display name describing the SNMP community.
    Name: core.Nullable([]const u8) = .absent,
    /// The SNMP community string.
    ///
    /// This property shall contain the SNMP community string used for accessing an SNMP service on this manager.  If `HideCommunityStrings` is `true`, this value shall be `null` in responses.
    CommunityString: core.Nullable([]const u8) = .absent,
    /// The access level of the SNMP community.
    ///
    /// This property shall contain the access/privilege level of the SNMP community used to access an SNMP manager.
    AccessMode: core.Nullable(SnmpCommunityAccessMode) = .absent,
    /// The lowest IPv4 address in the range allowed to access the service.
    ///
    /// This property shall contain the lowest IPv4 address in the range allowed to access the SNMP service using this community string.  If `RestrictCommunityToIPv4AddressRange` contains `true`, the service shall enforce this range.  If `RestrictCommunityToIPv4AddressRange` contains `false`, the service shall not enforce this range.
    IPv4AddressRangeLower: core.Nullable([]const u8) = .absent,
    /// The highest IPv4 address in the range allowed to access the service.
    ///
    /// This property shall contain the upper or highest IPv4 address in the range allowed to access the SNMP service using this community string.  If `RestrictCommunityToIPv4AddressRange` contains `true`, the service shall enforce this range.  If `RestrictCommunityToIPv4AddressRange` contains `false`, the service shall not enforce this range.
    IPv4AddressRangeUpper: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const SnmpProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// Indicates if access via SNMPv1 is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv1 protocol is enabled.
    EnableSNMPv1: ?bool = null,
    /// Indicates if access via SNMPv2c is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv2c protocol is enabled.
    EnableSNMPv2c: ?bool = null,
    /// Indicates if access via SNMPv3 is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv3 protocol is enabled.
    EnableSNMPv3: ?bool = null,
    /// The SNMP community strings.
    ///
    /// This property shall contain an array of the SNMP community strings used to access an SNMP manager.
    CommunityStrings: ?[]const ?SnmpCommunity = null,
    /// The access level of the SNMP community.
    ///
    /// This property shall contain the access/privilege level of the SNMP community used to access an SNMP manager.
    ///
    /// Deprecated in v1_10_0.
    /// This property has been deprecated in favor of `AccessMode` inside `CommunityStrings`.
    CommunityAccessMode: ?SnmpCommunityAccessMode = null,
    /// The engine ID.
    ///
    /// This property shall contain the RFC3411-defined engine ID.
    EngineId: ?EngineId = null,
    /// Indicates if the community strings should be hidden.
    ///
    /// This property shall indicate if the community strings should be hidden in responses.
    HideCommunityStrings: ?bool = null,
    /// The authentication protocol used for SNMPv3 access to this manager.
    ///
    /// This property shall contain the SNMPv3 authentication protocol used to access this manager.  When the property contains the value `Account`, the SNMP settings in each manager account are used for authentication.  SNMPv1 and SNMPv2c protocols, if enabled, use community string-based authentication.
    AuthenticationProtocol: ?SnmpAuthenticationProtocols = null,
    /// The encryption protocol used for SNMPv3 access to this manager.
    ///
    /// This property shall contain the SNMPv3 encryption protocol used to access this manager, unless `AuthenticationProtocol` contains the value `Account`.
    EncryptionProtocol: ?SnmpEncryptionProtocols = null,
    /// The SNMP trap port.
    ///
    /// This property shall contain the port assigned to SNMP traps.
    TrapPort: ?i64 = null,
};

/// What a client may change of `ManagerNetworkProtocol.SNMPProtocol`.
pub const SnmpProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,
    /// Indicates if access via SNMPv1 is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv1 protocol is enabled.
    EnableSNMPv1: core.Nullable(bool) = .absent,
    /// Indicates if access via SNMPv2c is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv2c protocol is enabled.
    EnableSNMPv2c: core.Nullable(bool) = .absent,
    /// Indicates if access via SNMPv3 is enabled.
    ///
    /// This property shall indicate if access to the SNMP service on this manager using the SNMPv3 protocol is enabled.
    EnableSNMPv3: core.Nullable(bool) = .absent,
    /// The SNMP community strings.
    ///
    /// This property shall contain an array of the SNMP community strings used to access an SNMP manager.
    CommunityStrings: ?[]const ?SnmpCommunityUpdate = null,
    /// The access level of the SNMP community.
    ///
    /// This property shall contain the access/privilege level of the SNMP community used to access an SNMP manager.
    ///
    /// Deprecated in v1_10_0.
    /// This property has been deprecated in favor of `AccessMode` inside `CommunityStrings`.
    CommunityAccessMode: core.Nullable(SnmpCommunityAccessMode) = .absent,
    /// The engine ID.
    ///
    /// This property shall contain the RFC3411-defined engine ID.
    EngineId: core.Nullable(EngineIdUpdate) = .absent,
    /// Indicates if the community strings should be hidden.
    ///
    /// This property shall indicate if the community strings should be hidden in responses.
    HideCommunityStrings: core.Nullable(bool) = .absent,
    /// The authentication protocol used for SNMPv3 access to this manager.
    ///
    /// This property shall contain the SNMPv3 authentication protocol used to access this manager.  When the property contains the value `Account`, the SNMP settings in each manager account are used for authentication.  SNMPv1 and SNMPv2c protocols, if enabled, use community string-based authentication.
    AuthenticationProtocol: core.Nullable(SnmpAuthenticationProtocols) = .absent,
    /// The encryption protocol used for SNMPv3 access to this manager.
    ///
    /// This property shall contain the SNMPv3 encryption protocol used to access this manager, unless `AuthenticationProtocol` contains the value `Account`.
    EncryptionProtocol: core.Nullable(SnmpEncryptionProtocols) = .absent,
    /// The SNMP trap port.
    ///
    /// This property shall contain the port assigned to SNMP traps.
    TrapPort: core.Nullable(i64) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const SsdProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// The time interval, in seconds, between transmissions of the multicast NOTIFY ALIVE message from this service for SSDP.
    ///
    /// This property shall contain the time interval, in seconds, between transmissions of the multicast NOTIFY ALIVE message.  A setting of 0 seconds shall disable this functionality.  The recommended value is 600 seconds.  When disabled, other NOTIFY messages are also disabled due to their dependency on previously sent NOTIFY ALIVE messages.
    NotifyMulticastIntervalSeconds: ?i64 = null,
    /// The time-to-live hop count for SSDP multicast NOTIFY messages.
    ///
    /// This property shall contain the time-to-live hop count used for multicast NOTIFY messages.  The recommended value is 2.
    NotifyTTL: ?i64 = null,
    /// The IPv6 scope for multicast NOTIFY messages for SSDP.
    ///
    /// This property shall contain the IPv6 scope for multicast NOTIFY messages.  The valid enumerations are a subset of the available IPv6 scope types.
    NotifyIPv6Scope: ?NotifyIpv6scope = null,
};

/// What a client may change of `ManagerNetworkProtocol.SSDProtocol`.
pub const SsdProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,
    /// The time interval, in seconds, between transmissions of the multicast NOTIFY ALIVE message from this service for SSDP.
    ///
    /// This property shall contain the time interval, in seconds, between transmissions of the multicast NOTIFY ALIVE message.  A setting of 0 seconds shall disable this functionality.  The recommended value is 600 seconds.  When disabled, other NOTIFY messages are also disabled due to their dependency on previously sent NOTIFY ALIVE messages.
    NotifyMulticastIntervalSeconds: core.Nullable(i64) = .absent,
    /// The time-to-live hop count for SSDP multicast NOTIFY messages.
    ///
    /// This property shall contain the time-to-live hop count used for multicast NOTIFY messages.  The recommended value is 2.
    NotifyTTL: core.Nullable(i64) = .absent,
    /// The IPv6 scope for multicast NOTIFY messages for SSDP.
    ///
    /// This property shall contain the IPv6 scope for multicast NOTIFY messages.  The valid enumerations are a subset of the available IPv6 scope types.
    NotifyIPv6Scope: core.Nullable(NotifyIpv6scope) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const SshProtocol = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: ?bool = null,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: ?i64 = null,
    /// The SSH authentication methods that are permitted and the order in which they are preferred to be attempted.
    ///
    /// This property shall contain the SSH authentication methods that are permitted and the order in which they are preferred to be attempted.  The first entry is the most preferred method.  Methods not listed shall be considered disallowed.
    PreferredAuthentications: ?[]const ?SshPreferredAuthentication = null,
};

/// What a client may change of `ManagerNetworkProtocol.SSHProtocol`.
pub const SshProtocolUpdate = struct {
    /// An indication of whether the protocol is enabled.
    ///
    /// This property shall indicate whether the protocol is enabled.
    ProtocolEnabled: core.Nullable(bool) = .absent,
    /// The protocol port.
    ///
    /// This property shall contain the port assigned to the protocol.
    Port: core.Nullable(i64) = .absent,
    /// The SSH authentication methods that are permitted and the order in which they are preferred to be attempted.
    ///
    /// This property shall contain the SSH authentication methods that are permitted and the order in which they are preferred to be attempted.  The first entry is the most preferred method.  Methods not listed shall be considered disallowed.
    PreferredAuthentications: ?[]const ?SshPreferredAuthentication = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The network service settings for the manager.
///
/// This resource shall represent the network service settings for the manager.
pub const ManagerNetworkProtocol = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The DNS host name of this manager, without any domain information.
    ///
    /// This property shall contain the host name without any domain information.
    HostName: ?[]const u8 = null,
    /// The fully qualified domain name for the manager obtained by DNS including the host name and top-level domain name.
    ///
    /// This property shall contain the fully qualified domain name for the manager.
    FQDN: ?[]const u8 = null,
    /// The settings for this manager's HTTP protocol support.
    ///
    /// This object shall contain the HTTP protocol settings for the manager.  The default `Port` property value should be `80` for compatibility with established client implementations.
    HTTP: ?Protocol = null,
    /// The settings for this manager's HTTPS protocol support.
    ///
    /// This object shall contain the HTTPS/SSL protocol settings for this manager.  The default `Port` property value should be `443` for compatibility with established client implementations.
    HTTPS: ?HttpsProtocol = null,
    /// The settings for this manager's SNMP support.
    ///
    /// This object shall contain the SNMP protocol settings for this manager.  The default `Port` property value should be `161` for compatibility with established client implementations.
    SNMP: ?SnmpProtocol = null,
    /// The settings for this manager's virtual media support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the virtual media protocol settings for this manager.  The `Port` property shall contain the TCP port assigned for virtual media usage.  If multiple systems are supported by this manager, these properties, if present, apply to all instances of virtual media controlled by this manager.
    VirtualMedia: ?Protocol = null,
    /// The settings for this manager's Telnet protocol support.
    ///
    /// This object shall contain the Telnet protocol settings for this manager.  The default `Port` property value should be `23` for compatibility with established client implementations.
    Telnet: ?Protocol = null,
    /// The settings for this manager's SSDP support.
    ///
    /// This object shall contain the SSDP protocol settings for this manager.  Simple Service Discovery Protocol (SSDP) is for network discovery of devices supporting the Redfish Service.  The default `Port` property value should be `1900` for compatibility with established client implementations.
    SSDP: ?SsdProtocol = null,
    /// The settings for this manager's IPMI-over-LAN protocol support.
    ///
    /// This object shall contain the IPMI over LAN protocol settings for the manager.  The default `Port` property value should be `623` for compatibility with established client implementations.
    IPMI: ?Protocol = null,
    /// The settings for this manager's Secure Shell (SSH) protocol support.
    ///
    /// This object shall contain the Secure Shell (SSH) protocol settings for the manager.  The default value should be `22` for compatibility with established client implementations.
    SSH: ?SshProtocol = null,
    /// The settings for this manager's KVM-IP protocol support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the KVM-IP (Keyboard, Video, Mouse over IP) protocol settings for the manager.  If multiple systems are supported by this manager, these properties, if present, apply to all instances of KVM-IP controlled by this manager.
    KVMIP: ?Protocol = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The settings for this manager's DHCPv4 protocol support.
    ///
    /// This object shall contain the DHCPv4 protocol settings for the manager.
    DHCP: ?Protocol = null,
    /// The settings for this manager's NTP protocol support.
    ///
    /// This object shall contain the NTP protocol settings for the manager.
    NTP: ?NtpProtocol = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The settings for this manager's DHCPv6 protocol support.
    ///
    /// This object shall contain the DHCPv6 protocol settings for the manager.
    DHCPv6: ?Protocol = null,
    /// The settings for this manager's Remote Desktop Protocol support.
    ///
    /// This object shall contain the Remote Desktop Protocol settings for the manager.
    RDP: ?Protocol = null,
    /// The settings for this manager's Remote Frame Buffer protocol support, which can support VNC.
    ///
    /// This object shall contain the Remote Frame Buffer protocol settings for the manager.
    RFB: ?Protocol = null,
    /// The HTTP/HTTPS proxy information for this manager.
    ///
    /// This property shall contain the HTTP/HTTPS proxy configuration for this manager.
    Proxy: ?ProxyConfiguration = null,
    /// The settings for this manager's FTP protocol support.
    ///
    /// This object shall contain the File Transfer Protocol (FTP) settings for the manager.  The default `Port` property value should be `21` for compatibility with established client implementations.
    FTP: ?Protocol = null,
    /// The settings for this manager's Secure Shell File Transfer Protocol (SFTP) support.
    ///
    /// This object shall contain the Secure Shell File Transfer Protocol (SFTP) protocol settings for the manager.  The default value should be `22` for compatibility with established client implementations.
    SFTP: ?Protocol = null,
    /// The settings for this manager's FTP over SSL (FTPS) protocol support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the File Transfer Protocol over SSL (FTPS) settings for the manager.  The default value should be `21` for compatibility with established client implementations.
    FTPS: ?Protocol = null,
    /// The settings for this manager's Multicast Domain Name Service (mDNS) protocol support.
    ///
    /// This object shall contain the Multicast Domain Name Service (mDNS) settings for the manager.  The default `Port` property value should be `5353` for compatibility with established client implementations.
    mDNS: ?Protocol = null,
    /// The settings for this manager's Modbus TCP protocol support.
    ///
    /// This object shall contain the Modbus TCP protocol settings for the manager.
    Modbus: ?ModbusProtocol = null,
    /// The IPv6 address generation mode.  This affects all Ethernet interfaces on this manager.
    ///
    /// This property shall contain the IPv6 address generation mode.  The configuration of this property shall apply to all Ethernet interfaces on this manager.
    IPv6AddressGenerationMode: ?Ipv6addressGenerationMode = null,
};

/// What a client may change of `ManagerNetworkProtocol.ManagerNetworkProtocol`.
///
/// The network service settings for the manager.
pub const ManagerNetworkProtocolUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The settings for this manager's HTTP protocol support.
    ///
    /// This object shall contain the HTTP protocol settings for the manager.  The default `Port` property value should be `80` for compatibility with established client implementations.
    HTTP: ?ProtocolUpdate = null,
    /// The settings for this manager's HTTPS protocol support.
    ///
    /// This object shall contain the HTTPS/SSL protocol settings for this manager.  The default `Port` property value should be `443` for compatibility with established client implementations.
    HTTPS: ?HttpsProtocolUpdate = null,
    /// The settings for this manager's SNMP support.
    ///
    /// This object shall contain the SNMP protocol settings for this manager.  The default `Port` property value should be `161` for compatibility with established client implementations.
    SNMP: ?SnmpProtocolUpdate = null,
    /// The settings for this manager's virtual media support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the virtual media protocol settings for this manager.  The `Port` property shall contain the TCP port assigned for virtual media usage.  If multiple systems are supported by this manager, these properties, if present, apply to all instances of virtual media controlled by this manager.
    VirtualMedia: ?ProtocolUpdate = null,
    /// The settings for this manager's Telnet protocol support.
    ///
    /// This object shall contain the Telnet protocol settings for this manager.  The default `Port` property value should be `23` for compatibility with established client implementations.
    Telnet: ?ProtocolUpdate = null,
    /// The settings for this manager's SSDP support.
    ///
    /// This object shall contain the SSDP protocol settings for this manager.  Simple Service Discovery Protocol (SSDP) is for network discovery of devices supporting the Redfish Service.  The default `Port` property value should be `1900` for compatibility with established client implementations.
    SSDP: ?SsdProtocolUpdate = null,
    /// The settings for this manager's IPMI-over-LAN protocol support.
    ///
    /// This object shall contain the IPMI over LAN protocol settings for the manager.  The default `Port` property value should be `623` for compatibility with established client implementations.
    IPMI: ?ProtocolUpdate = null,
    /// The settings for this manager's Secure Shell (SSH) protocol support.
    ///
    /// This object shall contain the Secure Shell (SSH) protocol settings for the manager.  The default value should be `22` for compatibility with established client implementations.
    SSH: ?SshProtocolUpdate = null,
    /// The settings for this manager's KVM-IP protocol support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the KVM-IP (Keyboard, Video, Mouse over IP) protocol settings for the manager.  If multiple systems are supported by this manager, these properties, if present, apply to all instances of KVM-IP controlled by this manager.
    KVMIP: ?ProtocolUpdate = null,
    /// The settings for this manager's DHCPv4 protocol support.
    ///
    /// This object shall contain the DHCPv4 protocol settings for the manager.
    DHCP: ?ProtocolUpdate = null,
    /// The settings for this manager's NTP protocol support.
    ///
    /// This object shall contain the NTP protocol settings for the manager.
    NTP: ?NtpProtocolUpdate = null,
    /// The settings for this manager's DHCPv6 protocol support.
    ///
    /// This object shall contain the DHCPv6 protocol settings for the manager.
    DHCPv6: ?ProtocolUpdate = null,
    /// The settings for this manager's Remote Desktop Protocol support.
    ///
    /// This object shall contain the Remote Desktop Protocol settings for the manager.
    RDP: ?ProtocolUpdate = null,
    /// The settings for this manager's Remote Frame Buffer protocol support, which can support VNC.
    ///
    /// This object shall contain the Remote Frame Buffer protocol settings for the manager.
    RFB: ?ProtocolUpdate = null,
    /// The HTTP/HTTPS proxy information for this manager.
    ///
    /// This property shall contain the HTTP/HTTPS proxy configuration for this manager.
    Proxy: ?ProxyConfigurationUpdate = null,
    /// The settings for this manager's FTP protocol support.
    ///
    /// This object shall contain the File Transfer Protocol (FTP) settings for the manager.  The default `Port` property value should be `21` for compatibility with established client implementations.
    FTP: ?ProtocolUpdate = null,
    /// The settings for this manager's Secure Shell File Transfer Protocol (SFTP) support.
    ///
    /// This object shall contain the Secure Shell File Transfer Protocol (SFTP) protocol settings for the manager.  The default value should be `22` for compatibility with established client implementations.
    SFTP: ?ProtocolUpdate = null,
    /// The settings for this manager's FTP over SSL (FTPS) protocol support that apply to all system instances controlled by this manager.
    ///
    /// This object shall contain the File Transfer Protocol over SSL (FTPS) settings for the manager.  The default value should be `21` for compatibility with established client implementations.
    FTPS: ?ProtocolUpdate = null,
    /// The settings for this manager's Multicast Domain Name Service (mDNS) protocol support.
    ///
    /// This object shall contain the Multicast Domain Name Service (mDNS) settings for the manager.  The default `Port` property value should be `5353` for compatibility with established client implementations.
    mDNS: ?ProtocolUpdate = null,
    /// The settings for this manager's Modbus TCP protocol support.
    ///
    /// This object shall contain the Modbus TCP protocol settings for the manager.
    Modbus: ?ModbusProtocolUpdate = null,
    /// The IPv6 address generation mode.  This affects all Ethernet interfaces on this manager.
    ///
    /// This property shall contain the IPv6 address generation mode.  The configuration of this property shall apply to all Ethernet interfaces on this manager.
    IPv6AddressGenerationMode: core.Nullable(Ipv6addressGenerationMode) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
