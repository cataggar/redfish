//! Namespace `UpdateService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const certificate_collection = @import("certificate_collection.zig");
const key = @import("key.zig");
const key_collection = @import("key_collection.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");
const software_inventory_collection = @import("software_inventory_collection.zig");
const update_service_capabilities = @import("update_service_capabilities.zig");

pub const ApplyTime = enum {
    /// Apply immediately.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied immediately.
    Immediate,
    /// Apply on a reset.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied when the system or service is reset.
    OnReset,
    /// Apply during an administrator-specified maintenance window.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied during the maintenance window specified by the `MaintenanceWindowStartTime` and `MaintenanceWindowDurationInSeconds` properties.  A service may perform resets during this maintenance window.
    AtMaintenanceWindowStart,
    /// Apply after a reset but within an administrator-specified maintenance window.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied during the maintenance window specified by the `MaintenanceWindowStartTime` and `MaintenanceWindowDurationInSeconds` properties, and if a reset occurs within the maintenance window.
    InMaintenanceWindowOnReset,
    /// Apply when the `StartUpdate` action of the update service is invoked.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied when the `StartUpdate` action of the update service is invoked.
    OnStartUpdateRequest,
    /// Apply when the target for the software update is reset.  Targets include devices, services, and systems.
    ///
    /// This value shall indicate the `HttpPushUri`-provided software is applied when the target is reset.
    OnTargetReset,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SupportedUpdateImageFormatType = enum {
    /// A PLDM for Firmware Update Specification v1.0 image.
    ///
    /// This value shall indicate an image that conforms to the v1.0 image format as defined in DMTF DSP0267.
    PLDMv1_0,
    /// A PLDM for Firmware Update Specification v1.1 image.
    ///
    /// This value shall indicate an image that conforms to the v1.1 image format as defined in DMTF DSP0267.
    PLDMv1_1,
    /// A PLDM for Firmware Update Specification v1.2 image.
    ///
    /// This value shall indicate an image that conforms to the v1.2 image format as defined in DMTF DSP0267.
    PLDMv1_2,
    /// A PLDM for Firmware Update Specification v1.3 image.
    ///
    /// This value shall indicate an image that conforms to the v1.3 image format as defined in DMTF DSP0267.
    PLDMv1_3,
    /// The image conforms to the capsule format described in the UEFI Specification.
    ///
    /// This value shall indicate an image that conforms to the UEFI capsule format as defined in the UEFI Specification.
    UEFICapsule,
    /// A vendor-defined image.
    ///
    /// This value shall indicate a vendor-defined format.
    VendorDefined,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TransferProtocolType = enum {
    /// Common Internet File System (CIFS).
    CIFS,
    /// File Transfer Protocol (FTP).
    FTP,
    /// SSH File Transfer Protocol (SFTP).
    SFTP,
    /// Hypertext Transfer Protocol (HTTP).
    HTTP,
    /// Hypertext Transfer Protocol Secure (HTTPS).
    HTTPS,
    /// Network File System (NFS).
    ///
    /// Deprecated in v1_3_0.
    /// This value has been deprecated in favor of NFS.
    NSF,
    /// Secure Copy Protocol (SCP).
    SCP,
    /// Trivial File Transfer Protocol (TFTP).
    TFTP,
    /// A manufacturer-defined protocol.
    OEM,
    /// Network File System (NFS).
    NFS,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `UpdateService.Activate`.
///
/// This action activates specified software inventory instance.
pub const UpdateServiceActivateAction = struct {
    /// The target software inventory instances to activate on staged devices.
    ///
    /// This parameter shall contain an array of target software inventory instances to activate on staged devices.  The service shall activate each software image on all applicable devices.
    Targets: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `UpdateService.GenerateSSHIdentityKeyPair`.
///
/// This action generates a new SSH identity key-pair to be used with the `UpdateService` resource.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentitySSHKey` property.  Any existing key-pair is deleted and replaced by the new key-pair.
pub const UpdateServiceGenerateSshIdentityKeyPairAction = struct {
    /// The type of SSH key.
    ///
    /// This parameter shall contain the type of SSH key.
    KeyType: ?key.SshKeyType = null,
    /// The length of the SSH key, in bits, if the `KeyType` parameter contains `RSA`.
    ///
    /// This parameter shall contain the length of the SSH key, in bits.  This parameter shall be required if the `KeyType` parameter contains `RSA` and shall be rejected for other values.
    KeyLength: ?i64 = null,
    /// The curve to use with the SSH key if the `KeyType` parameter contains `ECDSA`.
    ///
    /// This parameter shall contain the curve to use with the SSH key.  This parameter shall be required if the `KeyType` parameter contains `ECDSA` and shall be rejected for other values.
    Curve: ?key.EcdsaCurveType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `UpdateService.RemoveSSHIdentityKeyPair`.
///
/// This action removes the SSH identity key-pair used with the `UpdateService` resource.
pub const UpdateServiceRemoveSshIdentityKeyPairAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `UpdateService.SimpleUpdate`.
///
/// This action updates software components.
pub const UpdateServiceSimpleUpdateAction = struct {
    /// The URI of the software image to install.
    ///
    /// This parameter shall contain an RFC3986-defined URI that links to a software image that the update service retrieves to install software in that image.  The software image may contain multiple images if supported by the image format.  This URI should contain a scheme that describes the transfer protocol.  If the `TransferProtocol` parameter is absent or not supported, and a transfer protocol is not specified by a scheme contained within this URI, the service shall use HTTP to get the image.
    ImageURI: ?[]const u8 = null,
    /// The network protocol that the update service uses to retrieve the software image file located at the URI specified by the `ImageURI` parameter.  This parameter is ignored if the URI provided in `ImageURI` contains a scheme.
    ///
    /// This parameter shall contain the network protocol that the update service shall use to retrieve the software image located at the URI specified by the `ImageURI` parameter.  Services should ignore this parameter if the URI specified by the `ImageURI` parameter contains a scheme.  If this parameter is not provided (or supported), and a transfer protocol is not specified by a scheme contained within this URI, the service shall use HTTP to retrieve the image.
    TransferProtocol: ?TransferProtocolType = null,
    /// An array of URIs that indicate where to apply the update image.
    ///
    /// This parameter shall contain zero or more URIs that indicate where to apply the update image.  These targets should correspond to software inventory instances or their related items.  If this parameter is not present or contains no targets, the service shall apply the software image to all applicable targets, as determined by the service.  If the target specifies a device resource, the software image file shall be applied to the specified device.  If the target specifies a resource collection, the software image shall be applied to each applicable member of the specified collection.  If the target resource specifies an `Aggregate` resource, the software image file shall be applied to each applicable element of the specified aggregate.  If the target resource specifies a `ComputerSystem` resource, the software image file shall be applied to the applicable components within the specified computer system.  If the provided software image is a bundle of multiple images, the service shall apply each software image as specified by this parameter.
    Targets: ?[]const []const u8 = null,
    /// The username to access the URI specified by the `ImageURI` parameter.
    ///
    /// This parameter shall contain the username to access the URI specified by the `ImageURI` parameter.
    Username: ?[]const u8 = null,
    /// The password to access the URI specified by the `ImageURI` parameter.
    ///
    /// This parameter shall contain the password to access the URI specified by the `ImageURI` parameter.
    Password: ?[]const u8 = null,
    /// An indication of whether the service should bypass update policies when applying the provided image.  The default is `false`.
    ///
    /// This parameter shall indicate whether the service should bypass update policies when applying the provided image, such as allowing a component to be downgraded.  Services may contain update policies that are never bypassed, such as minimum version enforcement.  If the client does not provide this parameter, the service shall default this value to `false`.
    ForceUpdate: ?bool = null,
    /// An indication of whether the service stages the image on target devices for a client to activate at a later time.
    ///
    /// This parameter shall indicate whether the service stages the image on target devices for a client to activate at a later time with the `Activate` action on the `SoftwareInventory` resource.  If the client does not provide this parameter, the service shall default this value to `false`.
    Stage: ?bool = null,
    /// An indication of whether the service adds the image to the local image store.
    ///
    /// This parameter shall indicate whether the service adds the image to the resource collection referenced by the `LocalImageStore` property.  If the client does not provide this parameter, the service shall default this value to `false`.
    LocalImage: ?bool = null,
    /// An array of URIs that indicate where not to apply the update image.
    ///
    /// This parameter shall contain zero or more URIs that indicate where not to apply the update image.  This parameter shall be ignored if the `Targets` parameter is provided and contains at least one member.  These excluded targets should correspond to software inventory instances or their related items.  If this parameter is not present or contains no targets, the service shall apply the software image to all applicable targets, as determined by the service.  If an excluded target specifies a device resource, the software image file shall not be applied to that specified device.  If an excluded target specifies a resource collection, the software image shall not be applied to each applicable member of the specified collection.  If an excluded target resource specifies an `Aggregate` resource, the software image file shall not be applied to each applicable element of the specified aggregate.  If an excluded target resource specifies a `ComputerSystem` resource, the software image file shall not be applied to the applicable components within the specified computer system.
    ExcludeTargets: ?[]const []const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `UpdateService.StartUpdate`.
///
/// This action starts updating all images that have been previously invoked using an `OperationApplyTime` value of `OnStartUpdateRequest`.
pub const UpdateServiceStartUpdateAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action activates specified software inventory instance.
    ///
    /// This action shall activate specified software inventory instance.
    @"#UpdateService.Activate": ?core.Action(UpdateServiceActivateAction, std.json.Value) = null,
    /// This action generates a new SSH identity key-pair to be used with the `UpdateService` resource.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentitySSHKey` property.  Any existing key-pair is deleted and replaced by the new key-pair.
    ///
    /// This action shall generate a new SSH identity key-pair to be used with the `UpdateService` resource.  The service shall store the generated public key in the `Key` resource referenced by the `PublicIdentitySSHKey` property.  If the `UpdateService` resource already has an associated SSH identity key-pair, the service shall delete the key-pair and replace it with the new key-pair.
    @"#UpdateService.GenerateSSHIdentityKeyPair": ?core.Action(UpdateServiceGenerateSshIdentityKeyPairAction, std.json.Value) = null,
    /// This action removes the SSH identity key-pair used with the `UpdateService` resource.
    ///
    /// This action shall remove the private SSH identity key-pair used with the `UpdateService` resource.
    @"#UpdateService.RemoveSSHIdentityKeyPair": ?core.Action(UpdateServiceRemoveSshIdentityKeyPairAction, std.json.Value) = null,
    /// This action updates software components.
    ///
    /// This action shall update installed software components in a software image file located at an `ImageURI` parameter-specified URI.
    @"#UpdateService.SimpleUpdate": ?core.Action(UpdateServiceSimpleUpdateAction, std.json.Value) = null,
    /// This action starts updating all images that have been previously invoked using an `OperationApplyTime` value of `OnStartUpdateRequest`.
    ///
    /// This action shall start an update of software component that have been scheduled with the `OperationApplyTime` value of `OnStartUpdateRequest`.
    @"#UpdateService.StartUpdate": ?core.Action(UpdateServiceStartUpdateAction, std.json.Value) = null,

    /// This action activates specified software inventory instance.
    ///
    /// This action shall activate specified software inventory instance.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn activate(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: UpdateServiceActivateAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#UpdateService.Activate" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action generates a new SSH identity key-pair to be used with the `UpdateService` resource.  The generated public key is stored in the `Key` resource referenced by the `PublicIdentitySSHKey` property.  Any existing key-pair is deleted and replaced by the new key-pair.
    ///
    /// This action shall generate a new SSH identity key-pair to be used with the `UpdateService` resource.  The service shall store the generated public key in the `Key` resource referenced by the `PublicIdentitySSHKey` property.  If the `UpdateService` resource already has an associated SSH identity key-pair, the service shall delete the key-pair and replace it with the new key-pair.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn generateSshIdentityKeyPair(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: UpdateServiceGenerateSshIdentityKeyPairAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#UpdateService.GenerateSSHIdentityKeyPair" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action removes the SSH identity key-pair used with the `UpdateService` resource.
    ///
    /// This action shall remove the private SSH identity key-pair used with the `UpdateService` resource.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn removeSshIdentityKeyPair(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: UpdateServiceRemoveSshIdentityKeyPairAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#UpdateService.RemoveSSHIdentityKeyPair" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action updates software components.
    ///
    /// This action shall update installed software components in a software image file located at an `ImageURI` parameter-specified URI.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn simpleUpdate(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: UpdateServiceSimpleUpdateAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#UpdateService.SimpleUpdate" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action starts updating all images that have been previously invoked using an `OperationApplyTime` value of `OnStartUpdateRequest`.
    ///
    /// This action shall start an update of software component that have been scheduled with the `OperationApplyTime` value of `OnStartUpdateRequest`.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn startUpdate(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: UpdateServiceStartUpdateAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#UpdateService.StartUpdate" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The settings for when to apply `HttpPushUri`-provided software.
///
/// The properties in this object shall contain settings for when to apply `HttpPushUri`-provided software.
pub const HttpPushUriApplyTime = struct {
    /// The time when to apply the `HttpPushUri`-provided software update.
    ///
    /// This property shall indicate the time when to apply the `HttpPushUri`-provided software update.
    ApplyTime: ?ApplyTime = null,
    /// The start time of a maintenance window.
    ///
    /// This property shall indicate the date and time when the service can start to apply the `HttpPushUri`-provided software as part of a maintenance window.  This property shall be required if the `HttpPushUriApplyTime` property value is `AtMaintenanceWindowStart` or `InMaintenanceWindowOnReset`.
    MaintenanceWindowStartTime: ?core.DateTimeOffset = null,
    /// The expiry time, in seconds, of the maintenance window.
    ///
    /// This property shall indicate the end of the maintenance window as the number of seconds after the time specified by the `MaintenanceWindowStartTime` property.  This property shall be required if the `HttpPushUriApplyTime` property value is `AtMaintenanceWindowStart` or `InMaintenanceWindowOnReset`.
    MaintenanceWindowDurationInSeconds: ?i64 = null,
};

/// What a client may change of `UpdateService.HttpPushUriApplyTime`.
///
/// The settings for when to apply `HttpPushUri`-provided software.
pub const HttpPushUriApplyTimeUpdate = struct {
    /// The time when to apply the `HttpPushUri`-provided software update.
    ///
    /// This property shall indicate the time when to apply the `HttpPushUri`-provided software update.
    ApplyTime: ?ApplyTime = null,
    /// The start time of a maintenance window.
    ///
    /// This property shall indicate the date and time when the service can start to apply the `HttpPushUri`-provided software as part of a maintenance window.  This property shall be required if the `HttpPushUriApplyTime` property value is `AtMaintenanceWindowStart` or `InMaintenanceWindowOnReset`.
    MaintenanceWindowStartTime: ?core.DateTimeOffset = null,
    /// The expiry time, in seconds, of the maintenance window.
    ///
    /// This property shall indicate the end of the maintenance window as the number of seconds after the time specified by the `MaintenanceWindowStartTime` property.  This property shall be required if the `HttpPushUriApplyTime` property value is `AtMaintenanceWindowStart` or `InMaintenanceWindowOnReset`.
    MaintenanceWindowDurationInSeconds: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const HttpPushUriOptions = struct {
    /// The settings for when to apply `HttpPushUri`-provided firmware.
    ///
    /// This property shall contain settings for when to apply `HttpPushUri`-provided firmware.
    HttpPushUriApplyTime: ?HttpPushUriApplyTime = null,
    /// An indication of whether the service should bypass update policies when applying the `HttpPushUri`-provided image.
    ///
    /// This property shall indicate whether the service should bypass update policies when applying the `HttpPushUri`-provided image, such as allowing a component to be downgraded.  Services may contain update policies that are never bypassed, such as minimum version enforcement.  If this property is not present, it shall be assumed to be `false`.
    ForceUpdate: ?bool = null,
};

/// What a client may change of `UpdateService.HttpPushUriOptions`.
pub const HttpPushUriOptionsUpdate = struct {
    /// The settings for when to apply `HttpPushUri`-provided firmware.
    ///
    /// This property shall contain settings for when to apply `HttpPushUri`-provided firmware.
    HttpPushUriApplyTime: ?HttpPushUriApplyTimeUpdate = null,
    /// An indication of whether the service should bypass update policies when applying the `HttpPushUri`-provided image.
    ///
    /// This property shall indicate whether the service should bypass update policies when applying the `HttpPushUri`-provided image, such as allowing a component to be downgraded.  Services may contain update policies that are never bypassed, such as minimum version enforcement.  If this property is not present, it shall be assumed to be `false`.
    ForceUpdate: ?bool = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `UpdateService` schema describes the update service and the properties for the service itself with links to collections of firmware and software inventory.  The update service also provides methods for updating software and firmware of the resources in a Redfish service.
///
/// This resource shall represent an update service and the properties that affect the service itself for a Redfish implementation.
pub const UpdateService = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: ?bool = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The URI used to perform an HTTP or HTTPS push update to the update service.  The format of the message is vendor-specific.
    ///
    /// This property shall contain a URI at which the update service supports an HTTP or HTTPS `POST` of a software image for the purpose of installing software contained within the image.  Access to this URI shall require the same privilege as access to the update service.  If the service requires the `Content-Length` header for `POST` requests to this URI, the service should return HTTP `411 Length Required` status code if the client does not include this header in the `POST` request.  The value of this property should not contain a URI of a Redfish resource.  See the 'Redfish-defined URIs and relative reference rules' clause in the Redfish Specification.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of the `MultipartHttpPushUri` property.
    HttpPushUri: ?[]const u8 = null,
    /// An array of URIs that indicate where to apply the update image.
    ///
    /// This property shall contain zero or more URIs that indicate where to apply the update image when using the URI specified by the `HttpPushUri` property to push a software image.  These targets should correspond to software inventory instances or their related items.  If this property is not present or contains no targets, the service shall apply the software image to all applicable targets, as determined by the service.  If the target specifies a device resource, the software image file shall be applied to the specified device.  If the target specifies a resource collection, the software image shall be applied to each applicable member of the specified collection.  If the target resource specifies an `Aggregate` resource, the software image file shall be applied to each applicable element of the specified aggregate.  If the target resource specifies a `ComputerSystem` resource, the software image file shall be applied to the applicable components within the specified computer system.  If the provided software image is a bundle of multiple images, the service shall apply each software image as specified by this parameter.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of the update parameters used with `MultipartHttpPushUri`-provided software updates.
    HttpPushUriTargets: ?[]const ?[]const u8 = null,
    /// An indication of whether any client has reserved the `HttpPushUriTargets` property.
    ///
    /// This property shall indicate whether any client has reserved the `HttpPushUriTargets` property for firmware updates.  A client should set this property to `true` when it uses `HttpPushUriTargets` for firmware updates.  A client should set it to `false` when it no longer uses `HttpPushUriTargets` for updates.  The property can provide multiple clients a way to negotiate ownership of `HttpPushUriTargets` and helps clients determine whether another client is using `HttpPushUriTargets` to make firmware updates.  This property has no functional requirements for the service.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of `MultipartHttpPushUri`-provided software updates.
    HttpPushUriTargetsBusy: ?bool = null,
    /// The options for `HttpPushUri`-provided software updates.
    ///
    /// This property shall contain options and requirements of the service for `HttpPushUri`-provided software updates.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of the update parameters used with `MultipartHttpPushUri`-provided software updates.
    HttpPushUriOptions: ?HttpPushUriOptions = null,
    /// An indication of whether a client has reserved the `HttpPushUriOptions` properties for software updates.
    ///
    /// This property shall indicate whether a client uses the `HttpPushUriOptions` properties for software updates.  When a client uses any `HttpPushUriOptions` properties for software updates, it should set this property to `true`.  When a client no longer uses `HttpPushUriOptions` properties for software updates, it should set this property to `false`.  This property can provide multiple clients a way to negotiate ownership of `HttpPushUriOptions` properties.  Clients can use this property to determine whether another client uses `HttpPushUriOptions` properties for software updates.  This property has no functional requirements for the service.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of `MultipartHttpPushUri`-provided software updates.
    HttpPushUriOptionsBusy: ?bool = null,
    /// The maximum size in bytes of the software update image that this service supports.
    ///
    /// This property shall indicate the maximum size of the software update image that clients can send to this update service.
    MaxImageSizeBytes: ?i64 = null,
    /// The URI used to perform a Redfish Specification-defined multipart HTTP or HTTPS push update to the update service.
    ///
    /// This property shall contain a URI used to perform a Redfish Specification-defined multipart HTTP or HTTPS `POST` of a software image for the purpose of installing software contained within the image.  The value of this property should not contain a URI of a Redfish resource.  See the 'Redfish-defined URIs and relative reference rules' clause in the Redfish Specification.
    MultipartHttpPushUri: ?[]const u8 = null,
    /// An indication of whether the service will verify the certificate of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request with the certificates found in the collection referenced by the `RemoteServerCertificates` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the `SecurityPolicy` resource.
    VerifyRemoteServerCertificate: ?bool = null,
    /// An indication of whether the service will verify the SSH key of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request.
    ///
    /// This property shall indicate whether the service will verify the SSH key of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request with the keys found in the collection referenced by the `RemoteServerSSHKeys` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.
    VerifyRemoteServerSSHKey: ?bool = null,
    /// The image format types supported by the service.
    ///
    /// This property shall contain the image format types supported by the service.
    SupportedUpdateImageFormats: ?[]const ?SupportedUpdateImageFormatType = null,
    /// The number of bytes currently available in the local image store.
    ///
    /// This property shall contain the number of bytes currently available in the local image store.
    LocalImageStoreAvailableCapacityBytes: ?i64 = null,
    /// The total number of bytes available to the local image store.
    ///
    /// This property shall contain the total number of bytes available to the local image store.
    LocalImageStoreTotalCapacityBytes: ?i64 = null,
    /// An inventory of firmware.
    ///
    /// This property shall contain a link to a resource collection of type `SoftwareInventoryCollection`.  The resource collection should contain the set of software components generally referred to as platform firmware or that does not execute within a host operating system.  Software in this collection is generally updated using platform-specific methods or utilities.
    FirmwareInventory: ?core.NavProperty(software_inventory_collection.SoftwareInventoryCollection) = null,
    /// An inventory of software.
    ///
    /// This property shall contain a link to a resource collection of type `SoftwareInventoryCollection`.  The resource collection should contain the set of software components executed in the context of a host operating system.  This can include device drivers, applications, or offload workloads.  Software in this collection is generally updated using operating system-centric methods.
    SoftwareInventory: ?core.NavProperty(software_inventory_collection.SoftwareInventoryCollection) = null,
    /// The link to a collection of server certificates for the server referenced by the `ImageURI` parameter in SimpleUpdate.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the server certificates for the server referenced by the `ImageURI` parameter in `SimpleUpdate`.  If `VerifyRemoteServerCertificate` is `true`, services shall compare the certificates in this collection with the certificate obtained during handshaking with the image server in order to verify the identity of the image server prior to transferring the image.  If the server cannot be verified, the service shall not send the transfer request.  If `VerifyRemoteServerCertificate` is `false`, the service shall not perform certificate verification with certificates in this collection.  Regardless of the contents of this collection, services may perform additional verification based on other factors, such as the configuration of the `SecurityPolicy` resource.
    RemoteServerCertificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to a collection of client identity certificates provided to the server referenced by the `ImageURI` parameter in `SimpleUpdate`.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the client identity certificates that are provided to the server referenced by the `ImageURI` parameter in `SimpleUpdate` as part of TLS handshaking.
    ClientCertificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to a collection of keys that can be used to authenticate the server referenced by the `ImageURI` parameter in `SimpleUpdate`.
    ///
    /// This property shall contain a link to a resource collection of type `KeyCollection` that represents the server SSH keys for the server referenced by the `ImageURI` Parameter in `SimpleUpdate`.  If `VerifyRemoteServerSSHKey` is `true`, services shall compare the keys in this collection with the key obtained during handshaking with the image server in order to verify the identity of the image server prior to transferring the image.  If the server cannot be verified, the service shall not send the transfer request.  If `VerifyRemoteServerSSHKey` is `false`, the service shall not perform key verification with keys in this collection.
    RemoteServerSSHKeys: ?core.NavProperty(key_collection.KeyCollection) = null,
    /// A link to the public key that is used with the `SimpleUpdate` action for the key-based authentication.  The GenerateSSHIdentityKeyPair and RemoveSSHIdentityKeyPair are used to update the key for the `SimpleUpdate` action.
    ///
    /// This property shall contain a link to a resource of type `Key` that represents the public key that is used with the `SimpleUpdate` action for the key-based authentication.  This property shall not be present if a key-pair is not available.
    PublicIdentitySSHKey: ?core.NavProperty(key.Key) = null,
    /// A link to the capabilities of the update service.
    ///
    /// This property shall contain a link to a resource of type `UpdateServiceCapabilities` that contains the capabilities of the update service.
    UpdateServiceCapabilities: ?core.NavProperty(update_service_capabilities.UpdateServiceCapabilities) = null,
    /// An inventory of locally stored images.
    ///
    /// This property shall contain a link to a resource collection of type `SoftwareInventoryCollection`.  The resource collection shall contain the set of images that are stored locally by the service.  This may include operating system images, drivers, or other software components.  These images may be referenced by other features of the service, such as virtual media.
    LocalImageStore: ?core.NavProperty(software_inventory_collection.SoftwareInventoryCollection) = null,
};

/// What a client may change of `UpdateService.UpdateService`.
///
/// The `UpdateService` schema describes the update service and the properties for the service itself with links to collections of firmware and software inventory.  The update service also provides methods for updating software and firmware of the resources in a Redfish service.
pub const UpdateServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,
    /// An array of URIs that indicate where to apply the update image.
    ///
    /// This property shall contain zero or more URIs that indicate where to apply the update image when using the URI specified by the `HttpPushUri` property to push a software image.  These targets should correspond to software inventory instances or their related items.  If this property is not present or contains no targets, the service shall apply the software image to all applicable targets, as determined by the service.  If the target specifies a device resource, the software image file shall be applied to the specified device.  If the target specifies a resource collection, the software image shall be applied to each applicable member of the specified collection.  If the target resource specifies an `Aggregate` resource, the software image file shall be applied to each applicable element of the specified aggregate.  If the target resource specifies a `ComputerSystem` resource, the software image file shall be applied to the applicable components within the specified computer system.  If the provided software image is a bundle of multiple images, the service shall apply each software image as specified by this parameter.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of the update parameters used with `MultipartHttpPushUri`-provided software updates.
    HttpPushUriTargets: ?[]const ?[]const u8 = null,
    /// An indication of whether any client has reserved the `HttpPushUriTargets` property.
    ///
    /// This property shall indicate whether any client has reserved the `HttpPushUriTargets` property for firmware updates.  A client should set this property to `true` when it uses `HttpPushUriTargets` for firmware updates.  A client should set it to `false` when it no longer uses `HttpPushUriTargets` for updates.  The property can provide multiple clients a way to negotiate ownership of `HttpPushUriTargets` and helps clients determine whether another client is using `HttpPushUriTargets` to make firmware updates.  This property has no functional requirements for the service.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of `MultipartHttpPushUri`-provided software updates.
    HttpPushUriTargetsBusy: core.Nullable(bool) = .absent,
    /// The options for `HttpPushUri`-provided software updates.
    ///
    /// This property shall contain options and requirements of the service for `HttpPushUri`-provided software updates.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of the update parameters used with `MultipartHttpPushUri`-provided software updates.
    HttpPushUriOptions: ?HttpPushUriOptionsUpdate = null,
    /// An indication of whether a client has reserved the `HttpPushUriOptions` properties for software updates.
    ///
    /// This property shall indicate whether a client uses the `HttpPushUriOptions` properties for software updates.  When a client uses any `HttpPushUriOptions` properties for software updates, it should set this property to `true`.  When a client no longer uses `HttpPushUriOptions` properties for software updates, it should set this property to `false`.  This property can provide multiple clients a way to negotiate ownership of `HttpPushUriOptions` properties.  Clients can use this property to determine whether another client uses `HttpPushUriOptions` properties for software updates.  This property has no functional requirements for the service.
    ///
    /// Deprecated in v1_15_0.
    /// This property has been deprecated in favor of `MultipartHttpPushUri`-provided software updates.
    HttpPushUriOptionsBusy: core.Nullable(bool) = .absent,
    /// An indication of whether the service will verify the certificate of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request with the certificates found in the collection referenced by the `RemoteServerCertificates` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the `SecurityPolicy` resource.
    VerifyRemoteServerCertificate: core.Nullable(bool) = .absent,
    /// An indication of whether the service will verify the SSH key of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request.
    ///
    /// This property shall indicate whether the service will verify the SSH key of the server referenced by the `ImageURI` parameter in `SimpleUpdate` prior to sending the transfer request with the keys found in the collection referenced by the `RemoteServerSSHKeys` property.  If this property is not supported by the service, it shall be assumed to be `false`.  This property should default to `false` in order to maintain compatibility with older clients.
    VerifyRemoteServerSSHKey: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
