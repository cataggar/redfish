//! Namespace `Certificate`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");

pub const CertificateType = enum {
    /// A Privacy Enhanced Mail (PEM)-encoded single certificate.
    ///
    /// This value shall indicate the format of the certificate shall contain a Privacy Enhanced Mail (PEM)-encoded string, containing RFC5280-defined structures, representing a single certificate.
    PEM,
    /// A Privacy Enhanced Mail (PEM)-encoded certificate chain.
    ///
    /// This value shall indicate the format of the certificate shall contain a Privacy Enhanced Mail (PEM)-encoded string, containing RFC5280-defined structures, representing a certificate chain.  When this value is specified, the properties of the resource, except for `CertificateString`, shall contain the information from the leaf certificate.  When this value is specified, `CertificateString` shall contain the entire PEM chain.
    PEMchain,
    /// A Privacy Enhanced Mail (PEM)-encoded PKCS7 certificate.
    ///
    /// The format of the certificate shall contain a Privacy Enhanced Mail (PEM)-encoded string, containing RFC5280-defined and RFC2315-defined structures.  The service may discard additional certificates or other data in the structure.
    PKCS7,
    /// A Base64-encoded PKCS12 certificate bundle.
    ///
    /// This value shall indicate the format of the certificate shall contain a Base64-encoded string, with padding characters, containing RFC7292-defined structures, representing a PKCS12 archive of one or more certificates, keys, or other cryptographic data.  The service may discard additional certificates or other data in the structure.
    PKCS12,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const CertificateUsageType = enum {
    /// This certificate is a user certificate like those associated with a manager account.
    User,
    /// This certificate is a web or HTTPS certificate like those used for event destinations.
    Web,
    /// This certificate is used for SSH.
    SSH,
    /// This certificate is a device type certificate like those associated with SPDM and other standards.
    Device,
    /// This certificate is a platform type certificate like those associated with SPDM and other standards.
    Platform,
    /// This certificate is a BIOS certificate like those associated with UEFI.
    BIOS,
    /// This certificate is an IDevID certificate like those associated with TCG TPMs.
    IDevID,
    /// This certificate is an LDevID certificate like those associated with TCG TPMs.
    LDevID,
    /// This certificate is an IAK certificate like those associated with TCG TPMs.
    IAK,
    /// This certificate is an LAK certificate like those associated with TCG TPMs.
    LAK,
    /// This certificate is an EK certificate like those associated with TCG TPMs.
    EK,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The usages of a key contained within a certificate.
///
/// This type shall describe the usages of a key within a certificate, as specified by the 'Key Usage' and 'Extended Key Usage' definitions in RFC5280.
pub const KeyUsage = enum {
    /// Verifies digital signatures, other than signatures on certificates and CRLs.
    DigitalSignature,
    /// Verifies digital signatures, other than signatures on certificates and CRLs, and provides a non-repudiation service that protects against the signing entity falsely denying some action.
    NonRepudiation,
    /// Enciphers private or secret keys.
    KeyEncipherment,
    /// Directly enciphers raw user data without an intermediate symmetric cipher.
    DataEncipherment,
    /// Key agreement.
    KeyAgreement,
    /// Verifies signatures on public key certificates.
    KeyCertSign,
    /// Verifies signatures on certificate revocation lists (CRLs).
    CRLSigning,
    /// Enciphers data while performing a key agreement.
    EncipherOnly,
    /// Deciphers data while performing a key agreement.
    DecipherOnly,
    /// TLS WWW server authentication.
    ServerAuthentication,
    /// TLS WWW client authentication.
    ClientAuthentication,
    /// Signs downloadable executable code.
    CodeSigning,
    /// Email protection.
    EmailProtection,
    /// Binds the hash of an object to a time.
    Timestamping,
    /// Signs OCSP responses.
    OCSPSigning,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Certificate.ForceAutomaticRenew`.
///
/// This action forces an automatic renewal of the certificate, if this certificate is configured for automatic certificate enrollment.
pub const CertificateForceAutomaticRenewAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Certificate.Rekey`.
///
/// This action generates a new key-pair for a certificate and produces a certificate signing request.
pub const CertificateRekeyAction = struct {
    /// The type of key-pair for use with signing algorithms.
    ///
    /// This parameter shall contain the type of key-pair for use with signing algorithms.  The allowable values for this parameter shall be the strings in the 'Algorithm Name' field of the 'TCG_ALG_ID Constants' table, formerly the 'TPM_ALG_ID Constants' table, within the 'Trusted Computing Group Algorithm Registry'.
    KeyPairAlgorithm: ?[]const u8 = null,
    /// The length of the key, in bits, if needed based on the `KeyPairAlgorithm` parameter value.
    ///
    /// This parameter shall contain the length of the key, in bits, if needed based on the `KeyPairAlgorithm` parameter value.
    KeyBitLength: ?i64 = null,
    /// The curve ID to use with the key, if needed based on the `KeyPairAlgorithm` parameter value.
    ///
    /// This parameter shall contain the curve ID to use with the key, if needed based on the `KeyPairAlgorithm` parameter value.  The allowable values for this parameter shall be the strings in the 'Name' field of the 'TCG_ECC_CURVE Constants' table, formerly the 'TPM_ECC_CURVE Constants' table, within the 'Trusted Computing Group Algorithm Registry'.
    KeyCurveId: ?[]const u8 = null,
    /// The challenge password to apply to the certificate for revocation requests.
    ///
    /// This property shall contain the challenge password to apply to the certificate for revocation requests as defined by the RFC2985 'challengePassword' attribute.
    ChallengePassword: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Certificate.Renew`.
///
/// This action generates a certificate signing request by using the existing information and key-pair of the certificate.
pub const CertificateRenewAction = struct {
    /// The challenge password to apply to the certificate for revocation requests.
    ///
    /// This property shall contain the challenge password to apply to the certificate for revocation requests as defined by the RFC2985 'challengePassword' attribute.
    ChallengePassword: ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action forces an automatic renewal of the certificate, if this certificate is configured for automatic certificate enrollment.
    ///
    /// This action shall force an automatic renewal of the certificate, if this certificate is configured for automatic certificate enrollment with a `CertificateEnrollment` resource.  If the certificate is not configured for automatic certificate enrollment, the service shall reject the request and return the HTTP `400 Bad Request` status code.
    @"#Certificate.ForceAutomaticRenew": ?core.Action(CertificateForceAutomaticRenewAction, std.json.Value) = null,
    /// This action generates a new key-pair for a certificate and produces a certificate signing request.
    ///
    /// This action shall use the certificate data to generate a new key-pair for a certificate.  The response shall contain a signing request that a certificate authority (CA) will sign.  The service should retain the private key that generated this request for installation of the certificate.  The private key should not be part of the response.  The private key should not be part of the response.
    @"#Certificate.Rekey": ?core.Action(CertificateRekeyAction, RekeyResponse) = null,
    /// This action generates a certificate signing request by using the existing information and key-pair of the certificate.
    ///
    /// This action shall generate a certificate signing request using the existing information and key-pair of the certificate.  The response shall contain a signing request that a certificate authority (CA) will sign.  The service should retain the private key that this request generates for when the certificate is installed.  The private key should not be part of the response.
    @"#Certificate.Renew": ?core.Action(CertificateRenewAction, RenewResponse) = null,

    /// This action forces an automatic renewal of the certificate, if this certificate is configured for automatic certificate enrollment.
    ///
    /// This action shall force an automatic renewal of the certificate, if this certificate is configured for automatic certificate enrollment with a `CertificateEnrollment` resource.  If the certificate is not configured for automatic certificate enrollment, the service shall reject the request and return the HTTP `400 Bad Request` status code.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn forceAutomaticRenew(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: CertificateForceAutomaticRenewAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Certificate.ForceAutomaticRenew" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action generates a new key-pair for a certificate and produces a certificate signing request.
    ///
    /// This action shall use the certificate data to generate a new key-pair for a certificate.  The response shall contain a signing request that a certificate authority (CA) will sign.  The service should retain the private key that generated this request for installation of the certificate.  The private key should not be part of the response.  The private key should not be part of the response.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn rekey(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: CertificateRekeyAction,
    ) !core.Owned(core.ModificationResponse(RekeyResponse)) {
        const target = self.@"#Certificate.Rekey" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action generates a certificate signing request by using the existing information and key-pair of the certificate.
    ///
    /// This action shall generate a certificate signing request using the existing information and key-pair of the certificate.  The response shall contain a signing request that a certificate authority (CA) will sign.  The service should retain the private key that this request generates for when the certificate is installed.  The private key should not be part of the response.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn renew(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: CertificateRenewAction,
    ) !core.Owned(core.ModificationResponse(RenewResponse)) {
        const target = self.@"#Certificate.Renew" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

pub const Identifier = struct {
    /// The common name of the entity.
    ///
    /// This property shall contain the common name of the entity, as defined by the RFC5280 'commonName' attribute.
    CommonName: ?[]const u8 = null,
    /// The name of the organization of the entity.
    ///
    /// This property shall contain the name of the organization of the entity, as defined by the RFC5280 'organizationName' attribute.
    Organization: ?[]const u8 = null,
    /// The name of the unit or division of the organization of the entity.
    ///
    /// This property shall contain the name of the unit or division of the organization of the entity, as defined by the RFC5280 'organizationalUnitName' attribute.
    OrganizationalUnit: ?[]const u8 = null,
    /// The city or locality of the organization of the entity.
    ///
    /// This property shall contain the city or locality of the organization of the entity, as defined by the RFC5280 'localityName' attribute.
    City: ?[]const u8 = null,
    /// The state, province, or region of the organization of the entity.
    ///
    /// This property shall contain the state, province, or region of the organization of the entity, as defined by the RFC5280 'stateOrProvinceName' attribute.
    State: ?[]const u8 = null,
    /// The country of the organization of the entity.
    ///
    /// This property shall contain the two-letter ISO code for the country of the organization of the entity, as defined by the RFC5280 'countryName' attribute.
    Country: ?[]const u8 = null,
    /// The email address of the contact within the organization of the entity.
    ///
    /// This property shall contain the email address of the contact within the organization of the entity, as defined by the RFC2985 'emailAddress' attribute.
    Email: ?[]const u8 = null,
    /// A human-readable string for this identifier.
    ///
    /// The value of this property shall contain a display string that represents the entire identifier.  The string should be formatted using industry conventions, such as the single-line human-readable string described by RFC2253 and preserving the field order as shown in the certificate.
    DisplayString: ?[]const u8 = null,
    /// The domain components of the entity.
    ///
    /// The value of this property shall contain an array of domain component fields for the entity, as defined by the RFC4519 'domainComponent' attribute, in array order as they appear in the certificate.
    DomainComponents: ?[]const ?[]const u8 = null,
    /// Additional common names of the entity.
    ///
    /// The value of this property shall contain an array of additional common names for the entity, as defined by the RFC5280 'commonName' attribute, in array order as they appear in the certificate.  This property shall not be present if only one common name is found.  The first common name shall not appear in this property.
    AdditionalCommonNames: ?[]const ?[]const u8 = null,
    /// Additional organizational units of the entity.
    ///
    /// The value of this property shall contain an array of additional organizational units for the entity, as defined by the RFC5280 'organizationalUnitName' attribute, in array order as they appear in the certificate.  This property shall not be present if only one organizational unit is found.  The first organizational unit shall not appear in this property.
    AdditionalOrganizationalUnits: ?[]const ?[]const u8 = null,
    /// The additional host names of the entity.
    ///
    /// This property shall contain the additional host names of the entity, as defined by the RFC5280 'subjectAltName' attribute.  This property shall not be present in the `Issuer` property.
    AlternativeNames: ?[]const ?[]const u8 = null,
};

/// The links to other resources that are related to this resource.
///
/// This Redfish Specification-described type shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// A link to the certificate of the CA that issued this certificate.
    ///
    /// This property shall contain a link to a resources of type `Certificate` that represents the certificate of the CA that issued this certificate.
    Issuer: ?core.NavProperty(Certificate) = null,
    /// An array of links to certificates that were issued by the CA that is represented by this certificate.
    ///
    /// This property shall contain an array of links to resources of type `Certificate` that were issued by the CA that is represented by this certificate.
    Subjects: ?[]const core.NavProperty(Certificate) = null,
    /// How many members `Subjects` has in total, which is not how many this response carries.
    @"Subjects@odata.count": ?i64 = null,
    /// The next page of `Subjects`. Present only when this response left members out.
    @"Subjects@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Certificate.Links`.
///
/// The links to other resources that are related to this resource.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The response body for the `Rekey` action.
///
/// This type shall contain the properties found in the response body for the `Rekey` action.
pub const RekeyResponse = struct {
    /// The string for the certificate signing request.
    ///
    /// This property shall contain the certificate signing request as a PEM-encoded string, containing structures specified by RFC2986.  The private key should not be part of the string.
    CSRString: ?[]const u8 = null,
    /// The link to the certificate being rekeyed.
    ///
    /// This property shall contain a link to a resource of type `Certificate` that is replaced after the certificate authority (CA) signs the certificate.
    Certificate: ?core.NavProperty(Certificate) = null,
};

/// The response body for the `Renew` action.
///
/// This type shall contain the properties found in the response body for the `Renew` action.
pub const RenewResponse = struct {
    /// The string for the certificate signing request.
    ///
    /// This property shall contain the certificate signing request as a PEM-encoded string, containing structures specified by RFC2986.  The private key should not be part of the string.
    CSRString: ?[]const u8 = null,
    /// The link to the certificate being renewed.
    ///
    /// This property shall contain a link to a resource of type `Certificate` that is replaced after the certificate authority (CA) signs the certificate.
    Certificate: ?core.NavProperty(Certificate) = null,
};

/// SPDM-related information for a certificate.
///
/// This type shall contain SPDM-related information for a certificate.
pub const Spdm = struct {
    /// Slot identifier of the certificate.
    ///
    /// The value of this property shall contain an integer between 0 and 7, inclusive, that represents the slot identifier for an SPDM-provided certificate.
    SlotId: ?i64 = null,
};

/// The `Certificate` schema describes a certificate that proves the identity of a component, account, or service.
///
/// This resource shall represent a certificate for a Redfish implementation.
pub const Certificate = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The string for the certificate.
    ///
    /// This property shall contain the certificate, and the format shall follow the requirements specified by the `CertificateType` property value.  If the certificate contains any private keys, they shall be removed from the string in responses.  If the service does not know the private key for the certificate and is needed to use the certificate, the client shall provide the private key as part of the string in the `POST` request.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    CertificateString: ?[]const u8 = null,
    /// The format of the certificate.
    ///
    /// This property shall contain the format type for the certificate.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    CertificateType: ?CertificateType = null,
    /// The issuer of the certificate.
    ///
    /// This property shall contain an object containing information about the issuer of the certificate.
    Issuer: ?Identifier = null,
    /// The subject of the certificate.
    ///
    /// This property shall contain an object containing information about the subject of the certificate.
    Subject: ?Identifier = null,
    /// The date when the certificate becomes valid.
    ///
    /// This property shall contain the date when the certificate validity period begins.
    ValidNotBefore: ?core.DateTimeOffset = null,
    /// The date when the certificate is no longer valid.
    ///
    /// This property shall contain the date when the certificate validity period ends.
    ValidNotAfter: ?core.DateTimeOffset = null,
    /// The key usage extension, which defines the purpose of the public keys in this certificate.
    ///
    /// This property shall contain the key usage extension, which defines the purpose of the public keys in this certificate.
    KeyUsage: ?[]const ?KeyUsage = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The UEFI signature owner for this certificate.
    ///
    /// The value of this property shall contain the GUID of the UEFI signature owner for this certificate as defined by the UEFI Specification.  This property shall only be present for certificates managed by UEFI.
    UefiSignatureOwner: ?core.Guid = null,
    /// The serial number of the certificate.
    ///
    /// The value of this property shall be a string containing the ASCII representation of the serial number of the certificate, as defined by the RFC5280 'serialNumber' field.
    SerialNumber: ?[]const u8 = null,
    /// The fingerprint of the certificate.
    ///
    /// The value of this property shall be a string containing the ASCII representation of the fingerprint of the certificate.  The hash algorithm used to generate this fingerprint shall be specified by the `FingerprintHashAlgorithm` property.
    Fingerprint: ?[]const u8 = null,
    /// The hash algorithm for the fingerprint of the certificate.
    ///
    /// The value of this property shall be a string containing the hash algorithm used for generating the `Fingerprint` property.  The value shall be one of the strings in the 'Algorithm Name' field of the 'TCG_ALG_ID Constants' table, formerly the 'TPM_ALG_ID Constants' table, within the 'Trusted Computing Group Algorithm Registry'.
    FingerprintHashAlgorithm: ?[]const u8 = null,
    /// The algorithm used for creating the signature of the certificate.
    ///
    /// The value of this property shall be a string containing the algorithm used for generating the signature of the certificate, as defined by the RFC5280 'signatureAlgorithm' field.  The value shall be a string representing the ASN.1 OID of the signature algorithm as defined in, but not limited to, RFC3279, RFC4055, or RFC4491.
    SignatureAlgorithm: ?[]const u8 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The types or purposes for this certificate.
    ///
    /// The value of this property shall contain an array describing the types or purposes for this certificate.
    CertificateUsageTypes: ?[]const ?CertificateUsageType = null,
    /// SPDM-related information for the certificate.
    ///
    /// The value of this property shall contain SPDM-related information for the certificate.  This property shall only be present for SPDM certificates.
    SPDM: ?Spdm = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
};

/// What a client may change of `Certificate.Certificate`.
///
/// The `Certificate` schema describes a certificate that proves the identity of a component, account, or service.
pub const CertificateUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The password for the certificate.
    ///
    /// This property shall contain the password for the certificate contained in the `CertificateString` property.  This property shall be required in create requests if the `CertificateType` property contains `PKCS12` and the client-provided certificate is password protected.  This property shall not be present in responses.
    Password: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `Certificate.Certificate`.
///
/// The `Certificate` schema describes a certificate that proves the identity of a component, account, or service.
pub const CertificateCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The string for the certificate.
    ///
    /// This property shall contain the certificate, and the format shall follow the requirements specified by the `CertificateType` property value.  If the certificate contains any private keys, they shall be removed from the string in responses.  If the service does not know the private key for the certificate and is needed to use the certificate, the client shall provide the private key as part of the string in the `POST` request.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    CertificateString: []const u8,
    /// The format of the certificate.
    ///
    /// This property shall contain the format type for the certificate.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    CertificateType: CertificateType,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The password for the certificate.
    ///
    /// This property shall contain the password for the certificate contained in the `CertificateString` property.  This property shall be required in create requests if the `CertificateType` property contains `PKCS12` and the client-provided certificate is password protected.  This property shall not be present in responses.
    Password: core.Nullable([]const u8) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
