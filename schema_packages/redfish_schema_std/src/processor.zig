//! Namespace `Processor`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const acceleration_function = @import("acceleration_function.zig");
const acceleration_function_collection = @import("acceleration_function_collection.zig");
const assembly = @import("assembly.zig");
const certificate_collection = @import("certificate_collection.zig");
const chassis = @import("chassis.zig");
const control = @import("control.zig");
const endpoint = @import("endpoint.zig");
const environment_metrics = @import("environment_metrics.zig");
const fabric_adapter = @import("fabric_adapter.zig");
const graphics_controller = @import("graphics_controller.zig");
const memory = @import("memory.zig");
const memory_collection = @import("memory_collection.zig");
const memory_metrics = @import("memory_metrics.zig");
const network_device_function = @import("network_device_function.zig");
const operating_config = @import("operating_config.zig");
const operating_config_collection = @import("operating_config_collection.zig");
const pcie_device = @import("pcie_device.zig");
const pcie_function = @import("pcie_function.zig");
const port_collection = @import("port_collection.zig");
const processor_collection = @import("processor_collection.zig");
const processor_metrics = @import("processor_metrics.zig");
const resource = @import("resource.zig");
const software_inventory = @import("software_inventory.zig");

pub const InstructionSet = []const u8;

pub const ProcessorArchitecture = []const u8;

pub const BaseSpeedPriorityState = enum {
    /// Base speed priority is enabled.
    Enabled,
    /// Base speed priority is disabled.
    Disabled,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const FpgaType = enum {
    /// The FPGA device integrated with other processor in the single chip.
    Integrated,
    /// The discrete FPGA device.
    Discrete,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ProcessorMemoryType = enum {
    /// Processor cache, but no level is determined.
    Cache,
    /// L1 cache.
    L1Cache,
    /// L2 cache.
    L2Cache,
    /// L3 cache.
    L3Cache,
    /// L4 cache.
    L4Cache,
    /// L5 cache.
    L5Cache,
    /// L6 cache.
    L6Cache,
    /// L7 cache.
    L7Cache,
    /// High Bandwidth Memory.
    HBM1,
    /// The second generation of High Bandwidth Memory.
    HBM2,
    /// An updated version of the second generation of High Bandwidth Memory.
    HBM2E,
    /// The third generation of High Bandwidth Memory.
    HBM3,
    /// An updated version of the third generation of High Bandwidth Memory.
    HBM3E,
    /// The fourth generation of High Bandwidth Memory.
    HBM4,
    /// Synchronous graphics RAM.
    SGRAM,
    /// Synchronous graphics random-access memory.
    GDDR,
    /// Double data rate type two synchronous graphics random-access memory.
    GDDR2,
    /// Double data rate type three synchronous graphics random-access memory.
    GDDR3,
    /// Double data rate type four synchronous graphics random-access memory.
    GDDR4,
    /// Double data rate type five synchronous graphics random-access memory.
    GDDR5,
    /// Double data rate type five X synchronous graphics random-access memory.
    GDDR5X,
    /// Double data rate type six synchronous graphics random-access memory.
    GDDR6,
    /// Double data rate type seven synchronous graphics random-access memory.
    GDDR7,
    /// Double data rate synchronous dynamic random-access memory.
    DDR,
    /// Double data rate type two synchronous dynamic random-access memory.
    DDR2,
    /// Double data rate type three synchronous dynamic random-access memory.
    DDR3,
    /// Double data rate type four synchronous dynamic random-access memory.
    DDR4,
    /// Double data rate type five synchronous dynamic random-access memory.
    DDR5,
    /// Synchronous dynamic random-access memory.
    SDRAM,
    /// Static random-access memory.
    SRAM,
    /// Flash memory.
    Flash,
    /// OEM-defined.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ProcessorType = enum {
    /// A CPU.
    CPU,
    /// A GPU.
    GPU,
    /// An FPGA.
    FPGA,
    /// A DSP.
    DSP,
    /// An accelerator.
    Accelerator,
    /// A core in a processor.
    Core,
    /// A thread in a processor.
    Thread,
    /// A partition in a single processor.
    ///
    /// This value shall indicate a partition in a processor that is instantiated from a user configuration to carve out resources in a single processor.  An example of this is assigning memory to a set of cores in a GPU.
    Partition,
    /// An OEM-defined processing unit.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SystemInterfaceType = enum {
    /// The Intel QuickPath Interconnect.
    QPI,
    /// The Intel UltraPath Interconnect.
    UPI,
    /// A PCI Express interface.
    PCIe,
    /// An Ethernet interface.
    Ethernet,
    /// The Arm Advanced Microcontroller Bus Architecture interface.
    AMBA,
    /// The Cache Coherent Interconnect for Accelerators interface.
    CCIX,
    /// The Compute Express Link interface.
    CXL,
    /// An OEM-defined interface.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const ThrottleCause = enum {
    /// The cause of the processor being throttled is a power limit.
    PowerLimit,
    /// The cause of the processor being throttled is a thermal limit.
    ThermalLimit,
    /// The cause of the processor being throttled is a clock limit.
    ClockLimit,
    /// The cause of the processor being throttled is a fault detected by management hardware or firmware.
    ManagementDetectedFault,
    /// The cause of the processor being throttled is not known.
    Unknown,
    /// The cause of the processor being throttled is OEM-specific.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const TurboState = enum {
    /// Turbo is enabled.
    Enabled,
    /// Turbo is disabled.
    Disabled,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `Processor.Reset`.
///
/// This action resets the processor.
pub const ProcessorResetAction = struct {
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and perform an implementation-specific default reset.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Processor.ResetToDefaults`.
///
/// The action resets the values of writable properties to factory defaults.
pub const ProcessorResetToDefaultsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action resets the processor.
    ///
    /// This action shall reset the processor.
    @"#Processor.Reset": ?core.Action(ProcessorResetAction, std.json.Value) = null,
    /// The action resets the values of writable properties to factory defaults.
    ///
    /// This action shall reset the values of writable properties in this resource to their default values as specified by the manufacturer.
    @"#Processor.ResetToDefaults": ?core.Action(ProcessorResetToDefaultsAction, std.json.Value) = null,

    /// This action resets the processor.
    ///
    /// This action shall reset the processor.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ProcessorResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Processor.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// The action resets the values of writable properties to factory defaults.
    ///
    /// This action shall reset the values of writable properties in this resource to their default values as specified by the manufacturer.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resetToDefaults(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: ProcessorResetToDefaultsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Processor.ResetToDefaults" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// This type defines an Ethernet interface.
///
/// These properties shall contain the definition for an Ethernet interface for a Redfish implementation.
pub const EthernetInterface = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The maximum speed supported by this interface.
    ///
    /// This property shall contain the maximum speed supported by this interface.
    MaxSpeedMbps: ?i64 = null,
    /// The number of lanes supported by this interface.
    ///
    /// This property shall contain the maximum number of lanes supported by this interface.
    MaxLanes: ?i64 = null,
};

/// What a client may change of `Processor.EthernetInterface`.
///
/// This type defines an Ethernet interface.
pub const EthernetInterfaceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The properties of the FPGA device.
///
/// This object shall contain the properties of the FPGA device represented by a processor.
pub const Fpga = struct {
    /// The FPGA type.
    ///
    /// This property shall contain a type of the FPGA device.
    FpgaType: ?FpgaType = null,
    /// The FPGA model.
    ///
    /// This property shall contain a model of the FPGA device.
    Model: ?[]const u8 = null,
    /// The FPGA firmware identifier.
    ///
    /// This property shall contain a string describing the FPGA firmware identifier.
    FirmwareId: ?[]const u8 = null,
    /// The FPGA firmware manufacturer.
    ///
    /// This property shall contain a string describing the FPGA firmware manufacturer.
    FirmwareManufacturer: ?[]const u8 = null,
    /// The FPGA firmware version.
    ///
    /// This property shall contain a string describing the FPGA firmware version.
    ///
    /// Deprecated in v1_9_0.
    /// This property has been deprecated in favor of the `FirmwareVersion` property in the root of this resource.
    FirmwareVersion: ?[]const u8 = null,
    /// The FPGA interface to the host.
    ///
    /// This property shall contain an object that describes the connectivity to the host for system software to use.
    ///
    /// Deprecated in v1_8_0.
    /// This property has been deprecated in favor of the `SystemInterface` property in the root of this resource.
    HostInterface: ?ProcessorInterface = null,
    /// An array of the FPGA external interfaces.
    ///
    /// This property shall contain an array of objects that describe the external connectivity of the FPGA.
    ExternalInterfaces: ?[]const ProcessorInterface = null,
    /// The number of PCIe Virtual Functions.
    ///
    /// This property shall contain an integer that describes the number of PCIe Virtual Functions configured within the FPGA.
    PCIeVirtualFunctions: ?i64 = null,
    /// An indication of whether the FPGA firmware can be reprogrammed from the host by using system software.
    ///
    /// This property shall indicate whether the FPGA firmware can be reprogrammed from the host by using system software.  If `false`, system software shall not be able to program the FPGA firmware from the system interface.  In either state, a management controller may be able to program the FPGA firmware by using the sideband interface.
    ProgrammableFromHost: ?bool = null,
    /// An array of the FPGA reconfiguration slots.  An FPGA uses a reconfiguration slot to contain an acceleration function that can change as the FPGA is provisioned.
    ///
    /// This property shall contain an array of the structures that describe the FPGA reconfiguration slots that the acceleration functions can program.
    ReconfigurationSlots: ?[]const FpgaReconfigurationSlot = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
};

/// What a client may change of `Processor.FPGA`.
///
/// The properties of the FPGA device.
pub const FpgaUpdate = struct {
    /// The FPGA interface to the host.
    ///
    /// This property shall contain an object that describes the connectivity to the host for system software to use.
    ///
    /// Deprecated in v1_8_0.
    /// This property has been deprecated in favor of the `SystemInterface` property in the root of this resource.
    HostInterface: ?ProcessorInterfaceUpdate = null,
    /// An array of the FPGA external interfaces.
    ///
    /// This property shall contain an array of objects that describe the external connectivity of the FPGA.
    ExternalInterfaces: ?[]const ProcessorInterfaceUpdate = null,
    /// The number of PCIe Virtual Functions.
    ///
    /// This property shall contain an integer that describes the number of PCIe Virtual Functions configured within the FPGA.
    PCIeVirtualFunctions: ?i64 = null,
    /// An indication of whether the FPGA firmware can be reprogrammed from the host by using system software.
    ///
    /// This property shall indicate whether the FPGA firmware can be reprogrammed from the host by using system software.  If `false`, system software shall not be able to program the FPGA firmware from the system interface.  In either state, a management controller may be able to program the FPGA firmware by using the sideband interface.
    ProgrammableFromHost: core.Nullable(bool) = .absent,
    /// An array of the FPGA reconfiguration slots.  An FPGA uses a reconfiguration slot to contain an acceleration function that can change as the FPGA is provisioned.
    ///
    /// This property shall contain an array of the structures that describe the FPGA reconfiguration slots that the acceleration functions can program.
    ReconfigurationSlots: ?[]const FpgaReconfigurationSlotUpdate = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// This type describes the FPGA reconfiguration slot.  An FPGA uses a reconfiguration slot to contain an acceleration function that can change as the FPGA is provisioned.
///
/// This type shall contain information about the FPGA reconfiguration slot.
pub const FpgaReconfigurationSlot = struct {
    /// The FPGA reconfiguration slot identifier.
    ///
    /// This property shall contain the FPGA reconfiguration slot identifier.
    SlotId: ?[]const u8 = null,
    /// The UUID for this reconfiguration slot.
    ///
    /// This property shall contain a universally unique identifier number for the reconfiguration slot.
    UUID: ?resource.Uuid = null,
    /// An indication of whether the reconfiguration slot can be reprogrammed from the host by using system software.
    ///
    /// This property shall indicate whether the reconfiguration slot can be reprogrammed from the host by using system software.  If `false`, system software shall not be able to program the reconfiguration slot from the system interface.  In either state, a management controller may be able to program the reconfiguration slot by using the sideband interface.
    ProgrammableFromHost: ?bool = null,
    /// The link to the acceleration function that the code programmed into a reconfiguration slot provides.
    ///
    /// This property shall contain a link to a resource of type `AccelerationFunction` that represents the code programmed into this reconfiguration slot.
    AccelerationFunction: ?core.NavProperty(acceleration_function.AccelerationFunction) = null,
};

/// What a client may change of `Processor.FpgaReconfigurationSlot`.
///
/// This type describes the FPGA reconfiguration slot.  An FPGA uses a reconfiguration slot to contain an acceleration function that can change as the FPGA is provisioned.
pub const FpgaReconfigurationSlotUpdate = struct {
    /// An indication of whether the reconfiguration slot can be reprogrammed from the host by using system software.
    ///
    /// This property shall indicate whether the reconfiguration slot can be reprogrammed from the host by using system software.  If `false`, system software shall not be able to program the reconfiguration slot from the system interface.  In either state, a management controller may be able to program the reconfiguration slot by using the sideband interface.
    ProgrammableFromHost: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const Links = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    /// The link to the chassis that contains this processor.
    ///
    /// This property shall contain a link to a resource of type `Chassis` that represents the physical container associated with this processor.
    Chassis: ?core.NavProperty(chassis.Chassis) = null,
    /// An array of links to the endpoints that connect to this processor.
    ///
    /// This property shall contain an array of links to resources of type `Endpoint` that represent endpoints associated with this processor.
    Endpoints: ?[]const core.NavProperty(endpoint.Endpoint) = null,
    /// How many members `Endpoints` has in total, which is not how many this response carries.
    @"Endpoints@odata.count": ?i64 = null,
    /// The next page of `Endpoints`. Present only when this response left members out.
    @"Endpoints@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the processors directly connected to this processor.
    ///
    /// This property shall contain an array of links to resources of type `Processor` that are directly connected to this processor.
    ConnectedProcessors: ?[]const core.NavProperty(Processor) = null,
    /// How many members `ConnectedProcessors` has in total, which is not how many this response carries.
    @"ConnectedProcessors@odata.count": ?i64 = null,
    /// The next page of `ConnectedProcessors`. Present only when this response left members out.
    @"ConnectedProcessors@odata.nextLink": ?core.ODataId = null,
    /// The link to the PCIe device associated with this processor.
    ///
    /// This property shall contain a link to a resource of type `PCIeDevice` that represents the PCIe device associated with this processor.
    PCIeDevice: ?core.NavProperty(pcie_device.PcieDevice) = null,
    /// An array of links to the PCIeFunctions associated with this processor.
    ///
    /// This property shall contain an array of links to resources of type `PCIeFunction` that represent the PCIe functions associated with this processor.
    PCIeFunctions: ?[]const core.NavProperty(pcie_function.PcieFunction) = null,
    /// How many members `PCIeFunctions` has in total, which is not how many this response carries.
    @"PCIeFunctions@odata.count": ?i64 = null,
    /// The next page of `PCIeFunctions`. Present only when this response left members out.
    @"PCIeFunctions@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the memory associated with this processor.
    ///
    /// This property shall contain an array of links to resources of type `Memory` that are associated with this processor.
    Memory: ?[]const core.NavProperty(memory.Memory) = null,
    /// How many members `Memory` has in total, which is not how many this response carries.
    @"Memory@odata.count": ?i64 = null,
    /// The next page of `Memory`. Present only when this response left members out.
    @"Memory@odata.nextLink": ?core.ODataId = null,
    /// A link to the graphics controller associated with this processor.
    ///
    /// This property shall contain a link to a resource of type `GraphicsController` that is associated with this processor.
    GraphicsController: ?core.NavProperty(graphics_controller.GraphicsController) = null,
    /// The network device functions to which this processor performs offload computation, such as with a SmartNIC.
    ///
    /// This property shall contain an array of links to resources of type `NetworkDeviceFunction` that represent the network device functions to which this processor performs offload computation, such as with a SmartNIC.
    NetworkDeviceFunctions: ?[]const core.NavProperty(network_device_function.NetworkDeviceFunction) = null,
    /// How many members `NetworkDeviceFunctions` has in total, which is not how many this response carries.
    @"NetworkDeviceFunctions@odata.count": ?i64 = null,
    /// The next page of `NetworkDeviceFunctions`. Present only when this response left members out.
    @"NetworkDeviceFunctions@odata.nextLink": ?core.ODataId = null,
    /// An array of links to the fabric adapters that present this processor to a fabric.
    ///
    /// This property shall contain an array of links to resources of type `FabricAdapter` that represent the fabric adapters that present this processor to a fabric.
    FabricAdapters: ?[]const core.NavProperty(fabric_adapter.FabricAdapter) = null,
    /// How many members `FabricAdapters` has in total, which is not how many this response carries.
    @"FabricAdapters@odata.count": ?i64 = null,
    /// The next page of `FabricAdapters`. Present only when this response left members out.
    @"FabricAdapters@odata.nextLink": ?core.ODataId = null,
};

/// What a client may change of `Processor.Links`.
pub const LinksUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties contained in this object shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

pub const MemorySummary = struct {
    /// Total size of cache memory of this processor.
    ///
    /// This property shall contain the total size of cache memory of this processor.
    TotalCacheSizeMiB: ?i64 = null,
    /// Total size of non-cache volatile or non-volatile memory attached to this processor.  Examples include DRAMs and NV-DIMMs that are not configured as block storage.
    ///
    /// This property shall contain the total size of non-cache volatile or non-volatile memory attached to this processor.  Examples include DRAMs and NV-DIMMs that are not configured as block storage.  This value indicates the size of memory directly attached or with strong affinity to this processor, not the total memory accessible by the processor.  This property shall not be present for implementations where all processors have equal memory performance or access characteristics, such as hop count, for all system memory.
    TotalMemorySizeMiB: ?i64 = null,
    /// An indication of whether memory ECC mode is enabled for this processor.
    ///
    /// The value of this property shall indicate if memory ECC mode is enabled for this processor.  This value shall not affect system memory ECC mode.
    ECCModeEnabled: ?bool = null,
    /// The link to the memory metrics associated with all memory of this processor.
    ///
    /// This property shall contain a link to a resource of type `MemoryMetrics` that contains the metrics associated with all memory of this processor.
    Metrics: ?core.NavProperty(memory_metrics.MemoryMetrics) = null,
};

/// What a client may change of `Processor.MemorySummary`.
pub const MemorySummaryUpdate = struct {
    /// An indication of whether memory ECC mode is enabled for this processor.
    ///
    /// The value of this property shall indicate if memory ECC mode is enabled for this processor.  This value shall not affect system memory ECC mode.
    ECCModeEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const ProcessorId = struct {
    /// The vendor identification for this processor.
    ///
    /// This property shall contain the vendor identification information as provided by the manufacturer of this processor.  If this property represents raw register data, as determined by the value of the `ProcessorArchitecture` property, the service shall encode the value as a hex-encoded string following the regular expression pattern `^0x[0-9A-Fa-f]+$` or a decimal-encoded string following the regular expression pattern `^\d+$`.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    VendorId: ?[]const u8 = null,
    /// The raw manufacturer-provided processor identification registers for this processor.
    ///
    /// This property shall contain the raw manufacturer-provided processor-specific identification registers of this processor's features.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    IdentificationRegisters: ?[]const u8 = null,
    /// The effective family for this processor.
    ///
    /// This property shall contain the effective family information as provided by the manufacturer of this processor.  If this property represents raw register data, as determined by the value of the `ProcessorArchitecture` property, the service shall encode the value as a hex-encoded string following the regular expression pattern `^0x[0-9A-Fa-f]+$` or a decimal-encoded string following the regular expression pattern `^\d+$`.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    EffectiveFamily: ?[]const u8 = null,
    /// The effective model for this processor.
    ///
    /// This property shall contain the effective model information as provided by the manufacturer of this processor.  If this property represents raw register data, as determined by the value of the `ProcessorArchitecture` property, the service shall encode the value as a hex-encoded string following the regular expression pattern `^0x[0-9A-Fa-f]+$` or a decimal-encoded string following the regular expression pattern `^\d+$`.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    EffectiveModel: ?[]const u8 = null,
    /// The step value for this processor.
    ///
    /// This property shall contain the step or revision information as provided by the manufacturer of this processor.  If this property represents raw register data, as determined by the value of the `ProcessorArchitecture` property, the service shall encode the value as a hex-encoded string following the regular expression pattern `^0x[0-9A-Fa-f]+$` or a decimal-encoded string following the regular expression pattern `^\d+$`.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    Step: ?[]const u8 = null,
    /// The microcode information for this processor.
    ///
    /// This property shall contain the microcode information as provided by the manufacturer of this processor.  If this property represents raw register data, as determined by the value of the `ProcessorArchitecture` property, the service shall encode the value as a hex-encoded string following the regular expression pattern `^0x[0-9A-Fa-f]+$` or a decimal-encoded string following the regular expression pattern `^\d+$`.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    MicrocodeInfo: ?[]const u8 = null,
    /// The Protected Processor Identification Number (PPIN) for this processor.
    ///
    /// This property shall contain the Protected Processor Identification Number (PPIN) for this processor.
    ProtectedIdentificationNumber: ?[]const u8 = null,
};

/// This type describes an interface between the system, or external connection, and the processor.
///
/// This type shall contain information about the system interface, or external connection, to the processor.
pub const ProcessorInterface = struct {
    /// The interface type.
    ///
    /// This property shall contain an enumerated value that describes the type of interface between the system, or external connection, and the processor.
    InterfaceType: ?SystemInterfaceType = null,
    /// The PCIe-related information for this interface.
    ///
    /// This property shall contain an object the describes the PCIe-related information for this interface.
    PCIe: ?pcie_device.PcieInterface = null,
    /// The Ethernet-related information for this interface.
    ///
    /// This property shall contain an object the describes the Ethernet-related information for this interface.
    Ethernet: ?EthernetInterface = null,
};

/// What a client may change of `Processor.ProcessorInterface`.
///
/// This type describes an interface between the system, or external connection, and the processor.
pub const ProcessorInterfaceUpdate = struct {
    /// The PCIe-related information for this interface.
    ///
    /// This property shall contain an object the describes the PCIe-related information for this interface.
    PCIe: ?pcie_device.PcieInterfaceUpdate = null,
    /// The Ethernet-related information for this interface.
    ///
    /// This property shall contain an object the describes the Ethernet-related information for this interface.
    Ethernet: ?EthernetInterfaceUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// This type describes the memory directly attached or integrated within a processor.
///
/// This type shall contain information about memory directly attached or integrated within a processor.
pub const ProcessorMemory = struct {
    /// An indication of whether this memory is integrated within the processor.
    ///
    /// This property shall indicate whether this memory is integrated within the processor.  Otherwise, it is discrete memory attached to the processor.
    IntegratedMemory: ?bool = null,
    /// The type of memory used by this processor.
    ///
    /// This property shall contain a type of the processor memory type.
    MemoryType: ?ProcessorMemoryType = null,
    /// The memory capacity in MiB.
    ///
    /// This property shall contain the memory capacity in MiB.
    CapacityMiB: ?i64 = null,
    /// The operating speed of the memory in MHz.
    ///
    /// This property shall contain the operating speed of the memory in MHz.
    SpeedMHz: ?i64 = null,
};

/// The UALink attributes of a processor.
///
/// This type shall contain information about UALink fabric and port management within the processor.
pub const UaLink = struct {
    /// The UALink Accelerator ID of the processor.
    ///
    /// This property shall contain an integer that is unique within a UALink Pod and that serves as the Source and Destination Accelerator ID on the UALink network.
    AcceleratorID: ?i64 = null,
};

/// What a client may change of `Processor.UALink`.
///
/// The UALink attributes of a processor.
pub const UaLinkUpdate = struct {
    /// The UALink Accelerator ID of the processor.
    ///
    /// This property shall contain an integer that is unique within a UALink Pod and that serves as the Source and Destination Accelerator ID on the UALink network.
    AcceleratorID: ?i64 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `Processor` schema describes the information about a single processor that a system contains.  A processor includes both performance characteristics, clock speed, architecture, core count, and so on, and compatibility, such as the CPU ID instruction results.  It also describes the location, such as a slot, socket, or bay, where a unit can be installed, by populating a resource instance with an absent state if a unit is not present.
///
/// This resource shall represent a single processor that a system contains.  A processor includes both performance characteristics, clock speed, architecture, core count, and so on, and compatibility, such as the CPU ID instruction results.  It may also represent a location, such as a slot, socket, or bay, where a unit may be installed, but the `State` property within the `Status` property contains `Absent`.
pub const Processor = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The socket or location of the processor.
    ///
    /// This property shall contain the string that identifies the physical location or socket of the processor.
    Socket: ?[]const u8 = null,
    /// The type of processor.
    ///
    /// This property shall contain the string that identifies the type of processor contained in this socket.
    ProcessorType: ?ProcessorType = null,
    /// The architecture of the processor.
    ///
    /// This property shall contain the string that identifies the architecture of the processor contained in this socket.
    ProcessorArchitecture: ?ProcessorArchitecture = null,
    /// The instruction set of the processor.
    ///
    /// This property shall contain the string that identifies the instruction set of the processor contained in this socket.
    InstructionSet: ?InstructionSet = null,
    /// The identification information for this processor.
    ///
    /// This object shall contain identification information for this processor.  For additional property requirements, see the corresponding definition in the Redfish Data Model Specification.
    ProcessorId: ?ProcessorId = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The processor manufacturer.
    ///
    /// This property shall contain a string that identifies the manufacturer of the processor.
    Manufacturer: ?[]const u8 = null,
    /// The product model number of this device.
    ///
    /// This property shall indicate the model information as provided by the manufacturer of this processor.
    Model: ?[]const u8 = null,
    /// The maximum clock speed of the processor.
    ///
    /// This property shall indicate the maximum rated clock speed of the processor in MHz.
    MaxSpeedMHz: ?i64 = null,
    /// The total number of cores that this processor contains.
    ///
    /// This property shall indicate the total count of independent processor cores, including disabled cores, contained within this processor.
    TotalCores: ?i64 = null,
    /// The total number of execution threads that this processor supports.
    ///
    /// This property shall indicate the total count of independent execution threads, including disabled threads, that this processor supports.
    TotalThreads: ?i64 = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?Links = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The location of the processor.
    ///
    /// This property shall contain the location information of the associated processor.
    Location: ?resource.Location = null,
    /// The nominal Thermal Design Power (TDP) in watt units.
    ///
    /// This property shall contain the nominal Thermal Design Power (TDP) in watt units.
    TDPWatts: ?i64 = null,
    /// The maximum Thermal Design Power (TDP) in watt units.
    ///
    /// This property shall contain the maximum Thermal Design Power (TDP) in watt units.
    MaxTDPWatts: ?i64 = null,
    /// The UUID for this processor.
    ///
    /// This property shall contain a universally unique identifier number for the processor.  RFC4122 describes methods to use to create the value.  The value should be considered to be opaque.  Client software should only treat the overall value as a universally unique identifier and should not interpret any subfields within the UUID.
    UUID: ?resource.Uuid = null,
    /// The memory directly attached or integrated within this processor.  Examples include internal cache, dedicated memory for the processor, and system memory.
    ///
    /// This property shall contain the memory directly attached or integrated within this processor.
    ProcessorMemory: ?[]const ProcessorMemory = null,
    /// The properties for processors of the FPGA type.
    ///
    /// This property shall contain an object containing properties for processors of type `FPGA`.
    FPGA: ?Fpga = null,
    /// The total number of enabled cores that this processor contains.
    ///
    /// This property shall indicate the total count of enabled independent processor cores contained within this processor.
    TotalEnabledCores: ?i64 = null,
    /// The serial number of the processor.
    ///
    /// This property shall contain a manufacturer-allocated number that identifies the processor.
    SerialNumber: ?[]const u8 = null,
    /// The part number of the processor.
    ///
    /// This property shall contain a part number assigned by the organization that is responsible for producing or manufacturing the processor.
    PartNumber: ?[]const u8 = null,
    /// The hardware version of the processor.
    ///
    /// This property shall contain the hardware version of the processor as determined by the vendor or supplier.
    Version: ?[]const u8 = null,
    /// The firmware version of the processor.
    ///
    /// This property shall contain a string describing the firmware version of the processor as provided by the manufacturer.
    FirmwareVersion: ?[]const u8 = null,
    /// The interface between the system and the processor.
    ///
    /// This property shall contain an object that describes the connectivity between the host system and the processor.
    SystemInterface: ?ProcessorInterface = null,
    /// Operating speed of the processor in MHz.
    ///
    /// This property shall contain the operating speed of the processor in MHz.  The operating speed of the processor may change more frequently than the manager is able to monitor.
    OperatingSpeedMHz: ?i64 = null,
    /// The minimum clock speed of the processor in MHz.
    ///
    /// This property shall indicate the minimum rated clock speed of the processor in MHz.
    MinSpeedMHz: ?i64 = null,
    /// The state of turbo for this processor.
    ///
    /// This property shall contain the state of turbo for this processor.
    TurboState: ?TurboState = null,
    /// The state of the base frequency settings of the operation configuration applied to this processor.
    ///
    /// This property shall contain the state of the base frequency settings of the operating configuration applied to this processor.
    BaseSpeedPriorityState: ?BaseSpeedPriorityState = null,
    /// The list of core identifiers corresponding to the cores that have been configured with the higher clock speed from the operating configuration applied to this processor.
    ///
    /// This property shall contain an array of core identifiers corresponding to the cores that have been configured with the higher clock speed from the operating configuration applied to this processor.
    HighSpeedCoreIDs: ?[]const ?i64 = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: ?bool = null,
    /// The base (nominal) clock speed of the processor in MHz.
    ///
    /// This property shall contain the base (nominal) clock speed of the processor in MHz.
    BaseSpeedMHz: ?i64 = null,
    /// The clock limit of the processor in MHz.
    ///
    /// This property shall contain the clock limit of the processor in MHz.  This value shall be within the range of `MinSpeedMHz` and `MaxSpeedMHz` as provided by the manufacturer of this processor.
    SpeedLimitMHz: ?i64 = null,
    /// Indicates whether the clock speed of the processor is fixed at the value specified in the `SpeedLimitMHz` property.
    ///
    /// This property shall indicate whether the clock speed of the processor is fixed, where a value `true` shall indicate that the clock speed is fixed at the value specified in the `SpeedLimitMHz` property.
    SpeedLocked: ?bool = null,
    /// The summary of all memory associated with this processor.
    ///
    /// This property shall contain properties that describe the summary of all memory that is associated with this processor.
    MemorySummary: ?MemorySummary = null,
    /// The spare part number of the processor.
    ///
    /// This property shall contain the spare part number of the processor.
    SparePartNumber: ?[]const u8 = null,
    /// An array of DSP0274-defined measurement blocks.
    ///
    /// This property shall contain an array of DSP0274-defined measurement blocks.
    ///
    /// Deprecated in v1_14_0.
    /// This property has been deprecated in favor of the `ComponentIntegrity` resource.
    Measurements: ?[]const software_inventory.MeasurementBlock = null,
    /// An indication of whether this processor is enabled.
    ///
    /// The value of this property shall indicate if this processor is enabled.
    Enabled: ?bool = null,
    /// The additional firmware versions of the processor.
    ///
    /// This property shall contain the additional firmware versions of the processor.
    AdditionalFirmwareVersions: ?software_inventory.AdditionalVersions = null,
    /// An indication of whether this component can be independently replaced as allowed by the vendor's replacement policy.
    ///
    /// This property shall indicate whether this component can be independently replaced as allowed by the vendor's replacement policy.  A value of `false` indicates the component needs to be replaced by policy as part of another component.  If the `LocationType` property of this component contains `Embedded`, this property shall contain `false`.
    Replaceable: ?bool = null,
    /// An indication of whether the processor is throttled.
    ///
    /// This property shall indicate whether the processor is throttled.
    Throttled: ?bool = null,
    /// The causes of the processor being throttled.
    ///
    /// This property shall contain the causes of the processor being throttled.  If `Throttled` contains `false`, this property shall contain an empty array.
    ThrottleCauses: ?[]const ?ThrottleCause = null,
    /// The logical index of this processor within the system.
    ///
    /// This property shall contain the zero-based index of the processor, indexed within the next unit of containment.  The value of this property shall match the ordering in the operating system topology interfaces, with offset adjustments, if needed.
    ProcessorIndex: ?i64 = null,
    /// The processor family.
    ///
    /// This property shall contain a string that identifies the processor family, as specified by the combination of the `EffectiveFamily` and `EffectiveModel` properties.
    Family: ?[]const u8 = null,
    /// The current power state of the processor.
    ///
    /// This property shall contain the power state of the processor.  If the `PowerState` property in the associated `Chassis` resource contains the value `Off`, this property shall contain `Off`.
    PowerState: ?resource.PowerState = null,
    /// The total number of enabled execution threads contained in this processor.
    ///
    /// This property shall indicate the total count of enabled independent execution threads contained within this processor.
    TotalEnabledThreads: ?i64 = null,
    /// The UALink properties of the processor.
    ///
    /// This property shall contain UALink attributes of the processor.
    UALink: ?UaLink = null,
    /// The link to an assembly associated with this processor.
    ///
    /// This property shall contain a link to a resource of type `Assembly`.
    Assembly: ?core.NavProperty(assembly.Assembly) = null,
    /// The link to the collection of sub-processors associated with this processor, such as cores or threads, that are part of a processor.
    ///
    /// This property shall contain a link to a resource collection of type `ProcessorCollection`.
    SubProcessors: ?core.NavProperty(processor_collection.ProcessorCollection) = null,
    /// The link to the metrics associated with this processor.
    ///
    /// This property shall contain a link to a resource of type `ProcessorMetrics` that contains the metrics associated with this processor.
    Metrics: ?core.NavProperty(processor_metrics.ProcessorMetrics) = null,
    /// The link to the collection of acceleration functions associated with this processor.
    ///
    /// This property shall contain a link to a resource collection of type `AccelerationFunctionCollection`.
    AccelerationFunctions: ?core.NavProperty(acceleration_function_collection.AccelerationFunctionCollection) = null,
    /// The link to the collection of operating configurations that can be applied to this processor.
    ///
    /// This property shall contain a link to a resource collection of type `OperatingConfigCollection`.
    OperatingConfigs: ?core.NavProperty(operating_config_collection.OperatingConfigCollection) = null,
    /// The link to the operating configuration that is applied to this processor.
    ///
    /// This property shall contain a link to a resource of type `OperatingConfig` that specifies the configuration is applied to this processor.
    AppliedOperatingConfig: ?core.NavProperty(operating_config.OperatingConfig) = null,
    /// The link to the environment metrics for this processor.
    ///
    /// This property shall contain a link to a resource of type `EnvironmentMetrics` that specifies the environment metrics for this processor.
    EnvironmentMetrics: ?core.NavProperty(environment_metrics.EnvironmentMetrics) = null,
    /// The link to a collection of certificates for device identity and attestation.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that contains certificates for device identity and attestation.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// Range of allowed operating speeds (MHz).
    ///
    /// This property shall contain the operating speed control, measured in megahertz units, for this resource.  The value of the `DataSourceUri` property, if present, shall reference a resource of type `Control` with the `ControlType` property containing the value of `FrequencyMHz`.
    OperatingSpeedRangeMHz: ?control.ControlExcerptRange = null,
    /// The link to the collection of ports for this processor.
    ///
    /// This property shall contain a link to a resource collection of type `PortCollection`.  It shall contain the interconnect and fabric ports of this processor.  It shall not contain ports for `GraphicsController` resources, `USBController` resources, or other local adapter-related types of resources.
    Ports: ?core.NavProperty(port_collection.PortCollection) = null,
    /// The link to the collection of cache memory associated with this processor.
    ///
    /// This property shall contain a link to a resource collection of type `MemoryCollection` that represents the cache memory of this processor.
    CacheMemory: ?core.NavProperty(memory_collection.MemoryCollection) = null,
};

/// What a client may change of `Processor.Processor`.
///
/// The `Processor` schema describes the information about a single processor that a system contains.  A processor includes both performance characteristics, clock speed, architecture, core count, and so on, and compatibility, such as the CPU ID instruction results.  It also describes the location, such as a slot, socket, or bay, where a unit can be installed, by populating a resource instance with an absent state if a unit is not present.
pub const ProcessorUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The links to other resources that are related to this resource.
    ///
    /// This property shall contain links to resources that are related to but are not contained by, or subordinate to, this resource.
    Links: ?LinksUpdate = null,
    /// The location of the processor.
    ///
    /// This property shall contain the location information of the associated processor.
    Location: ?resource.LocationUpdate = null,
    /// The properties for processors of the FPGA type.
    ///
    /// This property shall contain an object containing properties for processors of type `FPGA`.
    FPGA: ?FpgaUpdate = null,
    /// The interface between the system and the processor.
    ///
    /// This property shall contain an object that describes the connectivity between the host system and the processor.
    SystemInterface: ?ProcessorInterfaceUpdate = null,
    /// An indicator allowing an operator to physically locate this resource.
    ///
    /// This property shall contain the state of the indicator used to physically identify or locate this resource.  A write to this property shall update the value of `IndicatorLED` in this resource, if supported, to reflect the implementation of the locating function.
    LocationIndicatorActive: core.Nullable(bool) = .absent,
    /// The clock limit of the processor in MHz.
    ///
    /// This property shall contain the clock limit of the processor in MHz.  This value shall be within the range of `MinSpeedMHz` and `MaxSpeedMHz` as provided by the manufacturer of this processor.
    SpeedLimitMHz: core.Nullable(i64) = .absent,
    /// Indicates whether the clock speed of the processor is fixed at the value specified in the `SpeedLimitMHz` property.
    ///
    /// This property shall indicate whether the clock speed of the processor is fixed, where a value `true` shall indicate that the clock speed is fixed at the value specified in the `SpeedLimitMHz` property.
    SpeedLocked: core.Nullable(bool) = .absent,
    /// The summary of all memory associated with this processor.
    ///
    /// This property shall contain properties that describe the summary of all memory that is associated with this processor.
    MemorySummary: ?MemorySummaryUpdate = null,
    /// An indication of whether this processor is enabled.
    ///
    /// The value of this property shall indicate if this processor is enabled.
    Enabled: ?bool = null,
    /// The additional firmware versions of the processor.
    ///
    /// This property shall contain the additional firmware versions of the processor.
    AdditionalFirmwareVersions: ?software_inventory.AdditionalVersionsUpdate = null,
    /// The UALink properties of the processor.
    ///
    /// This property shall contain UALink attributes of the processor.
    UALink: ?UaLinkUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
