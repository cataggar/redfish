//! Namespace `PrivilegeRegistry`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const privileges = @import("privileges.zig");
const resource = @import("resource.zig");

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
};

/// The mapping between a resource type and the relevant privileges that accesses the resource.
///
/// This type shall describe a mapping between a resource type and the relevant privileges that accesses the resource.
pub const Mapping = struct {
    /// The resource name, such as `Manager`.
    ///
    /// This property shall contain the resource name, such as `Manager`.
    Entity: ?[]const u8 = null,
    /// The privilege overrides of the subordinate resource.
    ///
    /// This property shall contain the privilege overrides of the subordinate resource.  The target lists are identified by resource type.
    SubordinateOverrides: ?[]const TargetPrivilegeMap = null,
    /// The privilege overrides of resource URIs.
    ///
    /// This property shall contain the privilege overrides of resource URIs.  The target lists the resource URI and the new privileges.
    ResourceURIOverrides: ?[]const TargetPrivilegeMap = null,
    /// The privilege overrides of properties within a resource.
    ///
    /// This property shall contain the privilege overrides of properties, such as the `Password` property in the `ManagerAccount` resource.
    PropertyOverrides: ?[]const TargetPrivilegeMap = null,
    /// List mapping between HTTP methods and privilege required for the resource.
    ///
    /// This property shall list the mapping between HTTP methods and the privilege required for the resource.
    OperationMap: ?OperationMap = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The specific privileges required to complete a set of HTTP operations.
///
/// This type shall describe the specific privileges required to complete a set of HTTP operations.
pub const OperationMap = struct {
    /// The privilege required to complete an HTTP `GET` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `GET` operation.
    GET: ?[]const OperationPrivilege = null,
    /// The privilege required to complete an HTTP `HEAD` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `HEAD` operation.
    HEAD: ?[]const OperationPrivilege = null,
    /// The privilege required to complete an HTTP `PATCH` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `PATCH` operation.
    PATCH: ?[]const OperationPrivilege = null,
    /// The privilege required to complete an HTTP `POST` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `POST` operation.
    POST: ?[]const OperationPrivilege = null,
    /// The privilege required to complete an HTTP `PUT` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `PUT` operation.
    PUT: ?[]const OperationPrivilege = null,
    /// The privilege required to complete an HTTP `DELETE` operation.
    ///
    /// This property shall contain the privilege required to complete an HTTP `DELETE` operation.
    DELETE: ?[]const OperationPrivilege = null,
};

/// The privileges for a specific HTTP operation.
///
/// This type shall describe the privileges required to complete a specific HTTP operation.
pub const OperationPrivilege = struct {
    /// An array of privileges that are required to complete a specific HTTP operation on a resource.
    ///
    /// This array shall contain an array of privileges that are required to complete a specific HTTP operation on a resource.  This set of strings match zero or more strings in the `PrivilegesUsed` and `OEMPrivilegesUsed` properties.
    Privilege: ?[]const []const u8 = null,
};

/// This type describes a mapping between one or more targets and the HTTP operations associated with them.
///
/// This type shall describe a mapping between one or more targets and the HTTP operations associated with them.
pub const TargetPrivilegeMap = struct {
    /// The set of URIs, resource types, or properties.
    ///
    /// This property shall contain the array of URIs, resource types, or properties.  For example, `/redfish/v1/Systems/1`, `Manager`, or `Password`.  When the `Targets` property is not present, no override is specified.
    Targets: ?[]const []const u8 = null,
    /// The mapping between the HTTP operation and the privilege required to complete the operation.
    ///
    /// This property shall contain the mapping between the HTTP operation and the privilege required to complete the operation.
    OperationMap: ?OperationMap = null,
};

/// The `PrivilegeRegistry` schema describes the operation-to-privilege mappings.
///
/// This resource shall contain operation-to-privilege mappings.
pub const PrivilegeRegistry = struct {
    /// Where the resource lives.
    @"@odata.id": core.ODataId,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: resource.Id,
    Description: ?resource.Description = null,
    Name: resource.Name,
    /// The set of Redfish standard privileges used in this mapping.
    ///
    /// This property shall contain an array of Redfish standard privileges used in this mapping.
    PrivilegesUsed: ?[]const privileges.PrivilegeType = null,
    /// The set of OEM privileges used in this mapping.
    ///
    /// This property shall contain an array of OEM privileges used in this mapping.
    OEMPrivilegesUsed: ?[]const []const u8 = null,
    /// The mappings between entities and the relevant privileges that access those entities.
    ///
    /// This property shall describe the mappings between entities and the relevant privileges that access those entities.
    Mappings: ?[]const Mapping = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The descriptions of the OEM privileges used in this mapping.
    ///
    /// This property shall contain an array of the descriptions of the values of the `OEMPrivilegesUsed` property.  The order of this array shall match the order of the `OEMPrivilegesUsed` property.
    OEMPrivilegeDescriptions: ?[]const []const u8 = null,
};

test {
    std.testing.refAllDecls(@This());
}
