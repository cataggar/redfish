//! Namespace `Aggregate`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const resource = @import("resource.zig");
const settings = @import("settings.zig");

/// Arguments to `Aggregate.AddElements`.
///
/// This action is used to add one or more resources to the aggregate.
pub const AggregateAddElementsAction = struct {
    /// An array of resource links to add to the `Elements` array.
    ///
    /// This parameter shall contain an array of links to the specified resources to add to the aggregate's `Elements` array.
    Elements: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Aggregate.RemoveElements`.
///
/// This action is used to remove one or more resources from the aggregate.
pub const AggregateRemoveElementsAction = struct {
    /// An array of resource links to remove from the `Elements` array.
    ///
    /// This parameter shall contain an array of links to the specified resources to remove from the aggregate's `Elements` array.
    Elements: ?[]const core.Reference = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Aggregate.Reset`.
///
/// This action is used to reset a collection of resources.  For example, this could be an aggregate or a list of computer systems.
pub const AggregateResetAction = struct {
    /// The delay of the batches of elements being reset in seconds.
    ///
    /// This parameter shall contain the delay of the batches of elements being reset in seconds.
    DelayBetweenBatchesInSeconds: ?i64 = null,
    /// The number of elements in each batch being reset.
    ///
    /// This parameter shall contain the number of elements in each batch simultaneously being issued a reset.
    BatchSize: ?i64 = null,
    /// The type of reset.
    ///
    /// This parameter shall contain the type of reset.  The service can accept a request without the parameter and perform an implementation-specific default reset.
    ResetType: ?resource.ResetType = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `Aggregate.SetDefaultBootOrder`.
///
/// This action is used to restore the boot order to the default state for the computer systems that are members of this aggregate.
pub const AggregateSetDefaultBootOrderAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action is used to add one or more resources to the aggregate.
    ///
    /// This action shall add one or more resources to the aggregate, with the result that the resources are included in the `Elements` array of the aggregate.
    @"#Aggregate.AddElements": ?core.Action(AggregateAddElementsAction, std.json.Value) = null,
    /// This action is used to remove one or more resources from the aggregate.
    ///
    /// This action shall remove one or more resources from the aggregate, with the result that the resources are removed from the `Elements` array of the aggregate.
    @"#Aggregate.RemoveElements": ?core.Action(AggregateRemoveElementsAction, std.json.Value) = null,
    /// This action is used to reset a collection of resources.  For example, this could be an aggregate or a list of computer systems.
    ///
    /// This action shall perform a reset of a collection of resources.
    @"#Aggregate.Reset": ?core.Action(AggregateResetAction, std.json.Value) = null,
    /// This action is used to restore the boot order to the default state for the computer systems that are members of this aggregate.
    ///
    /// This action shall restore the boot order to the default state for the computer systems that are members of this aggregate.
    @"#Aggregate.SetDefaultBootOrder": ?core.Action(AggregateSetDefaultBootOrderAction, std.json.Value) = null,

    /// This action is used to add one or more resources to the aggregate.
    ///
    /// This action shall add one or more resources to the aggregate, with the result that the resources are included in the `Elements` array of the aggregate.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn addElements(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregateAddElementsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Aggregate.AddElements" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to remove one or more resources from the aggregate.
    ///
    /// This action shall remove one or more resources from the aggregate, with the result that the resources are removed from the `Elements` array of the aggregate.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn removeElements(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregateRemoveElementsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Aggregate.RemoveElements" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to reset a collection of resources.  For example, this could be an aggregate or a list of computer systems.
    ///
    /// This action shall perform a reset of a collection of resources.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn reset(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregateResetAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Aggregate.Reset" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action is used to restore the boot order to the default state for the computer systems that are members of this aggregate.
    ///
    /// This action shall restore the boot order to the default state for the computer systems that are members of this aggregate.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn setDefaultBootOrder(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: AggregateSetDefaultBootOrderAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#Aggregate.SetDefaultBootOrder" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `Aggregate` schema describes a grouping method for an aggregation service.  Aggregates are formal groups of resources that are more persistent than ad hoc groupings.
///
/// This resource shall represent an aggregation service grouping method for a Redfish implementation.
pub const Aggregate = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The number of entries in the `Elements` array.
    ///
    /// This property shall contain the number of entries in the `Elements` array.
    ElementsCount: ?i64 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The elements of this aggregate.
    ///
    /// This property shall contain an array of links to the elements of this aggregate.
    Elements: ?[]const core.NavProperty(resource.Resource) = null,
    /// How many members `Elements` has in total, which is not how many this response carries.
    @"Elements@odata.count": ?i64 = null,
    /// The next page of `Elements`. Present only when this response left members out.
    @"Elements@odata.nextLink": ?core.ODataId = null,
};

/// What a client may supply when creating an instance of `Aggregate.Aggregate`.
///
/// The `Aggregate` schema describes a grouping method for an aggregation service.  Aggregates are formal groups of resources that are more persistent than ad hoc groupings.
pub const AggregateCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
