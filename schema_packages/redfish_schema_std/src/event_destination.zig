//! Namespace `EventDestination`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const certificate_collection = @import("certificate_collection.zig");
const event = @import("event.zig");
const metric_report_definition = @import("metric_report_definition.zig");
const resource = @import("resource.zig");

pub const DeliveryRetryPolicy = enum {
    /// The subscription is terminated after the maximum number of retries is reached.
    ///
    /// This value shall indicate the subscription is terminated after the maximum number of retries is reached, specified by the `DeliveryRetryAttempts` property in the event service.  The service shall delete this resource to terminate the subscription.
    TerminateAfterRetries,
    /// The subscription is suspended after the maximum number of retries is reached.
    ///
    /// This value shall indicate the subscription is suspended after the maximum number of retries is reached, specified by the `DeliveryRetryAttempts` property in the event service.  The value of the `State` property within `Status` shall contain `Disabled` for a suspended subscription.
    SuspendRetries,
    /// The subscription is not suspended or terminated, and attempts at delivery of future events continues regardless of the number of retries.
    ///
    /// This value shall indicate the subscription is not suspended or terminated, and attempts at delivery of future events shall continue regardless of the number of retries.  The interval between retries remains constant and is specified by the `DeliveryRetryIntervalSeconds` property in the event service.  The `DeliveryRetryAttempts` property in the event service shall not affect subscriptions configured with this policy.
    RetryForever,
    /// The subscription is not suspended or terminated, and attempts at delivery of future events continues regardless of the number of retries, but issued over time according to a service-defined backoff algorithm.
    ///
    /// This value shall indicate the subscription is not suspended or terminated, and attempts at delivery of future events shall continue regardless of the number of retries.  Retry attempts are issued over time according to a service-defined backoff algorithm.  The backoff algorithm may insert an increasing amount of delay between retry attempts and may reach a maximum.  The `DeliveryRetryAttempts` property in the event service shall not affect subscriptions configured with this policy.
    RetryForeverWithBackoff,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EventDestinationProtocol = enum {
    /// The destination follows the Redfish Specification for event notifications.
    ///
    /// This value shall indicate the destination follows the Redfish Specification for event notifications.  Destinations requesting `EventFormatType` of `Event` shall receive a Redfish resource of type `Event`.  Destinations requesting `EventFormatType` of `MetricReport` shall receive a Redfish resource of type `MetricReport`.
    Redfish,
    /// The destination follows the Kafka protocol for event notifications.
    ///
    /// This value shall indicate the destination follows the Apache-defined Kafka protocol as defined by the Kafka Protocol Guide.  The `Context` property shall contain the Kafka topic of the destination broker.
    Kafka,
    /// The destination follows the SNMPv1 protocol for event notifications.
    ///
    /// This value shall indicate the destination follows the RFC1157-defined SNMPv1 protocol.
    SNMPv1,
    /// The destination follows the SNMPv2c protocol for event notifications.
    ///
    /// This value shall indicate the destination follows the SNMPv2c protocol as defined by RFC1441 and RFC1452.
    SNMPv2c,
    /// The destination follows the SNMPv3 protocol for event notifications.
    ///
    /// This value shall indicate the destination follows the SNMPv3 protocol as defined by RFC3411 and RFC3418.
    SNMPv3,
    /// The destination follows the SMTP specification for event notifications.
    ///
    /// This value shall indicate the destination follows the RFC5321-defined SMTP specification.
    SMTP,
    /// The destination follows syslog TLS-based transport for event notifications.
    ///
    /// This value shall indicate the destination follows the TLS-based transport for syslog as defined in RFC5424.
    SyslogTLS,
    /// The destination follows syslog TCP-based transport for event notifications.
    ///
    /// This value shall indicate the destination follows the TCP-based transport for syslog as defined in RFC6587.
    SyslogTCP,
    /// The destination follows syslog UDP-based transport for event notifications.
    ///
    /// This value shall indicate the destination follows the UDP-based transport for syslog as defined in RFC5424.
    SyslogUDP,
    /// The destination follows syslog RELP for event notifications.
    ///
    /// This value shall indicate the destination follows the Reliable Event Logging Protocol (RELP) transport for syslog as defined by www.rsyslog.com.
    SyslogRELP,
    /// The destination follows an OEM protocol for event notifications.
    ///
    /// This value shall indicate an OEM-specific protocol.  The `OEMProtocol` property shall contain the specific OEM event destination protocol.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const EventFormatType = enum {
    /// The subscription destination receives an event payload.
    ///
    /// The subscription destination shall receive an event payload as defined by the value of the `Protocol` property.
    Event,
    /// The subscription destination receives a metric report.
    ///
    /// The subscription destination shall receive a metric report payload as defined by the value of the `Protocol` property.
    MetricReport,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpAuthenticationProtocols = enum {
    /// No authentication.
    ///
    /// This value shall indicate authentication is not required.
    None,
    /// Trap community string authentication.
    ///
    /// This value shall indicate authentication using SNMP community strings and the value of TrapCommunity.
    ///
    /// Deprecated in v1_17_0.
    /// This value has been deprecated to clarify that `AuthenticationProtocol` applies to SNMPv3 support, which does not support community string-based authentication.
    CommunityString,
    /// HMAC-MD5-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-MD5-96 authentication protocol.
    HMAC_MD5,
    /// HMAC-SHA-96 authentication.
    ///
    /// This value shall indicate authentication conforms to the RFC3414-defined HMAC-SHA-96 authentication protocol.
    HMAC_SHA96,
    /// HMAC-128-SHA-224 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC128SHA224AuthProtocol.
    HMAC128_SHA224,
    /// HMAC-192-SHA-256 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC192SHA256AuthProtocol.
    HMAC192_SHA256,
    /// HMAC-256-SHA-384 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC256SHA384AuthProtocol.
    HMAC256_SHA384,
    /// HMAC-384-SHA-512 authentication.
    ///
    /// This value shall indicate authentication for SNMPv3 access conforms to the RFC7860-defined usmHMAC384SHA512AuthProtocol.
    HMAC384_SHA512,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SnmpEncryptionProtocols = enum {
    /// No encryption.
    ///
    /// This value shall indicate there is no encryption.
    None,
    /// CBC-DES encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3414-defined CBC-DES encryption protocol.
    CBC_DES,
    /// CFB128-AES-128 encryption.
    ///
    /// This value shall indicate encryption conforms to the RFC3826-defined CFB128-AES-128 encryption protocol.
    CFB128_AES128,
    /// CFB128-AES-192 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-192 encryption protocol, extended from RFC3826.
    CFB128_AES192,
    /// CFB128-AES-256 encryption.
    ///
    /// This value shall indicate encryption conforms to the CFB128-AES-256 encryption protocol, extended from RFC3826.
    CFB128_AES256,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const SubscriptionType = enum {
    /// The subscription follows the Redfish Specification for event notifications.  To send an event notification, a service sends an HTTP `POST` to the subscriber's destination URI.
    RedfishEvent,
    /// The subscription follows the HTML5 server-sent event definition for event notifications.
    SSE,
    /// The subscription follows the various versions of SNMP Traps for event notifications.
    ///
    /// This value shall indicate the subscription follows the various versions of SNMP Traps for event notifications.  `Protocol` shall specify the appropriate version of SNMP.
    SNMPTrap,
    /// The subscription follows versions 2 and 3 of SNMP Inform for event notifications.
    ///
    /// This value shall indicate the subscription follows versions 2 and 3 of SNMP Inform for event notifications.  `Protocol` shall specify the appropriate version of SNMP.
    SNMPInform,
    /// The subscription sends syslog messages for event notifications.
    ///
    /// This value shall indicate the subscription forwards syslog messages to the event destination.  `Protocol` shall specify the appropriate syslog protocol.
    Syslog,
    /// The subscription is an OEM subscription.
    ///
    /// This value shall indicate an OEM subscription type.  The `OEMSubscriptionType` property shall contain the specific OEM subscription type.
    OEM,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The syslog facility code is an enumeration of program types.
///
/// This type shall specify the syslog facility codes as program types.  Facility values are described in the RFC5424.
pub const SyslogFacility = enum {
    /// Kernel messages.
    Kern,
    /// User-level messages.
    User,
    /// Mail system.
    Mail,
    /// System daemons.
    Daemon,
    /// Security/authentication messages.
    Auth,
    /// Messages generated internally by syslogd.
    Syslog,
    /// Line printer subsystem.
    LPR,
    /// Network news subsystem.
    News,
    /// UUCP subsystem.
    UUCP,
    /// Clock daemon.
    Cron,
    /// Security/authentication messages.
    Authpriv,
    /// FTP daemon.
    FTP,
    /// NTP subsystem.
    NTP,
    /// Log audit.
    Security,
    /// Log alert.
    Console,
    /// Scheduling daemon.
    SolarisCron,
    /// Locally used facility 0.
    Local0,
    /// Locally used facility 1.
    Local1,
    /// Locally used facility 2.
    Local2,
    /// Locally used facility 3.
    Local3,
    /// Locally used facility 4.
    Local4,
    /// Locally used facility 5.
    Local5,
    /// Locally used facility 6.
    Local6,
    /// Locally used facility 7.
    Local7,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// The syslog severity is an application-specific rating used to describe the urgency of the message.
///
/// This type shall specify the syslog severity levels as an application-specific rating used to describe the urgency of the message.  `Emergency` should be reserved for messages indicating the system is unusable and `Debug` should only be used when debugging a program.  Severity values are described in RFC5424.
pub const SyslogSeverity = enum {
    /// A panic condition.
    Emergency,
    /// A condition that should be corrected immediately, such as a corrupted system database.
    Alert,
    /// Hard device errors.
    Critical,
    /// An Error.
    Error,
    /// A Warning.
    Warning,
    /// Conditions that are not error conditions, but that might require special handling.
    Notice,
    /// Informational only.
    Informational,
    /// Messages that contain information normally of use only when debugging a program.
    Debug,
    /// A message of any severity.
    All,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `EventDestination.ResumeSubscription`.
///
/// This action resumes a suspended event subscription.
pub const EventDestinationResumeSubscriptionAction = struct {
    /// The maximum age of buffered events that should be delivered when resuming the subscription.
    ///
    /// This parameter shall indicate the event age of any buffered or otherwise undelivered events that shall be delivered to this event destination when the subscription is resumed.  The service shall deliver any available, previously undelivered event that was created within the duration specified.  A value that equates to zero time, such as `PT0S`, shall indicate that no previously undelivered events shall be sent.  If undelivered events within the duration may have been discarded due to a lack of buffer space, the service should send the `EventBufferExceeded` message from the Base Message Registry.  If the client does not provide this parameter, the service shall apply an implementation-specific duration.
    DeliverBufferedEventDuration: ?core.Duration = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// Arguments to `EventDestination.SuspendSubscription`.
///
/// This action suspends an event subscription.
pub const EventDestinationSuspendSubscriptionAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// This action resumes a suspended event subscription.
    ///
    /// This action shall resume a suspended event subscription, which affects the subscription status.  The service may deliver buffered events when the subscription is resumed.
    @"#EventDestination.ResumeSubscription": ?core.Action(EventDestinationResumeSubscriptionAction, std.json.Value) = null,
    /// This action suspends an event subscription.
    ///
    /// This action shall suspend an event subscription.  No events shall be sent to the event destination until invocation of the `ResumeSubscription` action.  The value of the `State` property within `Status` shall contain `Disabled` for a suspended subscription.  The service may buffer events while the subscription is suspended.
    @"#EventDestination.SuspendSubscription": ?core.Action(EventDestinationSuspendSubscriptionAction, std.json.Value) = null,

    /// This action resumes a suspended event subscription.
    ///
    /// This action shall resume a suspended event subscription, which affects the subscription status.  The service may deliver buffered events when the subscription is resumed.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn resumeSubscription(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: EventDestinationResumeSubscriptionAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#EventDestination.ResumeSubscription" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }

    /// This action suspends an event subscription.
    ///
    /// This action shall suspend an event subscription.  No events shall be sent to the event destination until invocation of the `ResumeSubscription` action.  The value of the `State` property within `Status` shall contain `Disabled` for a suspended subscription.  The service may buffer events while the subscription is suspended.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn suspendSubscription(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: EventDestinationSuspendSubscriptionAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#EventDestination.SuspendSubscription" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

/// The HTTP header value is the property value.  The header name is the property name.
///
/// This type shall contain the HTTP header name and value to include with every event `POST` to the event destination.
pub const HttpHeaderProperty = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

pub const SnmpSettings = struct {
    /// The SNMP trap community string.
    ///
    /// This property shall contain the SNMP trap community string.  If `HideCommunityStrings` contains `true` or is not supported, the value shall be `null` in responses.  Otherwise the trap community string shall be returned.
    TrapCommunity: ?[]const u8 = null,
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: ?SnmpAuthenticationProtocols = null,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: ?SnmpEncryptionProtocols = null,
    /// Indicates if the `AuthenticationKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `AuthenticationKey` property.  Otherwise, the property shall contain `false`.
    AuthenticationKeySet: ?bool = null,
    /// Indicates if the `EncryptionKey` property is set.
    ///
    /// This property shall contain `true` if a valid value was provided for the `EncryptionKey` property.  Otherwise, the property shall contain `false`.
    EncryptionKeySet: ?bool = null,
    /// Indicates whether the SNMP trap community string is hidden in responses.
    ///
    /// This property shall indicate whether the SNMP trap community string is hidden in responses.  If `true`, the `TrapCommunity` property shall be `null` in responses.  If `false`, the `TrapCommunity` property shall contain the trap community string in responses.
    HideCommunityStrings: ?bool = null,
};

/// What a client may change of `EventDestination.SNMPSettings`.
pub const SnmpSettingsUpdate = struct {
    /// The SNMP trap community string.
    ///
    /// This property shall contain the SNMP trap community string.  If `HideCommunityStrings` contains `true` or is not supported, the value shall be `null` in responses.  Otherwise the trap community string shall be returned.
    TrapCommunity: core.Nullable([]const u8) = .absent,
    /// The secret authentication key for SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 authentication.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    AuthenticationKey: core.Nullable([]const u8) = .absent,
    /// The authentication protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 authentication protocol.
    AuthenticationProtocol: core.Nullable(SnmpAuthenticationProtocols) = .absent,
    /// The secret authentication key for SNMPv3.
    ///
    /// This property shall contain the key for SNMPv3 encryption.  The value shall be `null` in responses.  This property accepts a passphrase or a hex-encoded key.  If the string starts with `Passphrase:`, the remainder of the string shall be the passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.  If the string starts with `Hex:`, then the remainder of the string shall be the key encoded in hexadecimal notation.  If the string starts with neither, the full string shall be a passphrase and shall be converted to the key as described in the 'Password to Key Algorithm' section of RFC3414.
    EncryptionKey: core.Nullable([]const u8) = .absent,
    /// The encryption protocol for SNMPv3.
    ///
    /// This property shall contain the SNMPv3 encryption protocol.
    EncryptionProtocol: core.Nullable(SnmpEncryptionProtocols) = .absent,
    /// Indicates whether the SNMP trap community string is hidden in responses.
    ///
    /// This property shall indicate whether the SNMP trap community string is hidden in responses.  If `true`, the `TrapCommunity` property shall be `null` in responses.  If `false`, the `TrapCommunity` property shall contain the trap community string in responses.
    HideCommunityStrings: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// A syslog filter.
///
/// This type shall contain the filter for a syslog message.  The filter shall describe the desired syslog message to forward to a remote syslog server.
pub const SyslogFilter = struct {
    /// The lowest severity level message that will be forwarded.
    ///
    /// This property shall contain the lowest syslog severity level that will be forwarded.  The service shall forward all messages equal to or greater than the value in this property.  The value `All` shall indicate all severities.
    LowestSeverity: ?SyslogSeverity = null,
    /// The types of programs that can log messages.
    ///
    /// This property shall contain the types of programs that can log messages.  If this property contains an empty array or is absent, all facilities shall be indicated.
    LogFacilities: ?[]const ?SyslogFacility = null,
};

/// What a client may change of `EventDestination.SyslogFilter`.
///
/// A syslog filter.
pub const SyslogFilterUpdate = struct {
    /// The lowest severity level message that will be forwarded.
    ///
    /// This property shall contain the lowest syslog severity level that will be forwarded.  The service shall forward all messages equal to or greater than the value in this property.  The value `All` shall indicate all severities.
    LowestSeverity: core.Nullable(SyslogSeverity) = .absent,
    /// The types of programs that can log messages.
    ///
    /// This property shall contain the types of programs that can log messages.  If this property contains an empty array or is absent, all facilities shall be indicated.
    LogFacilities: ?[]const ?SyslogFacility = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The `EventDestination` schema defines the target of an event subscription, including the event types and context to provide to the target in the event payload.
///
/// This resource shall represent the target of an event subscription, including the event types and context to provide to the target in the event payload.
pub const EventDestination = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The URI of the destination event receiver.
    ///
    /// This property shall contain a URI to the destination where the events are sent.  If `Protocol` is `SMTP`, the URI shall follow the RFC6068-described format.  SNMP URIs shall be consistent with RFC4088.  Specifically, for SNMPv3, if a username is specified in the SNMP URI, the SNMPv3 authentication and encryption configuration associated with that user shall be utilized in the SNMPv3 traps.  Syslog URIs shall be consistent with RFC3986 and contain the scheme `syslog://`.  Server-sent event destinations shall be in the form `redfish-sse://ip:port` where `ip` and `port` are the IP address and the port of the client with the open SSE connection.  For other URIs, such as HTTP or HTTPS, they shall be consistent with RFC3986.
    Destination: ?[]const u8 = null,
    /// The types of events that are sent to the destination.
    ///
    /// This property shall contain an array that contains the types of events that shall be sent to the destination.  To specify that a client is subscribing for metric reports, the `EventTypes` property should include 'MetricReport'.  If the subscription does not include this property, the service shall use a single element with a default of `Other`.
    ///
    /// Deprecated in v1_5_0.
    /// This property has been deprecated.  Starting with Redfish Specification v1.6 (Event v1.3), subscriptions are based on the `RegistryPrefix` and `ResourceType` properties and not on the `EventType` property.  Use `EventFormatType` to create subscriptions for metric reports.  If the subscription does not include this property, the service shall use a single element with a default of `Other`.
    EventTypes: ?[]const event.EventType = null,
    /// A client-supplied string that is stored with the event destination subscription.
    ///
    /// This property shall contain a client-supplied context that remains with the connection through the connection's lifetime.
    Context: ?[]const u8 = null,
    /// The protocol type of the event connection.
    ///
    /// This property shall contain the protocol type that the event uses to send the event to the destination.  A `Redfish` value shall indicate that the event type shall adhere to the type defined in the Redfish Specification.
    Protocol: ?EventDestinationProtocol = null,
    /// An array of settings for HTTP headers, such as authorization information.  This array is `null` or an empty array in responses.  An empty array is the preferred return value on read operations.
    ///
    /// This property shall contain an array of objects consisting of the names and values of the HTTP headers to include with every event `POST` to the event destination.  This object shall be `null` or an empty array in responses.  An empty array is the preferred return value in responses.
    HttpHeaders: ?[]const HttpHeaderProperty = null,
    /// The list of `MessageId` values that are sent to this event destination.
    ///
    /// This property shall contain an array of `MessageId` values that are the allowable values for the `MessageId` property within an event sent to the subscriber.  The `MessageId` should be in the `MessageRegistryPrefix.MessageKey` format.  If included, the `MessageId` major and minor version details should be ignored.  Events with a `MessageId` that is not contained in this array and is not from a message registry contained in RegistryPrefixes shall not be sent to the subscriber.  If this property is an empty array or is absent, no inclusive filtering based upon the `MessageId` of an event is performed.
    MessageIds: ?[]const ?[]const u8 = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The subscription type for events.
    ///
    /// This property shall indicate the type of subscription for events.  If this property is not present, the `SubscriptionType` shall be assumed to be `RedfishEvent`.
    SubscriptionType: ?SubscriptionType = null,
    /// The list of prefixes for the message registries that contain the `MessageId` values that are sent to this event destination.
    ///
    /// This property shall contain an array the prefixes of message registries that contain the `MessageId` values that are the allowable values for the `MessageId` property within an event sent to the subscriber.  Events with a `MessageId` that is not from a message registry contained in this array and is not contained by `MessageIds` shall not be sent to the subscriber.  If this property is an empty array or is absent, no inclusive filtering based upon message registry of the `MessageId` of an event is performed.
    RegistryPrefixes: ?[]const ?[]const u8 = null,
    /// The list of resource type values (schema names) that correspond to the `OriginOfCondition`.  The version and full namespace should not be specified.
    ///
    /// This property shall specify an array of resource type values that contain the allowable resource types for the resource referenced by the `OriginOfCondition` property.  Events with the resource type of the resource referenced by the `OriginOfCondition` property that is not contained in this array shall not be sent to the subscriber.  If this property is an empty array or is absent, no filtering based upon the resource type of the `OriginOfCondition` of an event is performed.  This property shall contain only the general namespace for the type and not the versioned value.  For example, it shall not contain `Task.v1_2_0.Task` and instead shall contain `Task`.  To specify that a client is subscribing to metric reports, the `EventTypes` property should include `MetricReport`.
    ResourceTypes: ?[]const ?[]const u8 = null,
    /// An indication of whether the subscription is for events in the `OriginResources` array and its subordinate resources.  If `true` and the `OriginResources` array is specified, the subscription is for events in the `OriginResources` array and its subordinate resources.  Note that resources associated through the Links section are not considered subordinate.  If `false` and the `OriginResources` array is specified, the subscription is for events in the `OriginResources` array only.  If the `OriginResources` array is not present, this property has no relevance.
    ///
    /// This property shall indicate whether the subscription is for events in the `OriginResources` array and its subordinate resources.  If `true` and the `OriginResources` array is specified, the subscription is for events in the `OriginResources` array and its subordinate resources.  Note that resources associated through the Links section are not considered subordinate.  If `false` and the `OriginResources` array is specified, the subscription shall be for events in the `OriginResources` array only.  If the `OriginResources` array is not present, this property shall have no relevance.
    SubordinateResources: ?bool = null,
    /// The content types of the message that are sent to the `EventDestination`.
    ///
    /// This property shall indicate the content types of the message that this service sends to the `EventDestination`.  If this property is not present, the `EventFormatType` shall be assumed to be Event.
    EventFormatType: ?EventFormatType = null,
    /// The subscription delivery retry policy for events, where the subscription type is `RedfishEvent`.
    ///
    /// This property shall indicate the subscription delivery retry policy for events where the subscription type is `RedfishEvent`.
    DeliveryRetryPolicy: ?DeliveryRetryPolicy = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// Settings for an SNMP event destination.
    ///
    /// This property shall contain the settings for an SNMP event destination.
    SNMP: ?SnmpSettings = null,
    /// An indication of whether the events subscribed to will also include the entire resource or object referenced by the `OriginOfCondition` property in the event payload.
    ///
    /// This property shall indicate whether the event payload sent to the subscription destination will expand the `OriginOfCondition` property to include the resource or object referenced by the `OriginOfCondition` property.
    IncludeOriginOfCondition: ?bool = null,
    /// An indication of whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event with the certificates found in the collection referenced by the `Certificates` property.  If this property is not supported by the service or specified by the client in the create request, it shall be assumed to be `false`.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    VerifyCertificate: ?bool = null,
    /// A list of filters applied to syslog messages before sending to a remote syslog server.  An empty list indicates all syslog messages are sent.
    ///
    /// This property shall describe all desired syslog messages to send to a remote syslog server.  If this property contains an empty array or is absent, all messages shall be sent.
    SyslogFilters: ?[]const ?SyslogFilter = null,
    /// The OEM-defined protocol type of the event connection.
    ///
    /// This property shall contain the protocol type that the event uses to send the event to the destination.  This property shall be present if `Protocol` is `OEM`.
    OEMProtocol: ?[]const u8 = null,
    /// The OEM-defined subscription type for events.
    ///
    /// This property shall indicate the OEM-defined type of subscription for events.  This property shall be present if `SubscriptionType` is `OEM`.
    OEMSubscriptionType: ?[]const u8 = null,
    /// Send a heartbeat event periodically to the destination.
    ///
    /// This property shall indicate that the service shall periodically send the `RedfishServiceFunctional` message defined in the Heartbeat Event Message Registry to the subscriber.  If this property is not present, no periodic event shall be sent.  This property shall not apply to event destinations if the `SubscriptionType` property contains the value `SSE`.
    SendHeartbeat: ?bool = null,
    /// Interval for sending heartbeat events to the destination in minutes.
    ///
    /// This property shall indicate the interval for sending periodic heartbeat events to the subscriber.  The value shall be the interval, in minutes, between each periodic event.  This property shall not be present if the `SendHeartbeat` property is not present.
    HeartbeatIntervalMinutes: ?i64 = null,
    /// The list of prefixes for the message registries that contain the `MessageId` values that are not sent to this event destination.
    ///
    /// This property shall contain an array of prefixes of excluded message registries that contain the `MessageId` values that are not allowed values for the `MessageId` property within an event sent to the subscriber.  Events with a `MessageId` that is from a message registry contained in this array shall not be sent to the subscriber.  If this property is an empty array or is absent, no exclusive filtering based upon message registry of the `MessageId` of an event is performed.
    ExcludeRegistryPrefixes: ?[]const ?[]const u8 = null,
    /// The list of `MessageId` values that are not sent to this event destination.
    ///
    /// This property shall contain an array of excluded `MessageId` values that are not allowed values for the `MessageId` property within an event sent to the subscriber.  The `MessageId` shall be in the `MessageRegistryPrefix.MessageKey` format.  If included, the `MessageId` major and minor version details should be ignored.  Events with a `MessageId` that is contained in this array shall not be sent to the subscriber.  If this property is an empty array or is absent, no exclusive filtering based upon the `MessageId` of an event is performed.
    ExcludeMessageIds: ?[]const ?[]const u8 = null,
    /// The list of severities that are sent to this event destination.
    ///
    /// This property shall contain an array of severities that are the allowable values for the `MessageSeverity` property within an event sent to the subscriber.  If this property is an empty array or is absent, no filtering based upon the `MessageSeverity` of an event is performed.
    Severities: ?[]const ?resource.Health = null,
    /// The backup destination URIs for this event receiver.  Events are sent to these URIs, in array order, when the destination URI is unreachable or returns an error.
    ///
    /// This property shall contain an array of URIs to destination where events are sent if the event receiver specified by `Destination` is unreachable or returns an error.  Events are sent to each of the backup destinations, in array order, until a destination has been reached.  An empty array shall indicate that the service supports backup event receivers, but none have been specified by the user.
    BackupDestinations: ?[]const ?[]const u8 = null,
    /// The array of resources for which the service sends only related events.
    ///
    /// This property shall specify an array of resources, resource collections, or referenceable members that are the only allowable values for the `OriginOfCondition` property within an event that the service sends to the subscriber.  Events with an `OriginOfCondition` that is not contained in this array, and is not subordinate to members of this array if `SubordinateResources` contains the value `true`, shall not be sent to the subscriber.  If this property is an empty array or is absent, no filtering based upon the URI of the `OriginOfCondition` of an event is performed.
    OriginResources: ?[]const core.NavProperty(resource.ItemOrCollection) = null,
    /// How many members `OriginResources` has in total, which is not how many this response carries.
    @"OriginResources@odata.count": ?i64 = null,
    /// The next page of `OriginResources`. Present only when this response left members out.
    @"OriginResources@odata.nextLink": ?core.ODataId = null,
    /// A list of metric report definitions for which the service only sends related metric reports.  If this property is absent or the array is empty, metric reports that originate from any metric report definition are sent to the subscriber.
    ///
    /// This property shall specify an array of metric report definitions that are the only allowable generators of metric reports for this subscription.  Metric reports originating from metric report definitions not contained in this array shall not be sent to the subscriber.  If this property is absent or the array is empty, the service shall send metric reports originating from any metric report definition to the subscriber.
    MetricReportDefinitions: ?[]const core.NavProperty(metric_report_definition.MetricReportDefinition) = null,
    /// How many members `MetricReportDefinitions` has in total, which is not how many this response carries.
    @"MetricReportDefinitions@odata.count": ?i64 = null,
    /// The next page of `MetricReportDefinitions`. Present only when this response left members out.
    @"MetricReportDefinitions@odata.nextLink": ?core.ODataId = null,
    /// The link to a collection of server certificates for the server referenced by the `Destination` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represent the server certificates for the server referenced by the `Destination` property.  If `VerifyCertificate` is `true`, services shall compare the certificates in this collection with the certificate obtained during handshaking with the event destination in order to verify the identity of the event destination prior to sending an event.  If the server cannot be verified, the service shall not send the event.  If `VerifyCertificate` is `false`, the service shall not perform certificate verification with certificates in this collection.  Regardless of the contents of this collection, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    Certificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
    /// The link to a collection of client identity certificates provided to the server referenced by the `Destination` property.
    ///
    /// This property shall contain a link to a resource collection of type `CertificateCollection` that represents the client identity certificates that are provided to the server referenced by the `Destination` property as part of TLS handshaking.
    ClientCertificates: ?core.NavProperty(certificate_collection.CertificateCollection) = null,
};

/// What a client may change of `EventDestination.EventDestination`.
///
/// The `EventDestination` schema defines the target of an event subscription, including the event types and context to provide to the target in the event payload.
pub const EventDestinationUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// A client-supplied string that is stored with the event destination subscription.
    ///
    /// This property shall contain a client-supplied context that remains with the connection through the connection's lifetime.
    Context: core.Nullable([]const u8) = .absent,
    /// The subscription delivery retry policy for events, where the subscription type is `RedfishEvent`.
    ///
    /// This property shall indicate the subscription delivery retry policy for events where the subscription type is `RedfishEvent`.
    DeliveryRetryPolicy: core.Nullable(DeliveryRetryPolicy) = .absent,
    /// Settings for an SNMP event destination.
    ///
    /// This property shall contain the settings for an SNMP event destination.
    SNMP: ?SnmpSettingsUpdate = null,
    /// An indication of whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event with the certificates found in the collection referenced by the `Certificates` property.  If this property is not supported by the service or specified by the client in the create request, it shall be assumed to be `false`.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    VerifyCertificate: core.Nullable(bool) = .absent,
    /// A list of filters applied to syslog messages before sending to a remote syslog server.  An empty list indicates all syslog messages are sent.
    ///
    /// This property shall describe all desired syslog messages to send to a remote syslog server.  If this property contains an empty array or is absent, all messages shall be sent.
    SyslogFilters: ?[]const ?SyslogFilterUpdate = null,
    /// The backup destination URIs for this event receiver.  Events are sent to these URIs, in array order, when the destination URI is unreachable or returns an error.
    ///
    /// This property shall contain an array of URIs to destination where events are sent if the event receiver specified by `Destination` is unreachable or returns an error.  Events are sent to each of the backup destinations, in array order, until a destination has been reached.  An empty array shall indicate that the service supports backup event receivers, but none have been specified by the user.
    BackupDestinations: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// What a client may supply when creating an instance of `EventDestination.EventDestination`.
///
/// The `EventDestination` schema defines the target of an event subscription, including the event types and context to provide to the target in the event payload.
pub const EventDestinationCreate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// The URI of the destination event receiver.
    ///
    /// This property shall contain a URI to the destination where the events are sent.  If `Protocol` is `SMTP`, the URI shall follow the RFC6068-described format.  SNMP URIs shall be consistent with RFC4088.  Specifically, for SNMPv3, if a username is specified in the SNMP URI, the SNMPv3 authentication and encryption configuration associated with that user shall be utilized in the SNMPv3 traps.  Syslog URIs shall be consistent with RFC3986 and contain the scheme `syslog://`.  Server-sent event destinations shall be in the form `redfish-sse://ip:port` where `ip` and `port` are the IP address and the port of the client with the open SSE connection.  For other URIs, such as HTTP or HTTPS, they shall be consistent with RFC3986.
    Destination: []const u8,
    /// A client-supplied string that is stored with the event destination subscription.
    ///
    /// This property shall contain a client-supplied context that remains with the connection through the connection's lifetime.
    Context: core.Nullable([]const u8) = .absent,
    /// The protocol type of the event connection.
    ///
    /// This property shall contain the protocol type that the event uses to send the event to the destination.  A `Redfish` value shall indicate that the event type shall adhere to the type defined in the Redfish Specification.
    Protocol: EventDestinationProtocol,
    /// The subscription delivery retry policy for events, where the subscription type is `RedfishEvent`.
    ///
    /// This property shall indicate the subscription delivery retry policy for events where the subscription type is `RedfishEvent`.
    DeliveryRetryPolicy: core.Nullable(DeliveryRetryPolicy) = .absent,
    /// Settings for an SNMP event destination.
    ///
    /// This property shall contain the settings for an SNMP event destination.
    SNMP: ?SnmpSettingsUpdate = null,
    /// An indication of whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event.
    ///
    /// This property shall indicate whether the service will verify the certificate of the server referenced by the `Destination` property prior to sending the event with the certificates found in the collection referenced by the `Certificates` property.  If this property is not supported by the service or specified by the client in the create request, it shall be assumed to be `false`.  Regardless of the value of this property, services may perform additional verification based on other factors, such as the configuration of the SecurityPolicy resource.
    VerifyCertificate: core.Nullable(bool) = .absent,
    /// A list of filters applied to syslog messages before sending to a remote syslog server.  An empty list indicates all syslog messages are sent.
    ///
    /// This property shall describe all desired syslog messages to send to a remote syslog server.  If this property contains an empty array or is absent, all messages shall be sent.
    SyslogFilters: ?[]const ?SyslogFilterUpdate = null,
    /// The backup destination URIs for this event receiver.  Events are sent to these URIs, in array order, when the destination URI is unreachable or returns an error.
    ///
    /// This property shall contain an array of URIs to destination where events are sent if the event receiver specified by `Destination` is unreachable or returns an error.  Events are sent to each of the backup destinations, in array order, until a destination has been reached.  An empty array shall indicate that the service supports backup event receivers, but none have been specified by the user.
    BackupDestinations: ?[]const ?[]const u8 = null,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
