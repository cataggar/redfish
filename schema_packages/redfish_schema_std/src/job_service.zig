//! Namespace `JobService`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_std`.

const std = @import("std");

const core = @import("redfish_core");
const job_collection = @import("job_collection.zig");
const job_document_collection = @import("job_document_collection.zig");
const job_executor_collection = @import("job_executor_collection.zig");
const log_service = @import("log_service.zig");
const resource = @import("resource.zig");
const settings = @import("settings.zig");

pub const ValidationPolicy = enum {
    /// Jobs are validated automatically.
    ///
    /// This value shall indicate that jobs are validated automatically.  The `JobState` property of a new job shall contain `Validating` when created with the `SubmitJob` action from the `JobDocument` resource.
    Automatic,
    /// Jobs are validated manually.
    ///
    /// This value shall indicate jobs are validated manually.  The `JobState` property of a new job shall contain `New` when created with the `SubmitJob` action from the `JobDocument` resource and wait for a user to perform the `Validate` action in the `Job` resource.
    Manual,
    /// Jobs are not validated.
    ///
    /// This value shall indicate jobs are not validated.  The `JobState` property of a new job shall contain `Pending` when created with the `SubmitJob` action from the `JobDocument` resource.
    Bypass,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

/// Arguments to `JobService.CancelAllJobs`.
///
/// Cancels all jobs.
pub const JobServiceCancelAllJobsAction = struct {
    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

/// The available actions for this resource.
///
/// This type shall contain the available actions for this resource.
pub const Actions = struct {
    /// The available OEM-specific actions for this resource.
    ///
    /// This property shall contain the available OEM-specific actions for this resource.
    Oem: ?OemActions = null,
    /// Cancels all jobs.
    ///
    /// This action shall cancel all jobs.  The service shall transition all jobs to the `Cancelled` state.
    @"#JobService.CancelAllJobs": ?core.Action(JobServiceCancelAllJobsAction, std.json.Value) = null,

    /// Cancels all jobs.
    ///
    /// This action shall cancel all jobs.  The service shall transition all jobs to the `Cancelled` state.
    ///
    /// Returns `error.ActionNotSupported` if the service did not
    /// advertise this action on the resource.
    pub fn cancelAllJobs(
        self: @This(),
        gpa: std.mem.Allocator,
        transport: *core.BmcTransport,
        params: JobServiceCancelAllJobsAction,
    ) !core.Owned(core.ModificationResponse(std.json.Value)) {
        const target = self.@"#JobService.CancelAllJobs" orelse return error.ActionNotSupported;
        return core.bmc.invokeAction(gpa, transport, target, params);
    }
};

pub const JobServiceCapabilities = struct {
    /// The maximum number of jobs supported.
    ///
    /// This property shall contain the maximum number of jobs supported by the implementation.
    MaxJobs: ?i64 = null,
    /// The maximum number of job steps supported.
    ///
    /// This property shall contain the maximum number of steps supported by a single job instance.
    MaxSteps: ?i64 = null,
    /// An indication of whether scheduling of jobs is supported.
    ///
    /// This property shall indicate whether the `Schedule` property within the job supports scheduling of jobs.
    Scheduling: ?bool = null,
    /// An indication of whether user-specified jobs are supported.
    ///
    /// This property shall indicate whether user-specified jobs are supported.  User-specified jobs are jobs that are created by performing an HTTP `POST` operation on the `JobCollection` resource referenced by the `Jobs` property.
    UserSpecifiedJobs: ?bool = null,
    /// An indication of whether document-based jobs are supported.
    ///
    /// This property shall indicate whether document-based jobs are supported.  Document-based jobs are jobs that are created by performing the `SubmitJob` action on a `JobDocument` resource in the `JobDocumentCollection` resource referenced by the `JobDocuments` property.
    DocumentBasedJobs: ?bool = null,
};

/// The available OEM-specific actions for this resource.
///
/// This type shall contain the available OEM-specific actions for this resource.
pub const OemActions = struct {
    /// Whatever the service sent that this schema version does not name.
    additional_properties: core.AdditionalProperties = .{},

    const open = core.OpenStruct(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
    pub const jsonStringify = open.jsonStringify;
};

/// The `JobService` schema contains properties for scheduling and execution of operations, represents the properties for the job service itself, and has links to jobs managed by the job service.
///
/// This resource shall represent a job service for a Redfish implementation.
pub const JobService = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// The schema version the service implements.
    @"@odata.type": ?[]const u8 = null,
    /// The pending settings for this resource. PATCH `SettingsObject` rather than the resource itself; the service applies the result on the schedule this names.
    @"@Redfish.Settings": ?settings.Settings = null,
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.Oem = null,
    Id: ?resource.Id = null,
    Description: ?resource.Description = null,
    Name: ?resource.Name = null,
    /// The current date and time setting for the job service.
    ///
    /// This property shall contain the current date and time setting for the job service.
    DateTime: ?core.DateTimeOffset = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: ?bool = null,
    /// The supported capabilities of this job service implementation.
    ///
    /// This property shall contain properties that describe the capabilities or supported features of this implementation of a job service.
    ServiceCapabilities: ?JobServiceCapabilities = null,
    /// The status and health of the resource and its subordinate or dependent resources.
    ///
    /// This property shall contain any status or health properties of the resource.
    Status: ?resource.Status = null,
    /// The available actions for this resource.
    ///
    /// This property shall contain the available actions for this resource.
    Actions: ?Actions = null,
    /// The policy for how document-based jobs are validated.
    ///
    /// This property shall contain policy for how document-based jobs are validated.
    ValidationPolicy: ?ValidationPolicy = null,
    /// The link to a log service that the job service uses.  This service can be a dedicated log service or a pointer to a log service under another resource, such as a manager.
    ///
    /// This property shall contain a link to a resource of type `LogService` that this job service uses.
    Log: ?core.NavProperty(log_service.LogService) = null,
    /// The links to the jobs collection.
    ///
    /// This property shall contain a link to a resource collection of type `JobCollection`.
    Jobs: ?core.NavProperty(job_collection.JobCollection) = null,
    /// The links to the job document collection.
    ///
    /// This property shall contain a link to a resource collection of type `JobDocumentCollection`.  This property shall only be present if the service supports document-based jobs.
    JobDocuments: ?core.NavProperty(job_document_collection.JobDocumentCollection) = null,
    /// The links to the job executor collection.
    ///
    /// This property shall contain a link to a resource collection of type `JobExecutorCollection`.  This property shall only be present if the service supports document-based jobs.
    JobExecutors: ?core.NavProperty(job_executor_collection.JobExecutorCollection) = null,

    const closed = core.ClosedStruct(@This());
    pub const jsonParse = closed.jsonParse;
    pub const jsonParseFromValue = closed.jsonParseFromValue;
};

/// What a client may change of `JobService.JobService`.
///
/// The `JobService` schema contains properties for scheduling and execution of operations, represents the properties for the job service itself, and has links to jobs managed by the job service.
pub const JobServiceUpdate = struct {
    /// The OEM extension property.
    ///
    /// This property shall contain the OEM extensions.  All values for properties that this object contains shall conform to the Redfish Specification-described requirements.
    Oem: ?resource.OemUpdate = null,
    /// An indication of whether this service is enabled.
    ///
    /// This property shall indicate whether this service is enabled.
    ServiceEnabled: core.Nullable(bool) = .absent,

    pub const jsonStringify = core.Payload(@This()).jsonStringify;
};

test {
    std.testing.refAllDecls(@This());
}
