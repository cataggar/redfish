//! Namespace `NvidiaComputerSystem`.
//!
//! Generated by `redfish-codegen`. DO NOT EDIT.
//!
//! Profile: `redfish_schema_oem_nvidia_bluefield`.

const std = @import("std");

const core = @import("redfish_core");

pub const Mode = enum {
    /// This DPU is in NIC mode.
    ///
    /// In this mode Bluefield device works as regular NIC for the host.
    NicMode,
    /// This Bluefield device is in DPU mode.
    ///
    /// In this mode Bluefield device represent 'bump in a wire' device that controls packet processing.
    DpuMode,

    /// A value this package's schema version does not name.
    UnsupportedValue,

    const open = core.OpenEnum(@This());
    pub const jsonParse = open.jsonParse;
    pub const jsonParseFromValue = open.jsonParseFromValue;
};

pub const NvidiaComputerSystem = struct {
    /// Where the resource lives.
    @"@odata.id": ?core.ODataId = null,
    /// The version of the resource this value was read at.
    @"@odata.etag": ?core.ODataETag = null,
    /// DPU Base MAC address.
    ///
    /// DPU Base MAC address is string that contains MAC address without colons. For example: '1070fd010203'.
    BaseMAC: ?[]const u8 = null,
    /// Bluefield device mode.
    ///
    /// Bluefield can work as regular NIC or as DPU (bump in a wire).
    Mode: ?Mode = null,
};

test {
    std.testing.refAllDecls(@This());
}
